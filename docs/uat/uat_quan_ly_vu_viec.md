# UAT Test Cases — Quản lý vụ việc / vụ án (Case Management) — toàn bộ chức năng

**Generated**: 23/05/2026 18:33  
**Complexity**: `complex`  
**Total TC**: 781  
**Companion Excel**: file `.xlsx` cùng tên trong thư mục này

## 🤖 Hướng dẫn cho Claude Code

File này được thiết kế để Claude Code đọc khi cần **fix bug** từ kết quả UAT.

**Workflow**:
1. Đọc section `## TC-XXX` của TC bị fail để hiểu context
2. Đọc `### 🔧 Fix Context` để biết khu vực code cần kiểm tra
3. Edit code → chạy lại test → đánh dấu `**Status**: Verified` trong Bug Report
4. File `.xlsx` companion được update bởi runner — KHÔNG edit thủ công

**Quy ước parsing**:
- TC ID nằm trong heading `## TC-XXX` (anchor-able)
- Bug report ở format YAML trong code fence
- Checklist `- [ ]` có thể tick bằng cách thay thành `- [x]`
- Test Data ở section riêng — load 1 lần dùng cho nhiều TC

## 📐 Scope

**In scope**:
- CRUD vụ án (tạo / xem / sửa / xóa mềm / khôi phục) qua API + UI
- 10 trạng thái CaseStatus (TIEP_NHAN, DANG_XAC_MINH, DA_XAC_MINH, DANG_DIEU_TRA, TAM_DINH_CHI, DINH_CHI, DA_KET_LUAN, DANG_TRUY_TO, DANG_XET_XU, DA_LUU_TRU) + chuyển trạng thái + lưu CaseStatusHistory
- CaseProvenance — FROM_PETITION/FROM_INCIDENT/DIRECT_DISCOVERY/TRANSFERRED/OTHER_LEGAL_SOURCE (BLTTHS Đ.143)
- Optimistic lock qua expectedUpdatedAt + expectedPetitionUpdatedAt + expectedIncidentUpdatedAt
- DataScope team-based access control (investigator + team + dispatcher + ADMIN + ward officer)
- Soft delete với reason 10-500 chars + ràng buộc status=TIEP_NHAN + 8-step validation chain + TOCTOU guard + restore ADMIN-only
- Assign / re-assign dispatcher-only + ward → non-ward escalation audit (v0.35a)
- Search / filter (status, investigator, unit, date range, overdue, district/ward, capDoToiPham, wardTeamId)
- Export Excel theo phường/xã + theo phân loại khác (rate-limited 5 req/60s) + audit CASE_EXPORTED
- TĐC tracking: lyDoTamDinhChiVuAn (Đ.229 BLTTHS), auto-set ngayTamDinhChi + soLanTamDinhChi increment; phục hồi với daRaSoat + ketQuaPhucHoiVuAn
- TDC backfill cho legacy data (PATCH /tdc-backfill)
- Status history view với changedBy + changedAt
- Audit log đầy đủ: CASE_CREATED, CASE_UPDATED, CASE_STATUS_CHANGED, CASE_DELETED, CASE_RESTORED, CASE_ASSIGNED, CASE_ESCALATED_FROM_WARD, CASE_EXPORTED
- BUG-001/002/004 (v0.37.2.7): trim + reject empty/whitespace-only cho name

**Out of scope**:
- Penetration test full (giao team Security)
- Load test >1000 concurrent user (dùng JMeter riêng)
- KPI Dashboard chi tiết — test trong feature KPI
- Comprehensive list / Initial cases / Cases Tdc Backfill page — đã tách feature
- Module Petition / Incident sâu — test riêng
- Sub-resources detail: Subject CRUD, Lawyer CRUD, Conclusion CRUD, Document upload — test riêng
- Auto-deadline calculation logic chi tiết theo SystemSetting — test riêng module Settings
- VKS meetings, Action Plans, Delegations, Proposals sub-modules — test riêng

**Exit Criteria**: 100% TC P0 PASS; ≥95% P1 PASS; ≥85% P2 PASS; 0 defect Critical/High mở; ≤5 defect Medium mở; tất cả SECURITY case PASS; A11Y axe-core 0 violation Level A

## 🔍 Self-Audit

**Tổng số TC**: 781

**Phân bố loại**:
- `RED`: 245
- `GREEN`: 156
- `SECURITY`: 66
- `STATE`: 46
- `BOUNDARY`: 45
- `DATA`: 41
- `A11Y`: 33
- `INTEGRATION`: 32
- `PERFORMANCE`: 26
- `COMPAT`: 25
- `AUDIT`: 23
- `DECISION`: 19
- `RECOVERY`: 14
- `EP`: 10

**Phân bố priority**:
- 🔴 `P0`: 446
- 🟠 `P1`: 326
- 🟡 `P2`: 9

**Phân bố severity nếu fail**:
- 🚨 `Critical`: 122
- ⚠️ `High`: 334
- ⚡ `Medium`: 238
- 📌 `Low`: 87

## 📦 Test Data

> Dữ liệu chia sẻ giữa các TC. Claude Code đọc 1 lần, reference bằng ID khi fix.

### 1. Tài khoản kiểm thử

| ID | Email | Mật khẩu | Vai trò | Trạng thái | Mục đích |
|----|-------|----------|---------|------------|----------|
| `U001` | `admin@pc02.local` | `Admin@2026!` | ADMIN | Active | Test ADMIN-only restore + listDeleted + bypass scope |
| `U002` | `dispatcher@pc02.local` | `Disp@2026!` | DISPATCHER | Active | Test PATCH /assign + canDispatch bypass |
| `U003` | `dieuTra1@pc02.local` | `DTV@2026!` | INVESTIGATOR | Active | Owner + same-team test, creator delete |
| `U004` | `dieuTra2@pc02.local` | `DTV@2026!` | INVESTIGATOR | Active | Same-team với U003 (Team-Q1) |
| `U005` | `dieuTra3-Q3@pc02.local` | `DTV@2026!` | INVESTIGATOR | Active | Different team (Team-Q3) — test scope reject |
| `U006` | `wardOfficer1@pc02.local` | `WO@2026!` | WARD_OFFICER | Active | Test isWardOfficer auto-set assignedTeamId override |
| `U007` | `viewer@pc02.local` | `View@2026!` | VIEWER | Active | Test 403 cho write/delete/restore |
| `U008` | `locked@pc02.local` | `-` | INVESTIGATOR | Locked | Test JWT của user locked |

### 2. Boundary Values (BVA)

| Field | Giá trị | Loại | Kỳ vọng | Ghi chú |
|-------|---------|------|---------|---------|
| name | `A` | `min` | **PASS** | 1 ký tự min |
| name | `X×500` | `max` | **PASS** | 500 ký tự max |
| name | `X×501` | `max+1` | **FAIL** | vượt MaxLength(500) |
| name | `` | `empty` | **FAIL** | IsNotEmpty |
| name | `   ` | `whitespace` | **FAIL** | Transform trim → '' |
| crime | `X×255` | `max` | **PASS** |  |
| crime | `X×256` | `max+1` | **FAIL** |  |
| unit | `X×255` | `max` | **PASS** |  |
| unit | `X×256` | `max+1` | **FAIL** |  |
| subjectsCount | `0` | `min` | **PASS** |  |
| subjectsCount | `-1` | `min-1` | **FAIL** | @Min(0) |
| delete-reason | `X×10` | `min` | **PASS** | MinLength(10) |
| delete-reason | `X×9` | `min-1` | **FAIL** |  |
| delete-reason | `X×500` | `max` | **PASS** |  |
| delete-reason | `X×501` | `max+1` | **FAIL** |  |
| sourceDocumentNote | `X×1000` | `max` | **PASS** |  |
| sourceDocumentNote | `X×1001` | `max+1` | **FAIL** |  |
| limit | `1` | `min` | **PASS** |  |
| limit | `100` | `max` | **PASS** |  |
| limit | `101` | `max+1` | **FAIL** | @Max(100) |
| limit | `0` | `min-1` | **FAIL** | @Min(1) |
| offset | `0` | `min` | **PASS** |  |
| offset | `-1` | `min-1` | **FAIL** |  |

### 3. Security Payloads (OWASP)

| Target | Payload | Loại tấn công | Kỳ vọng | OWASP Ref |
|--------|---------|---------------|---------|-----------|
| search | `' OR 1=1--` | SQL Injection | 200 data=[] (Prisma parameterized) | `A03:2021` |
| sortBy | `name; DROP TABLE cases--` | SQL Injection via orderBy | 200 fallback createdAt (whitelist) | `A03:2021` |
| name | `<script>alert(1)</script>` | XSS reflected | 201 lưu raw, React escape ở render | `A03:2021` |
| crime | `<img src=x onerror=alert(1)>` | XSS stored | 201, không exec JS khi render | `A03:2021` |
| metadata | `{"__proto__":{"isAdmin":true}}` | Prototype Pollution | 201 nhưng object không pollute | `A03:2021` |
| linkedPetitionId | `<id-thuộc-team-khác>` | IDOR | 404 (no enumeration leak) | `A01:2021` |
| GET /cases/<id-team-khác> | `-` | Horizontal privilege | 403 | `A01:2021` |
| PATCH /assign | `non-dispatcher token` | Vertical privilege | 403 DispatchGuard | `A01:2021` |
| POST /cases | `{createdById:'evil', id:'fake', deletedAt:'2030'}` | Mass assignment | 400 forbidNonWhitelisted | `A08:2021` |
| Authorization | `<expired JWT>` | Token expiry | 401 | `A07:2021` |
| Authorization | `<tampered signature JWT>` | Token tamper | 401 | `A07:2021` |
| GET /export/ward | `6 req/60s` | Rate limit bypass | 6th=429 | `A04:2021` |
| Origin | `https://evil.com` | CORS bypass | Reject preflight | `A05:2021` |
| Content-Type | `text/plain (mismatch)` | Content type bypass | 400 nếu mong JSON | `A05:2021` |

## 📋 Test Matrix

> Bảng tóm tắt tất cả TC. Format: `priority | type | module | title`.

| TC-ID | Pri | Loại | Module | Tiêu đề | Severity nếu fail |
|-------|-----|------|--------|---------|-------------------|
| [TC-001](#tc-001) | 🔴 P0 | `GREEN` | Create | Tạo Case DIRECT_DISCOVERY thành công với input đầy đủ | 🚨 Critical |
| [TC-002](#tc-002) | 🔴 P0 | `GREEN` | Create | Tạo Case FROM_PETITION link Petition đang status TIEP_NHAN + expectedPetitionUpdatedAt khớp | 🚨 Critical |
| [TC-003](#tc-003) | 🔴 P0 | `GREEN` | Create | Tạo Case FROM_INCIDENT link Incident chưa khởi tố + expectedIncidentUpdatedAt khớp | 🚨 Critical |
| [TC-004](#tc-004) | 🔴 P0 | `GREEN` | Create | Tạo Case TRANSFERRED với sourceDocumentNote | ⚠️ High |
| [TC-005](#tc-005) | 🔴 P0 | `GREEN` | Create | Tạo Case OTHER_LEGAL_SOURCE | ⚠️ High |
| [TC-006](#tc-006) | 🔴 P0 | `GREEN` | Create | Tạo Case với capDoToiPham=DAC_BIET_NGHIEM_TRONG để KPI-4 tính | ⚠️ High |
| [TC-007](#tc-007) | 🔴 P0 | `GREEN` | Create | Auto-deadline tính theo SystemSetting THOI_HAN_GIAI_QUYET | ⚠️ High |
| [TC-008](#tc-008) | 🔴 P0 | `GREEN` | Create | Tạo Case manual deadline override auto-deadline | ⚠️ High |
| [TC-009](#tc-009) | 🔴 P0 | `GREEN` | Create | Tạo Case với metadata custom (người báo tin) | ⚠️ High |
| [TC-010](#tc-010) | 🔴 P0 | `GREEN` | Create | Ward officer auto-set assignedTeamId theo wardTeamId (silent override) | ⚠️ High |
| [TC-017](#tc-017) | 🔴 P0 | `RED` | Create-name | Thiếu trường name (required) trả 400 | 🚨 Critical |
| [TC-018](#tc-018) | 🔴 P0 | `RED` | Create-name | name là chuỗi rỗng '' trả 400 'Tên vụ án bắt buộc' | 🚨 Critical |
| [TC-019](#tc-019) | 🔴 P0 | `RED` | Create-name | name chỉ whitespace '   ' bị trim thành '' → 400 | 🚨 Critical |
| [TC-020](#tc-020) | 🔴 P0 | `RED` | Create-name | name là null → 400 | 🚨 Critical |
| [TC-021](#tc-021) | 🔴 P0 | `RED` | Create-name | name là số 12345 không phải string → 400 IsString | ⚠️ High |
| [TC-022](#tc-022) | 🔴 P0 | `RED` | Create-name | name vượt 500 ký tự (501 chars) → 400 MaxLength | ⚠️ High |
| [TC-025](#tc-025) | 🔴 P0 | `RED` | Create-prov | Thiếu caseProvenance → 400 | 🚨 Critical |
| [TC-026](#tc-026) | 🔴 P0 | `RED` | Create-prov | caseProvenance không phải enum value → 400 | 🚨 Critical |
| [TC-027](#tc-027) | 🔴 P0 | `RED` | Create-prov | caseProvenance là tiếng Việt (legacy payload) → 400 | 🚨 Critical |
| [TC-028](#tc-028) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION nhưng thiếu linkedPetitionId → 400 | 🚨 Critical |
| [TC-029](#tc-029) | 🔴 P0 | `RED` | Create-prov | FROM_INCIDENT thiếu linkedIncidentId → 400 | 🚨 Critical |
| [TC-030](#tc-030) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION thiếu expectedPetitionUpdatedAt → 400 IsISO8601 | 🚨 Critical |
| [TC-031](#tc-031) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION với expectedPetitionUpdatedAt cũ hơn current → 409 Conflict | ⚠️ High |
| [TC-032](#tc-032) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION với linkedPetitionId KHÔNG tồn tại → 404 | ⚠️ High |
| [TC-033](#tc-033) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION với Petition của user KHÁC scope → 404 (không lộ tồn tại) | 🚨 Critical |
| [TC-034](#tc-034) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION với Petition đã có linkedCaseId (đã khởi tố trước) → 404 | 🚨 Critical |
| [TC-035](#tc-035) | 🔴 P0 | `RED` | Create-prov | FROM_PETITION với deletedAt != null → 404 | ⚠️ High |
| [TC-037](#tc-037) | 🔴 P0 | `RED` | Create-inv | investigatorId không tồn tại trong users table → 400 | ⚠️ High |
| [TC-044](#tc-044) | 🔴 P0 | `RED` | Create-deadline | deadline sai format 'không-phải-ngày' → 400 IsDateString | ⚠️ High |
| [TC-048](#tc-048) | 🔴 P0 | `RED` | Create-prov | caseProvenance=DIRECT_DISCOVERY nhưng vẫn truyền linkedPetitionId → field bị ignore | ⚡ Medium |
| [TC-053](#tc-053) | 🔴 P0 | `RED` | Create-perm | User KHÔNG có permission write/Case (role VIEWER) → 403 | 🚨 Critical |
| [TC-054](#tc-054) | 🔴 P0 | `RED` | Create-auth | POST không có Authorization header → 401 | 🚨 Critical |
| [TC-055](#tc-055) | 🔴 P0 | `RED` | Create-auth | POST JWT đã hết hạn → 401 | 🚨 Critical |
| [TC-056](#tc-056) | 🔴 P0 | `RED` | Create-auth | POST JWT bị tamper signature → 401 | 🚨 Critical |
| [TC-061](#tc-061) | 🔴 P0 | `RED` | Create-name | name='\t\t\t' (3 tab) bị trim thành '' → 400 | ⚠️ High |
| [TC-062](#tc-062) | 🔴 P0 | `RED` | Create-name | name='\n\n\n' (3 newline) bị trim → 400 | ⚠️ High |
| [TC-064](#tc-064) | 🔴 P0 | `RED` | Create-prov | Race condition: 2 user đồng thời FROM_PETITION cùng PET-001 → 1 ConflictException | 🚨 Critical |
| [TC-067](#tc-067) | 🔴 P0 | `GREEN` | List | GET /cases trả paginated 20 items default sorted createdAt desc | ⚠️ High |
| [TC-068](#tc-068) | 🔴 P0 | `GREEN` | List | GET /cases?limit=50 trả tối đa 50 | ⚡ Medium |
| [TC-069](#tc-069) | 🔴 P0 | `RED` | List | GET /cases?limit=101 → 400 @Max(100) | ⚡ Medium |
| [TC-073](#tc-073) | 🔴 P0 | `GREEN` | List | Filter status=DANG_DIEU_TRA chỉ trả Case status đó | ⚠️ High |
| [TC-075](#tc-075) | 🔴 P0 | `GREEN` | List | Filter overdue=true trả case quá hạn | ⚠️ High |
| [TC-076](#tc-076) | 🔴 P0 | `GREEN` | List | Filter overdue=true KHÔNG include Case DA_KET_LUAN dù deadline đã qua | ⚠️ High |
| [TC-077](#tc-077) | 🔴 P0 | `GREEN` | List | Search 'Trộm cắp' tìm case có name/crime/unit chứa từ này (case-insensitive) | ⚠️ High |
| [TC-078](#tc-078) | 🔴 P0 | `GREEN` | List | Search với chữ có dấu vs không dấu (Unicode Vietnamese) | ⚡ Medium |
| [TC-086](#tc-086) | 🔴 P0 | `GREEN` | Detail | GET /cases/:id trả full detail có investigator + petitions include | 🚨 Critical |
| [TC-087](#tc-087) | 🔴 P0 | `RED` | Detail | GET /cases/NOT_EXIST → 404 | ⚠️ High |
| [TC-088](#tc-088) | 🔴 P0 | `RED` | Detail | GET /cases/:id của Case đã soft-deleted → 404 (deletedAt IS NOT NULL) | ⚠️ High |
| [TC-089](#tc-089) | 🔴 P0 | `RED` | Detail | GET /cases/:id của Case ngoài scope → 403 | 🚨 Critical |
| [TC-090](#tc-090) | 🔴 P0 | `GREEN` | Detail | Dispatcher xem mọi Case không bị scope chặn | ⚠️ High |
| [TC-091](#tc-091) | 🔴 P0 | `GREEN` | Detail | ADMIN xem mọi Case (no scope) | ⚠️ High |
| [TC-092](#tc-092) | 🔴 P0 | `GREEN` | Detail | Owner (investigator) xem được Case mình phụ trách | ⚠️ High |
| [TC-093](#tc-093) | 🔴 P0 | `GREEN` | Detail | Cùng team xem được Case của đồng nghiệp | ⚠️ High |
| [TC-097](#tc-097) | 🔴 P0 | `GREEN` | Update | Update name + crime case mình owner | 🚨 Critical |
| [TC-098](#tc-098) | 🔴 P0 | `GREEN` | Update | Update với expectedUpdatedAt khớp current → success | ⚠️ High |
| [TC-099](#tc-099) | 🔴 P0 | `RED` | Update | Update với expectedUpdatedAt cũ → 409 Conflict | 🚨 Critical |
| [TC-100](#tc-100) | 🔴 P0 | `RED` | Update | Update Case ngoài write-scope → 403 | 🚨 Critical |
| [TC-101](#tc-101) | 🔴 P0 | `RED` | Update | Update Case không tồn tại → 404 | ⚠️ High |
| [TC-102](#tc-102) | 🔴 P0 | `RED` | Update | Update Case đã soft-deleted → 404 | ⚠️ High |
| [TC-103](#tc-103) | 🔴 P0 | `GREEN` | Update | Update status TIEP_NHAN → DANG_XAC_MINH tạo CaseStatusHistory + audit | 🚨 Critical |
| [TC-104](#tc-104) | 🔴 P0 | `GREEN` | Update | Update status DANG_DIEU_TRA → TAM_DINH_CHI với lyDoTamDinhChiVuAn auto-set ngày + count | 🚨 Critical |
| [TC-105](#tc-105) | 🔴 P0 | `RED` | Update | Update → TAM_DINH_CHI thiếu lyDoTamDinhChiVuAn (Case post-migration) → 400 | 🚨 Critical |
| [TC-107](#tc-107) | 🔴 P0 | `RED` | Update | lyDoTamDinhChiVuAn = invalid enum 'INVALID_REASON' → 400 | ⚠️ High |
| [TC-108](#tc-108) | 🔴 P0 | `GREEN` | Update | Update status TAM_DINH_CHI → DANG_DIEU_TRA (phục hồi) với daRaSoat + ngayRaSoat | 🚨 Critical |
| [TC-118](#tc-118) | 🔴 P0 | `GREEN` | Update | Audit CASE_UPDATED capture before/after diff đầy đủ | ⚠️ High |
| [TC-124](#tc-124) | 🔴 P0 | `GREEN` | Update | DTO whitelist reject field lạ trong UpdateCaseDto (forbidNonWhitelisted) | ⚡ Medium |
| [TC-125](#tc-125) | 🔴 P0 | `STATE` | Update-status | Transition TIEP_NHAN → DANG_XAC_MINH (valid) | ⚠️ High |
| [TC-126](#tc-126) | 🔴 P0 | `STATE` | Update-status | Transition DANG_XAC_MINH → DA_XAC_MINH (valid) | ⚠️ High |
| [TC-127](#tc-127) | 🔴 P0 | `STATE` | Update-status | Transition DA_XAC_MINH → DANG_DIEU_TRA (valid) | ⚠️ High |
| [TC-128](#tc-128) | 🔴 P0 | `STATE` | Update-status | Transition DANG_DIEU_TRA → DA_KET_LUAN (valid) | ⚠️ High |
| [TC-129](#tc-129) | 🔴 P0 | `STATE` | Update-status | Transition DA_KET_LUAN → DANG_TRUY_TO (valid) | ⚠️ High |
| [TC-130](#tc-130) | 🔴 P0 | `STATE` | Update-status | Transition DANG_TRUY_TO → DANG_XET_XU (valid) | ⚠️ High |
| [TC-131](#tc-131) | 🔴 P0 | `STATE` | Update-status | Transition DANG_XET_XU → DA_LUU_TRU (final state) | ⚠️ High |
| [TC-132](#tc-132) | 🔴 P0 | `STATE` | Update-status | Transition any → DINH_CHI (đình chỉ vĩnh viễn) | ⚠️ High |
| [TC-133](#tc-133) | 🔴 P0 | `STATE` | Update-status | Transition TAM_DINH_CHI → DA_KET_LUAN (phục hồi và kết luận luôn) | ⚠️ High |
| [TC-136](#tc-136) | 🔴 P0 | `STATE` | Update-status | DINH_CHI → DANG_DIEU_TRA (sai logic) — cần block ở backend | ⚠️ High |
| [TC-137](#tc-137) | 🔴 P0 | `STATE` | Update-status | DANG_XET_XU → TIEP_NHAN (rollback xa) — invalid logic | ⚠️ High |
| [TC-138](#tc-138) | 🔴 P0 | `STATE` | Update-status | TAM_DINH_CHI lần thứ 2 (recurrent suspend) — soLanTamDinhChi=2 | ⚠️ High |
| [TC-139](#tc-139) | 🔴 P0 | `GREEN` | Delete | Soft delete Case TIEP_NHAN, reason 50 chars, creator → success | 🚨 Critical |
| [TC-140](#tc-140) | 🔴 P0 | `RED` | Delete | Reason < 10 ký tự → 400 | ⚠️ High |
| [TC-141](#tc-141) | 🔴 P0 | `RED` | Delete | Reason > 500 ký tự → 400 | ⚠️ High |
| [TC-142](#tc-142) | 🔴 P0 | `RED` | Delete | Thiếu reason → 400 | ⚠️ High |
| [TC-143](#tc-143) | 🔴 P0 | `RED` | Delete | Reason rỗng '' → 400 | ⚠️ High |
| [TC-144](#tc-144) | 🔴 P0 | `RED` | Delete | Reason chỉ whitespace '          ' → 400 (Transform trim) | ⚠️ High |
| [TC-145](#tc-145) | 🔴 P0 | `RED` | Delete | Status ≠ TIEP_NHAN (DANG_DIEU_TRA) → 400 'Chỉ xóa được vụ án ở trạng thái Tiếp nhận' | 🚨 Critical |
| [TC-146](#tc-146) | 🔴 P0 | `RED` | Delete | Có ≥1 subject linked → 400 'có N đối tượng. Xóa các đối tượng trước' | ⚠️ High |
| [TC-147](#tc-147) | 🔴 P0 | `RED` | Delete | Có ≥1 lawyer linked → 400 'có N luật sư' | ⚠️ High |
| [TC-148](#tc-148) | 🔴 P0 | `RED` | Delete | Có ≥1 conclusion → 400 | ⚠️ High |
| [TC-149](#tc-149) | 🔴 P0 | `RED` | Delete | Có ≥1 document → 400 | ⚠️ High |
| [TC-150](#tc-150) | 🔴 P0 | `RED` | Delete | Có ≥1 linkedIncident → 400 | ⚠️ High |
| [TC-151](#tc-151) | 🔴 P0 | `RED` | Delete | User không phải creator + không phải ADMIN → 403 | 🚨 Critical |
| [TC-152](#tc-152) | 🔴 P0 | `RED` | Delete | createdById=NULL (legacy data) + user không ADMIN → 403 message riêng | ⚠️ High |
| [TC-153](#tc-153) | 🔴 P0 | `GREEN` | Delete | ADMIN xóa được Case của bất kỳ ai trong window | ⚠️ High |
| [TC-154](#tc-154) | 🔴 P0 | `RED` | Delete | Quá window (THOI_HAN_XOA_VU_AN=72h default) + không ADMIN → 400 | ⚠️ High |
| [TC-155](#tc-155) | 🔴 P0 | `GREEN` | Delete | ADMIN có thể xóa Case quá 72h window | ⚠️ High |
| [TC-156](#tc-156) | 🔴 P0 | `RED` | Delete | Race condition: status đổi trong lúc DELETE → P2025 catch → 400 friendly | ⚠️ High |
| [TC-157](#tc-157) | 🔴 P0 | `RED` | Delete | TOCTOU: subject vừa được insert giữa initial check và transaction → 400 trong tx | 🚨 Critical |
| [TC-158](#tc-158) | 🔴 P0 | `GREEN` | DeletePreflight | GET /cases/:id/delete-preflight trả canDelete=true cho TIEP_NHAN + không blocker | ⚠️ High |
| [TC-159](#tc-159) | 🔴 P0 | `RED` | DeletePreflight | Preflight Case status=DA_KET_LUAN trả canDelete=false + reason | ⚠️ High |
| [TC-160](#tc-160) | 🔴 P0 | `RED` | DeletePreflight | Preflight Case có 2 subjects + 1 document → reasons detailed | ⚡ Medium |
| [TC-161](#tc-161) | 🔴 P0 | `RED` | DeletePreflight | Preflight Case ngoài scope → 403 (checkRecordInScope) | ⚠️ High |
| [TC-162](#tc-162) | 🔴 P0 | `GREEN` | Restore | ADMIN khôi phục Case đã soft-deleted | 🚨 Critical |
| [TC-163](#tc-163) | 🔴 P0 | `RED` | Restore | Non-ADMIN gọi restore → 403 PermissionsGuard (action=restore) | 🚨 Critical |
| [TC-164](#tc-164) | 🔴 P0 | `RED` | Restore | Restore Case chưa từng bị xóa (deletedAt=null) → 404 | ⚠️ High |
| [TC-165](#tc-165) | 🔴 P0 | `RED` | Restore | Race: 2 ADMIN restore cùng case → 1 ConflictException friendly | ⚠️ High |
| [TC-167](#tc-167) | 🔴 P0 | `GREEN` | ListDeleted | ADMIN GET /cases/admin/deleted trả paginated deleted cases với deleteAudit | ⚠️ High |
| [TC-168](#tc-168) | 🔴 P0 | `RED` | ListDeleted | Non-ADMIN gọi /cases/admin/deleted → 403 | 🚨 Critical |
| [TC-169](#tc-169) | 🔴 P0 | `GREEN` | Assign | Dispatcher phân công Case sang Team-Q2 + investigator của team | 🚨 Critical |
| [TC-170](#tc-170) | 🔴 P0 | `RED` | Assign | Non-dispatcher gọi assign → 403 DispatchGuard | 🚨 Critical |
| [TC-171](#tc-171) | 🔴 P0 | `RED` | Assign | Case ngoài scope của dispatcher (nếu dispatcher có scope giới hạn) → ? | ⚠️ High |
| [TC-172](#tc-172) | 🔴 P0 | `RED` | Assign | assignedTeamId team đã isActive=false → 400 'Tổ điều tra không tồn tại hoặc đã ngừng hoạt động' | ⚠️ High |
| [TC-173](#tc-173) | 🔴 P0 | `RED` | Assign | investigatorId không thuộc team được chỉ định → 400 'Điều tra viên không thuộc tổ' | ⚠️ High |
| [TC-175](#tc-175) | 🔴 P0 | `RED` | Assign | Case không tồn tại → 404 | ⚠️ High |
| [TC-176](#tc-176) | 🔴 P0 | `RED` | Assign | Case đã soft-deleted → 404 | ⚠️ High |
| [TC-177](#tc-177) | 🔴 P0 | `RED` | Assign | expectedUpdatedAt mismatch → 409 | ⚠️ High |
| [TC-178](#tc-178) | 🔴 P0 | `GREEN` | Assign | Escalation: ward team → non-ward team → emit CASE_ESCALATED_FROM_WARD audit | ⚠️ High |
| [TC-184](#tc-184) | 🔴 P0 | `RED` | Assign | Non-dispatcher với JWT của dispatcher (token theft) → permission guard pass? — JWT check role thật | ⚠️ High |
| [TC-185](#tc-185) | 🔴 P0 | `GREEN` | Export | GET /cases/export/ward trả file Excel với header BCA + 8 cột | ⚠️ High |
| [TC-186](#tc-186) | 🔴 P0 | `GREEN` | Export | Export ward với fromDate + toDate trong period | ⚡ Medium |
| [TC-187](#tc-187) | 🔴 P0 | `GREEN` | Export | Export 'phân loại khác' với category filter | ⚡ Medium |
| [TC-188](#tc-188) | 🔴 P0 | `RED` | Export | Rate limit: gọi /export/ward 6 lần trong 60s → 429 | ⚠️ High |
| [TC-189](#tc-189) | 🔴 P0 | `RED` | Export | Export với scope filter — ward officer chỉ thấy case của ward mình | 🚨 Critical |
| [TC-190](#tc-190) | 🔴 P0 | `RED` | Export | Dispatcher xem tất cả case khi export | ⚠️ High |
| [TC-192](#tc-192) | 🔴 P0 | `RED` | Export | Permission: user role VIEWER không có 'read Case' → 403 | ⚠️ High |
| [TC-193](#tc-193) | 🔴 P0 | `RED` | Export | JWT thiếu → 401 | 🚨 Critical |
| [TC-197](#tc-197) | 🔴 P0 | `GREEN` | History | GET /cases/:id/status-history trả list sorted asc changedAt | ⚠️ High |
| [TC-199](#tc-199) | 🔴 P0 | `RED` | History | GET history của Case không tồn tại → 200 với data=[] (no 404 hiện tại) | ⚡ Medium |
| [TC-201](#tc-201) | 🔴 P0 | `RED` | History | User ngoài scope GET history → ? | ⚠️ High |
| [TC-203](#tc-203) | 🔴 P0 | `GREEN` | TdcBackfill | PATCH /cases/:id/tdc-backfill update lyDoTamDinhChiVuAn | ⚠️ High |
| [TC-204](#tc-204) | 🔴 P0 | `RED` | TdcBackfill | PATCH với Case không tồn tại → 404 'Case not found' | ⚠️ High |
| [TC-206](#tc-206) | 🔴 P0 | `RED` | TdcBackfill | User không có write permission → 403 | ⚠️ High |
| [TC-208](#tc-208) | 🔴 P0 | `SECURITY` | A01 | IDOR: User A trực tiếp GET /cases/<id-của-B> (URL guessing) → 403 | 🚨 Critical |
| [TC-209](#tc-209) | 🔴 P0 | `SECURITY` | A01 | Horizontal privilege escalation: dieuTra-Q1 sửa Case-Q3 → 403 | 🚨 Critical |
| [TC-210](#tc-210) | 🔴 P0 | `SECURITY` | A01 | Vertical: VIEWER role thử POST /cases → 403 | 🚨 Critical |
| [TC-211](#tc-211) | 🔴 P0 | `SECURITY` | A01 | DELETE Case của user khác (creator check) → 403 | 🚨 Critical |
| [TC-212](#tc-212) | 🔴 P0 | `SECURITY` | A01 | Non-ADMIN restore → 403 | 🚨 Critical |
| [TC-213](#tc-213) | 🔴 P0 | `SECURITY` | A01 | GET /cases/admin/deleted với non-ADMIN → 403 | 🚨 Critical |
| [TC-214](#tc-214) | 🔴 P0 | `SECURITY` | A01 | Non-dispatcher PATCH /assign → 403 DispatchGuard | 🚨 Critical |
| [TC-215](#tc-215) | 🔴 P0 | `SECURITY` | A02 | JWT là HS256 với secret strong (≥32 chars) — không yếu | 🚨 Critical |
| [TC-216](#tc-216) | 🔴 P0 | `SECURITY` | A02 | API chạy trên HTTPS (production) — HTTP redirect 301 | 🚨 Critical |
| [TC-217](#tc-217) | 🔴 P0 | `SECURITY` | A03 | SQLi qua search param: ?search=' OR 1=1-- → vẫn an toàn | 🚨 Critical |
| [TC-218](#tc-218) | 🔴 P0 | `SECURITY` | A03 | SQLi qua sortBy: ?sortBy=name; DROP TABLE cases-- → whitelist filter | 🚨 Critical |
| [TC-219](#tc-219) | 🔴 P0 | `SECURITY` | A03 | XSS reflected: name=<script>alert(1)</script> → React escape khi render | 🚨 Critical |
| [TC-220](#tc-220) | 🔴 P0 | `SECURITY` | A03 | XSS stored: crime=<img src=x onerror=alert(1)> render detail → escape | 🚨 Critical |
| [TC-221](#tc-221) | 🔴 P0 | `SECURITY` | A03 | XSS DOM: search query có script trong URL → no eval | ⚠️ High |
| [TC-222](#tc-222) | 🔴 P0 | `SECURITY` | A03 | JSON injection vào metadata: metadata={__proto__:{isAdmin:true}} | ⚠️ High |
| [TC-223](#tc-223) | 🔴 P0 | `SECURITY` | A04 | Export rate limit ngăn data exfiltration | ⚠️ High |
| [TC-224](#tc-224) | 🔴 P0 | `SECURITY` | A04 | Soft delete với reason mandatory ngăn data lost without trace | ⚠️ High |
| [TC-225](#tc-225) | 🔴 P0 | `SECURITY` | A04 | Restore window check (legacy NULL createdById → ADMIN-only) | ⚠️ High |
| [TC-226](#tc-226) | 🔴 P0 | `SECURITY` | A05 | CORS header restrict origin (không *) | 🚨 Critical |
| [TC-227](#tc-227) | 🔴 P0 | `SECURITY` | A05 | Response không có sensitive header X-Powered-By, Server version | ⚡ Medium |
| [TC-228](#tc-228) | 🔴 P0 | `SECURITY` | A05 | Security headers: HSTS, X-Content-Type-Options, X-Frame-Options | ⚠️ High |
| [TC-229](#tc-229) | 🔴 P0 | `SECURITY` | A07 | JWT hết hạn → 401, không refresh tự động infinite | 🚨 Critical |
| [TC-230](#tc-230) | 🔴 P0 | `SECURITY` | A07 | JWT signature tamper → 401 | 🚨 Critical |
| [TC-231](#tc-231) | 🔴 P0 | `SECURITY` | A07 | JWT của user đã deactivated/locked → 401 hoặc 403 | ⚠️ High |
| [TC-232](#tc-232) | 🔴 P0 | `SECURITY` | A08 | Optimistic lock prevent lost update qua expectedUpdatedAt | 🚨 Critical |
| [TC-233](#tc-233) | 🔴 P0 | `SECURITY` | A08 | FROM_PETITION race condition không double-link Petition | 🚨 Critical |
| [TC-234](#tc-234) | 🔴 P0 | `SECURITY` | A09 | Audit log CASE_DELETED ghi đầy đủ user, IP, UA, reason | ⚠️ High |
| [TC-235](#tc-235) | 🔴 P0 | `SECURITY` | A09 | Audit không lưu sensitive data (password, JWT) trong metadata | ⚠️ High |
| [TC-236](#tc-236) | 🔴 P0 | `SECURITY` | A09 | Audit retention ≥ 90 ngày (BLTTHS compliance) | ⚡ Medium |
| [TC-238](#tc-238) | 🔴 P0 | `SECURITY` | RateLimit | POST /cases brute force 100 req/s → throttle global | ⚠️ High |
| [TC-239](#tc-239) | 🔴 P0 | `SECURITY` | CSRF | Mutation POST/PUT/DELETE từ origin lạ không có cookie + custom header → reject CORS | ⚠️ High |
| [TC-240](#tc-240) | 🔴 P0 | `SECURITY` | MassAssign | POST kèm field 'createdById':'other-user' bị whitelist reject | 🚨 Critical |
| [TC-241](#tc-241) | 🔴 P0 | `SECURITY` | MassAssign | POST kèm 'id':'fake-id' không cho phép override | 🚨 Critical |
| [TC-242](#tc-242) | 🔴 P0 | `SECURITY` | MassAssign | POST kèm 'deletedAt':null override không cho phép | ⚠️ High |
| [TC-301](#tc-301) | 🔴 P0 | `BOUNDARY` | name | name 1 ký tự 'A' (min) → pass | ⚡ Medium |
| [TC-302](#tc-302) | 🔴 P0 | `BOUNDARY` | name | name 500 ký tự (max) → pass | ⚡ Medium |
| [TC-303](#tc-303) | 🔴 P0 | `BOUNDARY` | name | name 501 ký tự → 400 | ⚠️ High |
| [TC-304](#tc-304) | 🔴 P0 | `BOUNDARY` | name | name 499 ký tự → pass | 📌 Low |
| [TC-333](#tc-333) | 🔴 P0 | `DECISION` | DataScope-C1 | ADMIN (dataScope=null) xem mọi Case | 🚨 Critical |
| [TC-334](#tc-334) | 🔴 P0 | `DECISION` | DataScope-C2 | Dispatcher (canDispatch=true) xem mọi Case | 🚨 Critical |
| [TC-335](#tc-335) | 🔴 P0 | `DECISION` | DataScope-C3 | Owner (investigatorId match userIds) xem Case mình | ⚠️ High |
| [TC-336](#tc-336) | 🔴 P0 | `DECISION` | DataScope-C4 | Same team (assignedTeamId in teamIds) xem Case đồng nghiệp | ⚠️ High |
| [TC-337](#tc-337) | 🔴 P0 | `DECISION` | DataScope-C5 | Unassigned Case + user có team (teamIds.length>0) | ⚡ Medium |
| [TC-338](#tc-338) | 🔴 P0 | `DECISION` | DataScope-C6 | User khác team + không owner → 403 | 🚨 Critical |
| [TC-339](#tc-339) | 🔴 P0 | `DECISION` | WriteScope-C1 | Owner write Case mình → success | ⚠️ High |
| [TC-340](#tc-340) | 🔴 P0 | `DECISION` | WriteScope-C2 | Same writableTeam write → success | ⚠️ High |
| [TC-341](#tc-341) | 🔴 P0 | `DECISION` | WriteScope-C3 | Different team write → 403 | 🚨 Critical |
| [TC-342](#tc-342) | 🔴 P0 | `DECISION` | Delete-Decision | Decision matrix delete: TIEP_NHAN + creator + within 72h → success | ⚠️ High |
| [TC-343](#tc-343) | 🔴 P0 | `DECISION` | Delete-Decision | Decision: TIEP_NHAN + creator + >72h + non-ADMIN → 400 | ⚠️ High |
| [TC-344](#tc-344) | 🔴 P0 | `DECISION` | Delete-Decision | Decision: TIEP_NHAN + non-creator + non-ADMIN → 403 | ⚠️ High |
| [TC-345](#tc-345) | 🔴 P0 | `DECISION` | Delete-Decision | Decision: ≠TIEP_NHAN + ADMIN → 400 (status check first) | ⚠️ High |
| [TC-346](#tc-346) | 🔴 P0 | `DECISION` | Delete-Decision | Decision: TIEP_NHAN + ADMIN + creator NULL + within window → success | ⚡ Medium |
| [TC-347](#tc-347) | 🔴 P0 | `INTEGRATION` | E2E | Flow: Petition → Khởi tố vụ án → Thêm subject → Update status → Kết luận | 🚨 Critical |
| [TC-348](#tc-348) | 🔴 P0 | `INTEGRATION` | E2E | Flow: Incident → khởi tố Case → Assign → Delete fail → Restore | ⚠️ High |
| [TC-349](#tc-349) | 🔴 P0 | `INTEGRATION` | E2E | Bulk create Case + sequential STT trên Petition (DT-2026-NNNNN) | ⚠️ High |
| [TC-361](#tc-361) | 🔴 P0 | `AUDIT` | Log | CASE_CREATED log có userId, action, subject='Case', subjectId, metadata, ipAddress, userAgent, createdAt | ⚠️ High |
| [TC-362](#tc-362) | 🔴 P0 | `AUDIT` | Log | CASE_UPDATED audit có diff before/after (wrapUpdate) | ⚠️ High |
| [TC-363](#tc-363) | 🔴 P0 | `AUDIT` | Log | CASE_STATUS_CHANGED audit riêng entry với fromStatus/toStatus/changedAt | ⚠️ High |
| [TC-364](#tc-364) | 🔴 P0 | `AUDIT` | Log | CASE_DELETED có reason + hoursAfterCreation | ⚠️ High |
| [TC-365](#tc-365) | 🔴 P0 | `AUDIT` | Log | CASE_RESTORED có hoursAfterDeletion | ⚠️ High |
| [TC-366](#tc-366) | 🔴 P0 | `AUDIT` | Log | CASE_ASSIGNED có fromTeamId/toTeamId/fromInvestigatorId/toInvestigatorId/dispatchedBy | ⚠️ High |
| [TC-367](#tc-367) | 🔴 P0 | `AUDIT` | Log | CASE_EXPORTED có filters đầy đủ trong metadata | ⚡ Medium |
| [TC-368](#tc-368) | 🔴 P0 | `GREEN` | Subjects | Tạo Subject với CCCD 12 chữ số hợp lệ | 🚨 Critical |
| [TC-369](#tc-369) | 🔴 P0 | `GREEN` | Subjects | Tạo Subject với CCCD 9 chữ số (CMND cũ) | ⚠️ High |
| [TC-370](#tc-370) | 🔴 P0 | `RED` | Subjects | CCCD 8 chữ số → 400 regex fail | ⚠️ High |
| [TC-371](#tc-371) | 🔴 P0 | `RED` | Subjects | CCCD 13 chữ số → 400 | ⚠️ High |
| [TC-372](#tc-372) | 🔴 P0 | `RED` | Subjects | CCCD có chữ cái 'ABC079090123' → 400 regex | ⚠️ High |
| [TC-373](#tc-373) | 🔴 P0 | `RED` | Subjects | CCCD rỗng → 400 IsNotEmpty | ⚠️ High |
| [TC-374](#tc-374) | 🔴 P0 | `RED` | Subjects | fullName rỗng → 400 | ⚠️ High |
| [TC-375](#tc-375) | 🔴 P0 | `RED` | Subjects | dateOfBirth rỗng → 400 | ⚠️ High |
| [TC-376](#tc-376) | 🔴 P0 | `RED` | Subjects | dateOfBirth='15/01/1990' không ISO → 400 | ⚠️ High |
| [TC-377](#tc-377) | 🔴 P0 | `RED` | Subjects | dateOfBirth='2030-01-01' tương lai → ? (gap) | ⚡ Medium |
| [TC-378](#tc-378) | 🔴 P0 | `RED` | Subjects | address rỗng → 400 | ⚠️ High |
| [TC-379](#tc-379) | 🔴 P0 | `RED` | Subjects | caseId rỗng → 400 | ⚠️ High |
| [TC-380](#tc-380) | 🔴 P0 | `RED` | Subjects | caseId không tồn tại → 400 FK invalid | ⚠️ High |
| [TC-381](#tc-381) | 🔴 P0 | `RED` | Subjects | caseId của Case soft-deleted → 400 | ⚠️ High |
| [TC-382](#tc-382) | 🔴 P0 | `RED` | Subjects | crimeId rỗng → 400 | ⚠️ High |
| [TC-383](#tc-383) | 🔴 P0 | `RED` | Subjects | crimeId không tồn tại trong directory → 400 | ⚠️ High |
| [TC-392](#tc-392) | 🔴 P0 | `GREEN` | Subjects | GET /subjects?caseId=C001 trả subjects của Case | ⚠️ High |
| [TC-393](#tc-393) | 🔴 P0 | `GREEN` | Subjects | GET /subjects/:id detail include lawyers liên kết | ⚠️ High |
| [TC-394](#tc-394) | 🔴 P0 | `RED` | Subjects | GET /subjects/:id Subject ngoài scope → 403 | 🚨 Critical |
| [TC-395](#tc-395) | 🔴 P0 | `RED` | Subjects | GET /subjects/NOT_EXIST → 404 | ⚠️ High |
| [TC-396](#tc-396) | 🔴 P0 | `GREEN` | Subjects | PUT update fullName của Subject | ⚠️ High |
| [TC-397](#tc-397) | 🔴 P0 | `RED` | Subjects | PUT Subject ngoài write scope → 403 | 🚨 Critical |
| [TC-398](#tc-398) | 🔴 P0 | `RED` | Subjects | PUT Subject soft-deleted → 404 | ⚠️ High |
| [TC-399](#tc-399) | 🔴 P0 | `GREEN` | Subjects | DELETE Subject soft delete + Case.subjectsCount giảm | 🚨 Critical |
| [TC-400](#tc-400) | 🔴 P0 | `RED` | Subjects | DELETE Subject ngoài scope → 403 | 🚨 Critical |
| [TC-401](#tc-401) | 🔴 P0 | `RED` | Subjects | DELETE Subject không tồn tại → 404 | ⚠️ High |
| [TC-402](#tc-402) | 🔴 P0 | `RED` | Subjects | DELETE Subject đã soft-deleted (idempotent) → 404 hoặc 200 | ⚡ Medium |
| [TC-403](#tc-403) | 🔴 P0 | `SECURITY` | Subjects | IDOR: POST subject với caseId của Case khác user → 403/400 | 🚨 Critical |
| [TC-404](#tc-404) | 🔴 P0 | `SECURITY` | Subjects | XSS qua fullName='<script>alert(1)</script>' → React escape | 🚨 Critical |
| [TC-405](#tc-405) | 🔴 P0 | `SECURITY` | Subjects | SQL injection qua idNumber bypass regex (impossible vì regex strict) | ⚠️ High |
| [TC-413](#tc-413) | 🔴 P0 | `STATE` | Subjects | Transition INVESTIGATING → DETAINED (bắt tạm giam) | ⚠️ High |
| [TC-414](#tc-414) | 🔴 P0 | `STATE` | Subjects | Transition DETAINED → RELEASED (thả) | ⚠️ High |
| [TC-415](#tc-415) | 🔴 P0 | `STATE` | Subjects | Transition INVESTIGATING → WANTED (chuyển truy nã) | ⚠️ High |
| [TC-420](#tc-420) | 🔴 P0 | `RED` | Subjects | Duplicate idNumber + caseId + type (constraint unique per type per case) | ⚠️ High |
| [TC-422](#tc-422) | 🔴 P0 | `AUDIT` | Subjects | DELETE Subject ghi audit với name + reason | ⚠️ High |
| [TC-430](#tc-430) | 🔴 P0 | `RED` | Subjects | User VIEWER không có write/Subject → 403 | 🚨 Critical |
| [TC-431](#tc-431) | 🔴 P0 | `RED` | Subjects | JWT thiếu → 401 | 🚨 Critical |
| [TC-439](#tc-439) | 🔴 P0 | `AUDIT` | Subjects | SUBJECT_CREATED log có caseId trong metadata | ⚠️ High |
| [TC-440](#tc-440) | 🔴 P0 | `GREEN` | Lawyers | Tạo Lawyer với barNumber unique | ⚠️ High |
| [TC-441](#tc-441) | 🔴 P0 | `GREEN` | Lawyers | Tạo Lawyer gắn subjectId (bào chữa cho subject cụ thể) | ⚠️ High |
| [TC-443](#tc-443) | 🔴 P0 | `RED` | Lawyers | fullName rỗng → 400 | ⚠️ High |
| [TC-444](#tc-444) | 🔴 P0 | `RED` | Lawyers | barNumber rỗng → 400 | ⚠️ High |
| [TC-445](#tc-445) | 🔴 P0 | `RED` | Lawyers | barNumber duplicate (đã tồn tại) → 409 unique constraint | ⚠️ High |
| [TC-446](#tc-446) | 🔴 P0 | `RED` | Lawyers | caseId rỗng → 400 | ⚠️ High |
| [TC-447](#tc-447) | 🔴 P0 | `RED` | Lawyers | caseId không tồn tại → 400 FK | ⚠️ High |
| [TC-448](#tc-448) | 🔴 P0 | `RED` | Lawyers | subjectId không tồn tại trong DB → 400 FK | ⚠️ High |
| [TC-450](#tc-450) | 🔴 P0 | `GREEN` | Lawyers | GET /lawyers?caseId=C001 trả lawyers của Case | ⚠️ High |
| [TC-451](#tc-451) | 🔴 P0 | `GREEN` | Lawyers | GET /lawyers?subjectId=S001 trả lawyers của subject | ⚡ Medium |
| [TC-452](#tc-452) | 🔴 P0 | `GREEN` | Lawyers | GET /lawyers/:id detail include subject info | ⚠️ High |
| [TC-453](#tc-453) | 🔴 P0 | `RED` | Lawyers | GET /lawyers/NOT_EXIST → 404 | ⚠️ High |
| [TC-454](#tc-454) | 🔴 P0 | `RED` | Lawyers | GET Lawyer ngoài scope (Case không thuộc team mình) → 403 | 🚨 Critical |
| [TC-455](#tc-455) | 🔴 P0 | `GREEN` | Lawyers | PUT update lawFirm + phone | ⚠️ High |
| [TC-456](#tc-456) | 🔴 P0 | `RED` | Lawyers | PUT barNumber duplicate → 409 | ⚠️ High |
| [TC-457](#tc-457) | 🔴 P0 | `RED` | Lawyers | PUT Lawyer ngoài write scope → 403 | 🚨 Critical |
| [TC-458](#tc-458) | 🔴 P0 | `GREEN` | Lawyers | DELETE Lawyer soft delete | ⚠️ High |
| [TC-459](#tc-459) | 🔴 P0 | `RED` | Lawyers | DELETE Lawyer không tồn tại → 404 | ⚠️ High |
| [TC-460](#tc-460) | 🔴 P0 | `RED` | Lawyers | DELETE Lawyer ngoài scope → 403 | 🚨 Critical |
| [TC-461](#tc-461) | 🔴 P0 | `SECURITY` | Lawyers | IDOR: POST Lawyer với caseId ngoài team → 403 | 🚨 Critical |
| [TC-462](#tc-462) | 🔴 P0 | `SECURITY` | Lawyers | XSS qua fullName='<svg onload=alert(1)>' | 🚨 Critical |
| [TC-463](#tc-463) | 🔴 P0 | `SECURITY` | Lawyers | SQL injection qua barNumber | ⚠️ High |
| [TC-473](#tc-473) | 🔴 P0 | `RED` | Lawyers | User VIEWER POST → 403 | 🚨 Critical |
| [TC-474](#tc-474) | 🔴 P0 | `RED` | Lawyers | JWT thiếu → 401 | 🚨 Critical |
| [TC-475](#tc-475) | 🔴 P0 | `AUDIT` | Lawyers | LAWYER_CREATED log có barNumber + caseId | ⚠️ High |
| [TC-484](#tc-484) | 🔴 P0 | `GREEN` | Conclusions | Tạo Conclusion với type+content+caseId | 🚨 Critical |
| [TC-485](#tc-485) | 🔴 P0 | `RED` | Conclusions | caseId thiếu → 400 IsString | ⚠️ High |
| [TC-486](#tc-486) | 🔴 P0 | `RED` | Conclusions | caseId không tồn tại → 400 FK | ⚠️ High |
| [TC-487](#tc-487) | 🔴 P0 | `RED` | Conclusions | type rỗng → 400 | ⚠️ High |
| [TC-488](#tc-488) | 🔴 P0 | `RED` | Conclusions | content rỗng → 400 | ⚠️ High |
| [TC-489](#tc-489) | 🔴 P0 | `GREEN` | Conclusions | Tạo Conclusion với approvedById | ⚠️ High |
| [TC-490](#tc-490) | 🔴 P0 | `RED` | Conclusions | approvedById không tồn tại → 400 FK | ⚠️ High |
| [TC-491](#tc-491) | 🔴 P0 | `GREEN` | Conclusions | Default status khi POST không kèm | ⚡ Medium |
| [TC-493](#tc-493) | 🔴 P0 | `GREEN` | Conclusions | GET /conclusions?caseId=C001 list | ⚠️ High |
| [TC-494](#tc-494) | 🔴 P0 | `GREEN` | Conclusions | GET /conclusions/:id detail | ⚠️ High |
| [TC-495](#tc-495) | 🔴 P0 | `RED` | Conclusions | GET conclusion ngoài scope (Case khác team) → 403 | 🚨 Critical |
| [TC-496](#tc-496) | 🔴 P0 | `RED` | Conclusions | GET /conclusions/NOT_EXIST → 404 | ⚠️ High |
| [TC-497](#tc-497) | 🔴 P0 | `GREEN` | Conclusions | PUT update content | ⚠️ High |
| [TC-498](#tc-498) | 🔴 P0 | `RED` | Conclusions | PUT conclusion đã APPROVED không cho sửa | ⚠️ High |
| [TC-499](#tc-499) | 🔴 P0 | `GREEN` | Conclusions | DELETE conclusion soft delete | ⚠️ High |
| [TC-500](#tc-500) | 🔴 P0 | `RED` | Conclusions | DELETE conclusion đã APPROVED → 400 | ⚠️ High |
| [TC-501](#tc-501) | 🔴 P0 | `STATE` | Conclusions | Transition DRAFT → SUBMITTED (gửi đi phê duyệt) | ⚠️ High |
| [TC-502](#tc-502) | 🔴 P0 | `STATE` | Conclusions | Transition SUBMITTED → APPROVED (truong don vi duyet) | ⚠️ High |
| [TC-503](#tc-503) | 🔴 P0 | `STATE` | Conclusions | Transition SUBMITTED → REJECTED | ⚡ Medium |
| [TC-504](#tc-504) | 🔴 P0 | `STATE` | Conclusions | Transition REJECTED → DRAFT (chỉnh sửa lại) | ⚡ Medium |
| [TC-505](#tc-505) | 🔴 P0 | `RED` | Conclusions | APPROVED → DRAFT (downgrade) → block | ⚠️ High |
| [TC-506](#tc-506) | 🔴 P0 | `SECURITY` | Conclusions | User thường không có quyền APPROVE (không phải TRUONG_DON_VI) → 403 | 🚨 Critical |
| [TC-507](#tc-507) | 🔴 P0 | `SECURITY` | Conclusions | IDOR: POST conclusion với caseId của team khác | 🚨 Critical |
| [TC-508](#tc-508) | 🔴 P0 | `SECURITY` | Conclusions | XSS qua content='<script>' | 🚨 Critical |
| [TC-511](#tc-511) | 🔴 P0 | `INTEGRATION` | Conclusions | Conclusion APPROVED → Case status auto-update DA_KET_LUAN | 🚨 Critical |
| [TC-512](#tc-512) | 🔴 P0 | `AUDIT` | Conclusions | CONCLUSION_APPROVED log có approvedById + timestamp | ⚠️ High |
| [TC-517](#tc-517) | 🔴 P0 | `RED` | Conclusions | User VIEWER POST → 403 | ⚠️ High |
| [TC-518](#tc-518) | 🔴 P0 | `RED` | Conclusions | JWT thiếu → 401 | ⚠️ High |
| [TC-523](#tc-523) | 🔴 P0 | `GREEN` | Documents | Upload PDF 5MB qua POST /documents | 🚨 Critical |
| [TC-524](#tc-524) | 🔴 P0 | `GREEN` | Documents | Upload Word .docx | ⚠️ High |
| [TC-525](#tc-525) | 🔴 P0 | `GREEN` | Documents | Upload image JPG | ⚠️ High |
| [TC-526](#tc-526) | 🔴 P0 | `GREEN` | Documents | Upload PNG | ⚠️ High |
| [TC-527](#tc-527) | 🔴 P0 | `RED` | Documents | Upload file > 10MB (mặc định limit) → 413 | ⚠️ High |
| [TC-528](#tc-528) | 🔴 P0 | `RED` | Documents | Upload .exe → block by mimetype | 🚨 Critical |
| [TC-529](#tc-529) | 🔴 P0 | `RED` | Documents | Upload .sh script → block | 🚨 Critical |
| [TC-530](#tc-530) | 🔴 P0 | `RED` | Documents | Upload .js → block | 🚨 Critical |
| [TC-531](#tc-531) | 🔴 P0 | `SECURITY` | Documents | Upload PDF nhưng đổi extension thành .pdf (magic byte check) | 🚨 Critical |
| [TC-532](#tc-532) | 🔴 P0 | `SECURITY` | Documents | Path traversal qua originalName '../../../etc/passwd' | 🚨 Critical |
| [TC-533](#tc-533) | 🔴 P0 | `SECURITY` | Documents | Filename có null byte 'evil\0.jpg' | ⚠️ High |
| [TC-534](#tc-534) | 🔴 P0 | `SECURITY` | Documents | ZIP slip nếu support upload .zip | ⚠️ High |
| [TC-535](#tc-535) | 🔴 P0 | `RED` | Documents | title rỗng → 400 | ⚠️ High |
| [TC-536](#tc-536) | 🔴 P0 | `GREEN` | Documents | caseId optional — không kèm cũng OK (general document) | ⚡ Medium |
| [TC-537](#tc-537) | 🔴 P0 | `GREEN` | Documents | incidentId thay vì caseId (gắn với Incident) | ⚡ Medium |
| [TC-538](#tc-538) | 🔴 P0 | `RED` | Documents | Cả caseId VÀ incidentId → 400 'Chỉ chọn 1' | ⚡ Medium |
| [TC-540](#tc-540) | 🔴 P0 | `GREEN` | Documents | GET /documents?caseId=C001 list documents của Case | ⚠️ High |
| [TC-541](#tc-541) | 🔴 P0 | `GREEN` | Documents | GET /documents/:id metadata only (chưa download file) | ⚠️ High |
| [TC-542](#tc-542) | 🔴 P0 | `GREEN` | Documents | GET /documents/:id/download trả file binary | 🚨 Critical |
| [TC-543](#tc-543) | 🔴 P0 | `RED` | Documents | Download document ngoài scope → 403 | 🚨 Critical |
| [TC-544](#tc-544) | 🔴 P0 | `RED` | Documents | Download document đã soft-deleted → 404 | ⚠️ High |
| [TC-545](#tc-545) | 🔴 P0 | `RED` | Documents | Download bằng GET /uploads/file.pdf trực tiếp → 401/403 | 🚨 Critical |
| [TC-546](#tc-546) | 🔴 P0 | `GREEN` | Documents | DELETE document soft delete | ⚠️ High |
| [TC-547](#tc-547) | 🔴 P0 | `RED` | Documents | DELETE document ngoài scope → 403 | 🚨 Critical |
| [TC-548](#tc-548) | 🔴 P0 | `GREEN` | Documents | PUT update title + description | ⚠️ High |
| [TC-549](#tc-549) | 🔴 P0 | `RED` | Documents | PUT thay đổi fileName/filePath bị forbidNonWhitelisted reject | ⚠️ High |
| [TC-550](#tc-550) | 🔴 P0 | `SECURITY` | Documents | XSS qua originalName '<script>alert</script>.pdf' | ⚠️ High |
| [TC-551](#tc-551) | 🔴 P0 | `SECURITY` | Documents | CSV injection qua title '=cmd|calc' | ⚡ Medium |
| [TC-563](#tc-563) | 🔴 P0 | `SECURITY` | Documents | Quota: user upload nhiều file → check storage limit | ⚠️ High |
| [TC-564](#tc-564) | 🔴 P0 | `AUDIT` | Documents | DOCUMENT_CREATED có fileName + size + mimeType | ⚠️ High |
| [TC-565](#tc-565) | 🔴 P0 | `AUDIT` | Documents | DOCUMENT_DOWNLOADED audit mỗi lần download | ⚠️ High |
| [TC-566](#tc-566) | 🔴 P0 | `AUDIT` | Documents | DOCUMENT_DELETED có original fileName | ⚠️ High |
| [TC-571](#tc-571) | 🔴 P0 | `RED` | Documents | Multi-file upload (nếu hỗ trợ) — concurrent | ⚡ Medium |
| [TC-573](#tc-573) | 🔴 P0 | `INTEGRATION` | Documents | Delete Case bị block khi có Document linked | ⚠️ High |
| [TC-576](#tc-576) | 🔴 P0 | `RED` | Documents | VIEWER không có write/Document → 403 POST | ⚠️ High |
| [TC-577](#tc-577) | 🔴 P0 | `RED` | Documents | JWT thiếu POST → 401 | ⚠️ High |
| [TC-581](#tc-581) | 🔴 P0 | `GREEN` | Proposals | Tạo Proposal với proposalNumber + content | ⚠️ High |
| [TC-582](#tc-582) | 🔴 P0 | `RED` | Proposals | proposalNumber rỗng → 400 | ⚠️ High |
| [TC-583](#tc-583) | 🔴 P0 | `RED` | Proposals | content rỗng → 400 | ⚠️ High |
| [TC-584](#tc-584) | 🔴 P0 | `RED` | Proposals | proposalNumber duplicate → 409 unique | ⚠️ High |
| [TC-586](#tc-586) | 🔴 P0 | `RED` | Proposals | relatedCaseId không tồn tại → 400 FK | ⚠️ High |
| [TC-587](#tc-587) | 🔴 P0 | `GREEN` | Proposals | Tạo Proposal với sentDate | ⚡ Medium |
| [TC-589](#tc-589) | 🔴 P0 | `GREEN` | Proposals | GET /proposals list paginated | ⚠️ High |
| [TC-590](#tc-590) | 🔴 P0 | `GREEN` | Proposals | GET /proposals/:id detail | ⚠️ High |
| [TC-591](#tc-591) | 🔴 P0 | `RED` | Proposals | GET proposal ngoài scope → 403 | 🚨 Critical |
| [TC-592](#tc-592) | 🔴 P0 | `RED` | Proposals | GET /proposals/NOT_EXIST → 404 | ⚠️ High |
| [TC-593](#tc-593) | 🔴 P0 | `GREEN` | Proposals | PUT update response + responseDate | ⚠️ High |
| [TC-594](#tc-594) | 🔴 P0 | `GREEN` | Proposals | DELETE soft delete | ⚠️ High |
| [TC-595](#tc-595) | 🔴 P0 | `STATE` | Proposals | Transition PENDING → SENT → RESPONDED | ⚠️ High |
| [TC-596](#tc-596) | 🔴 P0 | `STATE` | Proposals | Transition SENT → CANCELLED | ⚡ Medium |
| [TC-598](#tc-598) | 🔴 P0 | `SECURITY` | Proposals | IDOR: POST relatedCaseId của team khác → 403 | 🚨 Critical |
| [TC-599](#tc-599) | 🔴 P0 | `SECURITY` | Proposals | XSS qua content | ⚠️ High |
| [TC-602](#tc-602) | 🔴 P0 | `AUDIT` | Proposals | PROPOSAL_CREATED audit log | ⚠️ High |
| [TC-603](#tc-603) | 🔴 P0 | `AUDIT` | Proposals | PROPOSAL_RESPONDED log với response timestamp | ⚠️ High |
| [TC-610](#tc-610) | 🔴 P0 | `RED` | Proposals | VIEWER POST → 403 | ⚠️ High |
| [TC-611](#tc-611) | 🔴 P0 | `RED` | Proposals | JWT thiếu → 401 | ⚠️ High |
| [TC-616](#tc-616) | 🔴 P0 | `GREEN` | Delegations | Tạo Delegation với delegationNumber + receivingUnit | ⚠️ High |
| [TC-617](#tc-617) | 🔴 P0 | `RED` | Delegations | delegationNumber rỗng → 400 | ⚠️ High |
| [TC-618](#tc-618) | 🔴 P0 | `RED` | Delegations | receivingUnit rỗng → 400 | ⚠️ High |
| [TC-619](#tc-619) | 🔴 P0 | `RED` | Delegations | content rỗng → 400 | ⚠️ High |
| [TC-620](#tc-620) | 🔴 P0 | `RED` | Delegations | delegationNumber duplicate → 409 | ⚠️ High |
| [TC-621](#tc-621) | 🔴 P0 | `GREEN` | Delegations | relatedCaseId optional | ⚡ Medium |
| [TC-622](#tc-622) | 🔴 P0 | `RED` | Delegations | relatedCaseId không tồn tại → 400 | ⚠️ High |
| [TC-623](#tc-623) | 🔴 P0 | `GREEN` | Delegations | GET list paginated | ⚠️ High |
| [TC-624](#tc-624) | 🔴 P0 | `GREEN` | Delegations | GET detail | ⚠️ High |
| [TC-625](#tc-625) | 🔴 P0 | `RED` | Delegations | GET ngoài scope → 403 | 🚨 Critical |
| [TC-626](#tc-626) | 🔴 P0 | `RED` | Delegations | GET NOT_EXIST → 404 | ⚠️ High |
| [TC-627](#tc-627) | 🔴 P0 | `GREEN` | Delegations | PUT update content + completedDate | ⚠️ High |
| [TC-628](#tc-628) | 🔴 P0 | `GREEN` | Delegations | DELETE soft delete | ⚠️ High |
| [TC-629](#tc-629) | 🔴 P0 | `STATE` | Delegations | Transition PENDING → IN_PROGRESS | ⚠️ High |
| [TC-630](#tc-630) | 🔴 P0 | `STATE` | Delegations | Transition IN_PROGRESS → COMPLETED | ⚠️ High |
| [TC-631](#tc-631) | 🔴 P0 | `STATE` | Delegations | Transition any → CANCELLED | ⚡ Medium |
| [TC-632](#tc-632) | 🔴 P0 | `RED` | Delegations | COMPLETED → PENDING (downgrade) → block | ⚠️ High |
| [TC-633](#tc-633) | 🔴 P0 | `SECURITY` | Delegations | IDOR relatedCaseId team khác | 🚨 Critical |
| [TC-634](#tc-634) | 🔴 P0 | `SECURITY` | Delegations | XSS content | ⚠️ High |
| [TC-639](#tc-639) | 🔴 P0 | `AUDIT` | Delegations | DELEGATION_CREATED log | ⚠️ High |
| [TC-640](#tc-640) | 🔴 P0 | `AUDIT` | Delegations | DELEGATION_COMPLETED log với completedDate | ⚠️ High |
| [TC-647](#tc-647) | 🔴 P0 | `RED` | Delegations | VIEWER POST → 403 | ⚠️ High |
| [TC-648](#tc-648) | 🔴 P0 | `RED` | Delegations | JWT thiếu → 401 | ⚠️ High |
| [TC-652](#tc-652) | 🔴 P0 | `GREEN` | Supplements | Tạo Supplement caseId + decisionNumber + reason + deadline | ⚠️ High |
| [TC-653](#tc-653) | 🔴 P0 | `RED` | Supplements | caseId rỗng → 400 | ⚠️ High |
| [TC-654](#tc-654) | 🔴 P0 | `RED` | Supplements | type rỗng → 400 | ⚠️ High |
| [TC-655](#tc-655) | 🔴 P0 | `RED` | Supplements | decisionNumber rỗng → 400 | ⚠️ High |
| [TC-656](#tc-656) | 🔴 P0 | `RED` | Supplements | reason rỗng → 400 | ⚠️ High |
| [TC-660](#tc-660) | 🔴 P0 | `GREEN` | Supplements | GET list paginated | ⚠️ High |
| [TC-661](#tc-661) | 🔴 P0 | `GREEN` | Supplements | GET detail | ⚠️ High |
| [TC-662](#tc-662) | 🔴 P0 | `RED` | Supplements | GET ngoài scope → 403 | 🚨 Critical |
| [TC-663](#tc-663) | 🔴 P0 | `RED` | Supplements | GET NOT_EXIST → 404 | ⚠️ High |
| [TC-664](#tc-664) | 🔴 P0 | `GREEN` | Supplements | PUT update reason | ⚠️ High |
| [TC-665](#tc-665) | 🔴 P0 | `GREEN` | Supplements | DELETE soft delete | ⚠️ High |
| [TC-666](#tc-666) | 🔴 P0 | `INTEGRATION` | Supplements | Tạo supplement → Case.soLanGiaHan auto-increment | ⚠️ High |
| [TC-667](#tc-667) | 🔴 P0 | `INTEGRATION` | Supplements | Tạo supplement với deadline mới → Case.deadline update | ⚠️ High |
| [TC-668](#tc-668) | 🔴 P0 | `SECURITY` | Supplements | IDOR caseId team khác → 403 | 🚨 Critical |
| [TC-669](#tc-669) | 🔴 P0 | `SECURITY` | Supplements | XSS reason | ⚠️ High |
| [TC-672](#tc-672) | 🔴 P0 | `AUDIT` | Supplements | SUPPLEMENT_CREATED log | ⚠️ High |
| [TC-678](#tc-678) | 🔴 P0 | `RED` | Supplements | VIEWER POST → 403 | ⚠️ High |
| [TC-679](#tc-679) | 🔴 P0 | `RED` | Supplements | JWT thiếu → 401 | ⚠️ High |
| [TC-682](#tc-682) | 🔴 P0 | `GREEN` | PetitionFlow | Petition convertToCase (POST /petitions/:id/convert) | 🚨 Critical |
| [TC-683](#tc-683) | 🔴 P0 | `RED` | PetitionFlow | convertToCase Petition đã linked → 400 'Đã khởi tố rồi' | ⚠️ High |
| [TC-684](#tc-684) | 🔴 P0 | `RED` | PetitionFlow | convertToCase ngoài scope → 403 | 🚨 Critical |
| [TC-685](#tc-685) | 🔴 P0 | `RED` | PetitionFlow | convertToCase Petition soft-deleted → 404 | ⚠️ High |
| [TC-686](#tc-686) | 🔴 P0 | `STATE` | PetitionFlow | Petition.status transition MOI_TIEP_NHAN → DA_CHUYEN_VU_AN (convert) | ⚠️ High |
| [TC-687](#tc-687) | 🔴 P0 | `STATE` | PetitionFlow | Petition.status MOI_TIEP_NHAN → DANG_XU_LY (manual) | ⚠️ High |
| [TC-688](#tc-688) | 🔴 P0 | `STATE` | PetitionFlow | Petition.status DANG_XU_LY → CHO_PHE_DUYET | ⚠️ High |
| [TC-689](#tc-689) | 🔴 P0 | `STATE` | PetitionFlow | Petition.status CHO_PHE_DUYET → DA_GIAI_QUYET | ⚠️ High |
| [TC-690](#tc-690) | 🔴 P0 | `STATE` | PetitionFlow | Petition.status any → DA_LUU_DON (lưu đơn) | ⚡ Medium |
| [TC-691](#tc-691) | 🔴 P0 | `STATE` | PetitionFlow | Petition.status → DA_CHUYEN_VU_VIEC (chuyển vụ việc) | ⚠️ High |
| [TC-692](#tc-692) | 🔴 P0 | `RED` | PetitionFlow | Petition DA_CHUYEN_VU_AN không sửa được | ⚠️ High |
| [TC-693](#tc-693) | 🔴 P0 | `INTEGRATION` | PetitionFlow | Cascade: Update Case.metadata.petitionType → sync Petition.petitionType | ⚠️ High |
| [TC-694](#tc-694) | 🔴 P0 | `INTEGRATION` | PetitionFlow | Update Case không có Petition link KHÔNG tạo phantom Petition | 🚨 Critical |
| [TC-695](#tc-695) | 🔴 P0 | `GREEN` | PetitionFlow | STT auto-generate format DT-YYYY-NNNNN | ⚠️ High |
| [TC-696](#tc-696) | 🔴 P0 | `RED` | PetitionFlow | STT race condition: 2 concurrent create không tạo duplicate STT | 🚨 Critical |
| [TC-697](#tc-697) | 🔴 P0 | `RED` | PetitionFlow | convertToCase Petition status không phải MOI_TIEP_NHAN/DANG_XU_LY → 400 | ⚠️ High |
| [TC-698](#tc-698) | 🔴 P0 | `AUDIT` | PetitionFlow | PETITION_CONVERTED log với linkedCaseId | ⚠️ High |
| [TC-699](#tc-699) | 🔴 P0 | `SECURITY` | PetitionFlow | convertToCase atomic — race 2 user → 1 conflict | 🚨 Critical |
| [TC-702](#tc-702) | 🔴 P0 | `INTEGRATION` | PetitionFlow | Delete Petition KHÔNG delete linked Case (Restrict) | ⚠️ High |
| [TC-703](#tc-703) | 🔴 P0 | `INTEGRATION` | PetitionFlow | Delete Case → Petition.linkedCaseId vẫn giữ (Restrict) | ⚠️ High |
| [TC-705](#tc-705) | 🔴 P0 | `RED` | PetitionFlow | convertToCase VIEWER → 403 | ⚠️ High |
| [TC-706](#tc-706) | 🔴 P0 | `RED` | PetitionFlow | convertToCase JWT thiếu → 401 | ⚠️ High |
| [TC-709](#tc-709) | 🔴 P0 | `STATE` | PetitionFlow | CaseProvenance=FROM_PETITION → Case có linkedPetitionId set | 🚨 Critical |
| [TC-710](#tc-710) | 🔴 P0 | `STATE` | PetitionFlow | CaseProvenance ≠ FROM_PETITION → linkedPetitionId=null | 🚨 Critical |
| [TC-714](#tc-714) | 🔴 P0 | `INTEGRATION` | PetitionFlow | Petition convertToCase preserve provenance traceability | ⚠️ High |
| [TC-715](#tc-715) | 🔴 P0 | `INTEGRATION` | PetitionFlow | E2E: Tạo Petition → review → convert Case → assign team | 🚨 Critical |
| [TC-717](#tc-717) | 🔴 P0 | `GREEN` | IncidentFlow | Incident convertToCase | 🚨 Critical |
| [TC-718](#tc-718) | 🔴 P0 | `RED` | IncidentFlow | Incident đã linked → 400 | ⚠️ High |
| [TC-719](#tc-719) | 🔴 P0 | `RED` | IncidentFlow | Incident ngoài scope → 403 | 🚨 Critical |
| [TC-720](#tc-720) | 🔴 P0 | `STATE` | IncidentFlow | Incident.status MOI_TIEP_NHAN → DANG_XAC_MINH | ⚠️ High |
| [TC-721](#tc-721) | 🔴 P0 | `STATE` | IncidentFlow | Incident → TAM_DINH_CHI | ⚠️ High |
| [TC-722](#tc-722) | 🔴 P0 | `STATE` | IncidentFlow | Incident → PHUC_HOI_NGUON_TIN | ⚠️ High |
| [TC-723](#tc-723) | 🔴 P0 | `STATE` | IncidentFlow | Incident TAM_DINH_CHI → DANG_XAC_MINH (phục hồi) | ⚠️ High |
| [TC-724](#tc-724) | 🔴 P0 | `INTEGRATION` | IncidentFlow | Incident → Case keep linkage (Incident.linkedCaseId) | 🚨 Critical |
| [TC-725](#tc-725) | 🔴 P0 | `INTEGRATION` | IncidentFlow | Delete Incident có linkedCase Restrict | ⚠️ High |
| [TC-726](#tc-726) | 🔴 P0 | `GREEN` | IncidentFlow | CaseProvenance=FROM_INCIDENT → linkedIncidentId set | 🚨 Critical |
| [TC-727](#tc-727) | 🔴 P0 | `RED` | IncidentFlow | FROM_INCIDENT thiếu expectedIncidentUpdatedAt → 400 | ⚠️ High |
| [TC-728](#tc-728) | 🔴 P0 | `SECURITY` | IncidentFlow | Race convertToCase 2 user same Incident → 1×409 | 🚨 Critical |
| [TC-729](#tc-729) | 🔴 P0 | `AUDIT` | IncidentFlow | INCIDENT_CONVERTED audit | ⚠️ High |
| [TC-734](#tc-734) | 🔴 P0 | `RED` | IncidentFlow | VIEWER convert → 403 | ⚠️ High |
| [TC-735](#tc-735) | 🔴 P0 | `RED` | IncidentFlow | JWT thiếu → 401 | ⚠️ High |
| [TC-736](#tc-736) | 🔴 P0 | `STATE` | IncidentFlow | Incident TAM_DINH_CHI_LAI (lần 2 trở đi) | ⚠️ High |
| [TC-740](#tc-740) | 🔴 P0 | `INTEGRATION` | IncidentFlow | Multiple Incidents linked to same Case (1-to-many) | ⚠️ High |
| [TC-741](#tc-741) | 🔴 P0 | `INTEGRATION` | IncidentFlow | Delete Case có linkedIncidents → block 400 | ⚠️ High |
| [TC-742](#tc-742) | 🔴 P0 | `STATE` | IncidentFlow | Incident status transitions match BLTTHS | ⚡ Medium |
| [TC-743](#tc-743) | 🔴 P0 | `SECURITY` | IncidentFlow | IDOR convertToCase với Incident ID team khác | 🚨 Critical |
| [TC-744](#tc-744) | 🔴 P0 | `INTEGRATION` | IncidentFlow | Convert Incident → Case → Subject (E2E) | ⚠️ High |
| [TC-745](#tc-745) | 🔴 P0 | `RED` | IncidentFlow | convertToCase Incident soft-deleted → 404 | ⚠️ High |
| [TC-746](#tc-746) | 🔴 P0 | `AUDIT` | IncidentFlow | INCIDENT_STATUS_CHANGED audit có fromStatus/toStatus | ⚠️ High |
| [TC-747](#tc-747) | 🔴 P0 | `STATE` | Extra | Update status TIEP_NHAN → DA_KET_LUAN skip middle steps | ⚠️ High |
| [TC-748](#tc-748) | 🔴 P0 | `STATE` | Extra | Update status với 5 lần TAM_DINH_CHI ↔ DANG_DIEU_TRA | ⚠️ High |
| [TC-749](#tc-749) | 🔴 P0 | `STATE` | Extra | Update concurrent: 2 user đổi status khác nhau | 🚨 Critical |
| [TC-750](#tc-750) | 🔴 P0 | `DECISION` | Extra | Combination: ADMIN + Case DELETED 1 năm trước → restore OK | ⚠️ High |
| [TC-751](#tc-751) | 🔴 P0 | `DECISION` | Extra | Combination: dispatcher xem case ward → bypass scope | ⚠️ High |
| [TC-752](#tc-752) | 🔴 P0 | `DECISION` | Extra | Combination: ward officer + assignedTeamId override | ⚠️ High |
| [TC-753](#tc-753) | 🔴 P0 | `DECISION` | Extra | User MULTI-TEAM (thuộc 2 teams) — scope union | ⚠️ High |
| [TC-754](#tc-754) | 🔴 P0 | `DECISION` | Extra | User has writableTeamIds ⊂ teamIds (read-only some) | ⚠️ High |
| [TC-755](#tc-755) | 🔴 P0 | `RED` | Extra | forbidNonWhitelisted strict cho mọi DTO update | ⚠️ High |
| [TC-756](#tc-756) | 🔴 P0 | `SECURITY` | Extra | Privilege escalation: change own role qua DTO mass-assign | 🚨 Critical |
| [TC-757](#tc-757) | 🔴 P0 | `SECURITY` | Extra | XSS reflected qua URL search → sanitize | ⚠️ High |
| [TC-758](#tc-758) | 🔴 P0 | `SECURITY` | Extra | Open redirect: ?returnUrl=https://evil.com | ⚠️ High |
| [TC-759](#tc-759) | 🔴 P0 | `SECURITY` | Extra | CSP header restrict inline script | ⚠️ High |
| [TC-760](#tc-760) | 🔴 P0 | `SECURITY` | Extra | Referrer-Policy: strict-origin-when-cross-origin | ⚡ Medium |
| [TC-780](#tc-780) | 🔴 P0 | `AUDIT` | Extra | Audit log không expose internal trace/stack | 🚨 Critical |
| [TC-781](#tc-781) | 🔴 P0 | `INTEGRATION` | Extra | Cross-feature: tạo Case → KPI dashboard refresh count | ⚠️ High |
| [TC-011](#tc-011) | 🟠 P1 | `GREEN` | Create | Tạo Case bỏ trống các field optional | ⚡ Medium |
| [TC-012](#tc-012) | 🟠 P1 | `GREEN` | Create | Tạo Case với investigatorId là user khác đang active | ⚡ Medium |
| [TC-013](#tc-013) | 🟠 P1 | `GREEN` | Create | Tạo Case với ngayKhoiTo lùi vài ngày trước hôm nay (đã khởi tố trước khi vào hệ thống) | ⚡ Medium |
| [TC-014](#tc-014) | 🟠 P1 | `GREEN` | Create | Dispatcher tạo Case có thể chọn assignedTeamId bất kỳ | ⚠️ High |
| [TC-015](#tc-015) | 🟠 P1 | `GREEN` | Create | Tạo Case ADMIN với toàn bộ field bao gồm field hiếm dùng | ⚡ Medium |
| [TC-016](#tc-016) | 🟠 P1 | `GREEN` | Create | POST trả response message tiếng Việt 'Tạo vụ án thành công' | 📌 Low |
| [TC-023](#tc-023) | 🟠 P1 | `RED` | Create-name | name là object JSON → 400 IsString | ⚡ Medium |
| [TC-024](#tc-024) | 🟠 P1 | `RED` | Create-name | name là array → 400 IsString | ⚡ Medium |
| [TC-036](#tc-036) | 🟠 P1 | `RED` | Create-prov | expectedPetitionUpdatedAt sai format (không ISO 8601) → 400 | ⚡ Medium |
| [TC-038](#tc-038) | 🟠 P1 | `RED` | Create-capDo | capDoToiPham là invalid enum 'INVALID' → 400 với message tiếng Việt | ⚡ Medium |
| [TC-039](#tc-039) | 🟠 P1 | `RED` | Create-subC | subjectsCount = -1 → 400 @Min(0) | ⚡ Medium |
| [TC-040](#tc-040) | 🟠 P1 | `RED` | Create-subC | subjectsCount = 1.5 (float) → 400 IsInt | ⚡ Medium |
| [TC-041](#tc-041) | 🟠 P1 | `RED` | Create-subC | subjectsCount = 'abc' string → 400 IsInt | ⚡ Medium |
| [TC-042](#tc-042) | 🟠 P1 | `RED` | Create-crime | crime > 255 ký tự → 400 MaxLength | ⚡ Medium |
| [TC-043](#tc-043) | 🟠 P1 | `RED` | Create-unit | unit > 255 ký tự → 400 | ⚡ Medium |
| [TC-045](#tc-045) | 🟠 P1 | `RED` | Create-deadline | deadline format DD/MM/YYYY → 400 IsDateString chỉ accept ISO | ⚡ Medium |
| [TC-046](#tc-046) | 🟠 P1 | `RED` | Create-ngayKT | ngayKhoiTo='2026-13-45' (tháng 13, ngày 45) → 400 | ⚡ Medium |
| [TC-047](#tc-047) | 🟠 P1 | `RED` | Create-src | sourceDocumentNote > 1000 ký tự → 400 | ⚡ Medium |
| [TC-049](#tc-049) | 🟠 P1 | `RED` | Create-meta | metadata là array thay vì object → 400 IsObject | ⚡ Medium |
| [TC-050](#tc-050) | 🟠 P1 | `RED` | Create-meta | metadata là string → 400 IsObject | ⚡ Medium |
| [TC-051](#tc-051) | 🟠 P1 | `RED` | Create-team | assignedTeamId KHÔNG tồn tại trong teams (FK invalid) → ? | ⚡ Medium |
| [TC-052](#tc-052) | 🟠 P1 | `RED` | Create-wl | POST kèm field lạ 'evilField=hack' với whitelist=true → 400 | ⚡ Medium |
| [TC-057](#tc-057) | 🟠 P1 | `RED` | Create-type | deadline là object → 400 IsDateString | ⚡ Medium |
| [TC-058](#tc-058) | 🟠 P1 | `RED` | Create-type | investigatorId là number 12345 → 400 IsString | ⚡ Medium |
| [TC-059](#tc-059) | 🟠 P1 | `RED` | Create-team | assignedTeamId của team đã isActive=false → field saved nhưng cảnh báo | ⚡ Medium |
| [TC-060](#tc-060) | 🟠 P1 | `RED` | Create-prov | caseProvenance=SELF_SURRENDER cần linkedPetitionId hoặc khác? — check enum | ⚡ Medium |
| [TC-063](#tc-063) | 🟠 P1 | `RED` | Create-name | name=' Vụ ABC ' (có space đầu/cuối) bị trim → save 'Vụ ABC' | ⚡ Medium |
| [TC-065](#tc-065) | 🟠 P1 | `RED` | Create-prov | FROM_INCIDENT với Incident updatedAt mismatch → 409 'Vụ việc đã chỉnh sửa' | ⚠️ High |
| [TC-066](#tc-066) | 🟠 P1 | `RED` | Create-prov | FROM_PETITION + ngayKhoiTo trước Petition.receivedDate? — business logic cần check | 📌 Low |
| [TC-070](#tc-070) | 🟠 P1 | `RED` | List | GET /cases?limit=0 → 400 @Min(1) | ⚡ Medium |
| [TC-071](#tc-071) | 🟠 P1 | `RED` | List | GET /cases?limit=-5 → 400 | ⚡ Medium |
| [TC-072](#tc-072) | 🟠 P1 | `RED` | List | GET /cases?limit=abc (NaN) → 400 @IsInt | ⚡ Medium |
| [TC-074](#tc-074) | 🟠 P1 | `RED` | List | Filter status=INVALID → 400 IsEnum | ⚡ Medium |
| [TC-079](#tc-079) | 🟠 P1 | `GREEN` | List | Filter fromDate='2026-05-01' & toDate='2026-05-23' khoảng ngày | ⚡ Medium |
| [TC-080](#tc-080) | 🟠 P1 | `RED` | List | toDate < fromDate → vẫn trả empty (không 400) | 📌 Low |
| [TC-081](#tc-081) | 🟠 P1 | `GREEN` | List | Filter capDoToiPham=RAT_NGHIEM_TRONG | ⚡ Medium |
| [TC-082](#tc-082) | 🟠 P1 | `GREEN` | List | Filter districtId qua subjects relation | ⚡ Medium |
| [TC-083](#tc-083) | 🟠 P1 | `GREEN` | List | Sort sortBy=name sortOrder=asc | ⚡ Medium |
| [TC-084](#tc-084) | 🟠 P1 | `RED` | List | sortBy='evil_field' → fallback về createdAt (security: prevent SQL injection) | ⚠️ High |
| [TC-085](#tc-085) | 🟠 P1 | `GREEN` | List | Pagination offset=20 trả page 2 | ⚡ Medium |
| [TC-094](#tc-094) | 🟠 P1 | `GREEN` | Detail | User được xem unassigned Case của team mình (assignedTeamId=null + teamIds.length>0) | ⚡ Medium |
| [TC-095](#tc-095) | 🟠 P1 | `GREEN` | List | Filter wardTeamId='WARD-Q5' (cross-ward view PC02) | ⚡ Medium |
| [TC-096](#tc-096) | 🟠 P1 | `GREEN` | List | Filter investigatorId trả case của ĐTV cụ thể | ⚡ Medium |
| [TC-106](#tc-106) | 🟠 P1 | `GREEN` | Update | Update → TAM_DINH_CHI thiếu lý do nhưng Case cũ (pre-migration) → SOFT warning | ⚡ Medium |
| [TC-109](#tc-109) | 🟠 P1 | `RED` | Update | ketQuaPhucHoiVuAn = invalid → 400 | ⚡ Medium |
| [TC-110](#tc-110) | 🟠 P1 | `RED` | Update | lyDoTamDinhChiText > 500 ký tự → 400 | ⚡ Medium |
| [TC-111](#tc-111) | 🟠 P1 | `RED` | Update | soQuyetDinhTamDinhChi > 100 ký tự → 400 | ⚡ Medium |
| [TC-112](#tc-112) | 🟠 P1 | `RED` | Update | daRaSoat = 'yes' string thay vì boolean → 400 IsBoolean | ⚡ Medium |
| [TC-113](#tc-113) | 🟠 P1 | `RED` | Update | ngayRaSoat sai ISO format → 400 | ⚡ Medium |
| [TC-114](#tc-114) | 🟠 P1 | `GREEN` | Update | Update KHÔNG đổi status → KHÔNG tạo CaseStatusHistory entry | ⚡ Medium |
| [TC-115](#tc-115) | 🟠 P1 | `GREEN` | Update | Update status với cùng giá trị status hiện tại → KHÔNG tạo history | ⚡ Medium |
| [TC-116](#tc-116) | 🟠 P1 | `GREEN` | Update | Update metadata.petitionType sync sang Petition liên kết | ⚠️ High |
| [TC-117](#tc-117) | 🟠 P1 | `GREEN` | Update | Update metadata.petitionType mà KHÔNG có Petition link → silent ignore (no phantom create) | ⚠️ High |
| [TC-119](#tc-119) | 🟠 P1 | `RED` | Update | PUT lyDoTamDinhChiVuAn nhưng status không phải TAM_DINH_CHI → vẫn save (no enforce) | 📌 Low |
| [TC-120](#tc-120) | 🟠 P1 | `RED` | Update | investigatorId không tồn tại → 400 | ⚡ Medium |
| [TC-121](#tc-121) | 🟠 P1 | `GREEN` | Update | Update deadline=null xóa deadline cũ | ⚡ Medium |
| [TC-122](#tc-122) | 🟠 P1 | `GREEN` | Update | Update capDoToiPham đổi từ NGHIEM_TRONG sang DAC_BIET_NGHIEM_TRONG | ⚡ Medium |
| [TC-123](#tc-123) | 🟠 P1 | `RED` | Update | Update Case khi user role VIEWER → 403 | ⚠️ High |
| [TC-134](#tc-134) | 🟠 P1 | `STATE` | Update-status | Idempotent: PUT cùng status không tạo history | ⚡ Medium |
| [TC-135](#tc-135) | 🟠 P1 | `STATE` | Update-status | DA_LUU_TRU → DA_KET_LUAN (rollback) — có cho phép? | 📌 Low |
| [TC-166](#tc-166) | 🟠 P1 | `RED` | Restore | Reason rỗng → 400 DTO validation | ⚡ Medium |
| [TC-174](#tc-174) | 🟠 P1 | `GREEN` | Assign | investigatorId=null khi assign (chỉ chuyển team, chưa giao cho ĐTV cụ thể) | ⚠️ High |
| [TC-179](#tc-179) | 🟠 P1 | `GREEN` | Assign | Re-assign cùng team (no-op assignedTeamId không đổi) → audit không emit escalation | ⚡ Medium |
| [TC-180](#tc-180) | 🟠 P1 | `GREEN` | Assign | Non-ward team → ward team (de-escalation) — audit không emit (chỉ from-ward) | ⚡ Medium |
| [TC-181](#tc-181) | 🟠 P1 | `RED` | Assign | assignedTeamId rỗng '' → 400 | ⚡ Medium |
| [TC-182](#tc-182) | 🟠 P1 | `RED` | Assign | Thiếu assignedTeamId → 400 DTO required | ⚠️ High |
| [TC-183](#tc-183) | 🟠 P1 | `GREEN` | Assign | Dispatcher với expectedUpdatedAt KHỚP → success | ⚡ Medium |
| [TC-191](#tc-191) | 🟠 P1 | `GREEN` | Export | Export với take=500 limit cứng (không trả >500 row) | ⚡ Medium |
| [TC-194](#tc-194) | 🟠 P1 | `GREEN` | Export | Export ward không có data → vẫn trả Excel với header + empty body + footer | ⚡ Medium |
| [TC-195](#tc-195) | 🟠 P1 | `RED` | Export | Server crash giữa export (write fail) → response 500 JSON nếu chưa send header | ⚠️ High |
| [TC-196](#tc-196) | 🟠 P1 | `GREEN` | Export | Audit CASE_EXPORTED capture filters đầy đủ | ⚠️ High |
| [TC-198](#tc-198) | 🟠 P1 | `GREEN` | History | Case không có status change → empty array | ⚡ Medium |
| [TC-200](#tc-200) | 🟠 P1 | `GREEN` | History | changedBy=null khi changedById=null (audit cũ chưa có user) | 📌 Low |
| [TC-202](#tc-202) | 🟠 P1 | `GREEN` | History | History entry có changedAt là datetime đầy đủ (ISO) | 📌 Low |
| [TC-205](#tc-205) | 🟠 P1 | `RED` | TdcBackfill | PATCH lyDoTamDinhChiVuAn='INVALID' → 400/500 (validation cast) | ⚡ Medium |
| [TC-207](#tc-207) | 🟠 P1 | `GREEN` | TdcBackfill | Banner TdcBackfillBanner ẩn sau khi backfill thành công | ⚡ Medium |
| [TC-237](#tc-237) | 🟠 P1 | `SECURITY` | A10 | Cases không fetch URL từ user input — N/A SSRF | 📌 Low |
| [TC-243](#tc-243) | 🟠 P1 | `A11Y` | Keyboard | Tab qua đủ field trong CaseFormPage không bị trap | ⚡ Medium |
| [TC-244](#tc-244) | 🟠 P1 | `A11Y` | Keyboard | Enter submit form khi focus ở input | ⚡ Medium |
| [TC-245](#tc-245) | 🟠 P1 | `A11Y` | Keyboard | Esc đóng modal SuspensionModal | ⚡ Medium |
| [TC-246](#tc-246) | 🟠 P1 | `A11Y` | Screenreader | Input có aria-label hoặc label[for] gắn id | ⚠️ High |
| [TC-247](#tc-247) | 🟠 P1 | `A11Y` | Screenreader | Status badge có aria-label='Trạng thái Đang điều tra' | ⚡ Medium |
| [TC-248](#tc-248) | 🟠 P1 | `A11Y` | Screenreader | Error message gắn aria-describedby với input | ⚠️ High |
| [TC-249](#tc-249) | 🟠 P1 | `A11Y` | Contrast | Text trên background đạt 4.5:1 (WCAG AA) | ⚠️ High |
| [TC-250](#tc-250) | 🟠 P1 | `A11Y` | Contrast | Badge status có contrast đủ (text white trên red ≥ 4.5) | ⚡ Medium |
| [TC-251](#tc-251) | 🟠 P1 | `A11Y` | Focus | Focus ring rõ ràng trên nút Lưu (outline 2px) | ⚠️ High |
| [TC-254](#tc-254) | 🟠 P1 | `A11Y` | Form | Form error message tiếng Việt, không dùng chỉ màu để biểu thị error | ⚠️ High |
| [TC-255](#tc-255) | 🟠 P1 | `A11Y` | Heading | Page có h1, h2 hierarchical | ⚡ Medium |
| [TC-256](#tc-256) | 🟠 P1 | `A11Y` | Image | Icon button có aria-label (vd 'Xóa vụ án') | ⚡ Medium |
| [TC-258](#tc-258) | 🟠 P1 | `A11Y` | TableSemantics | Bảng danh sách dùng <table><th scope='col'> | ⚡ Medium |
| [TC-259](#tc-259) | 🟠 P1 | `A11Y` | ModalFocus | Modal mở thì focus trap inside; close trả focus về trigger | ⚠️ High |
| [TC-260](#tc-260) | 🟠 P1 | `COMPAT` | Browser | Chrome 126 latest — toàn bộ flow OK | ⚠️ High |
| [TC-261](#tc-261) | 🟠 P1 | `COMPAT` | Browser | Chrome 125 (latest-1) — backward compat | ⚡ Medium |
| [TC-262](#tc-262) | 🟠 P1 | `COMPAT` | Browser | Firefox 127 latest | ⚡ Medium |
| [TC-263](#tc-263) | 🟠 P1 | `COMPAT` | Browser | Safari 17 latest (macOS) | ⚡ Medium |
| [TC-264](#tc-264) | 🟠 P1 | `COMPAT` | Browser | Edge 126 | ⚡ Medium |
| [TC-267](#tc-267) | 🟠 P1 | `COMPAT` | Mobile | iOS Safari 17 trên iPhone 14 — list page responsive | ⚡ Medium |
| [TC-268](#tc-268) | 🟠 P1 | `COMPAT` | Mobile | Android Chrome 126 trên Pixel 7 | ⚡ Medium |
| [TC-269](#tc-269) | 🟠 P1 | `COMPAT` | Resolution | 1920x1080 desktop — layout standard | ⚡ Medium |
| [TC-270](#tc-270) | 🟠 P1 | `COMPAT` | Resolution | 1366x768 laptop common — không scroll horizontal | ⚡ Medium |
| [TC-271](#tc-271) | 🟠 P1 | `COMPAT` | Resolution | 768x1024 tablet portrait | ⚡ Medium |
| [TC-272](#tc-272) | 🟠 P1 | `COMPAT` | Resolution | 375x667 iPhone SE | ⚡ Medium |
| [TC-273](#tc-273) | 🟠 P1 | `COMPAT` | Network | 3G slow — page load < 5s và loading indicator | ⚡ Medium |
| [TC-275](#tc-275) | 🟠 P1 | `COMPAT` | OS | Windows 11 + Chrome — OK | ⚡ Medium |
| [TC-276](#tc-276) | 🟠 P1 | `COMPAT` | OS | macOS Sonoma + Safari — OK | ⚡ Medium |
| [TC-277](#tc-277) | 🟠 P1 | `PERFORMANCE` | List | GET /cases?limit=20 response < 500ms với 10k row DB | ⚡ Medium |
| [TC-278](#tc-278) | 🟠 P1 | `PERFORMANCE` | Search | GET /cases?search=trộm cắp < 1s với 10k row | ⚡ Medium |
| [TC-279](#tc-279) | 🟠 P1 | `PERFORMANCE` | List | Filter overdue=true + pagination < 800ms | ⚡ Medium |
| [TC-280](#tc-280) | 🟠 P1 | `PERFORMANCE` | Detail | GET /cases/:id (include petitions + investigator) < 300ms | ⚡ Medium |
| [TC-281](#tc-281) | 🟠 P1 | `PERFORMANCE` | Create | POST /cases (FROM_PETITION transaction) < 600ms | ⚡ Medium |
| [TC-282](#tc-282) | 🟠 P1 | `PERFORMANCE` | Export | Export 500 row Excel < 5s | ⚡ Medium |
| [TC-285](#tc-285) | 🟠 P1 | `PERFORMANCE` | Pagination | Deep pagination offset=5000 < 1s (with index) | 📌 Low |
| [TC-287](#tc-287) | 🟠 P1 | `PERFORMANCE` | AuditWrite | Audit log insert không block main response > 50ms | 📌 Low |
| [TC-288](#tc-288) | 🟠 P1 | `DATA` | Unicode | name có dấu tiếng Việt 'Vụ án Nguyễn Quỳnh Trâm' | ⚠️ High |
| [TC-289](#tc-289) | 🟠 P1 | `DATA` | Unicode | name có dấu tổ hợp (Unicode NFC vs NFD) — normalize | ⚡ Medium |
| [TC-290](#tc-290) | 🟠 P1 | `DATA` | Emoji | name có emoji 'Vụ test 🎉' — 4-byte UTF-8 | ⚡ Medium |
| [TC-291](#tc-291) | 🟠 P1 | `DATA` | Whitespace | name='  Vụ ABC  ' leading/trailing trimmed | ⚡ Medium |
| [TC-292](#tc-292) | 🟠 P1 | `DATA` | Hidden | name có zero-width space \u200B → save raw nhưng có thể gây bug filter | 📌 Low |
| [TC-293](#tc-293) | 🟠 P1 | `DATA` | BOM | name='\uFEFFVụ' BOM character ở đầu | 📌 Low |
| [TC-294](#tc-294) | 🟠 P1 | `DATA` | HTML | name='<b>Vụ</b>' tag HTML literal | ⚡ Medium |
| [TC-295](#tc-295) | 🟠 P1 | `DATA` | Date | ngayKhoiTo='2026-02-29' (năm 2026 không nhuận) → 400 hoặc 2026-03-01? | ⚡ Medium |
| [TC-296](#tc-296) | 🟠 P1 | `DATA` | Timezone | ngayKhoiTo='2026-05-23T00:00:00+07:00' (GMT+7 Vietnam) | ⚡ Medium |
| [TC-297](#tc-297) | 🟠 P1 | `DATA` | Number | subjectsCount=Number.MAX_SAFE_INTEGER → save hoặc 400 | 📌 Low |
| [TC-298](#tc-298) | 🟠 P1 | `DATA` | Encoding | Multi-byte filename trong sourceDocumentNote | ⚡ Medium |
| [TC-299](#tc-299) | 🟠 P1 | `DATA` | SpecialChar | name có '&', '<', '>', '"', "'" — SQL escape | ⚡ Medium |
| [TC-300](#tc-300) | 🟠 P1 | `DATA` | LongString | metadata.note 5000 ký tự (JSONB không limit) | 📌 Low |
| [TC-305](#tc-305) | 🟠 P1 | `BOUNDARY` | crime | crime 255 ký tự (max) → pass | ⚡ Medium |
| [TC-306](#tc-306) | 🟠 P1 | `BOUNDARY` | crime | crime 256 ký tự → 400 | ⚡ Medium |
| [TC-307](#tc-307) | 🟠 P1 | `BOUNDARY` | unit | unit 255 ký tự → pass | 📌 Low |
| [TC-308](#tc-308) | 🟠 P1 | `BOUNDARY` | unit | unit 256 ký tự → 400 | 📌 Low |
| [TC-309](#tc-309) | 🟠 P1 | `BOUNDARY` | reason | Delete reason 10 ký tự (min) → pass | ⚡ Medium |
| [TC-310](#tc-310) | 🟠 P1 | `BOUNDARY` | reason | Delete reason 9 ký tự → 400 | ⚡ Medium |
| [TC-311](#tc-311) | 🟠 P1 | `BOUNDARY` | reason | Delete reason 500 ký tự (max) → pass | ⚡ Medium |
| [TC-312](#tc-312) | 🟠 P1 | `BOUNDARY` | reason | Delete reason 501 ký tự → 400 | ⚡ Medium |
| [TC-313](#tc-313) | 🟠 P1 | `BOUNDARY` | subC | subjectsCount = 0 (min) → pass | 📌 Low |
| [TC-314](#tc-314) | 🟠 P1 | `BOUNDARY` | subC | subjectsCount = -1 → 400 @Min(0) | ⚡ Medium |
| [TC-315](#tc-315) | 🟠 P1 | `BOUNDARY` | subC | subjectsCount = 1 → pass | 📌 Low |
| [TC-316](#tc-316) | 🟠 P1 | `BOUNDARY` | limit | limit=1 (min) → pass | 📌 Low |
| [TC-317](#tc-317) | 🟠 P1 | `BOUNDARY` | limit | limit=100 (max) → pass | ⚡ Medium |
| [TC-318](#tc-318) | 🟠 P1 | `BOUNDARY` | limit | limit=101 → 400 | ⚡ Medium |
| [TC-319](#tc-319) | 🟠 P1 | `BOUNDARY` | offset | offset=0 (min) → pass | 📌 Low |
| [TC-320](#tc-320) | 🟠 P1 | `BOUNDARY` | offset | offset=-1 → 400 @Min(0) | ⚡ Medium |
| [TC-321](#tc-321) | 🟠 P1 | `BOUNDARY` | srcDocNote | sourceDocumentNote 1000 ký tự (max) → pass | 📌 Low |
| [TC-322](#tc-322) | 🟠 P1 | `BOUNDARY` | srcDocNote | sourceDocumentNote 1001 → 400 | ⚡ Medium |
| [TC-323](#tc-323) | 🟠 P1 | `EP` | name-cls | name class hợp lệ: chuỗi VN có dấu | 📌 Low |
| [TC-324](#tc-324) | 🟠 P1 | `EP` | name-cls | name class không hợp lệ: rỗng | ⚡ Medium |
| [TC-325](#tc-325) | 🟠 P1 | `EP` | prov-cls | Provenance class valid enum (DIRECT_DISCOVERY) | 📌 Low |
| [TC-326](#tc-326) | 🟠 P1 | `EP` | prov-cls | Provenance class invalid (string ngẫu nhiên) | ⚡ Medium |
| [TC-327](#tc-327) | 🟠 P1 | `EP` | capDo-cls | CapDoToiPham class IT_NGHIEM_TRONG | 📌 Low |
| [TC-328](#tc-328) | 🟠 P1 | `EP` | capDo-cls | CapDoToiPham class NGHIEM_TRONG | 📌 Low |
| [TC-329](#tc-329) | 🟠 P1 | `EP` | capDo-cls | CapDoToiPham class RAT_NGHIEM_TRONG | 📌 Low |
| [TC-330](#tc-330) | 🟠 P1 | `EP` | capDo-cls | CapDoToiPham class DAC_BIET_NGHIEM_TRONG | 📌 Low |
| [TC-331](#tc-331) | 🟠 P1 | `EP` | status-cls | Status valid class TIEP_NHAN | 📌 Low |
| [TC-332](#tc-332) | 🟠 P1 | `EP` | status-cls | Status invalid class 'OPEN' (Anh) | ⚡ Medium |
| [TC-350](#tc-350) | 🟠 P1 | `INTEGRATION` | E2E | Cross-feature: KPI dashboard reload khi Case mới capDoToiPham=DBNT tạo | ⚡ Medium |
| [TC-351](#tc-351) | 🟠 P1 | `INTEGRATION` | E2E | Update Case sync Petition.petitionType | ⚠️ High |
| [TC-352](#tc-352) | 🟠 P1 | `INTEGRATION` | E2E | Assign Case ward → cross-ward → audit event chuỗi | ⚡ Medium |
| [TC-353](#tc-353) | 🟠 P1 | `INTEGRATION` | E2E | Soft delete → list deleted → restore → list active | ⚡ Medium |
| [TC-354](#tc-354) | 🟠 P1 | `INTEGRATION` | E2E | Multi-user race: A update + B delete đồng thời | ⚠️ High |
| [TC-355](#tc-355) | 🟠 P1 | `RECOVERY` | Cancel | User đóng tab giữa form Create — KHÔNG tạo Case partial | ⚡ Medium |
| [TC-356](#tc-356) | 🟠 P1 | `RECOVERY` | Cancel | User refresh giữa form Create — clear state hoặc preserve draft? | 📌 Low |
| [TC-357](#tc-357) | 🟠 P1 | `RECOVERY` | Network | Mất mạng giữa POST /cases — frontend retry hoặc toast error | ⚠️ High |
| [TC-358](#tc-358) | 🟠 P1 | `RECOVERY` | Server500 | Server 500 giữa POST — Case không tạo, audit không log | ⚠️ High |
| [TC-359](#tc-359) | 🟠 P1 | `RECOVERY` | Timeout | Request timeout 30s + retry → idempotency? | ⚠️ High |
| [TC-360](#tc-360) | 🟠 P1 | `RECOVERY` | PartialDelete | Audit fail giữa DELETE tx → rollback delete | 🚨 Critical |
| [TC-384](#tc-384) | 🟠 P1 | `RED` | Subjects | gender='UNKNOWN' không trong enum → 400 | ⚡ Medium |
| [TC-385](#tc-385) | 🟠 P1 | `GREEN` | Subjects | gender bỏ trống → default MALE | ⚡ Medium |
| [TC-386](#tc-386) | 🟠 P1 | `GREEN` | Subjects | type=VICTIM (Bị hại) — không phải SUSPECT default | ⚠️ High |
| [TC-387](#tc-387) | 🟠 P1 | `GREEN` | Subjects | type=WITNESS (Nhân chứng) | ⚡ Medium |
| [TC-388](#tc-388) | 🟠 P1 | `RED` | Subjects | type='INVALID' → 400 | ⚡ Medium |
| [TC-389](#tc-389) | 🟠 P1 | `GREEN` | Subjects | status=DETAINED (Đang tạm giam) | ⚠️ High |
| [TC-390](#tc-390) | 🟠 P1 | `GREEN` | Subjects | status=RELEASED khi đã thả | ⚡ Medium |
| [TC-391](#tc-391) | 🟠 P1 | `GREEN` | Subjects | status=WANTED (Truy nã) | ⚠️ High |
| [TC-406](#tc-406) | 🟠 P1 | `DATA` | Subjects | fullName tiếng Việt có dấu 'Nguyễn Quỳnh Trâm' | ⚡ Medium |
| [TC-407](#tc-407) | 🟠 P1 | `DATA` | Subjects | fullName 200 ký tự (long name) | 📌 Low |
| [TC-408](#tc-408) | 🟠 P1 | `DATA` | Subjects | address Unicode + multi-line | 📌 Low |
| [TC-409](#tc-409) | 🟠 P1 | `BOUNDARY` | Subjects | CCCD đúng 9 chữ → pass min | ⚡ Medium |
| [TC-410](#tc-410) | 🟠 P1 | `BOUNDARY` | Subjects | CCCD đúng 12 chữ → pass max | ⚡ Medium |
| [TC-411](#tc-411) | 🟠 P1 | `BOUNDARY` | Subjects | CCCD 10 chữ → 400 (regex chỉ accept 9 hoặc 12) | ⚠️ High |
| [TC-412](#tc-412) | 🟠 P1 | `BOUNDARY` | Subjects | CCCD 11 chữ → 400 | ⚠️ High |
| [TC-416](#tc-416) | 🟠 P1 | `STATE` | Subjects | Transition WANTED → DETAINED (bắt được rồi) | ⚡ Medium |
| [TC-417](#tc-417) | 🟠 P1 | `A11Y` | Subjects | Form Subject input có label gắn id | ⚡ Medium |
| [TC-418](#tc-418) | 🟠 P1 | `A11Y` | Subjects | Date picker dateOfBirth keyboard accessible | ⚡ Medium |
| [TC-419](#tc-419) | 🟠 P1 | `PERFORMANCE` | Subjects | GET /subjects?caseId=X với Case có 50 subjects < 500ms | ⚡ Medium |
| [TC-421](#tc-421) | 🟠 P1 | `GREEN` | Subjects | Cùng idNumber khác type (SUSPECT vs VICTIM) trong cùng Case OK | ⚡ Medium |
| [TC-423](#tc-423) | 🟠 P1 | `RED` | Subjects | PUT thay đổi caseId (chuyển vụ án) → có cho phép? | ⚡ Medium |
| [TC-424](#tc-424) | 🟠 P1 | `RED` | Subjects | occupationId không tồn tại directory → 400 | ⚡ Medium |
| [TC-425](#tc-425) | 🟠 P1 | `RED` | Subjects | nationalityId không tồn tại → 400 | ⚡ Medium |
| [TC-426](#tc-426) | 🟠 P1 | `RED` | Subjects | wardId không tồn tại → 400 | ⚡ Medium |
| [TC-427](#tc-427) | 🟠 P1 | `GREEN` | Subjects | districtName denormalized preserve sau cải cách hành chính | ⚡ Medium |
| [TC-428](#tc-428) | 🟠 P1 | `GREEN` | Subjects | Subject Filter by status=DETAINED | ⚡ Medium |
| [TC-429](#tc-429) | 🟠 P1 | `GREEN` | Subjects | Subject Filter by type=SUSPECT | ⚡ Medium |
| [TC-432](#tc-432) | 🟠 P1 | `BOUNDARY` | Subjects | fullName 1 ký tự 'A' | 📌 Low |
| [TC-433](#tc-433) | 🟠 P1 | `BOUNDARY` | Subjects | fullName 1000 ký tự (no max) | 📌 Low |
| [TC-434](#tc-434) | 🟠 P1 | `DATA` | Subjects | phone number format VN '0901234567' | ⚡ Medium |
| [TC-435](#tc-435) | 🟠 P1 | `DATA` | Subjects | phone với ký tự đặc biệt '+84 901 234 567' | 📌 Low |
| [TC-436](#tc-436) | 🟠 P1 | `INTEGRATION` | Subjects | Subject + Lawyer assign: thêm Lawyer cho 1 Subject cụ thể | ⚡ Medium |
| [TC-437](#tc-437) | 🟠 P1 | `INTEGRATION` | Subjects | Delete Subject có Lawyer link — Lawyer.subjectId=NULL (SetNull) | ⚠️ High |
| [TC-438](#tc-438) | 🟠 P1 | `RECOVERY` | Subjects | Atomic Subject create rollback nếu audit fail | ⚠️ High |
| [TC-442](#tc-442) | 🟠 P1 | `GREEN` | Lawyers | Lawyer không gắn subjectId (bào chữa chung) | ⚡ Medium |
| [TC-449](#tc-449) | 🟠 P1 | `RED` | Lawyers | subjectId không cùng caseId (cross-case) → 400 business rule | ⚠️ High |
| [TC-464](#tc-464) | 🟠 P1 | `DATA` | Lawyers | fullName tiếng Việt 'Luật sư Đỗ Thị Thanh Mai' | ⚡ Medium |
| [TC-465](#tc-465) | 🟠 P1 | `DATA` | Lawyers | lawFirm Unicode 'Văn phòng Luật sư Hà Nội & Cộng sự' | 📌 Low |
| [TC-466](#tc-466) | 🟠 P1 | `DATA` | Lawyers | phone số Quốc tế '+84 901 234 567' | 📌 Low |
| [TC-467](#tc-467) | 🟠 P1 | `RED` | Lawyers | barNumber chứa whitespace 'LS 2026 12345' → save raw (gap) | 📌 Low |
| [TC-468](#tc-468) | 🟠 P1 | `STATE` | Lawyers | Lawyer được assign sang Subject khác (re-assign) | ⚡ Medium |
| [TC-469](#tc-469) | 🟠 P1 | `STATE` | Lawyers | Unassign Lawyer khỏi Subject (subjectId=null) | ⚡ Medium |
| [TC-470](#tc-470) | 🟠 P1 | `INTEGRATION` | Lawyers | Soft-delete Subject → Lawyer.subjectId auto null (onDelete SetNull) | ⚠️ High |
| [TC-471](#tc-471) | 🟠 P1 | `A11Y` | Lawyers | Form barNumber có pattern hint | 📌 Low |
| [TC-472](#tc-472) | 🟠 P1 | `PERFORMANCE` | Lawyers | GET /lawyers với 100 lawyer DB < 500ms | ⚡ Medium |
| [TC-476](#tc-476) | 🟠 P1 | `BOUNDARY` | Lawyers | fullName 1 ký tự | 📌 Low |
| [TC-477](#tc-477) | 🟠 P1 | `BOUNDARY` | Lawyers | barNumber 1 ký tự | 📌 Low |
| [TC-478](#tc-478) | 🟠 P1 | `BOUNDARY` | Lawyers | fullName 500 ký tự | 📌 Low |
| [TC-479](#tc-479) | 🟠 P1 | `RED` | Lawyers | POST với forbidNonWhitelisted field 'evil':'x' → 400 | ⚡ Medium |
| [TC-480](#tc-480) | 🟠 P1 | `RED` | Lawyers | phone format không validate (gap) | 📌 Low |
| [TC-481](#tc-481) | 🟠 P1 | `GREEN` | Lawyers | Filter search by fullName | ⚡ Medium |
| [TC-482](#tc-482) | 🟠 P1 | `GREEN` | Lawyers | Pagination limit/offset | ⚡ Medium |
| [TC-483](#tc-483) | 🟠 P1 | `RED` | Lawyers | limit=200 → ? (max validation cần check) | 📌 Low |
| [TC-492](#tc-492) | 🟠 P1 | `RED` | Conclusions | status='INVALID' → 400 IsEnum | ⚡ Medium |
| [TC-509](#tc-509) | 🟠 P1 | `DATA` | Conclusions | content rất dài (10K characters) | 📌 Low |
| [TC-510](#tc-510) | 🟠 P1 | `DATA` | Conclusions | content có format Markdown | 📌 Low |
| [TC-513](#tc-513) | 🟠 P1 | `A11Y` | Conclusions | Form content textarea có aria-label | 📌 Low |
| [TC-514](#tc-514) | 🟠 P1 | `PERFORMANCE` | Conclusions | Save conclusion với content 5K chars < 1s | ⚡ Medium |
| [TC-515](#tc-515) | 🟠 P1 | `BOUNDARY` | Conclusions | content 1 ký tự → pass | 📌 Low |
| [TC-516](#tc-516) | 🟠 P1 | `BOUNDARY` | Conclusions | content 100K ký tự → ? (no max limit) | 📌 Low |
| [TC-519](#tc-519) | 🟠 P1 | `GREEN` | Conclusions | Filter status=DRAFT | ⚡ Medium |
| [TC-520](#tc-520) | 🟠 P1 | `GREEN` | Conclusions | notes optional bỏ trống | 📌 Low |
| [TC-521](#tc-521) | 🟠 P1 | `DATA` | Conclusions | type tiếng Việt 'Kết luận đề nghị truy tố' | 📌 Low |
| [TC-522](#tc-522) | 🟠 P1 | `INTEGRATION` | Conclusions | Delete Case bị block khi có Conclusion linked | ⚠️ High |
| [TC-539](#tc-539) | 🟠 P1 | `RED` | Documents | documentType='INVALID' enum → 400 | ⚡ Medium |
| [TC-552](#tc-552) | 🟠 P1 | `DATA` | Documents | title tiếng Việt có dấu | 📌 Low |
| [TC-553](#tc-553) | 🟠 P1 | `DATA` | Documents | filename Unicode 'tài liệu công văn.pdf' | ⚡ Medium |
| [TC-554](#tc-554) | 🟠 P1 | `DATA` | Documents | filename emoji '📄report.pdf' | 📌 Low |
| [TC-555](#tc-555) | 🟠 P1 | `BOUNDARY` | Documents | File 0 byte (empty) → 400 | ⚡ Medium |
| [TC-556](#tc-556) | 🟠 P1 | `BOUNDARY` | Documents | File 1 byte (smallest non-empty) → 201 | 📌 Low |
| [TC-557](#tc-557) | 🟠 P1 | `BOUNDARY` | Documents | File đúng limit 10MB → 201 | ⚡ Medium |
| [TC-558](#tc-558) | 🟠 P1 | `BOUNDARY` | Documents | File 10MB+1 byte → 413 | ⚡ Medium |
| [TC-559](#tc-559) | 🟠 P1 | `PERFORMANCE` | Documents | Upload 5MB < 5s | ⚡ Medium |
| [TC-560](#tc-560) | 🟠 P1 | `PERFORMANCE` | Documents | Download 5MB streaming không OOM | ⚡ Medium |
| [TC-561](#tc-561) | 🟠 P1 | `RECOVERY` | Documents | Upload bị interrupt giữa chừng → no partial file | ⚠️ High |
| [TC-562](#tc-562) | 🟠 P1 | `RECOVERY` | Documents | Disk full khi upload → 500 cleanup | ⚠️ High |
| [TC-567](#tc-567) | 🟠 P1 | `A11Y` | Documents | File input có aria-label | 📌 Low |
| [TC-568](#tc-568) | 🟠 P1 | `A11Y` | Documents | Upload progress có aria-live='polite' announce | ⚡ Medium |
| [TC-569](#tc-569) | 🟠 P1 | `COMPAT` | Documents | Drag-drop upload trên Chrome | ⚡ Medium |
| [TC-570](#tc-570) | 🟠 P1 | `COMPAT` | Documents | File picker mobile (iOS Safari) | ⚡ Medium |
| [TC-572](#tc-572) | 🟠 P1 | `RED` | Documents | File trùng tên (cùng originalName) → vẫn lưu (fileName generated unique) | ⚡ Medium |
| [TC-574](#tc-574) | 🟠 P1 | `GREEN` | Documents | Filter documentType=COURT_DECISION | ⚡ Medium |
| [TC-575](#tc-575) | 🟠 P1 | `GREEN` | Documents | Filter by uploadedById | 📌 Low |
| [TC-578](#tc-578) | 🟠 P1 | `SECURITY` | Documents | Content-Sniffing: response header X-Content-Type-Options: nosniff | ⚠️ High |
| [TC-579](#tc-579) | 🟠 P1 | `DATA` | Documents | mimeType validate against extension consistency | ⚡ Medium |
| [TC-580](#tc-580) | 🟠 P1 | `INTEGRATION` | Documents | Document gắn cả Subject (nếu schema cho phép) | 📌 Low |
| [TC-585](#tc-585) | 🟠 P1 | `GREEN` | Proposals | relatedCaseId optional bỏ trống (general proposal) | ⚡ Medium |
| [TC-588](#tc-588) | 🟠 P1 | `RED` | Proposals | sentDate format invalid → 400 | ⚡ Medium |
| [TC-597](#tc-597) | 🟠 P1 | `RED` | Proposals | status='INVALID' → 400 IsEnum | ⚡ Medium |
| [TC-600](#tc-600) | 🟠 P1 | `DATA` | Proposals | content rất dài (50K chars) | 📌 Low |
| [TC-601](#tc-601) | 🟠 P1 | `DATA` | Proposals | content tiếng Việt có dấu | 📌 Low |
| [TC-604](#tc-604) | 🟠 P1 | `BOUNDARY` | Proposals | proposalNumber 1 ký tự | 📌 Low |
| [TC-605](#tc-605) | 🟠 P1 | `BOUNDARY` | Proposals | proposalNumber 500 ký tự | 📌 Low |
| [TC-606](#tc-606) | 🟠 P1 | `PERFORMANCE` | Proposals | List 100 proposals < 500ms | ⚡ Medium |
| [TC-607](#tc-607) | 🟠 P1 | `A11Y` | Proposals | Form input có label | 📌 Low |
| [TC-608](#tc-608) | 🟠 P1 | `COMPAT` | Proposals | Chrome mobile OK | ⚡ Medium |
| [TC-609](#tc-609) | 🟠 P1 | `INTEGRATION` | Proposals | Proposal sent → notification gửi đến receiver | ⚡ Medium |
| [TC-612](#tc-612) | 🟠 P1 | `RECOVERY` | Proposals | Atomic rollback nếu audit fail | ⚠️ High |
| [TC-613](#tc-613) | 🟠 P1 | `GREEN` | Proposals | Filter status=PENDING | ⚡ Medium |
| [TC-614](#tc-614) | 🟠 P1 | `GREEN` | Proposals | Pagination | ⚡ Medium |
| [TC-615](#tc-615) | 🟠 P1 | `GREEN` | Proposals | Filter relatedCaseId | ⚡ Medium |
| [TC-635](#tc-635) | 🟠 P1 | `DATA` | Delegations | receivingUnit tiếng Việt 'Phòng Cảnh sát Hà Nội' | 📌 Low |
| [TC-636](#tc-636) | 🟠 P1 | `DATA` | Delegations | content nhiều paragraph | 📌 Low |
| [TC-637](#tc-637) | 🟠 P1 | `DATA` | Delegations | delegationDate ISO format | ⚡ Medium |
| [TC-638](#tc-638) | 🟠 P1 | `RED` | Delegations | delegationDate format invalid → 400 (nếu IsDateString) | ⚡ Medium |
| [TC-641](#tc-641) | 🟠 P1 | `BOUNDARY` | Delegations | delegationNumber 1 ký tự | 📌 Low |
| [TC-642](#tc-642) | 🟠 P1 | `BOUNDARY` | Delegations | content 100K ký tự | 📌 Low |
| [TC-643](#tc-643) | 🟠 P1 | `PERFORMANCE` | Delegations | List 50 delegations < 500ms | ⚡ Medium |
| [TC-644](#tc-644) | 🟠 P1 | `A11Y` | Delegations | Form A11Y compliant | 📌 Low |
| [TC-645](#tc-645) | 🟠 P1 | `COMPAT` | Delegations | Form mobile responsive | ⚡ Medium |
| [TC-646](#tc-646) | 🟠 P1 | `INTEGRATION` | Delegations | Delegation cùng caseId hiển thị trong Case detail tab | ⚡ Medium |
| [TC-649](#tc-649) | 🟠 P1 | `GREEN` | Delegations | Filter status=PENDING | ⚡ Medium |
| [TC-650](#tc-650) | 🟠 P1 | `GREEN` | Delegations | Filter by receivingUnit | ⚡ Medium |
| [TC-651](#tc-651) | 🟠 P1 | `GREEN` | Delegations | Pagination | ⚡ Medium |
| [TC-657](#tc-657) | 🟠 P1 | `RED` | Supplements | decisionDate sai ISO → 400 | ⚡ Medium |
| [TC-658](#tc-658) | 🟠 P1 | `RED` | Supplements | deadline sai ISO → 400 | ⚡ Medium |
| [TC-659](#tc-659) | 🟠 P1 | `RED` | Supplements | deadline < decisionDate (deadline trước quyết định) | ⚡ Medium |
| [TC-670](#tc-670) | 🟠 P1 | `DATA` | Supplements | reason tiếng Việt long text | 📌 Low |
| [TC-671](#tc-671) | 🟠 P1 | `DATA` | Supplements | decisionNumber format 'XYZ-2026-001' | 📌 Low |
| [TC-673](#tc-673) | 🟠 P1 | `BOUNDARY` | Supplements | decisionNumber 1 ký tự | 📌 Low |
| [TC-674](#tc-674) | 🟠 P1 | `BOUNDARY` | Supplements | reason 50K ký tự | 📌 Low |
| [TC-675](#tc-675) | 🟠 P1 | `PERFORMANCE` | Supplements | List < 500ms | ⚡ Medium |
| [TC-676](#tc-676) | 🟠 P1 | `A11Y` | Supplements | Form A11Y | 📌 Low |
| [TC-677](#tc-677) | 🟠 P1 | `COMPAT` | Supplements | Mobile responsive | ⚡ Medium |
| [TC-680](#tc-680) | 🟠 P1 | `GREEN` | Supplements | Filter by caseId | ⚡ Medium |
| [TC-681](#tc-681) | 🟠 P1 | `GREEN` | Supplements | Filter by type | ⚡ Medium |
| [TC-700](#tc-700) | 🟠 P1 | `DATA` | PetitionFlow | Petition senderName tiếng Việt có dấu | 📌 Low |
| [TC-701](#tc-701) | 🟠 P1 | `BOUNDARY` | PetitionFlow | stt cuối cùng năm 99999 → tràn? | 📌 Low |
| [TC-704](#tc-704) | 🟠 P1 | `PERFORMANCE` | PetitionFlow | convertToCase < 1s | ⚡ Medium |
| [TC-707](#tc-707) | 🟠 P1 | `GREEN` | PetitionFlow | Petition tab trong Case detail show linked petitions[] | ⚠️ High |
| [TC-708](#tc-708) | 🟠 P1 | `GREEN` | PetitionFlow | Multiple petitions linked to same Case (1-to-many) | ⚡ Medium |
| [TC-711](#tc-711) | 🟠 P1 | `DATA` | PetitionFlow | petitionType enum: TO_GIAC, KHIEU_NAI, TIN_BAO, ... | ⚡ Medium |
| [TC-712](#tc-712) | 🟠 P1 | `RED` | PetitionFlow | petitionType='Vietnamese label' → 400 (v0.37.2.4 BUG) | ⚠️ High |
| [TC-713](#tc-713) | 🟠 P1 | `GREEN` | PetitionFlow | petitionType='TO_GIAC' enum value pass | ⚠️ High |
| [TC-716](#tc-716) | 🟠 P1 | `RECOVERY` | PetitionFlow | convertToCase fail giữa tx → Petition vẫn nguyên | 🚨 Critical |
| [TC-730](#tc-730) | 🟠 P1 | `DATA` | IncidentFlow | Incident có deadlineRuleVersionId tracked | ⚡ Medium |
| [TC-731](#tc-731) | 🟠 P1 | `INTEGRATION` | IncidentFlow | E2E: Tạo Incident → assign → convert Case → KPI dashboard update | ⚡ Medium |
| [TC-732](#tc-732) | 🟠 P1 | `PERFORMANCE` | IncidentFlow | convertToCase < 1s | ⚡ Medium |
| [TC-733](#tc-733) | 🟠 P1 | `RECOVERY` | IncidentFlow | convertToCase fail giữa tx → Incident nguyên | ⚠️ High |
| [TC-737](#tc-737) | 🟠 P1 | `GREEN` | IncidentFlow | Incident detail show ketQuaXacMinh + ngayPhucHoi | ⚡ Medium |
| [TC-738](#tc-738) | 🟠 P1 | `DATA` | IncidentFlow | Incident kết quả phục hồi enum khác Case | ⚡ Medium |
| [TC-739](#tc-739) | 🟠 P1 | `BOUNDARY` | IncidentFlow | Incident deadline override Case deadline? | 📌 Low |
| [TC-761](#tc-761) | 🟠 P1 | `PERFORMANCE` | Extra | Cold start API after restart < 5s first request | 📌 Low |
| [TC-762](#tc-762) | 🟠 P1 | `PERFORMANCE` | Extra | DB connection pool exhaustion test | ⚡ Medium |
| [TC-763](#tc-763) | 🟠 P1 | `PERFORMANCE` | Extra | Memory leak test: 100 sequential create | ⚡ Medium |
| [TC-764](#tc-764) | 🟠 P1 | `PERFORMANCE` | Extra | Audit log không slow main response | ⚡ Medium |
| [TC-765](#tc-765) | 🟠 P1 | `PERFORMANCE` | Extra | Index hit on overdue+status query | 📌 Low |
| [TC-766](#tc-766) | 🟠 P1 | `A11Y` | Extra | CaseListPage table có scope='col' th | ⚡ Medium |
| [TC-767](#tc-767) | 🟠 P1 | `A11Y` | Extra | Pagination buttons có aria-label | ⚡ Medium |
| [TC-768](#tc-768) | 🟠 P1 | `A11Y` | Extra | Sort indicator aria-sort='ascending|descending' | ⚡ Medium |
| [TC-769](#tc-769) | 🟠 P1 | `A11Y` | Extra | Toast notification có role='alert' aria-live | ⚠️ High |
| [TC-770](#tc-770) | 🟠 P1 | `A11Y` | Extra | Skip-to-main-content link | 📌 Low |
| [TC-771](#tc-771) | 🟠 P1 | `A11Y` | Extra | Form fieldset/legend cho group | ⚡ Medium |
| [TC-772](#tc-772) | 🟠 P1 | `A11Y` | Extra | Color contrast cho disabled state ≥ 3:1 | 📌 Low |
| [TC-773](#tc-773) | 🟠 P1 | `COMPAT` | Extra | Chrome incognito mode — cookies/localStorage | ⚡ Medium |
| [TC-774](#tc-774) | 🟠 P1 | `COMPAT` | Extra | Safari iOS pinch-zoom enabled | ⚡ Medium |
| [TC-775](#tc-775) | 🟠 P1 | `COMPAT` | Extra | High DPI display Retina — text crisp | 📌 Low |
| [TC-776](#tc-776) | 🟠 P1 | `DATA` | Extra | UTF-8 BOM handling trong file upload (CSV import) | 📌 Low |
| [TC-777](#tc-777) | 🟠 P1 | `DATA` | Extra | Date timezone conversion cross-timezone view | ⚡ Medium |
| [TC-778](#tc-778) | 🟠 P1 | `RECOVERY` | Extra | Page refresh giữa edit Case → unsaved changes warning | ⚡ Medium |
| [TC-779](#tc-779) | 🟠 P1 | `RECOVERY` | Extra | Token expired during form edit → re-login flow without losing data | ⚠️ High |
| [TC-252](#tc-252) | 🟡 P2 | `A11Y` | Zoom | Zoom 200% không vỡ layout CaseListPage | ⚡ Medium |
| [TC-253](#tc-253) | 🟡 P2 | `A11Y` | Motion | prefers-reduced-motion respect — animation tắt | 📌 Low |
| [TC-257](#tc-257) | 🟡 P2 | `A11Y` | Lang | <html lang='vi'> để screen reader phát âm tiếng Việt | 📌 Low |
| [TC-265](#tc-265) | 🟡 P2 | `COMPAT` | Browser | Chrome 100 (legacy older) — graceful degradation | 📌 Low |
| [TC-266](#tc-266) | 🟡 P2 | `COMPAT` | Browser | IE11 — KHÔNG support (banner warning) | 📌 Low |
| [TC-274](#tc-274) | 🟡 P2 | `COMPAT` | Network | Offline — toast 'Mất kết nối', retry button | 📌 Low |
| [TC-283](#tc-283) | 🟡 P2 | `PERFORMANCE` | Concurrent | 10 user đồng thời POST /cases — không deadlock | ⚡ Medium |
| [TC-284](#tc-284) | 🟡 P2 | `PERFORMANCE` | Concurrent | 50 user đồng thời GET list — không nghẽn | 📌 Low |
| [TC-286](#tc-286) | 🟡 P2 | `PERFORMANCE` | Memory | Export 500 row không OOM (workbook stream) | ⚡ Medium |

## 📝 Test Cases chi tiết

---

## TC-001

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-001`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Tạo Case DIRECT_DISCOVERY thành công với input đầy đủ

### Điều kiện tiên quyết
- Login dieuTra1@pc02.local (role INVESTIGATOR)
- User thuộc Team-Q1
- JWT valid

### Các bước kiểm thử
- [ ] Navigate /cases/new
- [ ] Nhập name = 'Vụ trộm cắp xe máy tại Q1 ngày 12/05/2026'
- [ ] Chọn caseProvenance = DIRECT_DISCOVERY
- [ ] Nhập crime = 'Trộm cắp tài sản — Đ.173 BLHS'
- [ ] Chọn capDoToiPham = NGHIEM_TRONG
- [ ] Nhập subjectsCount = 2
- [ ] Nhập ngayKhoiTo = '2026-05-12'
- [ ] Click 'Lưu'

### Dữ liệu kiểm thử
```
name='Vụ trộm cắp xe máy tại Q1 ngày 12/05/2026', caseProvenance='DIRECT_DISCOVERY', crime='Trộm cắp tài sản — Đ.173 BLHS', capDoToiPham='NGHIEM_TRONG', subjectsCount=2, ngayKhoiTo='2026-05-12'
```

### Kết quả mong đợi
**UI**:
- Toast 'Tạo vụ án thành công'
- Redirect /cases/<id>
- Trang detail hiển thị đủ field
- Badge status = 'Tiếp nhận'

**API**:
- POST /api/v1/cases trả 201
- Body: success=true, data.id, data.status='TIEP_NHAN', data.caseProvenance='DIRECT_DISCOVERY', data.createdById=<userId>
- data.assignedTeamId auto-set từ dataScope

**Side effects** (DB, email, log, queue...):
- Row trong table cases, deletedAt=NULL
- Audit log CASE_CREATED có ipAddress, userAgent
- assignedTeamId auto từ ward officer scope (v0.33)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-001
severity: Critical
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-002

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-002`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Tạo Case FROM_PETITION link Petition đang status TIEP_NHAN + expectedPetitionUpdatedAt khớp

### Điều kiện tiên quyết
- Login INVESTIGATOR
- Có Petition PET-001 status=TIEP_NHAN, updatedAt='2026-05-20T10:00:00.000Z', linkedCaseId=NULL

### Các bước kiểm thử
- [ ] Vào /petitions/PET-001
- [ ] Click 'Khởi tố vụ án'
- [ ] Form mở với caseProvenance=FROM_PETITION, linkedPetitionId=PET-001 pre-filled
- [ ] Nhập name = 'Vụ án từ đơn thư PET-001'
- [ ] Submit

### Dữ liệu kiểm thử
```
linkedPetitionId='PET-001', expectedPetitionUpdatedAt='2026-05-20T10:00:00.000Z', caseProvenance='FROM_PETITION'
```

### Kết quả mong đợi
**UI**:
- Toast 'Tạo vụ án thành công'
- Redirect /cases/<newId>
- Tab 'Nguồn tin' hiển thị link sang Petition PET-001

**API**:
- POST /cases trả 201
- data.linkedPetitionId='PET-001'
- Atomic update Petition.status=DA_CHUYEN_VU_AN

**Side effects** (DB, email, log, queue...):
- cases row có linkedPetitionId
- petitions PET-001: linkedCaseId=newId, status=DA_CHUYEN_VU_AN
- Audit CASE_CREATED metadata.linkedPetitionId='PET-001'

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-002
severity: Critical
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-003

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-003`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Tạo Case FROM_INCIDENT link Incident chưa khởi tố + expectedIncidentUpdatedAt khớp

### Điều kiện tiên quyết
- Login INVESTIGATOR
- Có Incident INC-001 deletedAt=NULL, linkedCaseId=NULL, updatedAt='2026-05-20T11:00:00.000Z'

### Các bước kiểm thử
- [ ] Vào /incidents/INC-001
- [ ] Click 'Khởi tố vụ án'
- [ ] Form pre-fill caseProvenance=FROM_INCIDENT, linkedIncidentId=INC-001
- [ ] Nhập name='Vụ án khởi tố từ INC-001'
- [ ] Submit

### Dữ liệu kiểm thử
```
linkedIncidentId='INC-001', expectedIncidentUpdatedAt='2026-05-20T11:00:00.000Z', caseProvenance='FROM_INCIDENT'
```

### Kết quả mong đợi
**UI**:
- Toast success
- Redirect /cases/<newId>
- Detail page có badge 'Khởi tố từ vụ việc INC-001'

**API**:
- POST /cases trả 201
- data.linkedIncidentId='INC-001'
- Incident INC-001 update linkedCaseId=newId

**Side effects** (DB, email, log, queue...):
- cases.linkedIncidentId, incidents.linkedCaseId updated atomic
- Audit CASE_CREATED metadata.linkedIncidentId

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-003
severity: Critical
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-004

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-004`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Case TRANSFERRED với sourceDocumentNote

### Điều kiện tiên quyết
- Login INVESTIGATOR

### Các bước kiểm thử
- [ ] Mở form tạo vụ án
- [ ] Chọn caseProvenance=TRANSFERRED
- [ ] Nhập sourceDocumentNote='Công văn chuyển từ Cơ quan ANĐT Thành phố số 1234/CV ngày 10/05/2026'
- [ ] Nhập name + submit

### Dữ liệu kiểm thử
```
caseProvenance='TRANSFERRED', sourceDocumentNote='Công văn chuyển từ Cơ quan ANĐT Thành phố số 1234/CV ngày 10/05/2026', name='Vụ chuyển CQĐT khác'
```

### Kết quả mong đợi
**UI**:
- Toast 'Tạo vụ án thành công', detail hiện sourceDocumentNote

**API**:
- POST /cases 201, data.caseProvenance='TRANSFERRED', data.sourceDocumentNote set

**Side effects** (DB, email, log, queue...):
- cases row có sourceDocumentNote saved

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-004
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-005

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-005`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Case OTHER_LEGAL_SOURCE

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Form tạo
- [ ] Chọn provenance=OTHER_LEGAL_SOURCE
- [ ] Nhập name='Vụ phát hiện qua tin báo nặc danh', sourceDocumentNote='Tin báo Hotline 113'
- [ ] Submit

### Dữ liệu kiểm thử
```
caseProvenance='OTHER_LEGAL_SOURCE', name='Vụ phát hiện qua tin báo nặc danh', sourceDocumentNote='Tin báo Hotline 113'
```

### Kết quả mong đợi
**UI**:
- Toast success

**API**:
- with caseProvenance=OTHER_LEGAL_SOURCE

**Side effects** (DB, email, log, queue...):
- Row saved

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-005
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-006

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-006`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Case với capDoToiPham=DAC_BIET_NGHIEM_TRONG để KPI-4 tính

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tạo Case mới
- [ ] Chọn capDoToiPham=DAC_BIET_NGHIEM_TRONG
- [ ] crime='Giết người — Đ.123 BLHS'
- [ ] Submit

### Dữ liệu kiểm thử
```
capDoToiPham='DAC_BIET_NGHIEM_TRONG', crime='Giết người — Đ.123 BLHS'
```

### Kết quả mong đợi
**UI**:
- Detail hiện badge mức độ 'Đặc biệt nghiêm trọng' màu đỏ

**API**:
- data.capDoToiPham='DAC_BIET_NGHIEM_TRONG'

**Side effects** (DB, email, log, queue...):
- KPI-4 dashboard cập nhật count án ĐBNT +1

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-006
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-007

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-007`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Auto-deadline tính theo SystemSetting THOI_HAN_GIAI_QUYET

### Điều kiện tiên quyết
- SystemSetting THOI_HAN_GIAI_QUYET = 120 (ngày)

### Các bước kiểm thử
- [ ] Tạo Case mới ngày 2026-05-23
- [ ] KHÔNG nhập deadline
- [ ] Submit

### Dữ liệu kiểm thử
```
name='Test auto deadline', caseProvenance='DIRECT_DISCOVERY'
```

### Kết quả mong đợi
**UI**:
- Detail hiển thị deadline = 2026-09-20 (120 ngày sau)

**API**:
- data.deadline = createdAt + 120 days (UTC)

**Side effects** (DB, email, log, queue...):
- Deadline auto-set từ SystemSetting, không phải từ user input

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-007
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-008

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-008`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Case manual deadline override auto-deadline

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Form tạo
- [ ] Nhập deadline='2026-08-15' (60 ngày sau)
- [ ] Submit

### Dữ liệu kiểm thử
```
deadline='2026-08-15'
```

### Kết quả mong đợi
**UI**:
- Detail show deadline=15/08/2026

**API**:
- data.deadline=2026-08-15T00:00:00.000Z

**Side effects** (DB, email, log, queue...):
- cases.deadline = user input (không bị auto-calc override)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-008
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-009

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-009`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Case với metadata custom (người báo tin)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tạo Case
- [ ] Nhập metadata.nguoiBaoTin='Nguyễn Văn A', metadata.diaChi='12 Lê Lợi Q1'
- [ ] Submit

### Dữ liệu kiểm thử
```
metadata={nguoiBaoTin:'Nguyễn Văn A', diaChi:'12 Lê Lợi Q1', soDienThoai:'0901234567'}
```

### Kết quả mong đợi
**UI**:
- Detail tab 'Thông tin báo tin' show nguoiBaoTin, diaChi

**API**:
- data.metadata.nguoiBaoTin='Nguyễn Văn A'

**Side effects** (DB, email, log, queue...):
- JSONB metadata saved

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-009
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-010

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-010`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Ward officer auto-set assignedTeamId theo wardTeamId (silent override)

### Điều kiện tiên quyết
- Login wardOfficer1@pc02.local thuộc Team-WARD-01

### Các bước kiểm thử
- [ ] Form tạo
- [ ] CỐ NHẬP assignedTeamId='Team-DIFFERENT-99' (team khác)
- [ ] Submit

### Dữ liệu kiểm thử
```
dto.assignedTeamId='Team-DIFFERENT-99'
```

### Kết quả mong đợi
**UI**:
- Toast success (KHÔNG báo lỗi)

**API**:
- POST 201, data.assignedTeamId='Team-WARD-01' (auto override)

**Side effects** (DB, email, log, queue...):
- Server silent override theo dataScope.wardTeamId — UX safer per D-eng-fix M3

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-010
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-017

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-017`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Thiếu trường name (required) trả 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /cases với dto thiếu key name

### Dữ liệu kiểm thử
```
{caseProvenance:'DIRECT_DISCOVERY'}
```

### Kết quả mong đợi
**UI**:
- Form: hiện inline error 'Tên vụ án bắt buộc'

**API**:
- BadRequest, error message includes 'name'

**Side effects** (DB, email, log, queue...):
- Không tạo row

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-017
severity: Critical
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-018

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-018`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: name là chuỗi rỗng '' trả 400 'Tên vụ án bắt buộc'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /cases với name=''

### Dữ liệu kiểm thử
```
name=''
```

### Kết quả mong đợi
**UI**:
- Error 'Tên vụ án bắt buộc'

**API**:
- message ='Tên vụ án bắt buộc' (sau Transform trim)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-018
severity: Critical
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-019

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-019`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: name chỉ whitespace '   ' bị trim thành '' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST với name='     '

### Dữ liệu kiểm thử
```
name='     ' (5 spaces)
```

### Kết quả mong đợi
**UI**:
- Error 'Tên vụ án bắt buộc'

**API**:
- (Transform trim chạy trước IsNotEmpty)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-019
severity: Critical
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: UAT 2026-05-23 BUG-001 đã fix

---

## TC-020

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-020`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: name là null → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST với name=null

### Dữ liệu kiểm thử
```
name=null
```

### Kết quả mong đợi
**UI**:
- Error

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-020
severity: Critical
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-021

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-021`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: name là số 12345 không phải string → 400 IsString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name=12345 (number)

### Dữ liệu kiểm thử
```
name=12345
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsString failed

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-021
severity: High
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-022

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-022`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: name vượt 500 ký tự (501 chars) → 400 MaxLength

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST với name='A' lặp 501 lần

### Dữ liệu kiểm thử
```
name='A'.repeat(501)
```

### Kết quả mong đợi
**UI**:
- Error 'Tên không quá 500 ký tự'

**API**:
- MaxLength(500)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-022
severity: High
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-025

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-025`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Thiếu caseProvenance → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không có caseProvenance

### Dữ liệu kiểm thử
```
{name:'X'}
```

### Kết quả mong đợi
**UI**:
- Inline error 'caseProvenance bắt buộc'

**API**:
- IsEnum message includes 'BLTTHS Đ.143'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-025
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-026

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-026`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: caseProvenance không phải enum value → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseProvenance='INVALID_VALUE'

### Dữ liệu kiểm thử
```
caseProvenance='INVALID_VALUE'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsEnum violation

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-026
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-027

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-027`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: caseProvenance là tiếng Việt (legacy payload) → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseProvenance='Phát hiện trực tiếp'

### Dữ liệu kiểm thử
```
caseProvenance='Phát hiện trực tiếp'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- — v0.37.2.4 BLTTHS Đ.143 — phải dùng enum value tiếng Anh

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-027
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Root cause v0.37.2.4 P0 hot-fix

---

## TC-028

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-028`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: FROM_PETITION nhưng thiếu linkedPetitionId → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseProvenance=FROM_PETITION, không có linkedPetitionId

### Dữ liệu kiểm thử
```
caseProvenance='FROM_PETITION'
```

### Kết quả mong đợi
**UI**:
- Error 'linkedPetitionId required when caseProvenance is FROM_PETITION'

**API**:
- ValidateIf failed

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-028
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-029

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-029`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: FROM_INCIDENT thiếu linkedIncidentId → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseProvenance=FROM_INCIDENT, không linkedIncidentId

### Dữ liệu kiểm thử
```
caseProvenance='FROM_INCIDENT'
```

### Kết quả mong đợi
**UI**:
- Error

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-029
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-030

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-030`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: FROM_PETITION thiếu expectedPetitionUpdatedAt → 400 IsISO8601

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST có linkedPetitionId nhưng không expectedPetitionUpdatedAt

### Dữ liệu kiểm thử
```
caseProvenance=FROM_PETITION, linkedPetitionId='PET-001'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-030
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-031

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-031`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: FROM_PETITION với expectedPetitionUpdatedAt cũ hơn current → 409 Conflict

### Điều kiện tiên quyết
- Petition PET-001 updatedAt='2026-05-22T15:00:00.000Z' (đã được update bởi user khác)

### Các bước kiểm thử
- [ ] POST với expectedPetitionUpdatedAt='2026-05-20T10:00:00.000Z'

### Dữ liệu kiểm thử
```
expectedPetitionUpdatedAt='2026-05-20T10:00:00.000Z'
```

### Kết quả mong đợi
**UI**:
- Toast 'Đơn thư đã được chỉnh sửa hoặc link bởi người dùng khác'

**API**:
- ConflictException

**Side effects** (DB, email, log, queue...):
- Case KHÔNG tạo, Petition KHÔNG update

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-031
severity: High
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-032

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-032`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: FROM_PETITION với linkedPetitionId KHÔNG tồn tại → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST linkedPetitionId='PET-NOT-EXIST-999'

### Dữ liệu kiểm thử
```
linkedPetitionId='PET-NOT-EXIST-999', expectedPetitionUpdatedAt='2026-05-20T10:00:00.000Z'
```

### Kết quả mong đợi
**UI**:
- Toast 'Đơn thư không tồn tại hoặc không nằm trong phạm vi của bạn'

**API**:
- NotFoundException — no enumeration leak

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-032
severity: High
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: IDOR-safe: not-found vs out-of-scope cùng response

---

## TC-033

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-033`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: FROM_PETITION với Petition của user KHÁC scope → 404 (không lộ tồn tại)

### Điều kiện tiên quyết
- Login wardOfficer-Q1, Petition PET-A99 thuộc Team-Q3

### Các bước kiểm thử
- [ ] POST FROM_PETITION linkedPetitionId='PET-A99'

### Dữ liệu kiểm thử
```
linkedPetitionId='PET-A99'
```

### Kết quả mong đợi
**UI**:
- Toast 'Đơn thư không tồn tại hoặc không nằm trong phạm vi của bạn'

**API**:
- (không 403)

**Side effects** (DB, email, log, queue...):
- IDOR-safe enumeration prevention

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-033
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-034

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-034`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: FROM_PETITION với Petition đã có linkedCaseId (đã khởi tố trước) → 404

### Điều kiện tiên quyết
- PET-002 đã có linkedCaseId='CASE-OLD'

### Các bước kiểm thử
- [ ] POST FROM_PETITION với linkedPetitionId='PET-002'

### Dữ liệu kiểm thử
```
linkedPetitionId='PET-002'
```

### Kết quả mong đợi
**UI**:
- Toast 'Đơn thư đã được khởi tố rồi'

**API**:
- (filter linkedCaseId:null trong findFirst)

**Side effects** (DB, email, log, queue...):
- Không double-create

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-034
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-035

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-035`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: FROM_PETITION với deletedAt != null → 404

### Điều kiện tiên quyết
- PET-003 đã soft-deleted

### Các bước kiểm thử
- [ ] POST FROM_PETITION linkedPetitionId='PET-003'

### Dữ liệu kiểm thử
```
linkedPetitionId='PET-003'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-035
severity: High
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-037

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-inv`
- Yêu cầu: `REQ-CASE-CREATE-INV-037`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: investigatorId không tồn tại trong users table → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST investigatorId='NON_EXIST_USER_XXX'

### Dữ liệu kiểm thử
```
investigatorId='NON_EXIST_USER_XXX'
```

### Kết quả mong đợi
**UI**:
- Toast 'Điều tra viên không tồn tại'

**API**:
- BadRequestException msg=Điều tra viên không tồn tại

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-inv`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-inv`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-037
severity: High
module: Create-inv
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-044

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-deadline`
- Yêu cầu: `REQ-CASE-CREATE-DEADLINE-044`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: deadline sai format 'không-phải-ngày' → 400 IsDateString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST deadline='abc-xyz'

### Dữ liệu kiểm thử
```
deadline='abc-xyz'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsDateString

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-deadline`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-deadline`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-044
severity: High
module: Create-deadline
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-048

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-048`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: caseProvenance=DIRECT_DISCOVERY nhưng vẫn truyền linkedPetitionId → field bị ignore

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST DIRECT_DISCOVERY + linkedPetitionId='PET-001'

### Dữ liệu kiểm thử
```
caseProvenance='DIRECT_DISCOVERY', linkedPetitionId='PET-001'
```

### Kết quả mong đợi
**UI**:
- Tạo thành công

**API**:
- , data.linkedPetitionId=NULL (ignore)

**Side effects** (DB, email, log, queue...):
- DTO bỏ qua linkedPetitionId nếu provenance không phải FROM_PETITION (ValidateIf)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-048
severity: Medium
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Validation chỉ apply khi FROM_PETITION → DIRECT_DISCOVERY không validate field

---

## TC-053

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-perm`
- Yêu cầu: `REQ-CASE-CREATE-PERM-053`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: User KHÔNG có permission write/Case (role VIEWER) → 403

### Điều kiện tiên quyết
- Login viewer@pc02.local role VIEWER

### Các bước kiểm thử
- [ ] POST /cases với DTO hợp lệ

### Dữ liệu kiểm thử
```
any valid
```

### Kết quả mong đợi
**UI**:
- Toast 'Bạn không có quyền'

**API**:
- ForbiddenException PermissionsGuard

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-perm`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-perm`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-053
severity: Critical
module: Create-perm
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-054

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-auth`
- Yêu cầu: `REQ-CASE-CREATE-AUTH-054`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: POST không có Authorization header → 401

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] POST /cases không kèm JWT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Redirect login

**API**:
- JwtAuthGuard rejected

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-auth`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-auth`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-054
severity: Critical
module: Create-auth
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-055

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-auth`
- Yêu cầu: `REQ-CASE-CREATE-AUTH-055`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: POST JWT đã hết hạn → 401

### Điều kiện tiên quyết
- JWT exp < now()

### Các bước kiểm thử
- [ ] POST với JWT cũ

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- token expired

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-auth`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-auth`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-055
severity: Critical
module: Create-auth
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-056

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-auth`
- Yêu cầu: `REQ-CASE-CREATE-AUTH-056`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: POST JWT bị tamper signature → 401

### Điều kiện tiên quyết
- JWT signature thay đổi

### Các bước kiểm thử
- [ ] POST với JWT.split('.')[2]='evil_sig'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- JWT invalid signature

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-auth`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-auth`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-056
severity: Critical
module: Create-auth
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-061

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-061`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: name='\t\t\t' (3 tab) bị trim thành '' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='\t\t\t'

### Dữ liệu kiểm thử
```
name='\t\t\t' (3 tabs)
```

### Kết quả mong đợi
**UI**:
- Error 'Tên vụ án bắt buộc'

**API**:
- IsNotEmpty sau Transform trim

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-061
severity: High
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-062

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-062`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: name='\n\n\n' (3 newline) bị trim → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='\n\n\n'

### Dữ liệu kiểm thử
```
name='\n\n\n'
```

### Kết quả mong đợi
**UI**:
- Error

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-062
severity: High
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-064

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-064`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Race condition: 2 user đồng thời FROM_PETITION cùng PET-001 → 1 ConflictException

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] User A và B đồng thời POST FROM_PETITION với cùng linkedPetitionId='PET-001'

### Dữ liệu kiểm thử
```
2 request parallel
```

### Kết quả mong đợi
**UI**:
- User thắng race: success. User thua: 'Đơn thư đã được link bởi người dùng khác'

**API**:
- trả 201, 1 trả 409

**Side effects** (DB, email, log, queue...):
- Chỉ 1 Case tạo, Petition.linkedCaseId = winner's caseId, không double-link

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-064
severity: Critical
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Atomic via WHERE updatedAt + linkedCaseId=null + P2025/P2002 catch

---

## TC-067

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-067`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /cases trả paginated 20 items default sorted createdAt desc

### Điều kiện tiên quyết
- DB có ≥30 Case

### Các bước kiểm thử
- [ ] GET /api/v1/cases

### Dữ liệu kiểm thử
```
query: (none)
```

### Kết quả mong đợi
**UI**:
- Table show 20 row, pagination 1 of 2

**API**:
- , data=[20 items], total=30, page=1, pageSize=20, sort createdAt desc

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-067
severity: High
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-068

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-068`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?limit=50 trả tối đa 50

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases?limit=50

### Dữ liệu kiểm thử
```
limit=50
```

### Kết quả mong đợi
**UI**:
- Table 50 row

**API**:
- , data.length≤50

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-068
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-069

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-069`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?limit=101 → 400 @Max(100)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=101

### Dữ liệu kiểm thử
```
limit=101
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- max 100

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-069
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-073

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-073`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Filter status=DANG_DIEU_TRA chỉ trả Case status đó

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases?status=DANG_DIEU_TRA

### Dữ liệu kiểm thử
```
status='DANG_DIEU_TRA'
```

### Kết quả mong đợi
**UI**:
- Tất cả row có badge 'Đang điều tra'

**API**:
- , mọi data[].status=DANG_DIEU_TRA

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-073
severity: High
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-075

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-075`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Filter overdue=true trả case quá hạn

### Điều kiện tiên quyết
- Có Case deadline='2026-05-10' (đã qua) status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] GET /cases?overdue=true

### Dữ liệu kiểm thử
```
overdue=true
```

### Kết quả mong đợi
**UI**:
- Row hiển thị highlight đỏ 'Quá hạn'

**API**:
- , mỗi data[].deadline < now() AND status NOT IN [DA_KET_LUAN, DA_LUU_TRU, DINH_CHI]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-075
severity: High
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-076

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-076`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Filter overdue=true KHÔNG include Case DA_KET_LUAN dù deadline đã qua

### Điều kiện tiên quyết
- Case-X deadline='2026-04-01', status=DA_KET_LUAN

### Các bước kiểm thử
- [ ] GET ?overdue=true

### Dữ liệu kiểm thử
```
overdue=true
```

### Kết quả mong đợi
**UI**:
- Case-X KHÔNG xuất hiện

**API**:
- data[].id KHÔNG có Case-X

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-076
severity: High
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: status NOT IN clause

---

## TC-077

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-077`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Search 'Trộm cắp' tìm case có name/crime/unit chứa từ này (case-insensitive)

### Điều kiện tiên quyết
- Case A name='Vụ Trộm Cắp Xe', Case B crime='trộm cắp tài sản'

### Các bước kiểm thử
- [ ] GET /cases?search=Trộm cắp

### Dữ liệu kiểm thử
```
search='Trộm cắp'
```

### Kết quả mong đợi
**UI**:
- Show cả A và B

**API**:
- OR contains insensitive, return 2 results

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-077
severity: High
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-078

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-078`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Search với chữ có dấu vs không dấu (Unicode Vietnamese)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?search=tron cap (không dấu)

### Dữ liệu kiểm thử
```
search='tron cap'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Postgres không tìm thấy nếu thiếu collation → cần unaccent extension

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-078
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap nếu Postgres mặc định case-insensitive nhưng accent-sensitive

---

## TC-086

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-086`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET /cases/:id trả full detail có investigator + petitions include

### Điều kiện tiên quyết
- Case CASE-001

### Các bước kiểm thử
- [ ] GET /cases/CASE-001

### Dữ liệu kiểm thử
```
id='CASE-001'
```

### Kết quả mong đợi
**UI**:
- Detail page show name, status, investigator, deadline, petitions tab

**API**:
- , data.investigator{firstName,lastName,username,email}, data.petitions[]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-086
severity: Critical
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-087

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-087`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /cases/NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/NOT_EXIST

### Dữ liệu kiểm thử
```
id='NOT_EXIST'
```

### Kết quả mong đợi
**UI**:
- Toast 'Vụ án không tồn tại'

**API**:
- NotFoundException

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-087
severity: High
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-088

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-088`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /cases/:id của Case đã soft-deleted → 404 (deletedAt IS NOT NULL)

### Điều kiện tiên quyết
- CASE-DEL deletedAt='2026-05-20'

### Các bước kiểm thử
- [ ] GET /cases/CASE-DEL

### Dữ liệu kiểm thử
```
id of deleted
```

### Kết quả mong đợi
**UI**:
- page

**API**:
- (where deletedAt:null filter)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-088
severity: High
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-089

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-089`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET /cases/:id của Case ngoài scope → 403

### Điều kiện tiên quyết
- Login wardOfficer-Q1, CASE-Q3 thuộc Team-Q3

### Các bước kiểm thử
- [ ] GET /cases/CASE-Q3

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Bạn không có quyền truy cập bản ghi này'

**API**:
- ForbiddenException (checkRecordInScope)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-089
severity: Critical
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-090

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-090`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Dispatcher xem mọi Case không bị scope chặn

### Điều kiện tiên quyết
- Login dispatcher@ canDispatch=true

### Các bước kiểm thử
- [ ] GET /cases/CASE-FOREIGN

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- detail

**API**:
- (dispatcher bypass)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-090
severity: High
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-091

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-091`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: ADMIN xem mọi Case (no scope)

### Điều kiện tiên quyết
- Login admin@

### Các bước kiểm thử
- [ ] GET /cases/CASE-ANY

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:
- (dataScope=null → allow all)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-091
severity: High
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-092

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-092`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Owner (investigator) xem được Case mình phụ trách

### Điều kiện tiên quyết
- dieuTra1@ là investigatorId của CASE-001

### Các bước kiểm thử
- [ ] Login dieuTra1, GET /cases/CASE-001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:
- (ownerMatch)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-092
severity: High
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-093

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-093`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Cùng team xem được Case của đồng nghiệp

### Điều kiện tiên quyết
- dieuTra1 và dieuTra2 cùng Team-Q1, CASE-002 do dieuTra2 phụ trách assignedTeamId=Team-Q1

### Các bước kiểm thử
- [ ] Login dieuTra1, GET /cases/CASE-002

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:
- (teamMatch)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-093
severity: High
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-097

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-097`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update name + crime case mình owner

### Điều kiện tiên quyết
- CASE-001 investigatorId=dieuTra1, status=TIEP_NHAN

### Các bước kiểm thử
- [ ] PUT /cases/CASE-001 với {name:'Đã sửa', crime:'Đã sửa loại tội'}

### Dữ liệu kiểm thử
```
name='Đã sửa'
```

### Kết quả mong đợi
**UI**:
- Toast 'Cập nhật thành công', detail show new value

**API**:
- , data.name='Đã sửa'

**Side effects** (DB, email, log, queue...):
- Audit CASE_UPDATED với before/after diff

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-097
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-098

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-098`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update với expectedUpdatedAt khớp current → success

### Điều kiện tiên quyết
- CASE-001 updatedAt='2026-05-22T10:00:00.000Z'

### Các bước kiểm thử
- [ ] PUT với expectedUpdatedAt='2026-05-22T10:00:00.000Z' và body sửa

### Dữ liệu kiểm thử
```
expectedUpdatedAt='2026-05-22T10:00:00.000Z'
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- updatedAt mới refresh

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-098
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-099

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-099`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update với expectedUpdatedAt cũ → 409 Conflict

### Điều kiện tiên quyết
- CASE-001 updatedAt='2026-05-23T08:00' (đã có user khác update)

### Các bước kiểm thử
- [ ] PUT với expectedUpdatedAt='2026-05-22T10:00' (cũ)

### Dữ liệu kiểm thử
```
expectedUpdatedAt='2026-05-22T10:00:00.000Z'
```

### Kết quả mong đợi
**UI**:
- Toast 'Hồ sơ đã được chỉnh sửa bởi người dùng khác. Vui lòng tải lại trang'

**API**:
- ConflictException

**Side effects** (DB, email, log, queue...):
- Không update

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-099
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-100

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-100`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update Case ngoài write-scope → 403

### Điều kiện tiên quyết
- Login wardOfficer-Q1, CASE-Q3 thuộc Team-Q3

### Các bước kiểm thử
- [ ] PUT /cases/CASE-Q3 {name:'hack'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Bạn không có quyền chỉnh sửa bản ghi này'

**API**:
- ForbiddenException (checkWriteScope)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-100
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-101

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-101`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update Case không tồn tại → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT /cases/NOT_EXIST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-101
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-102

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-102`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update Case đã soft-deleted → 404

### Điều kiện tiên quyết
- CASE-DEL deletedAt set

### Các bước kiểm thử
- [ ] PUT /cases/CASE-DEL

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (where deletedAt:null)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-102
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-103

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-103`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update status TIEP_NHAN → DANG_XAC_MINH tạo CaseStatusHistory + audit

### Điều kiện tiên quyết
- CASE-001 status=TIEP_NHAN

### Các bước kiểm thử
- [ ] PUT {status:'DANG_XAC_MINH'}

### Dữ liệu kiểm thử
```
status='DANG_XAC_MINH'
```

### Kết quả mong đợi
**UI**:
- Badge change to 'Đang xác minh', tab History show entry

**API**:
- , data.status=DANG_XAC_MINH

**Side effects** (DB, email, log, queue...):
- Row trong case_status_history với fromStatus=TIEP_NHAN, toStatus=DANG_XAC_MINH, changedById
- Audit CASE_STATUS_CHANGED

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-103
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-104

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-104`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update status DANG_DIEU_TRA → TAM_DINH_CHI với lyDoTamDinhChiVuAn auto-set ngày + count

### Điều kiện tiên quyết
- CASE-001 status=DANG_DIEU_TRA, soLanTamDinhChi=0

### Các bước kiểm thử
- [ ] PUT {status:'TAM_DINH_CHI', lyDoTamDinhChiVuAn:'CHUA_XAC_DINH_BI_CAN', soQuyetDinhTamDinhChi='QĐ-123'}

### Dữ liệu kiểm thử
```
status, lyDoTamDinhChiVuAn, soQuyetDinhTamDinhChi
```

### Kết quả mong đợi
**UI**:
- Badge 'Tạm đình chỉ'

**API**:
- , data.ngayTamDinhChi=now(), data.soLanTamDinhChi=1

**Side effects** (DB, email, log, queue...):
- ngayTamDinhChi auto-set + soLanTamDinhChi increment +1

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-104
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-105

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-105`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update → TAM_DINH_CHI thiếu lyDoTamDinhChiVuAn (Case post-migration) → 400

### Điều kiện tiên quyết
- CASE-NEW createdAt='2026-05-15' (sau 2026-04-30 MIGRATION_DATE), status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT {status:'TAM_DINH_CHI'} (thiếu lyDoTamDinhChiVuAn)

### Dữ liệu kiểm thử
```
status='TAM_DINH_CHI'
```

### Kết quả mong đợi
**UI**:
- Toast 'Vui lòng chọn lý do tạm đình chỉ theo quy định Điều 229 BLTTHS 2015'

**API**:
- BadRequestException

**Side effects** (DB, email, log, queue...):
- Không update

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-105
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-107

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-107`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: lyDoTamDinhChiVuAn = invalid enum 'INVALID_REASON' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT lyDoTamDinhChiVuAn='INVALID_REASON'

### Dữ liệu kiểm thử
```
lyDoTamDinhChiVuAn='INVALID_REASON'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Lý do tạm đình chỉ phải theo quy định BLTTHS Điều 229'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-107
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-108

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-108`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update status TAM_DINH_CHI → DANG_DIEU_TRA (phục hồi) với daRaSoat + ngayRaSoat

### Điều kiện tiên quyết
- CASE-001 status=TAM_DINH_CHI

### Các bước kiểm thử
- [ ] PUT {status:'DANG_DIEU_TRA', daRaSoat:true, ngayRaSoat:'2026-05-23', soQuyetDinhPhucHoi:'PH-001', ketQuaPhucHoiVuAn:'DANG_DIEU_TRA_XAC_MINH'}

### Dữ liệu kiểm thử
```
daRaSoat=true, ngayRaSoat, soQuyetDinhPhucHoi, ketQuaPhucHoiVuAn
```

### Kết quả mong đợi
**UI**:
- Badge 'Đang điều tra', tab 'Phục hồi' show số QĐ + ngày

**API**:

**Side effects** (DB, email, log, queue...):
- Audit CASE_STATUS_CHANGED + cases row có daRaSoat=true

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-108
severity: Critical
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-118

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-118`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Audit CASE_UPDATED capture before/after diff đầy đủ

### Điều kiện tiên quyết
- CASE-001 name='Old', crime='Old crime'

### Các bước kiểm thử
- [ ] PUT {name:'New', crime:'New crime'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Audit log CASE_UPDATED metadata.diff={name:{before:'Old', after:'New'}, crime:{before:..., after:...}}

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-118
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: audit.wrapUpdate

---

## TC-124

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-124`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: DTO whitelist reject field lạ trong UpdateCaseDto (forbidNonWhitelisted)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT {name:'X', evilField:'hack'}

### Dữ liệu kiểm thử
```
evilField='hack'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'property evilField should not exist'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-124
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-125

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-125`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition TIEP_NHAN → DANG_XAC_MINH (valid)

### Điều kiện tiên quyết
- status=TIEP_NHAN

### Các bước kiểm thử
- [ ] PUT status=DANG_XAC_MINH

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Badge change

**API**:

**Side effects** (DB, email, log, queue...):
- History entry created

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-125
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-126

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-126`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DANG_XAC_MINH → DA_XAC_MINH (valid)

### Điều kiện tiên quyết
- status=DANG_XAC_MINH

### Các bước kiểm thử
- [ ] PUT status=DA_XAC_MINH

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-126
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-127

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-127`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DA_XAC_MINH → DANG_DIEU_TRA (valid)

### Điều kiện tiên quyết
- status=DA_XAC_MINH

### Các bước kiểm thử
- [ ] PUT status=DANG_DIEU_TRA

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-127
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-128

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-128`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DANG_DIEU_TRA → DA_KET_LUAN (valid)

### Điều kiện tiên quyết
- status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT status=DA_KET_LUAN

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-128
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-129

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-129`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DA_KET_LUAN → DANG_TRUY_TO (valid)

### Điều kiện tiên quyết
- status=DA_KET_LUAN

### Các bước kiểm thử
- [ ] PUT status=DANG_TRUY_TO

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-129
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-130

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-130`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DANG_TRUY_TO → DANG_XET_XU (valid)

### Điều kiện tiên quyết
- status=DANG_TRUY_TO

### Các bước kiểm thử
- [ ] PUT status=DANG_XET_XU

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-130
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-131

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-131`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DANG_XET_XU → DA_LUU_TRU (final state)

### Điều kiện tiên quyết
- status=DANG_XET_XU

### Các bước kiểm thử
- [ ] PUT status=DA_LUU_TRU

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-131
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-132

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-132`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition any → DINH_CHI (đình chỉ vĩnh viễn)

### Điều kiện tiên quyết
- status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT status=DINH_CHI

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Badge 'Đình chỉ' đỏ

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-132
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-133

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-133`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition TAM_DINH_CHI → DA_KET_LUAN (phục hồi và kết luận luôn)

### Điều kiện tiên quyết
- status=TAM_DINH_CHI, daRaSoat=true

### Các bước kiểm thử
- [ ] PUT status=DA_KET_LUAN, ketQuaPhucHoiVuAn=KET_LUAN_DE_NGHI_TRUY_TO

### Dữ liệu kiểm thử
```
ketQuaPhucHoiVuAn='KET_LUAN_DE_NGHI_TRUY_TO'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Có 2 entry history (1 lần phục hồi, 1 lần kết luận)?

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-133
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Implementation hiện chỉ tạo 1 entry — verify logic

---

## TC-136

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-136`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DINH_CHI → DANG_DIEU_TRA (sai logic) — cần block ở backend

### Điều kiện tiên quyết
- status=DINH_CHI

### Các bước kiểm thử
- [ ] PUT status=DANG_DIEU_TRA

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — gap: hiện cho phép trừ khi frontend disable

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-136
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần backend enforce transition map

---

## TC-137

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-137`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DANG_XET_XU → TIEP_NHAN (rollback xa) — invalid logic

### Điều kiện tiên quyết
- status=DANG_XET_XU

### Các bước kiểm thử
- [ ] PUT status=TIEP_NHAN

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Gap: hiện cho phép

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-137
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Sample case test gap: cần state validation

---

## TC-138

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-138`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: TAM_DINH_CHI lần thứ 2 (recurrent suspend) — soLanTamDinhChi=2

### Điều kiện tiên quyết
- status=DANG_DIEU_TRA, soLanTamDinhChi=1

### Các bước kiểm thử
- [ ] PUT status=TAM_DINH_CHI với lyDoTamDinhChiVuAn

### Dữ liệu kiểm thử
```
lyDoTamDinhChiVuAn='BI_CAN_BENH_TAM_THAN'
```

### Kết quả mong đợi
**UI**:
- Detail show 'Đã tạm đình chỉ 2 lần'

**API**:

**Side effects** (DB, email, log, queue...):
- soLanTamDinhChi increment to 2 (continuously)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-138
severity: High
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-139

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-139`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Soft delete Case TIEP_NHAN, reason 50 chars, creator → success

### Điều kiện tiên quyết
- CASE-001 status=TIEP_NHAN, createdById=dieuTra1, không có subjects/lawyers/conclusions

### Các bước kiểm thử
- [ ] Login dieuTra1
- [ ] DELETE /cases/CASE-001 với body {reason:'Tạo nhầm, cần xóa'}

### Dữ liệu kiểm thử
```
reason='Tạo nhầm, cần xóa, hạng mục thừa khi import'
```

### Kết quả mong đợi
**UI**:
- Toast 'Xóa vụ án thành công'

**API**:
- message='Xóa vụ án thành công'

**Side effects** (DB, email, log, queue...):
- cases.deletedAt=now()
- Audit CASE_DELETED metadata.reason + hoursAfterCreation

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-139
severity: Critical
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-140

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-140`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Reason < 10 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE body {reason:'Sai'}

### Dữ liệu kiểm thử
```
reason='Sai' (3 chars)
```

### Kết quả mong đợi
**UI**:
- Error 'Reason phải 10-500 ký tự'

**API**:
- (DTO MinLength(10))

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-140
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-141

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-141`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Reason > 500 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE reason='X'.repeat(501)

### Dữ liệu kiểm thử
```
length=501
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- MaxLength(500)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-141
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-142

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-142`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Thiếu reason → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE không body

### Dữ liệu kiểm thử
```
{}
```

### Kết quả mong đợi
**UI**:
- Error

**API**:
- reason required

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-142
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-143

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-143`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Reason rỗng '' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE reason=''

### Dữ liệu kiểm thử
```
reason=''
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-143
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-144

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-144`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Reason chỉ whitespace '          ' → 400 (Transform trim)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE reason='          '

### Dữ liệu kiểm thử
```
reason='          '
```

### Kết quả mong đợi
**UI**:
- Error 'Reason bắt buộc'

**API**:
- sau Transform trim

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-144
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Tương tự BUG-001 cho name field

---

## TC-145

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-145`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Status ≠ TIEP_NHAN (DANG_DIEU_TRA) → 400 'Chỉ xóa được vụ án ở trạng thái Tiếp nhận'

### Điều kiện tiên quyết
- CASE-002 status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] DELETE /cases/CASE-002 reason='Sai dữ liệu cần xóa'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast '... Vụ án đã chuyển trạng thái không thể xóa'

**API**:
- BadRequestException

**Side effects** (DB, email, log, queue...):
- Không soft-delete

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-145
severity: Critical
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-146

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-146`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Có ≥1 subject linked → 400 'có N đối tượng. Xóa các đối tượng trước'

### Điều kiện tiên quyết
- CASE-001 có 2 subjects deletedAt=null

### Các bước kiểm thử
- [ ] DELETE /cases/CASE-001 reason='...'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Không thể xóa: vụ án có 2 đối tượng. Xóa các đối tượng trước.'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-146
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-147

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-147`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Có ≥1 lawyer linked → 400 'có N luật sư'

### Điều kiện tiên quyết
- CASE-001 có 1 lawyer

### Các bước kiểm thử
- [ ] DELETE reason='...'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Không thể xóa: vụ án có 1 luật sư đăng ký. Xóa các luật sư trước.'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-147
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-148

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-148`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Có ≥1 conclusion → 400

### Điều kiện tiên quyết
- CASE-001 có 1 conclusion

### Các bước kiểm thử
- [ ] DELETE

### Kết quả mong đợi
**UI**:
- '... có 1 kết luận điều tra'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-148
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-149

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-149`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Có ≥1 document → 400

### Điều kiện tiên quyết
- CASE-001 có 3 documents

### Các bước kiểm thử
- [ ] DELETE

### Kết quả mong đợi
**UI**:
- '... 3 tài liệu đính kèm'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-149
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-150

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-150`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Có ≥1 linkedIncident → 400

### Điều kiện tiên quyết
- CASE-001 link với 1 Incident

### Các bước kiểm thử
- [ ] DELETE

### Kết quả mong đợi
**UI**:
- '... 1 vụ việc liên kết'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-150
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-151

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-151`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: User không phải creator + không phải ADMIN → 403

### Điều kiện tiên quyết
- CASE-001 createdById=dieuTra1, login bằng dieuTra2

### Các bước kiểm thử
- [ ] DELETE

### Kết quả mong đợi
**UI**:
- 'Chỉ người tạo vụ án hoặc quản trị viên mới được xóa.'

**API**:
- ForbiddenException

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-151
severity: Critical
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-152

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-152`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: createdById=NULL (legacy data) + user không ADMIN → 403 message riêng

### Điều kiện tiên quyết
- CASE-LEGACY createdById=NULL

### Các bước kiểm thử
- [ ] Login dieuTra1, DELETE

### Kết quả mong đợi
**UI**:
- 'Vụ án không có thông tin người tạo (dữ liệu cũ). Chỉ quản trị viên mới được xóa.'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-152
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-153

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-153`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: ADMIN xóa được Case của bất kỳ ai trong window

### Điều kiện tiên quyết
- Login admin@, CASE-X createdById=dieuTra1

### Các bước kiểm thử
- [ ] DELETE reason='ADMIN cleanup test data'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- Soft-deleted + Audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-153
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-154

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-154`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Quá window (THOI_HAN_XOA_VU_AN=72h default) + không ADMIN → 400

### Điều kiện tiên quyết
- CASE-OLD createdAt='2026-05-19' (>72h trước), createdById=dieuTra1

### Các bước kiểm thử
- [ ] Login dieuTra1, DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Đã quá 72 giờ kể từ khi tạo vụ án. Chỉ quản trị viên mới xóa được.'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-154
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-155

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-155`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: ADMIN có thể xóa Case quá 72h window

### Điều kiện tiên quyết
- CASE-OLD >72h

### Các bước kiểm thử
- [ ] Login admin@, DELETE

### Dữ liệu kiểm thử
```
reason='ADMIN xóa sau window'
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- Bypass window check (isAdmin branch)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-155
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-156

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-156`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Race condition: status đổi trong lúc DELETE → P2025 catch → 400 friendly

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE while another user updates status TIEP_NHAN→DANG_XAC_MINH

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Vụ án đã đổi trạng thái trong lúc thực hiện. Vui lòng tải lại danh sách.'

**API**:
- (P2025 catch)

**Side effects** (DB, email, log, queue...):
- Atomic status guard fired

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-156
severity: High
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Where: id, status=TIEP_NHAN, deletedAt=null

---

## TC-157

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delete`
- Yêu cầu: `REQ-CASE-DELETE-157`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: TOCTOU: subject vừa được insert giữa initial check và transaction → 400 trong tx

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Initial check pass (subjects=0), insert subject, transaction re-check (subjects=1)

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Không thể xóa: vụ án có 1 đối tượng (vừa được thêm). Tải lại danh sách.'

**API**:
- trong transaction

**Side effects** (DB, email, log, queue...):
- Atomic — không orphan delete

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-157
severity: Critical
module: Delete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: TOCTOU fix per codex P1

---

## TC-158

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `DeletePreflight`
- Yêu cầu: `REQ-CASE-DELETEPREFLIGHT-158`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /cases/:id/delete-preflight trả canDelete=true cho TIEP_NHAN + không blocker

### Điều kiện tiên quyết
- CASE-001 TIEP_NHAN, không sub-resource

### Các bước kiểm thử
- [ ] GET /cases/CASE-001/delete-preflight

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Modal show 'Có thể xóa', nút 'Xóa' enable

**API**:
- , {canDelete:true, status:'TIEP_NHAN', blockers:{subjects:0,lawyers:0,...}, reasonsIfBlocked:[]}

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DeletePreflight`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DeletePreflight`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-158
severity: High
module: DeletePreflight
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-159

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `DeletePreflight`
- Yêu cầu: `REQ-CASE-DELETEPREFLIGHT-159`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Preflight Case status=DA_KET_LUAN trả canDelete=false + reason

### Điều kiện tiên quyết
- CASE-003 status=DA_KET_LUAN

### Các bước kiểm thử
- [ ] GET preflight

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Modal hiện 'Không thể xóa', list reasons

**API**:
- canDelete=false, reasonsIfBlocked includes 'Trạng thái hiện tại không cho phép xóa (chỉ Tiếp nhận). Hiện: Đã kết luận.'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DeletePreflight`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DeletePreflight`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-159
severity: High
module: DeletePreflight
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-160

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `DeletePreflight`
- Yêu cầu: `REQ-CASE-DELETEPREFLIGHT-160`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Preflight Case có 2 subjects + 1 document → reasons detailed

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET preflight

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- reasons displayed

**API**:
- reasonsIfBlocked includes both

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DeletePreflight`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DeletePreflight`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-160
severity: Medium
module: DeletePreflight
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-161

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `DeletePreflight`
- Yêu cầu: `REQ-CASE-DELETEPREFLIGHT-161`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Preflight Case ngoài scope → 403 (checkRecordInScope)

### Điều kiện tiên quyết
- Login wardOfficer-Q1, CASE-Q3

### Các bước kiểm thử
- [ ] GET preflight

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DeletePreflight`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DeletePreflight`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-161
severity: High
module: DeletePreflight
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-162

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Restore`
- Yêu cầu: `REQ-CASE-RESTORE-162`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: ADMIN khôi phục Case đã soft-deleted

### Điều kiện tiên quyết
- CASE-DEL deletedAt set, login admin@

### Các bước kiểm thử
- [ ] POST /cases/CASE-DEL/restore body {reason:'Sai sót xóa nhầm'}

### Dữ liệu kiểm thử
```
reason='Sai sót xóa nhầm cần khôi phục lại'
```

### Kết quả mong đợi
**UI**:
- Toast 'Khôi phục vụ án thành công'

**API**:
- message

**Side effects** (DB, email, log, queue...):
- cases.deletedAt=NULL
- Audit CASE_RESTORED metadata.reason + hoursAfterDeletion

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Restore`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Restore`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-162
severity: Critical
module: Restore
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-163

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Restore`
- Yêu cầu: `REQ-CASE-RESTORE-163`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Non-ADMIN gọi restore → 403 PermissionsGuard (action=restore)

### Điều kiện tiên quyết
- Login dieuTra1@

### Các bước kiểm thử
- [ ] POST /cases/X/restore

### Dữ liệu kiểm thử
```
reason='try'
```

### Kết quả mong đợi
**UI**:

**API**:
- PermissionsGuard ('restore' permission)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Restore`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Restore`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-163
severity: Critical
module: Restore
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-164

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Restore`
- Yêu cầu: `REQ-CASE-RESTORE-164`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Restore Case chưa từng bị xóa (deletedAt=null) → 404

### Điều kiện tiên quyết
- CASE-ACTIVE deletedAt=null

### Các bước kiểm thử
- [ ] POST /cases/CASE-ACTIVE/restore

### Dữ liệu kiểm thử
```
reason='try restore'
```

### Kết quả mong đợi
**UI**:
- 'Vụ án không tồn tại hoặc chưa bị xóa'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Restore`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Restore`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-164
severity: High
module: Restore
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-165

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Restore`
- Yêu cầu: `REQ-CASE-RESTORE-165`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Race: 2 ADMIN restore cùng case → 1 ConflictException friendly

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Đồng thời ADMIN-A và ADMIN-B gọi restore

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- success, 1 'Vụ án đã được khôi phục bởi quản trị viên khác. Tải lại danh sách.'

**API**:
- ×200 + 1×400 (P2025 catch)

**Side effects** (DB, email, log, queue...):
- Chỉ 1 audit log

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Restore`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Restore`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-165
severity: High
module: Restore
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-167

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `ListDeleted`
- Yêu cầu: `REQ-CASE-LISTDELETED-167`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: ADMIN GET /cases/admin/deleted trả paginated deleted cases với deleteAudit

### Điều kiện tiên quyết
- DB có 5 case deleted

### Các bước kiểm thử
- [ ] Login admin@, GET /cases/admin/deleted

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Bảng danh sách xóa, mỗi row show người xóa + lý do

**API**:
- , data[].deleteAudit{userId, metadata:{reason, ...}}

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `ListDeleted`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `ListDeleted`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-167
severity: High
module: ListDeleted
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-168

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `ListDeleted`
- Yêu cầu: `REQ-CASE-LISTDELETED-168`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Non-ADMIN gọi /cases/admin/deleted → 403

### Điều kiện tiên quyết
- dieuTra1

### Các bước kiểm thử
- [ ] GET /cases/admin/deleted

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- PermissionsGuard (restore action gate)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `ListDeleted`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `ListDeleted`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-168
severity: Critical
module: ListDeleted
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-169

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-169`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Dispatcher phân công Case sang Team-Q2 + investigator của team

### Điều kiện tiên quyết
- Login dispatcher@, CASE-001 assignedTeamId=Team-Q1, dieuTra-Q2 thuộc Team-Q2

### Các bước kiểm thử
- [ ] PATCH /cases/CASE-001/assign {assignedTeamId:'Team-Q2', investigatorId:'<dieuTra-Q2>'}

### Dữ liệu kiểm thử
```
assignedTeamId='Team-Q2', investigatorId='<dieuTra-Q2.id>'
```

### Kết quả mong đợi
**UI**:
- Toast 'Phân công vụ án thành công', detail show team mới

**API**:

**Side effects** (DB, email, log, queue...):
- cases.assignedTeamId='Team-Q2'
- Audit CASE_ASSIGNED metadata.fromTeamId, toTeamId, fromInvestigatorId, toInvestigatorId

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-169
severity: Critical
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-170

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-170`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Non-dispatcher gọi assign → 403 DispatchGuard

### Điều kiện tiên quyết
- Login dieuTra1@ (canDispatch=false)

### Các bước kiểm thử
- [ ] PATCH /cases/CASE-001/assign

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:
- DispatchGuard

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-170
severity: Critical
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-171

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-171`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Case ngoài scope của dispatcher (nếu dispatcher có scope giới hạn) → ?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Dispatcher scope=ward, assign cross-ward

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Hiện DispatchGuard chỉ check role không check scope

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-171
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap potential: dispatcher có thể assign vượt scope của họ

---

## TC-172

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-172`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: assignedTeamId team đã isActive=false → 400 'Tổ điều tra không tồn tại hoặc đã ngừng hoạt động'

### Điều kiện tiên quyết
- Team-INACTIVE isActive=false

### Các bước kiểm thử
- [ ] PATCH assign assignedTeamId='Team-INACTIVE'

### Dữ liệu kiểm thử
```
assignedTeamId='Team-INACTIVE'
```

### Kết quả mong đợi
**UI**:
- Toast lỗi

**API**:
- BadRequestException

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-172
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-173

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-173`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: investigatorId không thuộc team được chỉ định → 400 'Điều tra viên không thuộc tổ'

### Điều kiện tiên quyết
- dieuTra-Q1 chỉ thuộc Team-Q1

### Các bước kiểm thử
- [ ] PATCH assign assignedTeamId='Team-Q2', investigatorId='<dieuTra-Q1>'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Điều tra viên không thuộc tổ được chỉ định'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-173
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: UserTeam join check

---

## TC-175

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-175`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Case không tồn tại → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH /cases/NOT_EXIST/assign

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-175
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-176

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-176`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Case đã soft-deleted → 404

### Điều kiện tiên quyết
- CASE-DEL

### Các bước kiểm thử
- [ ] PATCH assign

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-176
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-177

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-177`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: expectedUpdatedAt mismatch → 409

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH assign với expectedUpdatedAt cũ

### Dữ liệu kiểm thử
```
expectedUpdatedAt cũ
```

### Kết quả mong đợi
**UI**:
- Toast 'Vụ án đã được chỉnh sửa bởi người dùng khác'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-177
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-178

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-178`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Escalation: ward team → non-ward team → emit CASE_ESCALATED_FROM_WARD audit

### Điều kiện tiên quyết
- CASE-001 assignedTeam=Team-WARD-01 (wardId set)

### Các bước kiểm thử
- [ ] PATCH assign assignedTeamId='Team-PC02-CENTRAL' (wardId=null)

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Audit CASE_ASSIGNED
- Audit CASE_ESCALATED_FROM_WARD với metadata.oldWardId, oldWardName, newTeamId

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-178
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: v0.35a

---

## TC-184

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-184`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Non-dispatcher với JWT của dispatcher (token theft) → permission guard pass? — JWT check role thật

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Steal dispatcher's JWT, gọi từ user khác

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- JWT decode trả role=dispatcher → guard pass (limitation)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-184
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Mitigate: JWT short expiry + IP binding (out of scope UAT)

---

## TC-185

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-185`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /cases/export/ward trả file Excel với header BCA + 8 cột

### Điều kiện tiên quyết
- DB có 30 case

### Các bước kiểm thử
- [ ] GET /api/v1/cases/export/ward

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Browser download file 'VuAnPhuongXa_2026-05-23.xlsx'

**API**:
- Content-Type=spreadsheetml, Content-Disposition=attachment

**Side effects** (DB, email, log, queue...):
- File Excel valid, sheet 'Danh sách vụ án' header 'DANH SÁCH VỤ ÁN THEO PHƯỜNG/XÃ'
- Audit CASE_EXPORTED metadata.kind='ward', format='xlsx'

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-185
severity: High
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-186

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-186`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Export ward với fromDate + toDate trong period

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/export/ward?fromDate=2026-05-01&toDate=2026-05-23

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Excel header 'Từ ngày 01/05/2026 đến ngày 23/05/2026'

**API**:

**Side effects** (DB, email, log, queue...):
- Excel period row đúng format vi-VN

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-186
severity: Medium
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-187

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-187`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Export 'phân loại khác' với category filter

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/export/other-classification?category=lừa đảo

### Dữ liệu kiểm thử
```
category='lừa đảo'
```

### Kết quả mong đợi
**UI**:
- Download file

**API**:
- , data filter crime contains 'lừa đảo' (insensitive)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-187
severity: Medium
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-188

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-188`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Rate limit: gọi /export/ward 6 lần trong 60s → 429

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Gọi 6 lần liên tiếp trong 60s

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Lần 6: '429 Too Many Requests'

**API**:
- th call returns 429 (Throttle 5 req/60s)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-188
severity: High
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: @Throttle({default:{ttl:60000, limit:5}})

---

## TC-189

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-189`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Export với scope filter — ward officer chỉ thấy case của ward mình

### Điều kiện tiên quyết
- Login wardOfficer-Q1

### Các bước kiểm thử
- [ ] GET /cases/export/ward (không param)

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Excel chỉ chứa case của Team-WARD-Q1

**API**:
- , records filtered by scope

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-189
severity: Critical
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-190

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-190`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Dispatcher xem tất cả case khi export

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Login dispatcher@, GET export

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Excel có cả case của Team khác

**API**:
- not filtered

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-190
severity: High
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-192

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-192`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Permission: user role VIEWER không có 'read Case' → 403

### Điều kiện tiên quyết
- Login viewer (no read permission)

### Các bước kiểm thử
- [ ] GET export

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- PermissionsGuard

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-192
severity: High
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-193

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-193`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET export không Authorization

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-193
severity: Critical
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-197

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `History`
- Yêu cầu: `REQ-CASE-HISTORY-197`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /cases/:id/status-history trả list sorted asc changedAt

### Điều kiện tiên quyết
- CASE-001 có 3 entries: TIEP_NHAN→DANG_XAC_MINH→DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] GET /cases/CASE-001/status-history

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Tab 'Lịch sử' show 3 entries chronological

**API**:
- , data=[{fromStatus, toStatus, changedAt, changedBy:{firstName,lastName,username}}, ...]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `History`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `History`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-197
severity: High
module: History
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-199

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `History`
- Yêu cầu: `REQ-CASE-HISTORY-199`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET history của Case không tồn tại → 200 với data=[] (no 404 hiện tại)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/NOT_EXIST/status-history

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data=[]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `History`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `History`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-199
severity: Medium
module: History
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên 404 — hiện không check Case tồn tại

---

## TC-201

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `History`
- Yêu cầu: `REQ-CASE-HISTORY-201`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: User ngoài scope GET history → ?

### Điều kiện tiên quyết
- Login wardOfficer-Q1, CASE-Q3

### Các bước kiểm thử
- [ ] GET /cases/CASE-Q3/status-history

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Hiện không enforce scope ở getStatusHistory — gap

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `History`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `History`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-201
severity: High
module: History
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần thêm checkRecordInScope

---

## TC-203

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `TdcBackfill`
- Yêu cầu: `REQ-CASE-TDCBACKFILL-203`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PATCH /cases/:id/tdc-backfill update lyDoTamDinhChiVuAn

### Điều kiện tiên quyết
- CASE-OLD lyDoTamDinhChiVuAn=NULL (legacy)

### Các bước kiểm thử
- [ ] PATCH /cases/CASE-OLD/tdc-backfill {lyDoTamDinhChiVuAn:'CHUA_XAC_DINH_BI_CAN'}

### Dữ liệu kiểm thử
```
lyDoTamDinhChiVuAn='CHUA_XAC_DINH_BI_CAN'
```

### Kết quả mong đợi
**UI**:
- Toast success, banner backfill ẩn

**API**:

**Side effects** (DB, email, log, queue...):
- cases.lyDoTamDinhChiVuAn updated

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `TdcBackfill`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `TdcBackfill`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-203
severity: High
module: TdcBackfill
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-204

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `TdcBackfill`
- Yêu cầu: `REQ-CASE-TDCBACKFILL-204`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PATCH với Case không tồn tại → 404 'Case not found'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH /cases/NOT_EXIST/tdc-backfill

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `TdcBackfill`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `TdcBackfill`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-204
severity: High
module: TdcBackfill
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-206

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `TdcBackfill`
- Yêu cầu: `REQ-CASE-TDCBACKFILL-206`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: User không có write permission → 403

### Điều kiện tiên quyết
- Login viewer

### Các bước kiểm thử
- [ ] PATCH tdc-backfill

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- PermissionsGuard write/Case

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `TdcBackfill`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `TdcBackfill`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-206
severity: High
module: TdcBackfill
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-208

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-208`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR: User A trực tiếp GET /cases/<id-của-B> (URL guessing) → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Login userA, GET /cases/<id-Case-của-userB-khác-team>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ForbiddenException

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-208
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-209

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-209`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Horizontal privilege escalation: dieuTra-Q1 sửa Case-Q3 → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT /cases/CASE-Q3

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- checkWriteScope

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-209
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-210

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-210`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Vertical: VIEWER role thử POST /cases → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] VIEWER POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-210
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-211

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-211`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: DELETE Case của user khác (creator check) → 403

### Điều kiện tiên quyết
- CASE createdById=A, login B

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
reason='...'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-211
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-212

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-212`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Non-ADMIN restore → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra1 POST restore

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-212
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-213

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-213`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET /cases/admin/deleted với non-ADMIN → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra1 GET admin/deleted

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-213
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-214

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A01`
- Yêu cầu: `REQ-CASE-A01-214`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Non-dispatcher PATCH /assign → 403 DispatchGuard

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra1 PATCH /assign

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A01`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A01`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-214
severity: Critical
module: A01
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-215

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A02`
- Yêu cầu: `REQ-CASE-A02-215`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: JWT là HS256 với secret strong (≥32 chars) — không yếu

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Decode JWT, check alg=HS256, secret length

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- JWT verify OK, không alg=none

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A02`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A02`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-215
severity: Critical
module: A02
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: ASVS 6.2.3

---

## TC-216

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A02`
- Yêu cầu: `REQ-CASE-A02-216`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: API chạy trên HTTPS (production) — HTTP redirect 301

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] curl http://171.244.40.245/api/v1/cases

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- redirect to https

**Side effects** (DB, email, log, queue...):
- HSTS header strict-transport-security

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A02`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A02`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-216
severity: Critical
module: A02
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Production deploy yêu cầu domain + cert

---

## TC-217

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A03`
- Yêu cầu: `REQ-CASE-A03-217`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: SQLi qua search param: ?search=' OR 1=1-- → vẫn an toàn

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases?search=' OR 1=1--

### Dữ liệu kiểm thử
```
search="' OR 1=1--"
```

### Kết quả mong đợi
**UI**:
- Empty result (Prisma parametrized)

**API**:
- data=[] (no all-records leak)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A03`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A03`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-217
severity: Critical
module: A03
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Prisma uses parameterized queries

---

## TC-218

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A03`
- Yêu cầu: `REQ-CASE-A03-218`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: SQLi qua sortBy: ?sortBy=name; DROP TABLE cases-- → whitelist filter

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases?sortBy=name; DROP TABLE cases--

### Dữ liệu kiểm thử
```
sortBy='evil'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (whitelist allowedSortFields fallback createdAt)

**Side effects** (DB, email, log, queue...):
- Table không bị drop

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A03`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A03`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-218
severity: Critical
module: A03
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-219

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A03`
- Yêu cầu: `REQ-CASE-A03-219`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: XSS reflected: name=<script>alert(1)</script> → React escape khi render

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='<script>alert(1)</script>', xem detail

### Dữ liệu kiểm thử
```
name='<script>alert(1)</script>'
```

### Kết quả mong đợi
**UI**:
- Tên hiển thị literal text, KHÔNG popup alert

**API**:
- , data.name lưu nguyên (React handles escape ở render)

**Side effects** (DB, email, log, queue...):
- DB chứa raw — frontend phải escape

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A03`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A03`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-219
severity: Critical
module: A03
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-220

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A03`
- Yêu cầu: `REQ-CASE-A03-220`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: XSS stored: crime=<img src=x onerror=alert(1)> render detail → escape

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST crime với XSS payload

### Dữ liệu kiểm thử
```
crime='<img src=x onerror=alert(1)>'
```

### Kết quả mong đợi
**UI**:
- Hiển thị literal text

**API**:
- data.crime save raw

**Side effects** (DB, email, log, queue...):
- React-safe render

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A03`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A03`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-220
severity: Critical
module: A03
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-221

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A03`
- Yêu cầu: `REQ-CASE-A03-221`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: XSS DOM: search query có script trong URL → no eval

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases?search=<script>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- URL bar có script nhưng KHÔNG eval

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A03`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A03`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-221
severity: High
module: A03
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-222

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A03`
- Yêu cầu: `REQ-CASE-A03-222`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JSON injection vào metadata: metadata={__proto__:{isAdmin:true}}

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST metadata với prototype pollution

### Dữ liệu kiểm thử
```
metadata={__proto__:{isAdmin:true}}
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- nhưng object không pollute global

**Side effects** (DB, email, log, queue...):
- Object frozen / JSON parse safe

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A03`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A03`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-222
severity: High
module: A03
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Node.js modern JSON.parse safe

---

## TC-223

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A04`
- Yêu cầu: `REQ-CASE-A04-223`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Export rate limit ngăn data exfiltration

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] export calls/60s

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- th = 429

**Side effects** (DB, email, log, queue...):
- Audit CASE_EXPORTED log all attempts

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A04`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A04`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-223
severity: High
module: A04
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: A04 - rate-limit hidden

---

## TC-224

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A04`
- Yêu cầu: `REQ-CASE-A04-224`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Soft delete với reason mandatory ngăn data lost without trace

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- reason ≥10 chars enforced

**Side effects** (DB, email, log, queue...):
- Audit CASE_DELETED ghi reason

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A04`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A04`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-224
severity: High
module: A04
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-225

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A04`
- Yêu cầu: `REQ-CASE-A04-225`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Restore window check (legacy NULL createdById → ADMIN-only)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Try delete legacy

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- cho non-ADMIN

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A04`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A04`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-225
severity: High
module: A04
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-226

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A05`
- Yêu cầu: `REQ-CASE-A05-226`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: CORS header restrict origin (không *)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] curl -H 'Origin: https://evil.com' GET /cases

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- No Access-Control-Allow-Origin: * (chỉ trusted origins)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A05`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A05`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-226
severity: Critical
module: A05
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-227

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A05`
- Yêu cầu: `REQ-CASE-A05-227`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Response không có sensitive header X-Powered-By, Server version

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] curl -I /cases

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- No X-Powered-By header

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A05`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A05`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-227
severity: Medium
module: A05
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-228

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A05`
- Yêu cầu: `REQ-CASE-A05-228`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Security headers: HSTS, X-Content-Type-Options, X-Frame-Options

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect response headers

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Strict-Transport-Security: max-age=...; X-Content-Type-Options: nosniff; X-Frame-Options: DENY

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A05`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A05`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-228
severity: High
module: A05
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-229

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A07`
- Yêu cầu: `REQ-CASE-A07-229`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: JWT hết hạn → 401, không refresh tự động infinite

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Gửi JWT exp=past

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Redirect login

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A07`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A07`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-229
severity: Critical
module: A07
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-230

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A07`
- Yêu cầu: `REQ-CASE-A07-230`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: JWT signature tamper → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tamper signature part

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A07`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A07`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-230
severity: Critical
module: A07
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-231

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A07`
- Yêu cầu: `REQ-CASE-A07-231`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT của user đã deactivated/locked → 401 hoặc 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] User U001 disabled, gọi với JWT cũ

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- /403 (cần check user status mỗi request)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A07`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A07`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-231
severity: High
module: A07
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần verify backend check user.isActive

---

## TC-232

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A08`
- Yêu cầu: `REQ-CASE-A08-232`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Optimistic lock prevent lost update qua expectedUpdatedAt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] user update cùng case

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ×200 + 1×409

**Side effects** (DB, email, log, queue...):
- No data lost

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A08`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A08`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-232
severity: Critical
module: A08
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-233

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A08`
- Yêu cầu: `REQ-CASE-A08-233`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: FROM_PETITION race condition không double-link Petition

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] user FROM_PETITION cùng PET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ×201 + 1×409

**Side effects** (DB, email, log, queue...):
- Petition.linkedCaseId chỉ 1 giá trị

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A08`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A08`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-233
severity: Critical
module: A08
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-234

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A09`
- Yêu cầu: `REQ-CASE-A09-234`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Audit log CASE_DELETED ghi đầy đủ user, IP, UA, reason

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit_logs row {userId, action:'CASE_DELETED', subjectId, metadata:{reason, ...}, ipAddress, userAgent, createdAt}

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A09`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A09`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-234
severity: High
module: A09
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-235

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A09`
- Yêu cầu: `REQ-CASE-A09-235`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Audit không lưu sensitive data (password, JWT) trong metadata

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect audit row content

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Metadata không có password/token field

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A09`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A09`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-235
severity: High
module: A09
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-236

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `A09`
- Yêu cầu: `REQ-CASE-A09-236`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Audit retention ≥ 90 ngày (BLTTHS compliance)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Check audit_logs oldest row

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Retention policy ≥90d

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A09`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A09`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-236
severity: Medium
module: A09
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-238

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `RateLimit`
- Yêu cầu: `REQ-CASE-RATELIMIT-238`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: POST /cases brute force 100 req/s → throttle global

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] req/s POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — global throttle (need verify config)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `RateLimit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `RateLimit`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-238
severity: High
module: RateLimit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap nếu chưa có global throttle cho mutation

---

## TC-239

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `CSRF`
- Yêu cầu: `REQ-CASE-CSRF-239`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Mutation POST/PUT/DELETE từ origin lạ không có cookie + custom header → reject CORS

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] curl POST /cases from evil.com

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- CORS reject preflight

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `CSRF`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `CSRF`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-239
severity: High
module: CSRF
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-240

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `MassAssign`
- Yêu cầu: `REQ-CASE-MASSASSIGN-240`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: POST kèm field 'createdById':'other-user' bị whitelist reject

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST {name:..., createdById:'evil-id'}

### Dữ liệu kiểm thử
```
createdById='evil-id'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- forbidNonWhitelisted (createdById không trong DTO)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `MassAssign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `MassAssign`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-240
severity: Critical
module: MassAssign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-241

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `MassAssign`
- Yêu cầu: `REQ-CASE-MASSASSIGN-241`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: POST kèm 'id':'fake-id' không cho phép override

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST {id:'evil-id', name:...}

### Dữ liệu kiểm thử
```
id='evil-id'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc id auto-generated

**Side effects** (DB, email, log, queue...):
- data.id = cuid generated, không phải 'evil-id'

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `MassAssign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `MassAssign`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-241
severity: Critical
module: MassAssign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-242

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `MassAssign`
- Yêu cầu: `REQ-CASE-MASSASSIGN-242`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: POST kèm 'deletedAt':null override không cho phép

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST {deletedAt:'2030-01-01', ...}

### Dữ liệu kiểm thử
```
deletedAt='2030-01-01'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- forbidNonWhitelisted

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `MassAssign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `MassAssign`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-242
severity: High
module: MassAssign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-301

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P0` 🔴
- Module: `name`
- Yêu cầu: `REQ-CASE-NAME-301`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name 1 ký tự 'A' (min) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='A'

### Dữ liệu kiểm thử
```
name='A' (length 1)
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `name`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-301
severity: Medium
module: name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-302

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P0` 🔴
- Module: `name`
- Yêu cầu: `REQ-CASE-NAME-302`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name 500 ký tự (max) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='X'.repeat(500)

### Dữ liệu kiểm thử
```
length=500
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `name`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-302
severity: Medium
module: name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-303

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P0` 🔴
- Module: `name`
- Yêu cầu: `REQ-CASE-NAME-303`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: name 501 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=501
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- MaxLength

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `name`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-303
severity: High
module: name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-304

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P0` 🔴
- Module: `name`
- Yêu cầu: `REQ-CASE-NAME-304`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: name 499 ký tự → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=499
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `name`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-304
severity: Low
module: name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-333

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `DataScope-C1`
- Yêu cầu: `REQ-CASE-DATASCOPE-C1-333`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: ADMIN (dataScope=null) xem mọi Case

### Điều kiện tiên quyết
- admin@

### Các bước kiểm thử
- [ ] GET /cases/ANY

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DataScope-C1`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DataScope-C1`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-333
severity: Critical
module: DataScope-C1
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Decision C1: dataScope=null bypass

---

## TC-334

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `DataScope-C2`
- Yêu cầu: `REQ-CASE-DATASCOPE-C2-334`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Dispatcher (canDispatch=true) xem mọi Case

### Điều kiện tiên quyết
- dispatcher@

### Các bước kiểm thử
- [ ] GET /cases/ANY

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DataScope-C2`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DataScope-C2`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-334
severity: Critical
module: DataScope-C2
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-335

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `DataScope-C3`
- Yêu cầu: `REQ-CASE-DATASCOPE-C3-335`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Owner (investigatorId match userIds) xem Case mình

### Điều kiện tiên quyết
- dieuTra1 = investigator của CASE-001

### Các bước kiểm thử
- [ ] GET /cases/CASE-001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DataScope-C3`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DataScope-C3`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-335
severity: High
module: DataScope-C3
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: ownerMatch

---

## TC-336

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `DataScope-C4`
- Yêu cầu: `REQ-CASE-DATASCOPE-C4-336`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Same team (assignedTeamId in teamIds) xem Case đồng nghiệp

### Điều kiện tiên quyết
- dieuTra1 cùng Team-Q1 với CASE-002

### Các bước kiểm thử
- [ ] GET /cases/CASE-002

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DataScope-C4`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DataScope-C4`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-336
severity: High
module: DataScope-C4
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: teamMatch

---

## TC-337

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `DataScope-C5`
- Yêu cầu: `REQ-CASE-DATASCOPE-C5-337`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Unassigned Case + user có team (teamIds.length>0)

### Điều kiện tiên quyết
- CASE-UA assignedTeamId=null, dieuTra1 thuộc Team-Q1

### Các bước kiểm thử
- [ ] GET /cases/CASE-UA

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DataScope-C5`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DataScope-C5`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-337
severity: Medium
module: DataScope-C5
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: unassignedMatch

---

## TC-338

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `DataScope-C6`
- Yêu cầu: `REQ-CASE-DATASCOPE-C6-338`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: User khác team + không owner → 403

### Điều kiện tiên quyết
- dieuTra1-Q1, CASE-Q3 thuộc Team-Q3

### Các bước kiểm thử
- [ ] GET /cases/CASE-Q3

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `DataScope-C6`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `DataScope-C6`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-338
severity: Critical
module: DataScope-C6
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Default deny

---

## TC-339

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `WriteScope-C1`
- Yêu cầu: `REQ-CASE-WRITESCOPE-C1-339`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Owner write Case mình → success

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] PUT mình

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `WriteScope-C1`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `WriteScope-C1`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-339
severity: High
module: WriteScope-C1
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-340

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `WriteScope-C2`
- Yêu cầu: `REQ-CASE-WRITESCOPE-C2-340`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Same writableTeam write → success

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `WriteScope-C2`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `WriteScope-C2`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-340
severity: High
module: WriteScope-C2
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-341

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `WriteScope-C3`
- Yêu cầu: `REQ-CASE-WRITESCOPE-C3-341`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Different team write → 403

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `WriteScope-C3`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `WriteScope-C3`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-341
severity: Critical
module: WriteScope-C3
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-342

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Delete-Decision`
- Yêu cầu: `REQ-CASE-DELETE-DECISION-342`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Decision matrix delete: TIEP_NHAN + creator + within 72h → success

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete-Decision`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete-Decision`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-342
severity: High
module: Delete-Decision
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-343

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Delete-Decision`
- Yêu cầu: `REQ-CASE-DELETE-DECISION-343`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Decision: TIEP_NHAN + creator + >72h + non-ADMIN → 400

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete-Decision`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete-Decision`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-343
severity: High
module: Delete-Decision
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-344

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Delete-Decision`
- Yêu cầu: `REQ-CASE-DELETE-DECISION-344`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Decision: TIEP_NHAN + non-creator + non-ADMIN → 403

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete-Decision`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete-Decision`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-344
severity: High
module: Delete-Decision
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-345

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Delete-Decision`
- Yêu cầu: `REQ-CASE-DELETE-DECISION-345`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Decision: ≠TIEP_NHAN + ADMIN → 400 (status check first)

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- status check

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete-Decision`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete-Decision`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-345
severity: High
module: Delete-Decision
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Status check trước creator check

---

## TC-346

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Delete-Decision`
- Yêu cầu: `REQ-CASE-DELETE-DECISION-346`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Decision: TIEP_NHAN + ADMIN + creator NULL + within window → success

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delete-Decision`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delete-Decision`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-346
severity: Medium
module: Delete-Decision
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-347

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-347`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Flow: Petition → Khởi tố vụ án → Thêm subject → Update status → Kết luận

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tạo Petition
- [ ] Khởi tố thành Case (FROM_PETITION)
- [ ] Thêm Subject
- [ ] Update status DA_XAC_MINH → DANG_DIEU_TRA
- [ ] Tạo Conclusion
- [ ] Update DA_KET_LUAN

### Dữ liệu kiểm thử
```
full flow
```

### Kết quả mong đợi
**UI**:
- Mọi bước success

**API**:
- Mỗi POST/PUT 201/200

**Side effects** (DB, email, log, queue...):
- Petition.linkedCaseId set, Subject linked, History 3 entries, Conclusion created

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-347
severity: Critical
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-348

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-348`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Flow: Incident → khởi tố Case → Assign → Delete fail → Restore

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Create Incident → Case FROM_INCIDENT → Assign team → DELETE (fail vì có Incident link) → ADMIN restore

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast workflow

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Audit trail đầy đủ

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-348
severity: High
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-349

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-349`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Bulk create Case + sequential STT trên Petition (DT-2026-NNNNN)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tạo 3 Petition liên tiếp, check stt

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- STT increment

**API**:
- DT-2026-00001, DT-2026-00002, DT-2026-00003

**Side effects** (DB, email, log, queue...):
- generateStt no skip

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-349
severity: High
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-361

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-361`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CASE_CREATED log có userId, action, subject='Case', subjectId, metadata, ipAddress, userAgent, createdAt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /cases, query audit_logs

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Row đủ 7 field

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-361
severity: High
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-362

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-362`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CASE_UPDATED audit có diff before/after (wrapUpdate)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT /cases, query audit

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata.diff={field:{before, after}, ...}

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-362
severity: High
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-363

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-363`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CASE_STATUS_CHANGED audit riêng entry với fromStatus/toStatus/changedAt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status change

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit entries: CASE_UPDATED + CASE_STATUS_CHANGED

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-363
severity: High
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-364

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-364`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CASE_DELETED có reason + hoursAfterCreation

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata.reason, metadata.hoursAfterCreation = Math.round(...)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-364
severity: High
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-365

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-365`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CASE_RESTORED có hoursAfterDeletion

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST restore

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata.hoursAfterDeletion

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-365
severity: High
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-366

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-366`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CASE_ASSIGNED có fromTeamId/toTeamId/fromInvestigatorId/toInvestigatorId/dispatchedBy

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH /assign

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata đủ 5 field

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-366
severity: High
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-367

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Log`
- Yêu cầu: `REQ-CASE-LOG-367`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: CASE_EXPORTED có filters đầy đủ trong metadata

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /export/ward

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata={format,kind,filters:{...}}

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Log`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Log`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-367
severity: Medium
module: Log
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Sprint 2 S2.1

---

## TC-368

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-368`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Tạo Subject với CCCD 12 chữ số hợp lệ

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/subjects {fullName, dateOfBirth, idNumber 12 chữ, address, caseId, crimeId}

### Dữ liệu kiểm thử
```
fullName='Nguyễn Văn A', dateOfBirth='1990-01-15', idNumber='079090012345', address='12 Lê Lợi Q1', caseId='C001', crimeId='CRIME-173'
```

### Kết quả mong đợi
**UI**:
- Toast 'Tạo đối tượng thành công', detail show

**API**:

**Side effects** (DB, email, log, queue...):
- DB row + audit SUBJECT_CREATED + cases.subjectsCount auto-increment

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-368
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-369

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-369`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Subject với CCCD 9 chữ số (CMND cũ)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber='079090123'

### Dữ liệu kiểm thử
```
idNumber='079090123' (9 chữ)
```

### Kết quả mong đợi
**UI**:
- Success

**API**:
- (regex /^\d{9}$|^\d{12}$/ pass)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-369
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-370

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-370`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CCCD 8 chữ số → 400 regex fail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber='07909012'

### Dữ liệu kiểm thử
```
idNumber='07909012'
```

### Kết quả mong đợi
**UI**:
- Error 'Số CCCD/CMND phải có 9 hoặc 12 chữ số'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-370
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-371

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-371`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CCCD 13 chữ số → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber='0790900123456'

### Dữ liệu kiểm thử
```
idNumber 13 chars
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-371
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-372

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-372`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CCCD có chữ cái 'ABC079090123' → 400 regex

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber='ABC079090123'

### Dữ liệu kiểm thử
```
idNumber='ABC079090123'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-372
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-373

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-373`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CCCD rỗng → 400 IsNotEmpty

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber=''

### Dữ liệu kiểm thử
```
idNumber=''
```

### Kết quả mong đợi
**UI**:
- Error 'Số CCCD/CMND không được để trống'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-373
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-374

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-374`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: fullName rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName=''

### Dữ liệu kiểm thử
```
fullName=''
```

### Kết quả mong đợi
**UI**:
- Error 'Họ tên không được để trống'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-374
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-375

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-375`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: dateOfBirth rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST dateOfBirth=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsNotEmpty + IsDateString

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-375
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-376

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-376`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: dateOfBirth='15/01/1990' không ISO → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST dateOfBirth='15/01/1990'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Ngày sinh không hợp lệ'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-376
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-377

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-377`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: dateOfBirth='2030-01-01' tương lai → ? (gap)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST dateOfBirth='2030-01-01'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (không validate future date)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-377
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên reject

---

## TC-378

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-378`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: address rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST address=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsNotEmpty

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-378
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-379

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-379`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Vụ án không được để trống'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-379
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-380

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-380`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId không tồn tại → 400 FK invalid

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId='NOT_EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (FK P2003 wrapped)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-380
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-381

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-381`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId của Case soft-deleted → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId của CASE-DEL

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-381
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-382

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-382`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: crimeId rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST crimeId=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Tội danh không được để trống'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-382
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-383

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-383`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: crimeId không tồn tại trong directory → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST crimeId='CRIME-NOT-EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-383
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-392

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-392`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /subjects?caseId=C001 trả subjects của Case

### Điều kiện tiên quyết
- C001 có 3 subjects

### Các bước kiểm thử
- [ ] GET ?caseId=C001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Table 3 row

**API**:
- data=[3 items]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-392
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-393

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-393`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /subjects/:id detail include lawyers liên kết

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /subjects/S001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show lawyers tab

**API**:
- data.lawyers=[...]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-393
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-394

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-394`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET /subjects/:id Subject ngoài scope → 403

### Điều kiện tiên quyết
- User Q1, Subject thuộc Case Q3

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-394
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-395

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-395`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /subjects/NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-395
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-396

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-396`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update fullName của Subject

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT /subjects/S001 {fullName:'Tên đã sửa'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast success

**API**:

**Side effects** (DB, email, log, queue...):
- Audit SUBJECT_UPDATED diff

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-396
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-397

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-397`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: PUT Subject ngoài write scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT subject của case khác team

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-397
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-398

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-398`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT Subject soft-deleted → 404

### Điều kiện tiên quyết
- Subject deletedAt set

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-398
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-399

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-399`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: DELETE Subject soft delete + Case.subjectsCount giảm

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE /subjects/S001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Xóa đối tượng thành công', row mất khỏi list

**API**:

**Side effects** (DB, email, log, queue...):
- subjects.deletedAt=now() + cases.subjectsCount-1 + audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-399
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-400

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-400`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: DELETE Subject ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-400
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-401

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-401`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE Subject không tồn tại → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-401
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-402

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-402`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: DELETE Subject đã soft-deleted (idempotent) → 404 hoặc 200

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE 2 lần

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Lần 2: 404

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-402
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-403

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-403`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR: POST subject với caseId của Case khác user → 403/400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra-Q1 POST subject với caseId thuộc Case-Q3

### Dữ liệu kiểm thử
```
caseId='CASE-Q3'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — cần verify scope check

**Side effects** (DB, email, log, queue...):
- Service phải check Case.assignedTeamId của caseId nằm trong writableTeamIds

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-403
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Subject scope phụ thuộc Case parent scope

---

## TC-404

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-404`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: XSS qua fullName='<script>alert(1)</script>' → React escape

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName='<script>alert(1)</script>'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Hiển thị literal

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-404
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-405

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-405`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: SQL injection qua idNumber bypass regex (impossible vì regex strict)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber='123 OR 1=1'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- regex fail

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-405
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-413

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-413`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition INVESTIGATING → DETAINED (bắt tạm giam)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='DETAINED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Badge change

**API**:

**Side effects** (DB, email, log, queue...):
- Audit status change

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-413
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-414

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-414`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DETAINED → RELEASED (thả)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='RELEASED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-414
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-415

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-415`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition INVESTIGATING → WANTED (chuyển truy nã)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='WANTED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-415
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-420

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-420`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Duplicate idNumber + caseId + type (constraint unique per type per case)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 2 lần cùng idNumber+caseId+type

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Đối tượng đã tồn tại'

**API**:
- hoặc 400 (P2002 unique)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-420
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-422

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-422`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE Subject ghi audit với name + reason

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit row SUBJECT_DELETED metadata

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-422
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-430

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-430`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: User VIEWER không có write/Subject → 403

### Điều kiện tiên quyết
- viewer

### Các bước kiểm thử
- [ ] POST /subjects

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-430
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-431

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-431`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không token

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-431
severity: Critical
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-439

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-439`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: SUBJECT_CREATED log có caseId trong metadata

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata.caseId, fullName, idNumber preserved

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-439
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-440

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-440`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Lawyer với barNumber unique

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/lawyers {fullName, barNumber, caseId, lawFirm, phone}

### Dữ liệu kiểm thử
```
fullName='Luật sư Trần Văn B', barNumber='LS-2026-12345', caseId='C001', lawFirm='VP Luật ABC', phone='0901234567'
```

### Kết quả mong đợi
**UI**:
- Toast 'Tạo luật sư thành công'

**API**:

**Side effects** (DB, email, log, queue...):
- DB row + audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-440
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-441

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-441`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Lawyer gắn subjectId (bào chữa cho subject cụ thể)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST với subjectId='S001'

### Dữ liệu kiểm thử
```
subjectId='S001'
```

### Kết quả mong đợi
**UI**:
- Detail show 'Bào chữa cho Nguyễn Văn A'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-441
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-443

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-443`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: fullName rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Error 'Họ tên luật sư không được để trống'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-443
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-444

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-444`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: barNumber rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST barNumber=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Error 'Số thẻ luật sư không được để trống'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-444
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-445

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-445`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: barNumber duplicate (đã tồn tại) → 409 unique constraint

### Điều kiện tiên quyết
- LS-2026-12345 đã tồn tại

### Các bước kiểm thử
- [ ] POST barNumber='LS-2026-12345'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Số thẻ luật sư đã tồn tại'

**API**:
- (P2002)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-445
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-446

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-446`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Vụ án không được để trống'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-446
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-447

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-447`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId không tồn tại → 400 FK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId='NOT_EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-447
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-448

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-448`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: subjectId không tồn tại trong DB → 400 FK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectId='NOT_EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-448
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-450

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-450`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /lawyers?caseId=C001 trả lawyers của Case

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Table list

**API**:
- data filtered by caseId

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-450
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-451

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-451`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /lawyers?subjectId=S001 trả lawyers của subject

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?subjectId=S001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-451
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-452

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-452`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /lawyers/:id detail include subject info

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show subject linked

**API**:
- data.subject=...

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-452
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-453

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-453`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /lawyers/NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-453
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-454

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-454`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET Lawyer ngoài scope (Case không thuộc team mình) → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra-Q1 GET lawyer của Case-Q3

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-454
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-455

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-455`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update lawFirm + phone

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT /lawyers/L001 {lawFirm:'VP mới', phone:'0987654321'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast success

**API**:

**Side effects** (DB, email, log, queue...):
- Audit LAWYER_UPDATED diff

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-455
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-456

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-456`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT barNumber duplicate → 409

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT barNumber khớp với lawyer khác

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-456
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-457

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-457`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: PUT Lawyer ngoài write scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT lawyer của Case khác team

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-457
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-458

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-458`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE Lawyer soft delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE /lawyers/L001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Xóa luật sư thành công'

**API**:

**Side effects** (DB, email, log, queue...):
- lawyers.deletedAt set + audit LAWYER_DELETED

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-458
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-459

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-459`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE Lawyer không tồn tại → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-459
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-460

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-460`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: DELETE Lawyer ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-460
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-461

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-461`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR: POST Lawyer với caseId ngoài team → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId của Case khác team

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (scope check Case parent)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-461
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-462

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-462`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: XSS qua fullName='<svg onload=alert(1)>'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName='<svg onload=alert(1)>'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Literal text

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-462
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-463

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-463`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: SQL injection qua barNumber

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST barNumber="' OR 1=1--"

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- lưu literal (Prisma param) hoặc 409 nếu collision

**Side effects** (DB, email, log, queue...):
- No DB injection

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-463
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-473

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-473`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: User VIEWER POST → 403

### Điều kiện tiên quyết
- viewer

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-473
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-474

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-474`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không token

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-474
severity: Critical
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-475

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-475`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: LAWYER_CREATED log có barNumber + caseId

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata đầy đủ

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-475
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-484

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-484`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Tạo Conclusion với type+content+caseId

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/conclusions {caseId, type:'KET_LUAN_DE_NGHI_TRUY_TO', content:'Nội dung kết luận chi tiết...'}

### Dữ liệu kiểm thử
```
caseId='C001', type='KET_LUAN_DE_NGHI_TRUY_TO', content='Đề nghị truy tố theo Đ.173 BLHS'
```

### Kết quả mong đợi
**UI**:
- Toast 'Tạo kết luận thành công'

**API**:

**Side effects** (DB, email, log, queue...):
- conclusions row + audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-484
severity: Critical
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-485

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-485`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId thiếu → 400 IsString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không caseId

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-485
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-486

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-486`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId không tồn tại → 400 FK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-486
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-487

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-487`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: type rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST type=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-487
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-488

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-488`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: content rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-488
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-489

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-489`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Conclusion với approvedById

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST approvedById='<truong-don-vi>'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show 'Phê duyệt bởi: ...'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-489
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-490

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-490`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: approvedById không tồn tại → 400 FK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST approvedById='NOT_EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-490
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-491

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-491`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Default status khi POST không kèm

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- status=DRAFT (default)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-491
severity: Medium
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-493

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-493`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /conclusions?caseId=C001 list

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Table show conclusions

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-493
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-494

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-494`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /conclusions/:id detail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show approvedBy info

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-494
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-495

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-495`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET conclusion ngoài scope (Case khác team) → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-495
severity: Critical
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-496

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-496`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /conclusions/NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-496
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-497

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-497`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update content

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT /conclusions/CON-001 {content:'Sửa kết luận'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- Audit diff

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-497
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-498

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-498`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT conclusion đã APPROVED không cho sửa

### Điều kiện tiên quyết
- Conclusion status=APPROVED

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Kết luận đã phê duyệt không thể sửa'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-498
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần verify business rule

---

## TC-499

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-499`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE conclusion soft delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast success

**API**:

**Side effects** (DB, email, log, queue...):
- deletedAt + audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-499
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-500

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-500`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE conclusion đã APPROVED → 400

### Điều kiện tiên quyết
- status=APPROVED

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Không thể xóa kết luận đã phê duyệt'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-500
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-501

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-501`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition DRAFT → SUBMITTED (gửi đi phê duyệt)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='SUBMITTED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-501
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-502

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-502`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition SUBMITTED → APPROVED (truong don vi duyet)

### Điều kiện tiên quyết
- user role TRUONG_DON_VI

### Các bước kiểm thử
- [ ] PUT status='APPROVED' approvedById=<self>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-502
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-503

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-503`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Transition SUBMITTED → REJECTED

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='REJECTED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-503
severity: Medium
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-504

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-504`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Transition REJECTED → DRAFT (chỉnh sửa lại)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='DRAFT'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-504
severity: Medium
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-505

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-505`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: APPROVED → DRAFT (downgrade) → block

### Điều kiện tiên quyết
- status=APPROVED

### Các bước kiểm thử
- [ ] PUT status='DRAFT'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Không thể downgrade conclusion đã phê duyệt'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-505
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: State machine constraint

---

## TC-506

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-506`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: User thường không có quyền APPROVE (không phải TRUONG_DON_VI) → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra1 PUT status='APPROVED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc 400

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-506
severity: Critical
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-507

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-507`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR: POST conclusion với caseId của team khác

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-507
severity: Critical
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-508

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-508`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: XSS qua content='<script>'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- React escape

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-508
severity: Critical
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-511

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-511`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Conclusion APPROVED → Case status auto-update DA_KET_LUAN

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] APPROVE conclusion

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Case badge → 'Đã kết luận'

**API**:

**Side effects** (DB, email, log, queue...):
- Case.status=DA_KET_LUAN + history entry

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-511
severity: Critical
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify auto-trigger logic

---

## TC-512

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-512`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CONCLUSION_APPROVED log có approvedById + timestamp

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] APPROVE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit row đủ field

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-512
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-517

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-517`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: User VIEWER POST → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-517
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-518

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-518`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-518
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-523

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-523`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Upload PDF 5MB qua POST /documents

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST multipart/form-data file=test.pdf 5MB title='Hồ sơ vụ án' caseId='C001'

### Dữ liệu kiểm thử
```
file PDF 5MB
```

### Kết quả mong đợi
**UI**:
- Progress bar, Toast success

**API**:
- , data.filePath set

**Side effects** (DB, email, log, queue...):
- File lưu /uploads/, DB row + audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-523
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-524

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-524`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Upload Word .docx

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file=report.docx

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Success

**API**:
- mimeType=application/vnd.openxmlformats-officedocument.wordprocessingml.document

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-524
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-525

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-525`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Upload image JPG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file=evidence.jpg

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-525
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-526

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-526`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Upload PNG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file=screenshot.png

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-526
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-527

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-527`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Upload file > 10MB (mặc định limit) → 413

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file 15MB

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Error 'File quá lớn'

**API**:
- PayloadTooLarge

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-527
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-528

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-528`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Upload .exe → block by mimetype

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file=malware.exe

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Error 'Loại file không hỗ trợ'

**API**:
- mimetype rejected

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-528
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Multer filter cần whitelist mime

---

## TC-529

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-529`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Upload .sh script → block

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file=evil.sh

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-529
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-530

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-530`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Upload .js → block

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file=evil.js

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-530
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-531

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-531`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Upload PDF nhưng đổi extension thành .pdf (magic byte check)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Upload file MZ header (PE/exe) đổi tên .pdf

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — cần verify magic byte check

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-531
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: cần magic byte check

---

## TC-532

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-532`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Path traversal qua originalName '../../../etc/passwd'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST originalName='../../../etc/passwd'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify multer normalize

**Side effects** (DB, email, log, queue...):
- File path normalized, không escape uploads dir

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-532
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-533

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-533`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Filename có null byte 'evil\0.jpg'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST originalName='evil\0.jpg'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc strip null byte

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-533
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-534

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-534`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: ZIP slip nếu support upload .zip

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Upload zip có '../../../evil.txt' inside

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-534
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Skip nếu không support zip

---

## TC-535

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-535`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: title rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST title=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Error 'Tiêu đề tài liệu không được để trống'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-535
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-536

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-536`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: caseId optional — không kèm cũng OK (general document)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không caseId

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-536
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-537

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-537`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: incidentId thay vì caseId (gắn với Incident)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST incidentId='INC-001'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-537
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-538

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-538`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Cả caseId VÀ incidentId → 400 'Chỉ chọn 1'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId + incidentId

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify business rule

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-538
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: chưa enforce

---

## TC-540

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-540`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /documents?caseId=C001 list documents của Case

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-540
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-541

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-541`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /documents/:id metadata only (chưa download file)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data có filePath/fileName/size

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-541
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-542

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-542`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET /documents/:id/download trả file binary

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET download

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Browser download file

**API**:
- Content-Type theo mimeType, Content-Disposition attachment

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-542
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-543

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-543`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Download document ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET download của Case khác team

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-543
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-544

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-544`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Download document đã soft-deleted → 404

### Điều kiện tiên quyết
- deletedAt set

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-544
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-545

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-545`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Download bằng GET /uploads/file.pdf trực tiếp → 401/403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /uploads/file.pdf không qua API auth

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — cần verify static serve có auth

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-545
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Nếu serve raw upload → IDOR

---

## TC-546

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-546`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE document soft delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast success

**API**:

**Side effects** (DB, email, log, queue...):
- deletedAt + audit + file vẫn còn (soft)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-546
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-547

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-547`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: DELETE document ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-547
severity: Critical
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-548

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-548`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update title + description

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- Audit diff

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-548
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-549

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-549`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT thay đổi fileName/filePath bị forbidNonWhitelisted reject

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT {fileName:'evil.pdf'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (whitelist update DTO không có fileName)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-549
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify UpdateDocumentDto không có file fields

---

## TC-550

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-550`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: XSS qua originalName '<script>alert</script>.pdf'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST originalName='<script>alert</script>.pdf'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Hiển thị escape

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-550
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-551

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-551`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: CSV injection qua title '=cmd|calc'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST title='=cmd|calc'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Hiển thị raw

**API**:

**Side effects** (DB, email, log, queue...):
- Export Excel sau này cần escape '='

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-551
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: CSV/Excel injection nếu export sau

---

## TC-563

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-563`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Quota: user upload nhiều file → check storage limit

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Upload 100 file 10MB

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify storage quota

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-563
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: không có quota → DoS

---

## TC-564

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-564`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DOCUMENT_CREATED có fileName + size + mimeType

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata đủ

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-564
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-565

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-565`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DOCUMENT_DOWNLOADED audit mỗi lần download

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET download

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit row DOCUMENT_DOWNLOADED với userId + ip + ua

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-565
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: PII access tracking

---

## TC-566

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-566`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DOCUMENT_DELETED có original fileName

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-566
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-571

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-571`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Multi-file upload (nếu hỗ trợ) — concurrent

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 5 file cùng lúc

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- cho mỗi

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-571
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-573

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-573`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Case bị block khi có Document linked

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Case

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Không thể xóa: vụ án có 3 tài liệu đính kèm'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-573
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-576

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-576`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: VIEWER không có write/Document → 403 POST

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-576
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-577

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-577`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT thiếu POST → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-577
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-581

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-581`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Proposal với proposalNumber + content

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/proposals {proposalNumber:'DX-2026-001', relatedCaseId, content, unit}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-581
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-582

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-582`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: proposalNumber rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST proposalNumber=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-582
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-583

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-583`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: content rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content=''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-583
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-584

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-584`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: proposalNumber duplicate → 409 unique

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 2 lần cùng proposalNumber

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc 400

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-584
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify unique constraint

---

## TC-586

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-586`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: relatedCaseId không tồn tại → 400 FK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-586
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-587

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-587`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Tạo Proposal với sentDate

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST sentDate='2026-05-23'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-587
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-589

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-589`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /proposals list paginated

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-589
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-590

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-590`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /proposals/:id detail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-590
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-591

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-591`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET proposal ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-591
severity: Critical
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-592

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-592`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET /proposals/NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-592
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-593

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-593`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update response + responseDate

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT {response, responseDate}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Audit diff

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-593
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-594

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-594`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE soft delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-594
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-595

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-595`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition PENDING → SENT → RESPONDED

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] x PUT status

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-595
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-596

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-596`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Transition SENT → CANCELLED

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='CANCELLED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-596
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-598

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-598`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR: POST relatedCaseId của team khác → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-598
severity: Critical
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-599

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-599`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: XSS qua content

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content='<script>'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Escape

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-599
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-602

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-602`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PROPOSAL_CREATED audit log

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata.proposalNumber

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-602
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-603

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-603`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PROPOSAL_RESPONDED log với response timestamp

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-603
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-610

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-610`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: VIEWER POST → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-610
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-611

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-611`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-611
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-616

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-616`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Delegation với delegationNumber + receivingUnit

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/delegations {delegationNumber:'UT-2026-001', receivingUnit:'PC02-Hà Nội', content}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Success

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-616
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-617

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-617`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: delegationNumber rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-617
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-618

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-618`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: receivingUnit rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-618
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-619

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-619`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: content rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-619
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-620

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-620`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: delegationNumber duplicate → 409

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-620
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-621

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-621`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: relatedCaseId optional

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-621
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-622

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-622`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: relatedCaseId không tồn tại → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-622
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-623

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-623`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET list paginated

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-623
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-624

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-624`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET detail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-624
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-625

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-625`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-625
severity: Critical
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-626

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-626`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-626
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-627

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-627`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update content + completedDate

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-627
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-628

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-628`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE soft delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-628
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-629

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-629`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition PENDING → IN_PROGRESS

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-629
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-630

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-630`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Transition IN_PROGRESS → COMPLETED

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-630
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-631

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-631`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Transition any → CANCELLED

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-631
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-632

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-632`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: COMPLETED → PENDING (downgrade) → block

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-632
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: State machine constraint

---

## TC-633

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-633`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR relatedCaseId team khác

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-633
severity: Critical
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-634

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-634`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: XSS content

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- escape

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-634
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-639

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-639`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELEGATION_CREATED log

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-639
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-640

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-640`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELEGATION_COMPLETED log với completedDate

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-640
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-647

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-647`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: VIEWER POST → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-647
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-648

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-648`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-648
severity: High
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-652

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-652`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo Supplement caseId + decisionNumber + reason + deadline

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/investigation-supplements

### Dữ liệu kiểm thử
```
caseId, type='Gia hạn điều tra', decisionNumber:'GH-2026-001', reason:'Cần thêm thời gian', deadline='2026-09-23'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-652
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-653

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-653`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: caseId rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-653
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-654

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-654`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: type rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-654
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-655

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-655`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: decisionNumber rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-655
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-656

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-656`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: reason rỗng → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-656
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-660

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-660`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET list paginated

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-660
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-661

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-661`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET detail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-661
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-662

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-662`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: GET ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-662
severity: Critical
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-663

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-663`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: GET NOT_EXIST → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-663
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-664

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-664`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PUT update reason

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Audit

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-664
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-665

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-665`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: DELETE soft delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-665
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-666

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-666`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo supplement → Case.soLanGiaHan auto-increment

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST supplement type='Gia hạn'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Case.soLanGiaHan +1

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-666
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify business logic

---

## TC-667

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-667`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Tạo supplement với deadline mới → Case.deadline update

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST với deadline xa hơn

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Case.deadline = supplement.deadline mới

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-667
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify integration

---

## TC-668

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-668`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR caseId team khác → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-668
severity: Critical
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-669

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-669`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: XSS reason

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- escape

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-669
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-672

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-672`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: SUPPLEMENT_CREATED log

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-672
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-678

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-678`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: VIEWER POST → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-678
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-679

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-679`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-679
severity: High
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-682

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-682`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Petition convertToCase (POST /petitions/:id/convert)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/petitions/PET-001/convert

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Redirect /cases/<newCaseId>

**API**:
- , Petition.linkedCaseId set, status=DA_CHUYEN_VU_AN

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-682
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-683

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-683`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase Petition đã linked → 400 'Đã khởi tố rồi'

### Điều kiện tiên quyết
- PET-002 có linkedCaseId

### Các bước kiểm thử
- [ ] POST convert

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc 409

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-683
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-684

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-684`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: convertToCase ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-684
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-685

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-685`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase Petition soft-deleted → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-685
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-686

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-686`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition.status transition MOI_TIEP_NHAN → DA_CHUYEN_VU_AN (convert)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- auto on convert

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-686
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-687

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-687`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition.status MOI_TIEP_NHAN → DANG_XU_LY (manual)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT petition status

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-687
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-688

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-688`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition.status DANG_XU_LY → CHO_PHE_DUYET

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-688
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-689

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-689`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition.status CHO_PHE_DUYET → DA_GIAI_QUYET

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-689
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-690

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-690`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Petition.status any → DA_LUU_DON (lưu đơn)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-690
severity: Medium
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-691

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-691`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition.status → DA_CHUYEN_VU_VIEC (chuyển vụ việc)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-691
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-692

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-692`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition DA_CHUYEN_VU_AN không sửa được

### Điều kiện tiên quyết
- status=DA_CHUYEN_VU_AN

### Các bước kiểm thử
- [ ] PUT petition

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-692
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-693

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-693`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Cascade: Update Case.metadata.petitionType → sync Petition.petitionType

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT Case.metadata.petitionType='KHIEU_NAI'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Petition.petitionType updated automatically (v0.37.2.5)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-693
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-694

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-694`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update Case không có Petition link KHÔNG tạo phantom Petition

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT Case metadata.petitionType, không có linkedPetition

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- No new Petition created (v0.37.2.5 BLTTHS Đ.143)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-694
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-695

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-695`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: STT auto-generate format DT-YYYY-NNNNN

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 3 petition liên tiếp

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- stt = DT-2026-00001, 00002, 00003

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-695
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-696

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-696`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: STT race condition: 2 concurrent create không tạo duplicate STT

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] parallel POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- stt khác nhau

**Side effects** (DB, email, log, queue...):
- generateStt + unique constraint

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-696
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-697

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-697`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase Petition status không phải MOI_TIEP_NHAN/DANG_XU_LY → 400

### Điều kiện tiên quyết
- status=DA_LUU_DON

### Các bước kiểm thử
- [ ] POST convert

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-697
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify state guard

---

## TC-698

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-698`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: PETITION_CONVERTED log với linkedCaseId

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST convert

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit row PETITION_CONVERTED metadata.caseId

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-698
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-699

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-699`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: convertToCase atomic — race 2 user → 1 conflict

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ×201 + 1×409

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-699
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-702

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-702`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Petition KHÔNG delete linked Case (Restrict)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Petition có linkedCase

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc Restrict block

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-702
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: onDelete: Restrict

---

## TC-703

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-703`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Case → Petition.linkedCaseId vẫn giữ (Restrict)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Case (status TIEP_NHAN không có Petition link)

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- No FK cascade

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-703
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-705

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-705`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase VIEWER → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-705
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-706

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-706`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-706
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-709

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-709`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: CaseProvenance=FROM_PETITION → Case có linkedPetitionId set

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST Case FROM_PETITION

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data.linkedPetitionId='PET-001'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-709
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-710

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-710`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: CaseProvenance ≠ FROM_PETITION → linkedPetitionId=null

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST DIRECT_DISCOVERY

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data.linkedPetitionId=null

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-710
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-714

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-714`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition convertToCase preserve provenance traceability

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Case detail show 'Khởi tố từ Petition PET-001'

**API**:
- data.linkedPetition included

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-714
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-715

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-715`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: E2E: Tạo Petition → review → convert Case → assign team

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Full flow E2E

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Mọi bước OK

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Audit trail đủ 4 events

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-715
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-717

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-717`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Incident convertToCase

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /api/incidents/INC-001/convert

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-717
severity: Critical
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-718

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-718`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Incident đã linked → 400

### Điều kiện tiên quyết
- -

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-718
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-719

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-719`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Incident ngoài scope → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-719
severity: Critical
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-720

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-720`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Incident.status MOI_TIEP_NHAN → DANG_XAC_MINH

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-720
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-721

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-721`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Incident → TAM_DINH_CHI

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-721
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-722

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-722`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Incident → PHUC_HOI_NGUON_TIN

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-722
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-723

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-723`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Incident TAM_DINH_CHI → DANG_XAC_MINH (phục hồi)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-723
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-724

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-724`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Incident → Case keep linkage (Incident.linkedCaseId)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST Case FROM_INCIDENT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Incident.linkedCaseId=newCaseId

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-724
severity: Critical
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-725

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-725`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Incident có linkedCase Restrict

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Incident có linkedCase

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Restrict block

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-725
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-726

**Meta**:
- Loại: `GREEN`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-726`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: CaseProvenance=FROM_INCIDENT → linkedIncidentId set

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-726
severity: Critical
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-727

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-727`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: FROM_INCIDENT thiếu expectedIncidentUpdatedAt → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST Case FROM_INCIDENT không expectedIncidentUpdatedAt

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-727
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-728

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-728`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Race convertToCase 2 user same Incident → 1×409

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-728
severity: Critical
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-729

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-729`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: INCIDENT_CONVERTED audit

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- metadata.caseId

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-729
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-734

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-734`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: VIEWER convert → 403

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-734
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-735

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-735`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: JWT thiếu → 401

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-735
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-736

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-736`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Incident TAM_DINH_CHI_LAI (lần 2 trở đi)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-736
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-740

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-740`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Multiple Incidents linked to same Case (1-to-many)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Incident.linkedCaseId multiple rows for same Case

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-740
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: linkedIncidents in Case schema

---

## TC-741

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-741`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Case có linkedIncidents → block 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Case có Incident link

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Không thể xóa: vụ án đang liên kết N vụ việc'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-741
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-742

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-742`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Incident status transitions match BLTTHS

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-742
severity: Medium
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-743

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-743`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: IDOR convertToCase với Incident ID team khác

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (IDOR-safe)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-743
severity: Critical
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-744

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-744`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Convert Incident → Case → Subject (E2E)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-744
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-745

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-745`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase Incident soft-deleted → 404

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-745
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-746

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-746`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: INCIDENT_STATUS_CHANGED audit có fromStatus/toStatus

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-746
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-747

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-747`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update status TIEP_NHAN → DA_KET_LUAN skip middle steps

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify state machine

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-747
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap nếu cho phép skip

---

## TC-748

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-748`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update status với 5 lần TAM_DINH_CHI ↔ DANG_DIEU_TRA

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] lần phục hồi + 2 lần TĐC

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- soLanTamDinhChi=2

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-748
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-749

**Meta**:
- Loại: `STATE`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-749`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Update concurrent: 2 user đổi status khác nhau

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] A→DANG_DIEU_TRA, B→TAM_DINH_CHI cùng lúc

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- success, 1 P2025/409 if expectedUpdatedAt

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-749
severity: Critical
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-750

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-750`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Combination: ADMIN + Case DELETED 1 năm trước → restore OK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-750
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-751

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-751`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Combination: dispatcher xem case ward → bypass scope

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-751
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-752

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-752`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Combination: ward officer + assignedTeamId override

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- with override

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-752
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-753

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-753`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: User MULTI-TEAM (thuộc 2 teams) — scope union

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] dieuTra-X thuộc Team-Q1 và Team-Q2, GET cases

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data có case của cả 2 team

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-753
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-754

**Meta**:
- Loại: `DECISION`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-754`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: User has writableTeamIds ⊂ teamIds (read-only some)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-754
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: DataScope split: read vs write

---

## TC-755

**Meta**:
- Loại: `RED`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-755`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: forbidNonWhitelisted strict cho mọi DTO update

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT {evilField:'x'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-755
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-756

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-756`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Privilege escalation: change own role qua DTO mass-assign

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- forbidNonWhitelisted

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-756
severity: Critical
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-757

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-757`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: XSS reflected qua URL search → sanitize

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases?search=<script>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (Prisma escape)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-757
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-758

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-758`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Open redirect: ?returnUrl=https://evil.com

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-758
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: N/A nếu app không dùng returnUrl

---

## TC-759

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-759`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CSP header restrict inline script

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Header set

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-759
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-760

**Meta**:
- Loại: `SECURITY`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-760`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Referrer-Policy: strict-origin-when-cross-origin

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Header

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-760
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-780

**Meta**:
- Loại: `AUDIT`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-780`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Audit log không expose internal trace/stack

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Production error sanitize

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-780
severity: Critical
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-781

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P0` 🔴
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-781`
- Kỹ thuật: `Use case testing`
- Risk: `Cao`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Cross-feature: tạo Case → KPI dashboard refresh count

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-781
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-011

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-011`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Tạo Case bỏ trống các field optional

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Form tạo
- [ ] CHỈ nhập name + caseProvenance
- [ ] Submit

### Dữ liệu kiểm thử
```
name='Vụ tối thiểu', caseProvenance='DIRECT_DISCOVERY'
```

### Kết quả mong đợi
**UI**:
- Toast success, detail hiện các field optional là '-'

**API**:
- , crime=NULL, investigatorId=NULL, capDoToiPham=NULL, subjectsCount=0 (default)

**Side effects** (DB, email, log, queue...):
- DB row với null optional fields

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-011
severity: Medium
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-012

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-012`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Tạo Case với investigatorId là user khác đang active

### Điều kiện tiên quyết
- Có user dieuTra2@pc02.local đang active

### Các bước kiểm thử
- [ ] Tạo Case
- [ ] Chọn investigator='Nguyễn ĐTV2' từ FK dropdown
- [ ] Submit

### Dữ liệu kiểm thử
```
investigatorId='<dieuTra2.id>'
```

### Kết quả mong đợi
**UI**:
- Detail show investigator name = 'Nguyễn ĐTV2'

**API**:
- data.investigatorId=<dieuTra2.id>

**Side effects** (DB, email, log, queue...):
- cases.investigatorId set

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-012
severity: Medium
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-013

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-013`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Tạo Case với ngayKhoiTo lùi vài ngày trước hôm nay (đã khởi tố trước khi vào hệ thống)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tạo Case
- [ ] ngayKhoiTo='2026-05-15' (8 ngày trước)
- [ ] Submit

### Dữ liệu kiểm thử
```
ngayKhoiTo='2026-05-15'
```

### Kết quả mong đợi
**UI**:
- Detail show ngày khởi tố 15/05/2026

**API**:
- data.ngayKhoiTo='2026-05-15T00:00:00.000Z'

**Side effects** (DB, email, log, queue...):
- Saved without 'future date' validation

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-013
severity: Medium
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-014

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-014`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Dispatcher tạo Case có thể chọn assignedTeamId bất kỳ

### Điều kiện tiên quyết
- Login dispatcher@pc02.local (canDispatch=true)

### Các bước kiểm thử
- [ ] Tạo Case
- [ ] assignedTeamId='Team-OTHER-WARD-05' (team khác phường)
- [ ] Submit

### Dữ liệu kiểm thử
```
assignedTeamId='Team-OTHER-WARD-05'
```

### Kết quả mong đợi
**UI**:
- Toast success, detail show team đã chọn

**API**:
- data.assignedTeamId='Team-OTHER-WARD-05' (KHÔNG bị override)

**Side effects** (DB, email, log, queue...):
- Dispatcher bypass ward auto-override

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-014
severity: High
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-015

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-015`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Tạo Case ADMIN với toàn bộ field bao gồm field hiếm dùng

### Điều kiện tiên quyết
- Login admin@pc02.local

### Các bước kiểm thử
- [ ] Tạo Case với laCongNgheCao=false, soLanGiaHan=0, daRaSoat=false (default), submit

### Dữ liệu kiểm thử
```
laCongNgheCao=false, soLanGiaHan=0
```

### Kết quả mong đợi
**UI**:
- Detail hiển thị 'Không phải án CNC'

**API**:
- , các field default applied

**Side effects** (DB, email, log, queue...):
- Default values saved

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-015
severity: Medium
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-016

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-016`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: POST trả response message tiếng Việt 'Tạo vụ án thành công'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /cases với DTO hợp lệ

### Dữ liệu kiểm thử
```
dto đầy đủ
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Response: {success:true, data:{...}, message:'Tạo vụ án thành công'}

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-016
severity: Low
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify response shape khớp với contract message field

---

## TC-023

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-023`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name là object JSON → 400 IsString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name={text:'abc'}

### Dữ liệu kiểm thử
```
name={text:'abc'}
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-023
severity: Medium
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-024

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-024`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name là array → 400 IsString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name=['abc']

### Dữ liệu kiểm thử
```
name=['abc']
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-024
severity: Medium
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-036

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-036`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: expectedPetitionUpdatedAt sai format (không ISO 8601) → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST expectedPetitionUpdatedAt='20/05/2026'

### Dữ liệu kiểm thử
```
expectedPetitionUpdatedAt='20/05/2026'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsISO8601 failed

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-036
severity: Medium
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-038

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-capDo`
- Yêu cầu: `REQ-CASE-CREATE-CAPDO-038`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: capDoToiPham là invalid enum 'INVALID' → 400 với message tiếng Việt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST capDoToiPham='INVALID'

### Dữ liệu kiểm thử
```
capDoToiPham='INVALID'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'capDoToiPham phải là IT_NGHIEM_TRONG, NGHIEM_TRONG, RAT_NGHIEM_TRONG hoặc DAC_BIET_NGHIEM_TRONG'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-capDo`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-capDo`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-038
severity: Medium
module: Create-capDo
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-039

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-subC`
- Yêu cầu: `REQ-CASE-CREATE-SUBC-039`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: subjectsCount = -1 → 400 @Min(0)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount=-1

### Dữ liệu kiểm thử
```
subjectsCount=-1
```

### Kết quả mong đợi
**UI**:
- Error 'Phải ≥ 0'

**API**:
- @Min(0)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-subC`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-subC`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-039
severity: Medium
module: Create-subC
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-040

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-subC`
- Yêu cầu: `REQ-CASE-CREATE-SUBC-040`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: subjectsCount = 1.5 (float) → 400 IsInt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount=1.5

### Dữ liệu kiểm thử
```
subjectsCount=1.5
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsInt

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-subC`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-subC`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-040
severity: Medium
module: Create-subC
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-041

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-subC`
- Yêu cầu: `REQ-CASE-CREATE-SUBC-041`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: subjectsCount = 'abc' string → 400 IsInt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount='abc'

### Dữ liệu kiểm thử
```
subjectsCount='abc'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-subC`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-subC`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-041
severity: Medium
module: Create-subC
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-042

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-crime`
- Yêu cầu: `REQ-CASE-CREATE-CRIME-042`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: crime > 255 ký tự → 400 MaxLength

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST crime với 256 chars

### Dữ liệu kiểm thử
```
crime='X'.repeat(256)
```

### Kết quả mong đợi
**UI**:
- Error 'không quá 255 ký tự'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-crime`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-crime`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-042
severity: Medium
module: Create-crime
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-043

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-unit`
- Yêu cầu: `REQ-CASE-CREATE-UNIT-043`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: unit > 255 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST unit với 256 chars

### Dữ liệu kiểm thử
```
unit='X'.repeat(256)
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- MaxLength(255)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-unit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-unit`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-043
severity: Medium
module: Create-unit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-045

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-deadline`
- Yêu cầu: `REQ-CASE-CREATE-DEADLINE-045`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: deadline format DD/MM/YYYY → 400 IsDateString chỉ accept ISO

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST deadline='15/08/2026'

### Dữ liệu kiểm thử
```
deadline='15/08/2026'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsDateString cần ISO 8601

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-deadline`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-deadline`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-045
severity: Medium
module: Create-deadline
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Frontend phải convert sang ISO trước POST

---

## TC-046

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-ngayKT`
- Yêu cầu: `REQ-CASE-CREATE-NGAYKT-046`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: ngayKhoiTo='2026-13-45' (tháng 13, ngày 45) → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST ngayKhoiTo='2026-13-45'

### Dữ liệu kiểm thử
```
ngayKhoiTo='2026-13-45'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsDateString

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-ngayKT`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-ngayKT`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-046
severity: Medium
module: Create-ngayKT
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-047

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-src`
- Yêu cầu: `REQ-CASE-CREATE-SRC-047`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: sourceDocumentNote > 1000 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST sourceDocumentNote='Y'.repeat(1001)

### Dữ liệu kiểm thử
```
sourceDocumentNote.length=1001
```

### Kết quả mong đợi
**UI**:
- Error 'không quá 1000 ký tự'

**API**:
- MaxLength(1000)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-src`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-src`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-047
severity: Medium
module: Create-src
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-049

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-meta`
- Yêu cầu: `REQ-CASE-CREATE-META-049`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: metadata là array thay vì object → 400 IsObject

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST metadata=[1,2,3]

### Dữ liệu kiểm thử
```
metadata=[1,2,3]
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsObject

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-meta`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-meta`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-049
severity: Medium
module: Create-meta
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-050

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-meta`
- Yêu cầu: `REQ-CASE-CREATE-META-050`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: metadata là string → 400 IsObject

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST metadata='abc'

### Dữ liệu kiểm thử
```
metadata='abc'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-meta`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-meta`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-050
severity: Medium
module: Create-meta
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-051

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-team`
- Yêu cầu: `REQ-CASE-CREATE-TEAM-051`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: assignedTeamId KHÔNG tồn tại trong teams (FK invalid) → ?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST assignedTeamId='TEAM_NOT_EXIST'

### Dữ liệu kiểm thử
```
assignedTeamId='TEAM_NOT_EXIST'
```

### Kết quả mong đợi
**UI**:
- Toast lỗi

**API**:
- hoặc 400 do Prisma FK constraint violation

**Side effects** (DB, email, log, queue...):
- Không tạo row

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-team`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-team`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-051
severity: Medium
module: Create-team
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Backend nên catch P2003 trả 400 thân thiện

---

## TC-052

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-wl`
- Yêu cầu: `REQ-CASE-CREATE-WL-052`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: POST kèm field lạ 'evilField=hack' với whitelist=true → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST {name:'X', caseProvenance:'DIRECT_DISCOVERY', evilField:'hack'}

### Dữ liệu kiểm thử
```
evilField='hack'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'property evilField should not exist'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-wl`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-wl`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-052
severity: Medium
module: Create-wl
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: forbidNonWhitelisted: true

---

## TC-057

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-type`
- Yêu cầu: `REQ-CASE-CREATE-TYPE-057`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: deadline là object → 400 IsDateString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST deadline={year:2026}

### Dữ liệu kiểm thử
```
deadline={year:2026}
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-type`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-type`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-057
severity: Medium
module: Create-type
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-058

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-type`
- Yêu cầu: `REQ-CASE-CREATE-TYPE-058`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: investigatorId là number 12345 → 400 IsString

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST investigatorId=12345

### Dữ liệu kiểm thử
```
investigatorId=12345
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsString

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-type`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-type`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-058
severity: Medium
module: Create-type
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-059

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-team`
- Yêu cầu: `REQ-CASE-CREATE-TEAM-059`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: assignedTeamId của team đã isActive=false → field saved nhưng cảnh báo

### Điều kiện tiên quyết
- Team-INACTIVE-01 isActive=false

### Các bước kiểm thử
- [ ] POST assignedTeamId='Team-INACTIVE-01'

### Dữ liệu kiểm thử
```
assignedTeamId='Team-INACTIVE-01'
```

### Kết quả mong đợi
**UI**:
- Toast cảnh báo 'Team đã ngừng hoạt động'

**API**:
- ? — hiện tại không validate trong create

**Side effects** (DB, email, log, queue...):
- Cần thêm validation isActive=true

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-team`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-team`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-059
severity: Medium
module: Create-team
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap có thể trong logic — refer service check chỉ ở assignCase, không create

---

## TC-060

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-060`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: caseProvenance=SELF_SURRENDER cần linkedPetitionId hoặc khác? — check enum

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST SELF_SURRENDER không link

### Dữ liệu kiểm thử
```
caseProvenance='SELF_SURRENDER'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- nếu không có ValidateIf riêng cho SELF_SURRENDER

**Side effects** (DB, email, log, queue...):
- Check schema xem có ValidateIf không

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-060
severity: Medium
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: ValidateIf chỉ enforce với FROM_PETITION/FROM_INCIDENT

---

## TC-063

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-name`
- Yêu cầu: `REQ-CASE-CREATE-NAME-063`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name=' Vụ ABC ' (có space đầu/cuối) bị trim → save 'Vụ ABC'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name=' Vụ ABC '

### Dữ liệu kiểm thử
```
name=' Vụ ABC '
```

### Kết quả mong đợi
**UI**:
- Detail show name='Vụ ABC' (đã trim)

**API**:
- , data.name='Vụ ABC'

**Side effects** (DB, email, log, queue...):
- DB name='Vụ ABC' (no leading/trailing space)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-name`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-name`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-063
severity: Medium
module: Create-name
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-065

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-065`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: FROM_INCIDENT với Incident updatedAt mismatch → 409 'Vụ việc đã chỉnh sửa'

### Điều kiện tiên quyết
- INC-001 updatedAt='2026-05-22T12:00'

### Các bước kiểm thử
- [ ] POST FROM_INCIDENT expectedIncidentUpdatedAt='2026-05-20T10:00' (cũ)

### Dữ liệu kiểm thử
```
expectedIncidentUpdatedAt='2026-05-20T10:00:00.000Z'
```

### Kết quả mong đợi
**UI**:
- Toast 'Vụ việc đã chỉnh sửa hoặc link bởi người dùng khác'

**API**:
- ConflictException

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-065
severity: High
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-066

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Create-prov`
- Yêu cầu: `REQ-CASE-CREATE-PROV-066`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: FROM_PETITION + ngayKhoiTo trước Petition.receivedDate? — business logic cần check

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST FROM_PETITION với ngayKhoiTo='2025-01-01' (trước Petition.receivedDate)

### Dữ liệu kiểm thử
```
ngayKhoiTo='2025-01-01'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — hiện không validate

**Side effects** (DB, email, log, queue...):
- Cần audit business rule

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create-prov`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create-prov`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-066
severity: Low
module: Create-prov
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap nếu cần enforce

---

## TC-070

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-070`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?limit=0 → 400 @Min(1)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=0

### Dữ liệu kiểm thử
```
limit=0
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- min 1

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-070
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-071

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-071`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?limit=-5 → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=-5

### Dữ liệu kiểm thử
```
limit=-5
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-071
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-072

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-072`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?limit=abc (NaN) → 400 @IsInt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=abc

### Dữ liệu kiểm thử
```
limit='abc'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-072
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-074

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-074`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter status=INVALID → 400 IsEnum

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?status=INVALID

### Dữ liệu kiểm thử
```
status='INVALID'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-074
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-079

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-079`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter fromDate='2026-05-01' & toDate='2026-05-23' khoảng ngày

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?fromDate=2026-05-01&toDate=2026-05-23

### Dữ liệu kiểm thử
```
fromDate, toDate
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data[].createdAt BETWEEN '2026-05-01' AND '2026-05-23T23:59:59.999Z'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-079
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-080

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-080`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: toDate < fromDate → vẫn trả empty (không 400)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?fromDate=2026-05-23&toDate=2026-05-01

### Dữ liệu kiểm thử
```
fromDate>toDate
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- với data=[]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-080
severity: Low
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap nếu cần 400 cho invalid range — hiện chấp nhận

---

## TC-081

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-081`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter capDoToiPham=RAT_NGHIEM_TRONG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?capDoToiPham=RAT_NGHIEM_TRONG

### Dữ liệu kiểm thử
```
capDoToiPham='RAT_NGHIEM_TRONG'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data[].capDoToiPham=RAT_NGHIEM_TRONG

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-081
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-082

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-082`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter districtId qua subjects relation

### Điều kiện tiên quyết
- Subject S1 districtId='DIS-Q1' caseId='C1'

### Các bước kiểm thử
- [ ] GET ?districtId=DIS-Q1

### Dữ liệu kiểm thử
```
districtId='DIS-Q1'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data include C1 (qua subjects.some)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-082
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-083

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-083`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Sort sortBy=name sortOrder=asc

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?sortBy=name&sortOrder=asc

### Dữ liệu kiểm thử
```
sortBy='name', sortOrder='asc'
```

### Kết quả mong đợi
**UI**:
- Table sort A→Z by name

**API**:
- orderBy name asc

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-083
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-084

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-084`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: sortBy='evil_field' → fallback về createdAt (security: prevent SQL injection)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?sortBy=password

### Dữ liệu kiểm thử
```
sortBy='password'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- , allowedSortFields filter → orderBy createdAt

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-084
severity: High
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Whitelist sortBy chống SQLi qua orderBy

---

## TC-085

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-085`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Pagination offset=20 trả page 2

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=20&offset=20

### Dữ liệu kiểm thử
```
limit=20, offset=20
```

### Kết quả mong đợi
**UI**:
- Page 2 of N

**API**:
- data starts from item 21

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-085
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-094

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-094`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: User được xem unassigned Case của team mình (assignedTeamId=null + teamIds.length>0)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Login dieuTra1, GET /cases/CASE-UNASSIGNED

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:

**API**:
- (unassignedMatch)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-094
severity: Medium
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-095

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-095`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter wardTeamId='WARD-Q5' (cross-ward view PC02)

### Điều kiện tiên quyết
- Login PC02 user

### Các bước kiểm thử
- [ ] GET ?wardTeamId='WARD-Q5'

### Dữ liệu kiểm thử
```
wardTeamId='WARD-Q5'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data filter assignedTeam.wardId=WARD-Q5

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-095
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-096

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-096`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter investigatorId trả case của ĐTV cụ thể

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?investigatorId=<U001>

### Dữ liệu kiểm thử
```
investigatorId='U001'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data[].investigatorId='U001'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-096
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-106

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-106`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Update → TAM_DINH_CHI thiếu lý do nhưng Case cũ (pre-migration) → SOFT warning

### Điều kiện tiên quyết
- CASE-OLD createdAt='2026-04-01' (trước MIGRATION_DATE)

### Các bước kiểm thử
- [ ] PUT {status:'TAM_DINH_CHI'} (không lý do)

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Banner warning 'Khuyến nghị cập nhật lý do theo Điều 229'

**API**:
- , response.warning='Khuyến nghị: Vui lòng cập nhật lý do tạm đình chỉ theo quy định Điều 229 BLTTHS 2015 (áp dụng bắt buộc từ 30/04/2026)'

**Side effects** (DB, email, log, queue...):
- Update vẫn thành công với warning

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-106
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: 90-day grace period

---

## TC-109

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-109`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: ketQuaPhucHoiVuAn = invalid → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT ketQuaPhucHoiVuAn='INVALID'

### Dữ liệu kiểm thử
```
ketQuaPhucHoiVuAn='INVALID'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Kết quả phục hồi vụ án phải là enum KetQuaPhucHoiVuAn'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-109
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-110

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-110`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: lyDoTamDinhChiText > 500 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT lyDoTamDinhChiText='X'.repeat(501)

### Dữ liệu kiểm thử
```
lyDoTamDinhChiText.length=501
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- MaxLength(500)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-110
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-111

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-111`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: soQuyetDinhTamDinhChi > 100 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT soQuyetDinhTamDinhChi='X'.repeat(101)

### Dữ liệu kiểm thử
```
length=101
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- MaxLength(100)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-111
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-112

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-112`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: daRaSoat = 'yes' string thay vì boolean → 400 IsBoolean

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT daRaSoat='yes'

### Dữ liệu kiểm thử
```
daRaSoat='yes'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'daRaSoat phải là boolean'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-112
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-113

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-113`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: ngayRaSoat sai ISO format → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT ngayRaSoat='23/05/2026'

### Dữ liệu kiểm thử
```
ngayRaSoat='23/05/2026'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'ngayRaSoat không đúng định dạng ISO 8601'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-113
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-114

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-114`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Update KHÔNG đổi status → KHÔNG tạo CaseStatusHistory entry

### Điều kiện tiên quyết
- CASE-001 status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT {name:'sửa tên'} (không động status)

### Dữ liệu kiểm thử
```
name='sửa tên'
```

### Kết quả mong đợi
**UI**:
- Tab History KHÔNG có entry mới

**API**:

**Side effects** (DB, email, log, queue...):
- case_status_history KHÔNG insert row mới

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-114
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Logic: if (dto.status !== undefined && dto.status !== existing.status)

---

## TC-115

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-115`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Update status với cùng giá trị status hiện tại → KHÔNG tạo history

### Điều kiện tiên quyết
- CASE-001 status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT {status:'DANG_DIEU_TRA'} (idempotent)

### Dữ liệu kiểm thử
```
status='DANG_DIEU_TRA'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- , không có entry history

**Side effects** (DB, email, log, queue...):
- case_status_history empty for this op

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-115
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-116

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-116`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update metadata.petitionType sync sang Petition liên kết

### Điều kiện tiên quyết
- CASE-001 có linkedPetition PET-001 petitionType='KHIEU_NAI'

### Các bước kiểm thử
- [ ] PUT metadata={petitionType:'TO_GIAC'}

### Dữ liệu kiểm thử
```
metadata.petitionType='TO_GIAC'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Petition PET-001.petitionType=TO_GIAC (sync v0.37.2.5)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-116
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-117

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-117`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update metadata.petitionType mà KHÔNG có Petition link → silent ignore (no phantom create)

### Điều kiện tiên quyết
- CASE-002 không có linkedPetition

### Các bước kiểm thử
- [ ] PUT metadata={petitionType:'TO_GIAC'}

### Dữ liệu kiểm thử
```
metadata.petitionType='TO_GIAC'
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- KHÔNG tạo Petition mới (v0.37.2.5 fix phantom)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-117
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: BLTTHS Đ.143 compliance — provenance model forbids side-effect Petition

---

## TC-119

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-119`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: PUT lyDoTamDinhChiVuAn nhưng status không phải TAM_DINH_CHI → vẫn save (no enforce)

### Điều kiện tiên quyết
- CASE-001 status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT lyDoTamDinhChiVuAn='CHUA_XAC_DINH_BI_CAN' (status không đổi)

### Dữ liệu kiểm thử
```
lyDoTamDinhChiVuAn='CHUA_XAC_DINH_BI_CAN'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- , save value

**Side effects** (DB, email, log, queue...):
- cases row có lyDoTamDinhChiVuAn nhưng status=DANG_DIEU_TRA

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-119
severity: Low
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap nếu cần consistency — hiện không enforce

---

## TC-120

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-120`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: investigatorId không tồn tại → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT investigatorId='NOT_EXIST'

### Dữ liệu kiểm thử
```
investigatorId='NOT_EXIST'
```

### Kết quả mong đợi
**UI**:
- Toast 'Điều tra viên không tồn tại'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-120
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-121

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-121`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Update deadline=null xóa deadline cũ

### Điều kiện tiên quyết
- CASE-001 deadline='2026-08-01'

### Các bước kiểm thử
- [ ] PUT {deadline:null}

### Dữ liệu kiểm thử
```
deadline=null
```

### Kết quả mong đợi
**UI**:
- Detail show 'Chưa có deadline'

**API**:
- , data.deadline=null

**Side effects** (DB, email, log, queue...):
- cases.deadline=NULL

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-121
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-122

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-122`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Update capDoToiPham đổi từ NGHIEM_TRONG sang DAC_BIET_NGHIEM_TRONG

### Điều kiện tiên quyết
- capDoToiPham=NGHIEM_TRONG

### Các bước kiểm thử
- [ ] PUT capDoToiPham='DAC_BIET_NGHIEM_TRONG'

### Dữ liệu kiểm thử
```
capDoToiPham='DAC_BIET_NGHIEM_TRONG'
```

### Kết quả mong đợi
**UI**:
- Badge update

**API**:

**Side effects** (DB, email, log, queue...):
- KPI-4 dashboard recount

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-122
severity: Medium
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-123

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Update`
- Yêu cầu: `REQ-CASE-UPDATE-123`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update Case khi user role VIEWER → 403

### Điều kiện tiên quyết
- Login viewer

### Các bước kiểm thử
- [ ] PUT /cases/CASE-001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- PermissionsGuard

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-123
severity: High
module: Update
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-134

**Meta**:
- Loại: `STATE`
- Priority: `P1` 🟠
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-134`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Idempotent: PUT cùng status không tạo history

### Điều kiện tiên quyết
- status=DANG_DIEU_TRA

### Các bước kiểm thử
- [ ] PUT status=DANG_DIEU_TRA

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- History KHÔNG entry mới

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-134
severity: Medium
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-135

**Meta**:
- Loại: `STATE`
- Priority: `P1` 🟠
- Module: `Update-status`
- Yêu cầu: `REQ-CASE-UPDATE-STATUS-135`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: DA_LUU_TRU → DA_KET_LUAN (rollback) — có cho phép?

### Điều kiện tiên quyết
- status=DA_LUU_TRU

### Các bước kiểm thử
- [ ] PUT status=DA_KET_LUAN

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — hiện không enforce transition map ở backend

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Update-status`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Update-status`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-135
severity: Low
module: Update-status
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: thiếu state machine validation. Frontend disable button.

---

## TC-166

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Restore`
- Yêu cầu: `REQ-CASE-RESTORE-166`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Reason rỗng → 400 DTO validation

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST restore reason=''

### Dữ liệu kiểm thử
```
reason=''
```

### Kết quả mong đợi
**UI**:
- Error

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Restore`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Restore`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-166
severity: Medium
module: Restore
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-174

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-174`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: investigatorId=null khi assign (chỉ chuyển team, chưa giao cho ĐTV cụ thể)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH assign assignedTeamId='Team-Q2', investigatorId=null

### Dữ liệu kiểm thử
```
investigatorId=null
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- , data.investigatorId=null

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-174
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-179

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-179`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Re-assign cùng team (no-op assignedTeamId không đổi) → audit không emit escalation

### Điều kiện tiên quyết
- CASE-001 Team-Q1

### Các bước kiểm thử
- [ ] PATCH assign assignedTeamId='Team-Q1', investigatorId='<other>'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- CASE_ASSIGNED có, ESCALATED_FROM_WARD KHÔNG

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-179
severity: Medium
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-180

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-180`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Non-ward team → ward team (de-escalation) — audit không emit (chỉ from-ward)

### Điều kiện tiên quyết
- CASE-001 Team-PC02-CENTRAL (wardId=null)

### Các bước kiểm thử
- [ ] PATCH assign Team-WARD-01

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- CASE_ASSIGNED có, không ESCALATED_FROM_WARD (logic chỉ wardId→null)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-180
severity: Medium
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-181

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-181`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: assignedTeamId rỗng '' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH assignedTeamId=''

### Dữ liệu kiểm thử
```
assignedTeamId=''
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-181
severity: Medium
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-182

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-182`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Thiếu assignedTeamId → 400 DTO required

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH /assign body={}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-182
severity: High
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-183

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Assign`
- Yêu cầu: `REQ-CASE-ASSIGN-183`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Dispatcher với expectedUpdatedAt KHỚP → success

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH assign với expectedUpdatedAt match current

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- updatedAt refreshed

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Assign`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Assign`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-183
severity: Medium
module: Assign
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-191

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-191`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Export với take=500 limit cứng (không trả >500 row)

### Điều kiện tiên quyết
- DB có 1000 case

### Các bước kiểm thử
- [ ] GET export

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Excel max 500 row

**API**:
- findMany take:500

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-191
severity: Medium
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Limit cứng để tránh memory blow up

---

## TC-194

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-194`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Export ward không có data → vẫn trả Excel với header + empty body + footer

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET export filter range không match record nào

### Dữ liệu kiểm thử
```
fromDate=2030-01-01&toDate=2030-01-02
```

### Kết quả mong đợi
**UI**:
- Empty Excel

**API**:
- file valid

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-194
severity: Medium
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-195

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-195`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Server crash giữa export (write fail) → response 500 JSON nếu chưa send header

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mock workbook.xlsx.write throw

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Export failed'

**API**:
- {error:'Export failed'}

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-195
severity: High
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Best-effort fallback

---

## TC-196

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-196`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Audit CASE_EXPORTED capture filters đầy đủ

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/export/ward?unitId=Q1&fromDate=...

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Audit log metadata={format:'xlsx', kind:'ward', filters:{unitId:'Q1', fromDate, toDate}}

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-196
severity: High
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Sprint 2 S2.1 audit PII bulk leak

---

## TC-198

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `History`
- Yêu cầu: `REQ-CASE-HISTORY-198`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Case không có status change → empty array

### Điều kiện tiên quyết
- CASE-NEW chưa update status

### Các bước kiểm thử
- [ ] GET /cases/CASE-NEW/status-history

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Tab show 'Chưa có lịch sử'

**API**:
- data=[]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `History`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `History`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-198
severity: Medium
module: History
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-200

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `History`
- Yêu cầu: `REQ-CASE-HISTORY-200`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: changedBy=null khi changedById=null (audit cũ chưa có user)

### Điều kiện tiên quyết
- Entry legacy changedById=null

### Các bước kiểm thử
- [ ] GET history

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Row hiện 'Hệ thống'

**API**:
- entry.changedBy=null

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `History`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `History`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-200
severity: Low
module: History
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-202

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `History`
- Yêu cầu: `REQ-CASE-HISTORY-202`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: History entry có changedAt là datetime đầy đủ (ISO)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET history

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- UI format DD/MM/YYYY HH:MM

**API**:
- data[].changedAt='2026-05-22T10:30:00.000Z'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `History`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `History`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-202
severity: Low
module: History
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-205

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `TdcBackfill`
- Yêu cầu: `REQ-CASE-TDCBACKFILL-205`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: PATCH lyDoTamDinhChiVuAn='INVALID' → 400/500 (validation cast)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PATCH lyDoTamDinhChiVuAn='INVALID'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- do Prisma enum constraint (no DTO validation)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `TdcBackfill`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `TdcBackfill`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-205
severity: Medium
module: TdcBackfill
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: tdcBackfill DTO không validate enum

---

## TC-207

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `TdcBackfill`
- Yêu cầu: `REQ-CASE-TDCBACKFILL-207`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Banner TdcBackfillBanner ẩn sau khi backfill thành công

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] UI flow: thấy banner, click 'Backfill', nhập lý do, submit

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Banner disappear

**API**:
- Reload Case detail

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `TdcBackfill`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `TdcBackfill`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-207
severity: Medium
module: TdcBackfill
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-237

**Meta**:
- Loại: `SECURITY`
- Priority: `P1` 🟠
- Module: `A10`
- Yêu cầu: `REQ-CASE-A10-237`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Cases không fetch URL từ user input — N/A SSRF

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Search code 'fetch(' với user input

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `A10`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `A10`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-237
severity: Low
module: A10
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: N/A — Cases không có flow fetch URL

---

## TC-243

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Keyboard`
- Yêu cầu: `REQ-CASE-KEYBOARD-243`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Tab qua đủ field trong CaseFormPage không bị trap

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tab từ name → crime → status → ... cuối cùng đến nút Lưu

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Focus visible trên mỗi field, không skip

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Keyboard`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Keyboard`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-243
severity: Medium
module: Keyboard
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-244

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Keyboard`
- Yêu cầu: `REQ-CASE-KEYBOARD-244`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Enter submit form khi focus ở input

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Focus name, type, Enter

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Form submit

**API**:
- POST trigger

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Keyboard`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Keyboard`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-244
severity: Medium
module: Keyboard
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-245

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Keyboard`
- Yêu cầu: `REQ-CASE-KEYBOARD-245`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Esc đóng modal SuspensionModal

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mở modal TĐC, nhấn Esc

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Modal close

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Keyboard`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Keyboard`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-245
severity: Medium
module: Keyboard
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-246

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Screenreader`
- Yêu cầu: `REQ-CASE-SCREENREADER-246`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Input có aria-label hoặc label[for] gắn id

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect <input id=name> có <label for=name>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Screenreader`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Screenreader`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-246
severity: High
module: Screenreader
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: WCAG 1.3.1 Info & Relationships

---

## TC-247

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Screenreader`
- Yêu cầu: `REQ-CASE-SCREENREADER-247`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Status badge có aria-label='Trạng thái Đang điều tra'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] VoiceOver/NVDA đọc badge

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Screenreader`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Screenreader`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-247
severity: Medium
module: Screenreader
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-248

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Screenreader`
- Yêu cầu: `REQ-CASE-SCREENREADER-248`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Error message gắn aria-describedby với input

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Nhập invalid name, inspect aria-describedby

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- <input aria-describedby='name-error'> + <p id='name-error'>

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Screenreader`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Screenreader`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-248
severity: High
module: Screenreader
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-249

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Contrast`
- Yêu cầu: `REQ-CASE-CONTRAST-249`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Text trên background đạt 4.5:1 (WCAG AA)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect color of body text vs background

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Contrast`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Contrast`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-249
severity: High
module: Contrast
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Use axe-core tool

---

## TC-250

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Contrast`
- Yêu cầu: `REQ-CASE-CONTRAST-250`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Badge status có contrast đủ (text white trên red ≥ 4.5)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect status badge

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Contrast`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Contrast`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-250
severity: Medium
module: Contrast
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-251

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Focus`
- Yêu cầu: `REQ-CASE-FOCUS-251`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Focus ring rõ ràng trên nút Lưu (outline 2px)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tab tới nút Lưu

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Focus visible với outline rõ

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Focus`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Focus`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-251
severity: High
module: Focus
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-254

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Form`
- Yêu cầu: `REQ-CASE-FORM-254`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Form error message tiếng Việt, không dùng chỉ màu để biểu thị error

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Submit invalid

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Border đỏ + icon + text 'Tên vụ án bắt buộc'

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Form`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Form`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-254
severity: High
module: Form
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: WCAG 1.4.1 Use of Color

---

## TC-255

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Heading`
- Yêu cầu: `REQ-CASE-HEADING-255`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Page có h1, h2 hierarchical

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect heading order

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Heading`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Heading`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-255
severity: Medium
module: Heading
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: WCAG 2.4.6

---

## TC-256

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Image`
- Yêu cầu: `REQ-CASE-IMAGE-256`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Icon button có aria-label (vd 'Xóa vụ án')

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect <button aria-label='Xóa vụ án'>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Image`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Image`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-256
severity: Medium
module: Image
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-258

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `TableSemantics`
- Yêu cầu: `REQ-CASE-TABLESEMANTICS-258`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Bảng danh sách dùng <table><th scope='col'>

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect CaseListPage table

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `TableSemantics`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `TableSemantics`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-258
severity: Medium
module: TableSemantics
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-259

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `ModalFocus`
- Yêu cầu: `REQ-CASE-MODALFOCUS-259`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Modal mở thì focus trap inside; close trả focus về trigger

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Click 'Xóa', modal mở, Tab vòng quanh modal

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Focus không escape modal

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `ModalFocus`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `ModalFocus`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-259
severity: High
module: ModalFocus
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-260

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-260`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Chrome 126 latest — toàn bộ flow OK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Test Chrome 126

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-260
severity: High
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-261

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-261`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Chrome 125 (latest-1) — backward compat

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Chrome 125

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-261
severity: Medium
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-262

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-262`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Firefox 127 latest

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Firefox 127

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-262
severity: Medium
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-263

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-263`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Safari 17 latest (macOS)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Safari 17

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-263
severity: Medium
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-264

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-264`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Edge 126

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Edge 126

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-264
severity: Medium
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-267

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Mobile`
- Yêu cầu: `REQ-CASE-MOBILE-267`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: iOS Safari 17 trên iPhone 14 — list page responsive

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] iPhone 14

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Layout responsive

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Mobile`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Mobile`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-267
severity: Medium
module: Mobile
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-268

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Mobile`
- Yêu cầu: `REQ-CASE-MOBILE-268`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Android Chrome 126 trên Pixel 7

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Pixel 7

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Mobile`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Mobile`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-268
severity: Medium
module: Mobile
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-269

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Resolution`
- Yêu cầu: `REQ-CASE-RESOLUTION-269`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: 1920x1080 desktop — layout standard

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Chrome 1920x1080

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Resolution`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Resolution`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-269
severity: Medium
module: Resolution
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-270

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Resolution`
- Yêu cầu: `REQ-CASE-RESOLUTION-270`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: 1366x768 laptop common — không scroll horizontal

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Resize 1366x768

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Resolution`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Resolution`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-270
severity: Medium
module: Resolution
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-271

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Resolution`
- Yêu cầu: `REQ-CASE-RESOLUTION-271`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: 768x1024 tablet portrait

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] iPad portrait

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Responsive

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Resolution`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Resolution`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-271
severity: Medium
module: Resolution
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-272

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Resolution`
- Yêu cầu: `REQ-CASE-RESOLUTION-272`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: 375x667 iPhone SE

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] iPhone SE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Compact layout

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Resolution`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Resolution`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-272
severity: Medium
module: Resolution
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-273

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Network`
- Yêu cầu: `REQ-CASE-NETWORK-273`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: 3G slow — page load < 5s và loading indicator

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DevTools throttle 3G slow

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Loading spinner xuất hiện

**API**:
- Timeout 30s server-side

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Network`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Network`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-273
severity: Medium
module: Network
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-275

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `OS`
- Yêu cầu: `REQ-CASE-OS-275`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Windows 11 + Chrome — OK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Win 11 Chrome

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `OS`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `OS`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-275
severity: Medium
module: OS
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-276

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `OS`
- Yêu cầu: `REQ-CASE-OS-276`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: macOS Sonoma + Safari — OK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] macOS Safari

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `OS`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `OS`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-276
severity: Medium
module: OS
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-277

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-277`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?limit=20 response < 500ms với 10k row DB

### Điều kiện tiên quyết
- DB 10000 cases

### Các bước kiểm thử
- [ ] Time GET /cases?limit=20

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Response time p95 < 500ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-277
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Indexed by createdAt, status, deletedAt

---

## TC-278

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Search`
- Yêu cầu: `REQ-CASE-SEARCH-278`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases?search=trộm cắp < 1s với 10k row

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Time search

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- p95 < 1s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Search`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Search`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-278
severity: Medium
module: Search
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Full-text search có thể cần tsvector index

---

## TC-279

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `List`
- Yêu cầu: `REQ-CASE-LIST-279`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter overdue=true + pagination < 800ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Time GET ?overdue=true&limit=50

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <800ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `List`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `List`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-279
severity: Medium
module: List
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-280

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Detail`
- Yêu cầu: `REQ-CASE-DETAIL-280`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /cases/:id (include petitions + investigator) < 300ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Time GET /cases/X

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <300ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Detail`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Detail`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-280
severity: Medium
module: Detail
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-281

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Create`
- Yêu cầu: `REQ-CASE-CREATE-281`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: POST /cases (FROM_PETITION transaction) < 600ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Time POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <600ms (gồm tx + audit)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Create`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Create`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-281
severity: Medium
module: Create
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-282

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Export`
- Yêu cầu: `REQ-CASE-EXPORT-282`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Export 500 row Excel < 5s

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Time GET export với DB ≥500

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Download start < 5s

**API**:
- Stream response

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Export`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Export`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-282
severity: Medium
module: Export
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-285

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Pagination`
- Yêu cầu: `REQ-CASE-PAGINATION-285`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Deep pagination offset=5000 < 1s (with index)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?offset=5000&limit=20

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <1s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Pagination`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Pagination`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-285
severity: Low
module: Pagination
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cursor pagination tốt hơn nếu nhiều

---

## TC-287

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `AuditWrite`
- Yêu cầu: `REQ-CASE-AUDITWRITE-287`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Audit log insert không block main response > 50ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST /cases, profile audit insert

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Audit insert in tx, ≤50ms overhead

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `AuditWrite`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `AuditWrite`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-287
severity: Low
module: AuditWrite
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-288

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Unicode`
- Yêu cầu: `REQ-CASE-UNICODE-288`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: name có dấu tiếng Việt 'Vụ án Nguyễn Quỳnh Trâm'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='Vụ án Nguyễn Quỳnh Trâm'

### Dữ liệu kiểm thử
```
name='Vụ án Nguyễn Quỳnh Trâm'
```

### Kết quả mong đợi
**UI**:
- Detail show đúng dấu

**API**:

**Side effects** (DB, email, log, queue...):
- DB UTF-8 stored correct

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Unicode`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Unicode`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-288
severity: High
module: Unicode
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-289

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Unicode`
- Yêu cầu: `REQ-CASE-UNICODE-289`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name có dấu tổ hợp (Unicode NFC vs NFD) — normalize

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name với 'ặ' NFD (2 codepoint) vs NFC (1 codepoint)

### Dữ liệu kiểm thử
```
name='ặ' (NFD) vs name='ặ' (NFC)
```

### Kết quả mong đợi
**UI**:
- Hiển thị giống

**API**:
- both

**Side effects** (DB, email, log, queue...):
- Cần normalize NFC khi save (gap nếu thiếu)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Unicode`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Unicode`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-289
severity: Medium
module: Unicode
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap potential

---

## TC-290

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Emoji`
- Yêu cầu: `REQ-CASE-EMOJI-290`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name có emoji 'Vụ test 🎉' — 4-byte UTF-8

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='Vụ test 🎉'

### Dữ liệu kiểm thử
```
name='Vụ test 🎉'
```

### Kết quả mong đợi
**UI**:
- Hiển thị emoji

**API**:

**Side effects** (DB, email, log, queue...):
- MySQL cần utf8mb4 — PostgreSQL OK

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Emoji`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Emoji`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-290
severity: Medium
module: Emoji
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-291

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Whitespace`
- Yêu cầu: `REQ-CASE-WHITESPACE-291`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name='  Vụ ABC  ' leading/trailing trimmed

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST '  Vụ ABC  '

### Dữ liệu kiểm thử
```
name='  Vụ ABC  '
```

### Kết quả mong đợi
**UI**:
- Detail name='Vụ ABC'

**API**:
- , data.name='Vụ ABC'

**Side effects** (DB, email, log, queue...):
- Transform trim apply

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Whitespace`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Whitespace`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-291
severity: Medium
module: Whitespace
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-292

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Hidden`
- Yêu cầu: `REQ-CASE-HIDDEN-292`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: name có zero-width space \u200B → save raw nhưng có thể gây bug filter

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='Vụ\u200BABC'

### Dữ liệu kiểm thử
```
name='Vụ​ABC'
```

### Kết quả mong đợi
**UI**:
- Hiển thị giống 'Vụ ABC'

**API**:

**Side effects** (DB, email, log, queue...):
- Search 'Vụ ABC' không match (gap)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Hidden`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Hidden`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-292
severity: Low
module: Hidden
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-293

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `BOM`
- Yêu cầu: `REQ-CASE-BOM-293`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: name='\uFEFFVụ' BOM character ở đầu

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
name='﻿Vụ'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- lưu nguyên (gap nếu cần strip)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `BOM`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `BOM`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-293
severity: Low
module: BOM
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-294

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `HTML`
- Yêu cầu: `REQ-CASE-HTML-294`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name='<b>Vụ</b>' tag HTML literal

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
name='<b>Vụ</b>'
```

### Kết quả mong đợi
**UI**:
- React escape → hiển thị '<b>Vụ</b>'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `HTML`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `HTML`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-294
severity: Medium
module: HTML
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-295

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Date`
- Yêu cầu: `REQ-CASE-DATE-295`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: ngayKhoiTo='2026-02-29' (năm 2026 không nhuận) → 400 hoặc 2026-03-01?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST ngayKhoiTo='2026-02-29'

### Dữ liệu kiểm thử
```
ngayKhoiTo='2026-02-29'
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- IsDateString strict hoặc auto-coerce — verify

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Date`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Date`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-295
severity: Medium
module: Date
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Postgres date coerce 2026-03-01

---

## TC-296

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Timezone`
- Yêu cầu: `REQ-CASE-TIMEZONE-296`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: ngayKhoiTo='2026-05-23T00:00:00+07:00' (GMT+7 Vietnam)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST ngayKhoiTo='2026-05-23T00:00:00+07:00'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show 23/05/2026

**API**:
- , stored as UTC=2026-05-22T17:00:00Z

**Side effects** (DB, email, log, queue...):
- DB stored as UTC, frontend convert TZ

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Timezone`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Timezone`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-296
severity: Medium
module: Timezone
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-297

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Number`
- Yêu cầu: `REQ-CASE-NUMBER-297`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: subjectsCount=Number.MAX_SAFE_INTEGER → save hoặc 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount=9007199254740991

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — Int field PostgreSQL 32-bit hoặc bigint

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Number`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Number`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-297
severity: Low
module: Number
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần check Prisma Int = int4

---

## TC-298

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Encoding`
- Yêu cầu: `REQ-CASE-ENCODING-298`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Multi-byte filename trong sourceDocumentNote

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST sourceDocumentNote='Công văn số 123/CV-PC02 ngày 12/05/2026 — tiếng Việt'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail OK

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Encoding`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Encoding`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-298
severity: Medium
module: Encoding
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-299

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `SpecialChar`
- Yêu cầu: `REQ-CASE-SPECIALCHAR-299`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name có '&', '<', '>', '"', "'" — SQL escape

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST name='Vụ A & B <test> "quote"'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Hiển thị đúng

**API**:

**Side effects** (DB, email, log, queue...):
- DB lưu raw, SQL safe (Prisma param)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `SpecialChar`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `SpecialChar`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-299
severity: Medium
module: SpecialChar
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-300

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `LongString`
- Yêu cầu: `REQ-CASE-LONGSTRING-300`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: metadata.note 5000 ký tự (JSONB không limit)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST metadata={note:'X'.repeat(5000)}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- JSONB stored

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `LongString`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `LongString`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-300
severity: Low
module: LongString
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-305

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `crime`
- Yêu cầu: `REQ-CASE-CRIME-305`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: crime 255 ký tự (max) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=255
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `crime`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `crime`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-305
severity: Medium
module: crime
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-306

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `crime`
- Yêu cầu: `REQ-CASE-CRIME-306`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: crime 256 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=256
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `crime`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `crime`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-306
severity: Medium
module: crime
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-307

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `unit`
- Yêu cầu: `REQ-CASE-UNIT-307`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: unit 255 ký tự → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=255
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `unit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `unit`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-307
severity: Low
module: unit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-308

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `unit`
- Yêu cầu: `REQ-CASE-UNIT-308`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: unit 256 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=256
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `unit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `unit`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-308
severity: Low
module: unit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-309

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `reason`
- Yêu cầu: `REQ-CASE-REASON-309`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Delete reason 10 ký tự (min) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE reason='1234567890' (length 10)

### Dữ liệu kiểm thử
```
length=10
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `reason`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `reason`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-309
severity: Medium
module: reason
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-310

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `reason`
- Yêu cầu: `REQ-CASE-REASON-310`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Delete reason 9 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE reason='123456789' (length 9)

### Dữ liệu kiểm thử
```
length=9
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- MinLength(10)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `reason`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `reason`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-310
severity: Medium
module: reason
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-311

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `reason`
- Yêu cầu: `REQ-CASE-REASON-311`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Delete reason 500 ký tự (max) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
length=500
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `reason`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `reason`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-311
severity: Medium
module: reason
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-312

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `reason`
- Yêu cầu: `REQ-CASE-REASON-312`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Delete reason 501 ký tự → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE

### Dữ liệu kiểm thử
```
length=501
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `reason`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `reason`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-312
severity: Medium
module: reason
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-313

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `subC`
- Yêu cầu: `REQ-CASE-SUBC-313`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: subjectsCount = 0 (min) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount=0

### Dữ liệu kiểm thử
```
0
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `subC`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `subC`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-313
severity: Low
module: subC
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-314

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `subC`
- Yêu cầu: `REQ-CASE-SUBC-314`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: subjectsCount = -1 → 400 @Min(0)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount=-1

### Dữ liệu kiểm thử
```
-1
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `subC`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `subC`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-314
severity: Medium
module: subC
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-315

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `subC`
- Yêu cầu: `REQ-CASE-SUBC-315`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: subjectsCount = 1 → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST subjectsCount=1

### Dữ liệu kiểm thử
```
1
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `subC`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `subC`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-315
severity: Low
module: subC
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-316

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `limit`
- Yêu cầu: `REQ-CASE-LIMIT-316`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: limit=1 (min) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=1

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- row

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `limit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `limit`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-316
severity: Low
module: limit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-317

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `limit`
- Yêu cầu: `REQ-CASE-LIMIT-317`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: limit=100 (max) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=100

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- max 100

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `limit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `limit`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-317
severity: Medium
module: limit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-318

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `limit`
- Yêu cầu: `REQ-CASE-LIMIT-318`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: limit=101 → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=101

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `limit`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `limit`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-318
severity: Medium
module: limit
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-319

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `offset`
- Yêu cầu: `REQ-CASE-OFFSET-319`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: offset=0 (min) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?offset=0

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `offset`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `offset`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-319
severity: Low
module: offset
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-320

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `offset`
- Yêu cầu: `REQ-CASE-OFFSET-320`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: offset=-1 → 400 @Min(0)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?offset=-1

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `offset`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `offset`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-320
severity: Medium
module: offset
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-321

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `srcDocNote`
- Yêu cầu: `REQ-CASE-SRCDOCNOTE-321`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: sourceDocumentNote 1000 ký tự (max) → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=1000
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `srcDocNote`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `srcDocNote`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-321
severity: Low
module: srcDocNote
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-322

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `srcDocNote`
- Yêu cầu: `REQ-CASE-SRCDOCNOTE-322`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: sourceDocumentNote 1001 → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
length=1001
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `srcDocNote`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `srcDocNote`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-322
severity: Medium
module: srcDocNote
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-323

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `name-cls`
- Yêu cầu: `REQ-CASE-NAME-CLS-323`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: name class hợp lệ: chuỗi VN có dấu

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 'Vụ trộm cắp xe'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `name-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `name-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-323
severity: Low
module: name-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-324

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `name-cls`
- Yêu cầu: `REQ-CASE-NAME-CLS-324`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: name class không hợp lệ: rỗng

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST ''

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `name-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `name-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-324
severity: Medium
module: name-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-325

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `prov-cls`
- Yêu cầu: `REQ-CASE-PROV-CLS-325`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Provenance class valid enum (DIRECT_DISCOVERY)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST DIRECT_DISCOVERY

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `prov-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `prov-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-325
severity: Low
module: prov-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-326

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `prov-cls`
- Yêu cầu: `REQ-CASE-PROV-CLS-326`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Provenance class invalid (string ngẫu nhiên)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 'XXX'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `prov-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `prov-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-326
severity: Medium
module: prov-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-327

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `capDo-cls`
- Yêu cầu: `REQ-CASE-CAPDO-CLS-327`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: CapDoToiPham class IT_NGHIEM_TRONG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST IT_NGHIEM_TRONG

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `capDo-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `capDo-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-327
severity: Low
module: capDo-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-328

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `capDo-cls`
- Yêu cầu: `REQ-CASE-CAPDO-CLS-328`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: CapDoToiPham class NGHIEM_TRONG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST NGHIEM_TRONG

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `capDo-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `capDo-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-328
severity: Low
module: capDo-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-329

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `capDo-cls`
- Yêu cầu: `REQ-CASE-CAPDO-CLS-329`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: CapDoToiPham class RAT_NGHIEM_TRONG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST RAT_NGHIEM_TRONG

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `capDo-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `capDo-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-329
severity: Low
module: capDo-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-330

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `capDo-cls`
- Yêu cầu: `REQ-CASE-CAPDO-CLS-330`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: CapDoToiPham class DAC_BIET_NGHIEM_TRONG

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST DAC_BIET_NGHIEM_TRONG

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `capDo-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `capDo-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-330
severity: Low
module: capDo-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-331

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `status-cls`
- Yêu cầu: `REQ-CASE-STATUS-CLS-331`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Status valid class TIEP_NHAN

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST status=TIEP_NHAN

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `status-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `status-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-331
severity: Low
module: status-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-332

**Meta**:
- Loại: `EP`
- Priority: `P1` 🟠
- Module: `status-cls`
- Yêu cầu: `REQ-CASE-STATUS-CLS-332`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Status invalid class 'OPEN' (Anh)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST status='OPEN'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `status-cls`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `status-cls`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-332
severity: Medium
module: status-cls
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-350

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-350`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Cross-feature: KPI dashboard reload khi Case mới capDoToiPham=DBNT tạo

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tạo Case ĐBNT, reload KPI-4

### Dữ liệu kiểm thử
```
capDoToiPham='DAC_BIET_NGHIEM_TRONG'
```

### Kết quả mong đợi
**UI**:
- KPI-4 count +1

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-350
severity: Medium
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-351

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-351`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Update Case sync Petition.petitionType

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT Case metadata.petitionType='TO_GIAC', verify Petition record

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Petition.petitionType updated (v0.37.2.5)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-351
severity: High
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-352

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-352`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Assign Case ward → cross-ward → audit event chuỗi

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Assign WARD → CENTRAL → check audit log

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit events: CASE_ASSIGNED + CASE_ESCALATED_FROM_WARD

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-352
severity: Medium
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-353

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-353`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Soft delete → list deleted → restore → list active

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE → admin/deleted → restore → /cases

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Case round-trip

**API**:
- -

**Side effects** (DB, email, log, queue...):
- audit 2 events

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-353
severity: Medium
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-354

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `E2E`
- Yêu cầu: `REQ-CASE-E2E-354`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Multi-user race: A update + B delete đồng thời

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] user thao tác cùng case

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- success, 1 P2025/409

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Atomic guards work

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `E2E`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `E2E`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-354
severity: High
module: E2E
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-355

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Cancel`
- Yêu cầu: `REQ-CASE-CANCEL-355`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: User đóng tab giữa form Create — KHÔNG tạo Case partial

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mở form, nhập half, đóng tab

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- DB không có row partial

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Cancel`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Cancel`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-355
severity: Medium
module: Cancel
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-356

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Cancel`
- Yêu cầu: `REQ-CASE-CANCEL-356`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: User refresh giữa form Create — clear state hoặc preserve draft?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Nhập form, F5

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Form clear (hoặc warning 'Mất dữ liệu')

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Cancel`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Cancel`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-356
severity: Low
module: Cancel
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-357

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Network`
- Yêu cầu: `REQ-CASE-NETWORK-357`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Mất mạng giữa POST /cases — frontend retry hoặc toast error

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DevTools offline → POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Lỗi kết nối, thử lại'

**API**:
- Network error

**Side effects** (DB, email, log, queue...):
- No partial create

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Network`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Network`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-357
severity: High
module: Network
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-358

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Server500`
- Yêu cầu: `REQ-CASE-SERVER500-358`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Server 500 giữa POST — Case không tạo, audit không log

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mock prisma.case.create throw

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Lỗi máy chủ'

**API**:

**Side effects** (DB, email, log, queue...):
- Atomic: no row, no audit (tx rollback)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Server500`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Server500`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-358
severity: High
module: Server500
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-359

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Timeout`
- Yêu cầu: `REQ-CASE-TIMEOUT-359`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Request timeout 30s + retry → idempotency?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Slow network, POST retry

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Có thể double-create (gap) nếu không idempotency-key

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Timeout`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Timeout`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-359
severity: High
module: Timeout
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần idempotency-key cho mutation

---

## TC-360

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `PartialDelete`
- Yêu cầu: `REQ-CASE-PARTIALDELETE-360`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: Audit fail giữa DELETE tx → rollback delete

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mock audit.log throw

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast 'Xóa thất bại'

**API**:

**Side effects** (DB, email, log, queue...):
- cases.deletedAt KHÔNG set (tx rollback)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PartialDelete`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PartialDelete`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-360
severity: Critical
module: PartialDelete
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Atomic transaction guarantee

---

## TC-384

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-384`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: gender='UNKNOWN' không trong enum → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST gender='UNKNOWN'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Giới tính không hợp lệ'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-384
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-385

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-385`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: gender bỏ trống → default MALE

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không kèm gender

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- , data.gender='MALE'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-385
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-386

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-386`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: type=VICTIM (Bị hại) — không phải SUSPECT default

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST type='VICTIM'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show 'Bị hại'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-386
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-387

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-387`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: type=WITNESS (Nhân chứng)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST type='WITNESS'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-387
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-388

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-388`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: type='INVALID' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST type='INVALID'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-388
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-389

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-389`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: status=DETAINED (Đang tạm giam)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST status='DETAINED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Badge 'Đang tạm giam'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-389
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-390

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-390`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: status=RELEASED khi đã thả

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT subject {status:'RELEASED'}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Badge update

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-390
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-391

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-391`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: status=WANTED (Truy nã)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='WANTED'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Badge đỏ 'Truy nã'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-391
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-406

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-406`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: fullName tiếng Việt có dấu 'Nguyễn Quỳnh Trâm'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show đúng dấu

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-406
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-407

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-407`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: fullName 200 ký tự (long name)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName 'X'*200

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-407
severity: Low
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-408

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-408`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: address Unicode + multi-line

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST address='Số 12\nLê Lợi, Quận 1\nTP.HCM'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail preserve newline

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-408
severity: Low
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-409

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-409`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: CCCD đúng 9 chữ → pass min

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber='079090123'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-409
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-410

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-410`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: CCCD đúng 12 chữ → pass max

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber 12 chars

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-410
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-411

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-411`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CCCD 10 chữ → 400 (regex chỉ accept 9 hoặc 12)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber 10 chars

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- regex

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-411
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-412

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-412`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: CCCD 11 chữ → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST idNumber 11 chars

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-412
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-416

**Meta**:
- Loại: `STATE`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-416`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Transition WANTED → DETAINED (bắt được rồi)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-416
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-417

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-417`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Form Subject input có label gắn id

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect form

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- <label for=fullName>

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-417
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-418

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-418`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Date picker dateOfBirth keyboard accessible

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Tab vào date picker, dùng arrow keys

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-418
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-419

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-419`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /subjects?caseId=X với Case có 50 subjects < 500ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <500ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-419
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-421

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-421`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Cùng idNumber khác type (SUSPECT vs VICTIM) trong cùng Case OK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 2 Subject idNumber giống type khác

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- cả 2 (unique constraint per type)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-421
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-423

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-423`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: PUT thay đổi caseId (chuyển vụ án) → có cho phép?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT caseId='OTHER_CASE'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify business rule. Cần block hoặc audit

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-423
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên freeze caseId sau create

---

## TC-424

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-424`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: occupationId không tồn tại directory → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST occupationId='OCC-NOT-EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- FK

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-424
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-425

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-425`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: nationalityId không tồn tại → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST nationalityId='NAT-NOT-EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-425
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-426

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-426`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: wardId không tồn tại → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST wardId='WARD-NOT-EXIST'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-426
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-427

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-427`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: districtName denormalized preserve sau cải cách hành chính

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST districtName='Quận 1'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail show 'Quận 1' kể cả khi đơn vị hành chính đã đổi

**API**:

**Side effects** (DB, email, log, queue...):
- districtName lưu raw, không update khi directory đổi

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-427
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-428

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-428`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Subject Filter by status=DETAINED

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /subjects?status=DETAINED

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data[].status=DETAINED

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-428
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-429

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-429`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Subject Filter by type=SUSPECT

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?type=SUSPECT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data[].type=SUSPECT

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-429
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-432

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-432`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: fullName 1 ký tự 'A'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName='A'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (no min length)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-432
severity: Low
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-433

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-433`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: fullName 1000 ký tự (no max)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName 'X'*1000

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (no MaxLength constraint — gap)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-433
severity: Low
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên có MaxLength để chống DoS

---

## TC-434

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-434`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: phone number format VN '0901234567'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST phone='0901234567'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (no validation — gap)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-434
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-435

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-435`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: phone với ký tự đặc biệt '+84 901 234 567'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST phone='+84 901 234 567'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- raw

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-435
severity: Low
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-436

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-436`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Subject + Lawyer assign: thêm Lawyer cho 1 Subject cụ thể

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST Lawyer với subjectId=S001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- lawyer.subjectId=S001

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-436
severity: Medium
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-437

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-437`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Subject có Lawyer link — Lawyer.subjectId=NULL (SetNull)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Subject có 1 Lawyer

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- subject deleted

**Side effects** (DB, email, log, queue...):
- Lawyer.subjectId=NULL (Prisma onDelete: SetNull)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-437
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-438

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Subjects`
- Yêu cầu: `REQ-CASE-SUBJECTS-438`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Atomic Subject create rollback nếu audit fail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mock audit throw

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- No subject row created

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Subjects`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Subjects`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-438
severity: High
module: Subjects
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-442

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-442`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Lawyer không gắn subjectId (bào chữa chung)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không subjectId

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail 'Bào chữa chung'

**API**:
- subjectId=null

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-442
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-449

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-449`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: subjectId không cùng caseId (cross-case) → 400 business rule

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST caseId='C001', subjectId='S-of-C002'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Đối tượng không thuộc vụ án này'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-449
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Cần verify service business check

---

## TC-464

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-464`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: fullName tiếng Việt 'Luật sư Đỗ Thị Thanh Mai'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Hiển thị dấu đúng

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-464
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-465

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-465`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: lawFirm Unicode 'Văn phòng Luật sư Hà Nội & Cộng sự'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-465
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-466

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-466`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: phone số Quốc tế '+84 901 234 567'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST phone='+84 901 234 567'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-466
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-467

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-467`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: barNumber chứa whitespace 'LS 2026 12345' → save raw (gap)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- raw

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-467
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên normalize barNumber (strip spaces)

---

## TC-468

**Meta**:
- Loại: `STATE`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-468`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Lawyer được assign sang Subject khác (re-assign)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT subjectId='S002'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-468
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-469

**Meta**:
- Loại: `STATE`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-469`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Unassign Lawyer khỏi Subject (subjectId=null)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT subjectId=null

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-469
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-470

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-470`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Soft-delete Subject → Lawyer.subjectId auto null (onDelete SetNull)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Subject có Lawyer link

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Lawyer.subjectId=null

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-470
severity: High
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-471

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-471`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Form barNumber có pattern hint

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect form

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- placeholder='LS-YYYY-XXXXX'

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-471
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-472

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-472`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: GET /lawyers với 100 lawyer DB < 500ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <500ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-472
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-476

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-476`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: fullName 1 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST fullName='A'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-476
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-477

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-477`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: barNumber 1 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST barNumber='X'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (no min length — gap)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-477
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-478

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-478`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: fullName 500 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (no max)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-478
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-479

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-479`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: POST với forbidNonWhitelisted field 'evil':'x' → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST {evil:'x',...}

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-479
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-480

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-480`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: phone format không validate (gap)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST phone='ABCDEF'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (no IsPhoneNumber validation)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-480
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên validate format

---

## TC-481

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-481`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter search by fullName

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?search='Trần'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- data có name LIKE 'Trần'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-481
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-482

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-482`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Pagination limit/offset

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=10&offset=20

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-482
severity: Medium
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-483

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Lawyers`
- Yêu cầu: `REQ-CASE-LAWYERS-483`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: limit=200 → ? (max validation cần check)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=200

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Verify DTO @Max

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lawyers`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lawyers`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-483
severity: Low
module: Lawyers
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-492

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-492`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: status='INVALID' → 400 IsEnum

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST status='INVALID'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-492
severity: Medium
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-509

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-509`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content rất dài (10K characters)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content='X'*10000

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (text field)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-509
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-510

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-510`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content có format Markdown

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content với **bold** *italic*

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Detail render markdown nếu support

**API**:
- lưu raw

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-510
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-513

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-513`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Form content textarea có aria-label

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-513
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-514

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-514`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Save conclusion với content 5K chars < 1s

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <1s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-514
severity: Medium
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-515

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-515`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content 1 ký tự → pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content='X'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-515
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-516

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-516`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content 100K ký tự → ? (no max limit)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST content 'X'*100000

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify limit

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-516
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: nên có max

---

## TC-519

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-519`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter status=DRAFT

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?status=DRAFT

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-519
severity: Medium
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-520

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-520`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: notes optional bỏ trống

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không notes

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-520
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-521

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-521`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: type tiếng Việt 'Kết luận đề nghị truy tố'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST type='Kết luận đề nghị truy tố'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- (free string)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-521
severity: Low
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Type field không phải enum — free string

---

## TC-522

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Conclusions`
- Yêu cầu: `REQ-CASE-CONCLUSIONS-522`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Delete Case bị block khi có Conclusion linked

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DELETE Case có Conclusion

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- 'Không thể xóa: vụ án có 1 kết luận điều tra'

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Conclusions`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Conclusions`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-522
severity: High
module: Conclusions
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-539

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-539`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: documentType='INVALID' enum → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST documentType='INVALID'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'Loại tài liệu không hợp lệ'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-539
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-552

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-552`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: title tiếng Việt có dấu

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST title='Hồ sơ vụ án Nguyễn'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-552
severity: Low
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-553

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-553`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: filename Unicode 'tài liệu công văn.pdf'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- DB preserve UTF-8

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-553
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-554

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-554`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: filename emoji '📄report.pdf'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- hoặc normalize

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-554
severity: Low
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-555

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-555`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: File 0 byte (empty) → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file empty

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- 'File rỗng'

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-555
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-556

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-556`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: File 1 byte (smallest non-empty) → 201

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file 1 byte

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-556
severity: Low
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-557

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-557`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: File đúng limit 10MB → 201

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file 10MB

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-557
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-558

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-558`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: File 10MB+1 byte → 413

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST file 10485761 bytes

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-558
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-559

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-559`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Upload 5MB < 5s

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <5s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-559
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-560

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-560`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Download 5MB streaming không OOM

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET download

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Memory peak < 100MB

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-560
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-561

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-561`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Upload bị interrupt giữa chừng → no partial file

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Disconnect during upload

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Multer cleanup partial

**Side effects** (DB, email, log, queue...):
- No orphan file

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-561
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-562

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-562`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Disk full khi upload → 500 cleanup

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mock disk full

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- No orphan + audit fail

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-562
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-567

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-567`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: File input có aria-label

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-567
severity: Low
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-568

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-568`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Upload progress có aria-live='polite' announce

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-568
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-569

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-569`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Drag-drop upload trên Chrome

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-569
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-570

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-570`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: File picker mobile (iOS Safari)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-570
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-572

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-572`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: File trùng tên (cùng originalName) → vẫn lưu (fileName generated unique)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST 2 file cùng tên

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- x201 với fileName khác

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-572
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-574

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-574`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter documentType=COURT_DECISION

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?documentType=COURT_DECISION

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-574
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-575

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-575`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Filter by uploadedById

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?uploadedById=<userId>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-575
severity: Low
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-578

**Meta**:
- Loại: `SECURITY`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-578`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Content-Sniffing: response header X-Content-Type-Options: nosniff

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET download

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Header present

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Input sanitization: check ORM parameterization, escaping
- Authentication/Authorization middleware

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-578
severity: High
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-579

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-579`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: mimeType validate against extension consistency

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST .pdf với mimeType=image/png

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- raw (gap)

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-579
severity: Medium
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-580

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Documents`
- Yêu cầu: `REQ-CASE-DOCUMENTS-580`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Document gắn cả Subject (nếu schema cho phép)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Documents`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Documents`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-580
severity: Low
module: Documents
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Check schema if Document.subjectId exists

---

## TC-585

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-585`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: relatedCaseId optional bỏ trống (general proposal)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST không relatedCaseId

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-585
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-588

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-588`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: sentDate format invalid → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST sentDate='23/05/2026'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify IsDateString

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-588
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: DTO dùng IsString — gap, cần IsDateString

---

## TC-597

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-597`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: status='INVALID' → 400 IsEnum

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-597
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-600

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-600`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content rất dài (50K chars)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-600
severity: Low
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-601

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-601`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content tiếng Việt có dấu

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-601
severity: Low
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-604

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-604`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: proposalNumber 1 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST proposalNumber='X'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-604
severity: Low
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-605

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-605`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: proposalNumber 500 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-605
severity: Low
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-606

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-606`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: List 100 proposals < 500ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <500ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-606
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-607

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-607`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Form input có label

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-607
severity: Low
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-608

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-608`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Chrome mobile OK

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-608
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-609

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-609`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Proposal sent → notification gửi đến receiver

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] PUT status='SENT'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Notification dispatched (nếu có integration)

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-609
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Verify workflow

---

## TC-612

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-612`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Atomic rollback nếu audit fail

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- No partial

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-612
severity: High
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-613

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-613`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter status=PENDING

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?status=PENDING

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-613
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-614

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-614`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Pagination

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?limit=10&offset=20

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-614
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-615

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Proposals`
- Yêu cầu: `REQ-CASE-PROPOSALS-615`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter relatedCaseId

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET ?relatedCaseId=C001

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Proposals`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Proposals`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-615
severity: Medium
module: Proposals
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-635

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-635`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: receivingUnit tiếng Việt 'Phòng Cảnh sát Hà Nội'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-635
severity: Low
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-636

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-636`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content nhiều paragraph

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-636
severity: Low
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-637

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-637`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: delegationDate ISO format

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-637
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-638

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-638`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: delegationDate format invalid → 400 (nếu IsDateString)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-638
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: DTO dùng IsString — verify gap

---

## TC-641

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-641`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: delegationNumber 1 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-641
severity: Low
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-642

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-642`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: content 100K ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-642
severity: Low
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-643

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-643`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: List 50 delegations < 500ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <500ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-643
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-644

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-644`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Form A11Y compliant

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-644
severity: Low
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-645

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-645`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Form mobile responsive

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-645
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-646

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-646`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Delegation cùng caseId hiển thị trong Case detail tab

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/X (include delegations)

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Tab show

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-646
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-649

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-649`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter status=PENDING

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-649
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-650

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-650`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter by receivingUnit

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-650
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-651

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Delegations`
- Yêu cầu: `REQ-CASE-DELEGATIONS-651`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Pagination

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Delegations`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Delegations`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-651
severity: Medium
module: Delegations
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-657

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-657`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: decisionDate sai ISO → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-657
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-658

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-658`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: deadline sai ISO → 400

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-658
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-659

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-659`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: deadline < decisionDate (deadline trước quyết định)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify business

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-659
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap

---

## TC-670

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-670`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: reason tiếng Việt long text

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-670
severity: Low
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-671

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-671`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: decisionNumber format 'XYZ-2026-001'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-671
severity: Low
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-673

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-673`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: decisionNumber 1 ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-673
severity: Low
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-674

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-674`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: reason 50K ký tự

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-674
severity: Low
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-675

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-675`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: List < 500ms

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <500ms

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-675
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-676

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-676`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Form A11Y

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-676
severity: Low
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-677

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-677`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Mobile responsive

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-677
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-680

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-680`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter by caseId

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-680
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-681

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `Supplements`
- Yêu cầu: `REQ-CASE-SUPPLEMENTS-681`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Filter by type

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Supplements`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Supplements`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-681
severity: Medium
module: Supplements
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-700

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-700`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Petition senderName tiếng Việt có dấu

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-700
severity: Low
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-701

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-701`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: stt cuối cùng năm 99999 → tràn?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST khi đã có DT-2026-99999

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- ? — verify overflow handling

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-701
severity: Low
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Gap: padStart 5 — 100000 sẽ thành 100000 (6 chars)

---

## TC-704

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-704`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: convertToCase < 1s

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <1s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-704
severity: Medium
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-707

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-707`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Petition tab trong Case detail show linked petitions[]

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] GET /cases/X

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Tab show petitions

**API**:
- data.petitions=[{stt, petitionType, status, receivedDate, ...}]

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-707
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-708

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-708`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Multiple petitions linked to same Case (1-to-many)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-708
severity: Medium
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Petition.linkedCaseId not unique

---

## TC-711

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-711`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: petitionType enum: TO_GIAC, KHIEU_NAI, TIN_BAO, ...

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-711
severity: Medium
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-712

**Meta**:
- Loại: `RED`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-712`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: petitionType='Vietnamese label' → 400 (v0.37.2.4 BUG)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] POST petitionType='Tố giác'

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- phải dùng enum value

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Error handling: check validation, exception handler

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-712
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: Root cause v0.37.2.4 P0

---

## TC-713

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-713`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: petitionType='TO_GIAC' enum value pass

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-713
severity: High
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-716

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `PetitionFlow`
- Yêu cầu: `REQ-CASE-PETITIONFLOW-716`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Critical` 🚨

**Tiêu đề**: convertToCase fail giữa tx → Petition vẫn nguyên

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Mock fail in tx

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- Petition.status không đổi, linkedCaseId vẫn null

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `PetitionFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `PetitionFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-716
severity: Critical
module: PetitionFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-730

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-730`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Incident có deadlineRuleVersionId tracked

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- THOI_HAN_XAC_MINH rule version snapshot

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-730
severity: Medium
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: DeadlineRules feature

---

## TC-731

**Meta**:
- Loại: `INTEGRATION`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-731`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: E2E: Tạo Incident → assign → convert Case → KPI dashboard update

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-731
severity: Medium
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-732

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-732`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: convertToCase < 1s

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <1s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-732
severity: Medium
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-733

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-733`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: convertToCase fail giữa tx → Incident nguyên

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-733
severity: High
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-737

**Meta**:
- Loại: `GREEN`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-737`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Incident detail show ketQuaXacMinh + ngayPhucHoi

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Happy path flow: check business logic chính

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-737
severity: Medium
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-738

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-738`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Incident kết quả phục hồi enum khác Case

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-738
severity: Medium
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-739

**Meta**:
- Loại: `BOUNDARY`
- Priority: `P1` 🟠
- Module: `IncidentFlow`
- Yêu cầu: `REQ-CASE-INCIDENTFLOW-739`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Incident deadline override Case deadline?

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `IncidentFlow`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `IncidentFlow`
- Validation rules: check min/max constraints trong schema/DTO

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-739
severity: Low
module: IncidentFlow
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-761

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-761`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Cold start API after restart < 5s first request

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- <5s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-761
severity: Low
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-762

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-762`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: DB connection pool exhaustion test

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] concurrent slow query

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Connection pool wait, no timeout

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-762
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-763

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-763`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Memory leak test: 100 sequential create

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Heap not growing

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-763
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-764

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-764`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Audit log không slow main response

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-764
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-765

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-765`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Index hit on overdue+status query

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] EXPLAIN ANALYZE

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Index usage on @@index([deadline, status])

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-765
severity: Low
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-766

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-766`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: CaseListPage table có scope='col' th

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-766
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-767

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-767`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Pagination buttons có aria-label

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-767
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-768

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-768`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Sort indicator aria-sort='ascending|descending'

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-768
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-769

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-769`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Toast notification có role='alert' aria-live

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-769
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-770

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-770`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Skip-to-main-content link

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-770
severity: Low
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-771

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-771`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Form fieldset/legend cho group

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-771
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-772

**Meta**:
- Loại: `A11Y`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-772`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Color contrast cho disabled state ≥ 3:1

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-772
severity: Low
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-773

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-773`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Chrome incognito mode — cookies/localStorage

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-773
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-774

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-774`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Safari iOS pinch-zoom enabled

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-774
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-775

**Meta**:
- Loại: `COMPAT`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-775`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: High DPI display Retina — text crisp

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-775
severity: Low
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-776

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-776`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: UTF-8 BOM handling trong file upload (CSV import)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] -

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-776
severity: Low
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: N/A nếu không support CSV upload

---

## TC-777

**Meta**:
- Loại: `DATA`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-777`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Date timezone conversion cross-timezone view

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] User UTC view case từ +07:00

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- DB UTC stored

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`
- Encoding handling (UTF-8), validation regex

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-777
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-778

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-778`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Page refresh giữa edit Case → unsaved changes warning

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Edit form, F5

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Browser warn 'You have unsaved changes'

**API**:
- -

**Side effects** (DB, email, log, queue...):
- beforeunload listener

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-778
severity: Medium
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-779

**Meta**:
- Loại: `RECOVERY`
- Priority: `P1` 🟠
- Module: `Extra`
- Yêu cầu: `REQ-CASE-EXTRA-779`
- Kỹ thuật: `Use case testing`
- Risk: `TB`
- Severity nếu fail: `High` ⚠️

**Tiêu đề**: Token expired during form edit → re-login flow without losing data

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] JWT exp giữa edit, submit fail 401, re-login

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Form data preserved

**API**:

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Extra`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Extra`

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-779
severity: High
module: Extra
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-252

**Meta**:
- Loại: `A11Y`
- Priority: `P2` 🟡
- Module: `Zoom`
- Yêu cầu: `REQ-CASE-ZOOM-252`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Zoom 200% không vỡ layout CaseListPage

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Chrome zoom 200%

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Table responsive, không overflow

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Zoom`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Zoom`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-252
severity: Medium
module: Zoom
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: WCAG 1.4.4 Resize Text

---

## TC-253

**Meta**:
- Loại: `A11Y`
- Priority: `P2` 🟡
- Module: `Motion`
- Yêu cầu: `REQ-CASE-MOTION-253`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: prefers-reduced-motion respect — animation tắt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] OS setting reduce motion

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast transition off

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Motion`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Motion`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-253
severity: Low
module: Motion
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

**Ghi chú**: WCAG 2.3.3

---

## TC-257

**Meta**:
- Loại: `A11Y`
- Priority: `P2` 🟡
- Module: `Lang`
- Yêu cầu: `REQ-CASE-LANG-257`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: <html lang='vi'> để screen reader phát âm tiếng Việt

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Inspect <html>

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Lang`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Lang`
- Component template/JSX: check semantic HTML, ARIA attrs
- CSS focus styles, tab order

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-257
severity: Low
module: Lang
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-265

**Meta**:
- Loại: `COMPAT`
- Priority: `P2` 🟡
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-265`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Chrome 100 (legacy older) — graceful degradation

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Chrome 100

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-265
severity: Low
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-266

**Meta**:
- Loại: `COMPAT`
- Priority: `P2` 🟡
- Module: `Browser`
- Yêu cầu: `REQ-CASE-BROWSER-266`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: IE11 — KHÔNG support (banner warning)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] IE11

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Banner 'Trình duyệt không hỗ trợ'

**API**:
- -

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Browser`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Browser`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-266
severity: Low
module: Browser
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-274

**Meta**:
- Loại: `COMPAT`
- Priority: `P2` 🟡
- Module: `Network`
- Yêu cầu: `REQ-CASE-NETWORK-274`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: Offline — toast 'Mất kết nối', retry button

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] DevTools offline

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- Toast offline

**API**:
- Request fail with network error

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Network`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Network`
- CSS prefixes, polyfills, browser API checks

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-274
severity: Low
module: Network
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-283

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P2` 🟡
- Module: `Concurrent`
- Yêu cầu: `REQ-CASE-CONCURRENT-283`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: 10 user đồng thời POST /cases — không deadlock

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] parallel POST

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Tất cả 201, không Prisma deadlock

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Concurrent`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Concurrent`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-283
severity: Medium
module: Concurrent
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-284

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P2` 🟡
- Module: `Concurrent`
- Yêu cầu: `REQ-CASE-CONCURRENT-284`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Low` 📌

**Tiêu đề**: 50 user đồng thời GET list — không nghẽn

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] parallel GET

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- Tất cả 200 < 2s

**Side effects** (DB, email, log, queue...):
- -

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Concurrent`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Concurrent`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-284
severity: Low
module: Concurrent
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## TC-286

**Meta**:
- Loại: `PERFORMANCE`
- Priority: `P2` 🟡
- Module: `Memory`
- Yêu cầu: `REQ-CASE-MEMORY-286`
- Kỹ thuật: `Use case testing`
- Risk: `Thấp`
- Severity nếu fail: `Medium` ⚡

**Tiêu đề**: Export 500 row không OOM (workbook stream)

### Điều kiện tiên quyết
- User đã login
- Token JWT hợp lệ

### Các bước kiểm thử
- [ ] Export, monitor heap

### Dữ liệu kiểm thử
```
-
```

### Kết quả mong đợi
**UI**:
- -

**API**:
- -

**Side effects** (DB, email, log, queue...):
- Memory peak < 256MB

### 🔧 Fix Context (cho Claude Code khi TC này fail)

**Khu vực có thể cần kiểm tra:**
- Source code module `Memory`: tìm component/page tương ứng
- API route handler: tìm endpoint cho `Memory`
- DB queries: check N+1, missing indexes
- API caching, payload size

**Bug report template** (Claude Code fill khi TC này fail):
```yaml
bug_id: BUG-XXX
tc_id: TC-286
severity: Medium
module: Memory
reproduce_steps: |  # copy từ Các bước kiểm thử ở trên
actual_behavior: |  # observed sau khi chạy
root_cause: |       # Claude Code phân tích
files_changed:
  - path/to/file.ts
fix_summary: |
verified_by_retest: false  # đặt true sau khi chạy lại pass
```

---

## 🐛 Bug Reports (Claude Code điền khi fix bug)

> Mỗi bug được phát hiện sẽ thêm 1 block ở đây. Reference TC bằng ID.

> Template (copy + fill):


````markdown
### BUG-XXX

- **TC**: TC-XXX
- **Severity**: Critical | High | Medium | Low
- **Status**: Open | Fixing | Fixed | Verified | Closed | Reopened
- **Phát hiện**: DD/MM/YYYY
- **Module**: module name

**Reproduce**:
1. step 1
2. step 2

**Expected**: ...
**Actual**: ...

**Root cause**:
> Phân tích nguyên nhân gốc bởi Claude Code

**Fix**:
- [ ] File: `path/to/file.ts` line X-Y — thay đổi: ...
- [ ] Add test case mới nếu cần
- [ ] Update doc nếu cần

**Verify**: chạy lại TC-XXX → đặt Status = Verified
````

## ✅ Execution Checklist

> Claude Code tick khi hoàn thành từng TC. Mỗi line là 1 trạng thái có thể chuyển đổi.

- [ ] **TC-001** [P0] Tạo Case DIRECT_DISCOVERY thành công với input đầy đủ
- [ ] **TC-002** [P0] Tạo Case FROM_PETITION link Petition đang status TIEP_NHAN + expectedPetitionUpdatedAt khớp
- [ ] **TC-003** [P0] Tạo Case FROM_INCIDENT link Incident chưa khởi tố + expectedIncidentUpdatedAt khớp
- [ ] **TC-004** [P0] Tạo Case TRANSFERRED với sourceDocumentNote
- [ ] **TC-005** [P0] Tạo Case OTHER_LEGAL_SOURCE
- [ ] **TC-006** [P0] Tạo Case với capDoToiPham=DAC_BIET_NGHIEM_TRONG để KPI-4 tính
- [ ] **TC-007** [P0] Auto-deadline tính theo SystemSetting THOI_HAN_GIAI_QUYET
- [ ] **TC-008** [P0] Tạo Case manual deadline override auto-deadline
- [ ] **TC-009** [P0] Tạo Case với metadata custom (người báo tin)
- [ ] **TC-010** [P0] Ward officer auto-set assignedTeamId theo wardTeamId (silent override)
- [ ] **TC-017** [P0] Thiếu trường name (required) trả 400
- [ ] **TC-018** [P0] name là chuỗi rỗng '' trả 400 'Tên vụ án bắt buộc'
- [ ] **TC-019** [P0] name chỉ whitespace '   ' bị trim thành '' → 400
- [ ] **TC-020** [P0] name là null → 400
- [ ] **TC-021** [P0] name là số 12345 không phải string → 400 IsString
- [ ] **TC-022** [P0] name vượt 500 ký tự (501 chars) → 400 MaxLength
- [ ] **TC-025** [P0] Thiếu caseProvenance → 400
- [ ] **TC-026** [P0] caseProvenance không phải enum value → 400
- [ ] **TC-027** [P0] caseProvenance là tiếng Việt (legacy payload) → 400
- [ ] **TC-028** [P0] FROM_PETITION nhưng thiếu linkedPetitionId → 400
- [ ] **TC-029** [P0] FROM_INCIDENT thiếu linkedIncidentId → 400
- [ ] **TC-030** [P0] FROM_PETITION thiếu expectedPetitionUpdatedAt → 400 IsISO8601
- [ ] **TC-031** [P0] FROM_PETITION với expectedPetitionUpdatedAt cũ hơn current → 409 Conflict
- [ ] **TC-032** [P0] FROM_PETITION với linkedPetitionId KHÔNG tồn tại → 404
- [ ] **TC-033** [P0] FROM_PETITION với Petition của user KHÁC scope → 404 (không lộ tồn tại)
- [ ] **TC-034** [P0] FROM_PETITION với Petition đã có linkedCaseId (đã khởi tố trước) → 404
- [ ] **TC-035** [P0] FROM_PETITION với deletedAt != null → 404
- [ ] **TC-037** [P0] investigatorId không tồn tại trong users table → 400
- [ ] **TC-044** [P0] deadline sai format 'không-phải-ngày' → 400 IsDateString
- [ ] **TC-048** [P0] caseProvenance=DIRECT_DISCOVERY nhưng vẫn truyền linkedPetitionId → field bị ignore
- [ ] **TC-053** [P0] User KHÔNG có permission write/Case (role VIEWER) → 403
- [ ] **TC-054** [P0] POST không có Authorization header → 401
- [ ] **TC-055** [P0] POST JWT đã hết hạn → 401
- [ ] **TC-056** [P0] POST JWT bị tamper signature → 401
- [ ] **TC-061** [P0] name='\t\t\t' (3 tab) bị trim thành '' → 400
- [ ] **TC-062** [P0] name='\n\n\n' (3 newline) bị trim → 400
- [ ] **TC-064** [P0] Race condition: 2 user đồng thời FROM_PETITION cùng PET-001 → 1 ConflictException
- [ ] **TC-067** [P0] GET /cases trả paginated 20 items default sorted createdAt desc
- [ ] **TC-068** [P0] GET /cases?limit=50 trả tối đa 50
- [ ] **TC-069** [P0] GET /cases?limit=101 → 400 @Max(100)
- [ ] **TC-073** [P0] Filter status=DANG_DIEU_TRA chỉ trả Case status đó
- [ ] **TC-075** [P0] Filter overdue=true trả case quá hạn
- [ ] **TC-076** [P0] Filter overdue=true KHÔNG include Case DA_KET_LUAN dù deadline đã qua
- [ ] **TC-077** [P0] Search 'Trộm cắp' tìm case có name/crime/unit chứa từ này (case-insensitive)
- [ ] **TC-078** [P0] Search với chữ có dấu vs không dấu (Unicode Vietnamese)
- [ ] **TC-086** [P0] GET /cases/:id trả full detail có investigator + petitions include
- [ ] **TC-087** [P0] GET /cases/NOT_EXIST → 404
- [ ] **TC-088** [P0] GET /cases/:id của Case đã soft-deleted → 404 (deletedAt IS NOT NULL)
- [ ] **TC-089** [P0] GET /cases/:id của Case ngoài scope → 403
- [ ] **TC-090** [P0] Dispatcher xem mọi Case không bị scope chặn
- [ ] **TC-091** [P0] ADMIN xem mọi Case (no scope)
- [ ] **TC-092** [P0] Owner (investigator) xem được Case mình phụ trách
- [ ] **TC-093** [P0] Cùng team xem được Case của đồng nghiệp
- [ ] **TC-097** [P0] Update name + crime case mình owner
- [ ] **TC-098** [P0] Update với expectedUpdatedAt khớp current → success
- [ ] **TC-099** [P0] Update với expectedUpdatedAt cũ → 409 Conflict
- [ ] **TC-100** [P0] Update Case ngoài write-scope → 403
- [ ] **TC-101** [P0] Update Case không tồn tại → 404
- [ ] **TC-102** [P0] Update Case đã soft-deleted → 404
- [ ] **TC-103** [P0] Update status TIEP_NHAN → DANG_XAC_MINH tạo CaseStatusHistory + audit
- [ ] **TC-104** [P0] Update status DANG_DIEU_TRA → TAM_DINH_CHI với lyDoTamDinhChiVuAn auto-set ngày + count
- [ ] **TC-105** [P0] Update → TAM_DINH_CHI thiếu lyDoTamDinhChiVuAn (Case post-migration) → 400
- [ ] **TC-107** [P0] lyDoTamDinhChiVuAn = invalid enum 'INVALID_REASON' → 400
- [ ] **TC-108** [P0] Update status TAM_DINH_CHI → DANG_DIEU_TRA (phục hồi) với daRaSoat + ngayRaSoat
- [ ] **TC-118** [P0] Audit CASE_UPDATED capture before/after diff đầy đủ
- [ ] **TC-124** [P0] DTO whitelist reject field lạ trong UpdateCaseDto (forbidNonWhitelisted)
- [ ] **TC-125** [P0] Transition TIEP_NHAN → DANG_XAC_MINH (valid)
- [ ] **TC-126** [P0] Transition DANG_XAC_MINH → DA_XAC_MINH (valid)
- [ ] **TC-127** [P0] Transition DA_XAC_MINH → DANG_DIEU_TRA (valid)
- [ ] **TC-128** [P0] Transition DANG_DIEU_TRA → DA_KET_LUAN (valid)
- [ ] **TC-129** [P0] Transition DA_KET_LUAN → DANG_TRUY_TO (valid)
- [ ] **TC-130** [P0] Transition DANG_TRUY_TO → DANG_XET_XU (valid)
- [ ] **TC-131** [P0] Transition DANG_XET_XU → DA_LUU_TRU (final state)
- [ ] **TC-132** [P0] Transition any → DINH_CHI (đình chỉ vĩnh viễn)
- [ ] **TC-133** [P0] Transition TAM_DINH_CHI → DA_KET_LUAN (phục hồi và kết luận luôn)
- [ ] **TC-136** [P0] DINH_CHI → DANG_DIEU_TRA (sai logic) — cần block ở backend
- [ ] **TC-137** [P0] DANG_XET_XU → TIEP_NHAN (rollback xa) — invalid logic
- [ ] **TC-138** [P0] TAM_DINH_CHI lần thứ 2 (recurrent suspend) — soLanTamDinhChi=2
- [ ] **TC-139** [P0] Soft delete Case TIEP_NHAN, reason 50 chars, creator → success
- [ ] **TC-140** [P0] Reason < 10 ký tự → 400
- [ ] **TC-141** [P0] Reason > 500 ký tự → 400
- [ ] **TC-142** [P0] Thiếu reason → 400
- [ ] **TC-143** [P0] Reason rỗng '' → 400
- [ ] **TC-144** [P0] Reason chỉ whitespace '          ' → 400 (Transform trim)
- [ ] **TC-145** [P0] Status ≠ TIEP_NHAN (DANG_DIEU_TRA) → 400 'Chỉ xóa được vụ án ở trạng thái Tiếp nhận'
- [ ] **TC-146** [P0] Có ≥1 subject linked → 400 'có N đối tượng. Xóa các đối tượng trước'
- [ ] **TC-147** [P0] Có ≥1 lawyer linked → 400 'có N luật sư'
- [ ] **TC-148** [P0] Có ≥1 conclusion → 400
- [ ] **TC-149** [P0] Có ≥1 document → 400
- [ ] **TC-150** [P0] Có ≥1 linkedIncident → 400
- [ ] **TC-151** [P0] User không phải creator + không phải ADMIN → 403
- [ ] **TC-152** [P0] createdById=NULL (legacy data) + user không ADMIN → 403 message riêng
- [ ] **TC-153** [P0] ADMIN xóa được Case của bất kỳ ai trong window
- [ ] **TC-154** [P0] Quá window (THOI_HAN_XOA_VU_AN=72h default) + không ADMIN → 400
- [ ] **TC-155** [P0] ADMIN có thể xóa Case quá 72h window
- [ ] **TC-156** [P0] Race condition: status đổi trong lúc DELETE → P2025 catch → 400 friendly
- [ ] **TC-157** [P0] TOCTOU: subject vừa được insert giữa initial check và transaction → 400 trong tx
- [ ] **TC-158** [P0] GET /cases/:id/delete-preflight trả canDelete=true cho TIEP_NHAN + không blocker
- [ ] **TC-159** [P0] Preflight Case status=DA_KET_LUAN trả canDelete=false + reason
- [ ] **TC-160** [P0] Preflight Case có 2 subjects + 1 document → reasons detailed
- [ ] **TC-161** [P0] Preflight Case ngoài scope → 403 (checkRecordInScope)
- [ ] **TC-162** [P0] ADMIN khôi phục Case đã soft-deleted
- [ ] **TC-163** [P0] Non-ADMIN gọi restore → 403 PermissionsGuard (action=restore)
- [ ] **TC-164** [P0] Restore Case chưa từng bị xóa (deletedAt=null) → 404
- [ ] **TC-165** [P0] Race: 2 ADMIN restore cùng case → 1 ConflictException friendly
- [ ] **TC-167** [P0] ADMIN GET /cases/admin/deleted trả paginated deleted cases với deleteAudit
- [ ] **TC-168** [P0] Non-ADMIN gọi /cases/admin/deleted → 403
- [ ] **TC-169** [P0] Dispatcher phân công Case sang Team-Q2 + investigator của team
- [ ] **TC-170** [P0] Non-dispatcher gọi assign → 403 DispatchGuard
- [ ] **TC-171** [P0] Case ngoài scope của dispatcher (nếu dispatcher có scope giới hạn) → ?
- [ ] **TC-172** [P0] assignedTeamId team đã isActive=false → 400 'Tổ điều tra không tồn tại hoặc đã ngừng hoạt động'
- [ ] **TC-173** [P0] investigatorId không thuộc team được chỉ định → 400 'Điều tra viên không thuộc tổ'
- [ ] **TC-175** [P0] Case không tồn tại → 404
- [ ] **TC-176** [P0] Case đã soft-deleted → 404
- [ ] **TC-177** [P0] expectedUpdatedAt mismatch → 409
- [ ] **TC-178** [P0] Escalation: ward team → non-ward team → emit CASE_ESCALATED_FROM_WARD audit
- [ ] **TC-184** [P0] Non-dispatcher với JWT của dispatcher (token theft) → permission guard pass? — JWT check role thật
- [ ] **TC-185** [P0] GET /cases/export/ward trả file Excel với header BCA + 8 cột
- [ ] **TC-186** [P0] Export ward với fromDate + toDate trong period
- [ ] **TC-187** [P0] Export 'phân loại khác' với category filter
- [ ] **TC-188** [P0] Rate limit: gọi /export/ward 6 lần trong 60s → 429
- [ ] **TC-189** [P0] Export với scope filter — ward officer chỉ thấy case của ward mình
- [ ] **TC-190** [P0] Dispatcher xem tất cả case khi export
- [ ] **TC-192** [P0] Permission: user role VIEWER không có 'read Case' → 403
- [ ] **TC-193** [P0] JWT thiếu → 401
- [ ] **TC-197** [P0] GET /cases/:id/status-history trả list sorted asc changedAt
- [ ] **TC-199** [P0] GET history của Case không tồn tại → 200 với data=[] (no 404 hiện tại)
- [ ] **TC-201** [P0] User ngoài scope GET history → ?
- [ ] **TC-203** [P0] PATCH /cases/:id/tdc-backfill update lyDoTamDinhChiVuAn
- [ ] **TC-204** [P0] PATCH với Case không tồn tại → 404 'Case not found'
- [ ] **TC-206** [P0] User không có write permission → 403
- [ ] **TC-208** [P0] IDOR: User A trực tiếp GET /cases/<id-của-B> (URL guessing) → 403
- [ ] **TC-209** [P0] Horizontal privilege escalation: dieuTra-Q1 sửa Case-Q3 → 403
- [ ] **TC-210** [P0] Vertical: VIEWER role thử POST /cases → 403
- [ ] **TC-211** [P0] DELETE Case của user khác (creator check) → 403
- [ ] **TC-212** [P0] Non-ADMIN restore → 403
- [ ] **TC-213** [P0] GET /cases/admin/deleted với non-ADMIN → 403
- [ ] **TC-214** [P0] Non-dispatcher PATCH /assign → 403 DispatchGuard
- [ ] **TC-215** [P0] JWT là HS256 với secret strong (≥32 chars) — không yếu
- [ ] **TC-216** [P0] API chạy trên HTTPS (production) — HTTP redirect 301
- [ ] **TC-217** [P0] SQLi qua search param: ?search=' OR 1=1-- → vẫn an toàn
- [ ] **TC-218** [P0] SQLi qua sortBy: ?sortBy=name; DROP TABLE cases-- → whitelist filter
- [ ] **TC-219** [P0] XSS reflected: name=<script>alert(1)</script> → React escape khi render
- [ ] **TC-220** [P0] XSS stored: crime=<img src=x onerror=alert(1)> render detail → escape
- [ ] **TC-221** [P0] XSS DOM: search query có script trong URL → no eval
- [ ] **TC-222** [P0] JSON injection vào metadata: metadata={__proto__:{isAdmin:true}}
- [ ] **TC-223** [P0] Export rate limit ngăn data exfiltration
- [ ] **TC-224** [P0] Soft delete với reason mandatory ngăn data lost without trace
- [ ] **TC-225** [P0] Restore window check (legacy NULL createdById → ADMIN-only)
- [ ] **TC-226** [P0] CORS header restrict origin (không *)
- [ ] **TC-227** [P0] Response không có sensitive header X-Powered-By, Server version
- [ ] **TC-228** [P0] Security headers: HSTS, X-Content-Type-Options, X-Frame-Options
- [ ] **TC-229** [P0] JWT hết hạn → 401, không refresh tự động infinite
- [ ] **TC-230** [P0] JWT signature tamper → 401
- [ ] **TC-231** [P0] JWT của user đã deactivated/locked → 401 hoặc 403
- [ ] **TC-232** [P0] Optimistic lock prevent lost update qua expectedUpdatedAt
- [ ] **TC-233** [P0] FROM_PETITION race condition không double-link Petition
- [ ] **TC-234** [P0] Audit log CASE_DELETED ghi đầy đủ user, IP, UA, reason
- [ ] **TC-235** [P0] Audit không lưu sensitive data (password, JWT) trong metadata
- [ ] **TC-236** [P0] Audit retention ≥ 90 ngày (BLTTHS compliance)
- [ ] **TC-238** [P0] POST /cases brute force 100 req/s → throttle global
- [ ] **TC-239** [P0] Mutation POST/PUT/DELETE từ origin lạ không có cookie + custom header → reject CORS
- [ ] **TC-240** [P0] POST kèm field 'createdById':'other-user' bị whitelist reject
- [ ] **TC-241** [P0] POST kèm 'id':'fake-id' không cho phép override
- [ ] **TC-242** [P0] POST kèm 'deletedAt':null override không cho phép
- [ ] **TC-301** [P0] name 1 ký tự 'A' (min) → pass
- [ ] **TC-302** [P0] name 500 ký tự (max) → pass
- [ ] **TC-303** [P0] name 501 ký tự → 400
- [ ] **TC-304** [P0] name 499 ký tự → pass
- [ ] **TC-333** [P0] ADMIN (dataScope=null) xem mọi Case
- [ ] **TC-334** [P0] Dispatcher (canDispatch=true) xem mọi Case
- [ ] **TC-335** [P0] Owner (investigatorId match userIds) xem Case mình
- [ ] **TC-336** [P0] Same team (assignedTeamId in teamIds) xem Case đồng nghiệp
- [ ] **TC-337** [P0] Unassigned Case + user có team (teamIds.length>0)
- [ ] **TC-338** [P0] User khác team + không owner → 403
- [ ] **TC-339** [P0] Owner write Case mình → success
- [ ] **TC-340** [P0] Same writableTeam write → success
- [ ] **TC-341** [P0] Different team write → 403
- [ ] **TC-342** [P0] Decision matrix delete: TIEP_NHAN + creator + within 72h → success
- [ ] **TC-343** [P0] Decision: TIEP_NHAN + creator + >72h + non-ADMIN → 400
- [ ] **TC-344** [P0] Decision: TIEP_NHAN + non-creator + non-ADMIN → 403
- [ ] **TC-345** [P0] Decision: ≠TIEP_NHAN + ADMIN → 400 (status check first)
- [ ] **TC-346** [P0] Decision: TIEP_NHAN + ADMIN + creator NULL + within window → success
- [ ] **TC-347** [P0] Flow: Petition → Khởi tố vụ án → Thêm subject → Update status → Kết luận
- [ ] **TC-348** [P0] Flow: Incident → khởi tố Case → Assign → Delete fail → Restore
- [ ] **TC-349** [P0] Bulk create Case + sequential STT trên Petition (DT-2026-NNNNN)
- [ ] **TC-361** [P0] CASE_CREATED log có userId, action, subject='Case', subjectId, metadata, ipAddress, userAgent, createdAt
- [ ] **TC-362** [P0] CASE_UPDATED audit có diff before/after (wrapUpdate)
- [ ] **TC-363** [P0] CASE_STATUS_CHANGED audit riêng entry với fromStatus/toStatus/changedAt
- [ ] **TC-364** [P0] CASE_DELETED có reason + hoursAfterCreation
- [ ] **TC-365** [P0] CASE_RESTORED có hoursAfterDeletion
- [ ] **TC-366** [P0] CASE_ASSIGNED có fromTeamId/toTeamId/fromInvestigatorId/toInvestigatorId/dispatchedBy
- [ ] **TC-367** [P0] CASE_EXPORTED có filters đầy đủ trong metadata
- [ ] **TC-368** [P0] Tạo Subject với CCCD 12 chữ số hợp lệ
- [ ] **TC-369** [P0] Tạo Subject với CCCD 9 chữ số (CMND cũ)
- [ ] **TC-370** [P0] CCCD 8 chữ số → 400 regex fail
- [ ] **TC-371** [P0] CCCD 13 chữ số → 400
- [ ] **TC-372** [P0] CCCD có chữ cái 'ABC079090123' → 400 regex
- [ ] **TC-373** [P0] CCCD rỗng → 400 IsNotEmpty
- [ ] **TC-374** [P0] fullName rỗng → 400
- [ ] **TC-375** [P0] dateOfBirth rỗng → 400
- [ ] **TC-376** [P0] dateOfBirth='15/01/1990' không ISO → 400
- [ ] **TC-377** [P0] dateOfBirth='2030-01-01' tương lai → ? (gap)
- [ ] **TC-378** [P0] address rỗng → 400
- [ ] **TC-379** [P0] caseId rỗng → 400
- [ ] **TC-380** [P0] caseId không tồn tại → 400 FK invalid
- [ ] **TC-381** [P0] caseId của Case soft-deleted → 400
- [ ] **TC-382** [P0] crimeId rỗng → 400
- [ ] **TC-383** [P0] crimeId không tồn tại trong directory → 400
- [ ] **TC-392** [P0] GET /subjects?caseId=C001 trả subjects của Case
- [ ] **TC-393** [P0] GET /subjects/:id detail include lawyers liên kết
- [ ] **TC-394** [P0] GET /subjects/:id Subject ngoài scope → 403
- [ ] **TC-395** [P0] GET /subjects/NOT_EXIST → 404
- [ ] **TC-396** [P0] PUT update fullName của Subject
- [ ] **TC-397** [P0] PUT Subject ngoài write scope → 403
- [ ] **TC-398** [P0] PUT Subject soft-deleted → 404
- [ ] **TC-399** [P0] DELETE Subject soft delete + Case.subjectsCount giảm
- [ ] **TC-400** [P0] DELETE Subject ngoài scope → 403
- [ ] **TC-401** [P0] DELETE Subject không tồn tại → 404
- [ ] **TC-402** [P0] DELETE Subject đã soft-deleted (idempotent) → 404 hoặc 200
- [ ] **TC-403** [P0] IDOR: POST subject với caseId của Case khác user → 403/400
- [ ] **TC-404** [P0] XSS qua fullName='<script>alert(1)</script>' → React escape
- [ ] **TC-405** [P0] SQL injection qua idNumber bypass regex (impossible vì regex strict)
- [ ] **TC-413** [P0] Transition INVESTIGATING → DETAINED (bắt tạm giam)
- [ ] **TC-414** [P0] Transition DETAINED → RELEASED (thả)
- [ ] **TC-415** [P0] Transition INVESTIGATING → WANTED (chuyển truy nã)
- [ ] **TC-420** [P0] Duplicate idNumber + caseId + type (constraint unique per type per case)
- [ ] **TC-422** [P0] DELETE Subject ghi audit với name + reason
- [ ] **TC-430** [P0] User VIEWER không có write/Subject → 403
- [ ] **TC-431** [P0] JWT thiếu → 401
- [ ] **TC-439** [P0] SUBJECT_CREATED log có caseId trong metadata
- [ ] **TC-440** [P0] Tạo Lawyer với barNumber unique
- [ ] **TC-441** [P0] Tạo Lawyer gắn subjectId (bào chữa cho subject cụ thể)
- [ ] **TC-443** [P0] fullName rỗng → 400
- [ ] **TC-444** [P0] barNumber rỗng → 400
- [ ] **TC-445** [P0] barNumber duplicate (đã tồn tại) → 409 unique constraint
- [ ] **TC-446** [P0] caseId rỗng → 400
- [ ] **TC-447** [P0] caseId không tồn tại → 400 FK
- [ ] **TC-448** [P0] subjectId không tồn tại trong DB → 400 FK
- [ ] **TC-450** [P0] GET /lawyers?caseId=C001 trả lawyers của Case
- [ ] **TC-451** [P0] GET /lawyers?subjectId=S001 trả lawyers của subject
- [ ] **TC-452** [P0] GET /lawyers/:id detail include subject info
- [ ] **TC-453** [P0] GET /lawyers/NOT_EXIST → 404
- [ ] **TC-454** [P0] GET Lawyer ngoài scope (Case không thuộc team mình) → 403
- [ ] **TC-455** [P0] PUT update lawFirm + phone
- [ ] **TC-456** [P0] PUT barNumber duplicate → 409
- [ ] **TC-457** [P0] PUT Lawyer ngoài write scope → 403
- [ ] **TC-458** [P0] DELETE Lawyer soft delete
- [ ] **TC-459** [P0] DELETE Lawyer không tồn tại → 404
- [ ] **TC-460** [P0] DELETE Lawyer ngoài scope → 403
- [ ] **TC-461** [P0] IDOR: POST Lawyer với caseId ngoài team → 403
- [ ] **TC-462** [P0] XSS qua fullName='<svg onload=alert(1)>'
- [ ] **TC-463** [P0] SQL injection qua barNumber
- [ ] **TC-473** [P0] User VIEWER POST → 403
- [ ] **TC-474** [P0] JWT thiếu → 401
- [ ] **TC-475** [P0] LAWYER_CREATED log có barNumber + caseId
- [ ] **TC-484** [P0] Tạo Conclusion với type+content+caseId
- [ ] **TC-485** [P0] caseId thiếu → 400 IsString
- [ ] **TC-486** [P0] caseId không tồn tại → 400 FK
- [ ] **TC-487** [P0] type rỗng → 400
- [ ] **TC-488** [P0] content rỗng → 400
- [ ] **TC-489** [P0] Tạo Conclusion với approvedById
- [ ] **TC-490** [P0] approvedById không tồn tại → 400 FK
- [ ] **TC-491** [P0] Default status khi POST không kèm
- [ ] **TC-493** [P0] GET /conclusions?caseId=C001 list
- [ ] **TC-494** [P0] GET /conclusions/:id detail
- [ ] **TC-495** [P0] GET conclusion ngoài scope (Case khác team) → 403
- [ ] **TC-496** [P0] GET /conclusions/NOT_EXIST → 404
- [ ] **TC-497** [P0] PUT update content
- [ ] **TC-498** [P0] PUT conclusion đã APPROVED không cho sửa
- [ ] **TC-499** [P0] DELETE conclusion soft delete
- [ ] **TC-500** [P0] DELETE conclusion đã APPROVED → 400
- [ ] **TC-501** [P0] Transition DRAFT → SUBMITTED (gửi đi phê duyệt)
- [ ] **TC-502** [P0] Transition SUBMITTED → APPROVED (truong don vi duyet)
- [ ] **TC-503** [P0] Transition SUBMITTED → REJECTED
- [ ] **TC-504** [P0] Transition REJECTED → DRAFT (chỉnh sửa lại)
- [ ] **TC-505** [P0] APPROVED → DRAFT (downgrade) → block
- [ ] **TC-506** [P0] User thường không có quyền APPROVE (không phải TRUONG_DON_VI) → 403
- [ ] **TC-507** [P0] IDOR: POST conclusion với caseId của team khác
- [ ] **TC-508** [P0] XSS qua content='<script>'
- [ ] **TC-511** [P0] Conclusion APPROVED → Case status auto-update DA_KET_LUAN
- [ ] **TC-512** [P0] CONCLUSION_APPROVED log có approvedById + timestamp
- [ ] **TC-517** [P0] User VIEWER POST → 403
- [ ] **TC-518** [P0] JWT thiếu → 401
- [ ] **TC-523** [P0] Upload PDF 5MB qua POST /documents
- [ ] **TC-524** [P0] Upload Word .docx
- [ ] **TC-525** [P0] Upload image JPG
- [ ] **TC-526** [P0] Upload PNG
- [ ] **TC-527** [P0] Upload file > 10MB (mặc định limit) → 413
- [ ] **TC-528** [P0] Upload .exe → block by mimetype
- [ ] **TC-529** [P0] Upload .sh script → block
- [ ] **TC-530** [P0] Upload .js → block
- [ ] **TC-531** [P0] Upload PDF nhưng đổi extension thành .pdf (magic byte check)
- [ ] **TC-532** [P0] Path traversal qua originalName '../../../etc/passwd'
- [ ] **TC-533** [P0] Filename có null byte 'evil\0.jpg'
- [ ] **TC-534** [P0] ZIP slip nếu support upload .zip
- [ ] **TC-535** [P0] title rỗng → 400
- [ ] **TC-536** [P0] caseId optional — không kèm cũng OK (general document)
- [ ] **TC-537** [P0] incidentId thay vì caseId (gắn với Incident)
- [ ] **TC-538** [P0] Cả caseId VÀ incidentId → 400 'Chỉ chọn 1'
- [ ] **TC-540** [P0] GET /documents?caseId=C001 list documents của Case
- [ ] **TC-541** [P0] GET /documents/:id metadata only (chưa download file)
- [ ] **TC-542** [P0] GET /documents/:id/download trả file binary
- [ ] **TC-543** [P0] Download document ngoài scope → 403
- [ ] **TC-544** [P0] Download document đã soft-deleted → 404
- [ ] **TC-545** [P0] Download bằng GET /uploads/file.pdf trực tiếp → 401/403
- [ ] **TC-546** [P0] DELETE document soft delete
- [ ] **TC-547** [P0] DELETE document ngoài scope → 403
- [ ] **TC-548** [P0] PUT update title + description
- [ ] **TC-549** [P0] PUT thay đổi fileName/filePath bị forbidNonWhitelisted reject
- [ ] **TC-550** [P0] XSS qua originalName '<script>alert</script>.pdf'
- [ ] **TC-551** [P0] CSV injection qua title '=cmd|calc'
- [ ] **TC-563** [P0] Quota: user upload nhiều file → check storage limit
- [ ] **TC-564** [P0] DOCUMENT_CREATED có fileName + size + mimeType
- [ ] **TC-565** [P0] DOCUMENT_DOWNLOADED audit mỗi lần download
- [ ] **TC-566** [P0] DOCUMENT_DELETED có original fileName
- [ ] **TC-571** [P0] Multi-file upload (nếu hỗ trợ) — concurrent
- [ ] **TC-573** [P0] Delete Case bị block khi có Document linked
- [ ] **TC-576** [P0] VIEWER không có write/Document → 403 POST
- [ ] **TC-577** [P0] JWT thiếu POST → 401
- [ ] **TC-581** [P0] Tạo Proposal với proposalNumber + content
- [ ] **TC-582** [P0] proposalNumber rỗng → 400
- [ ] **TC-583** [P0] content rỗng → 400
- [ ] **TC-584** [P0] proposalNumber duplicate → 409 unique
- [ ] **TC-586** [P0] relatedCaseId không tồn tại → 400 FK
- [ ] **TC-587** [P0] Tạo Proposal với sentDate
- [ ] **TC-589** [P0] GET /proposals list paginated
- [ ] **TC-590** [P0] GET /proposals/:id detail
- [ ] **TC-591** [P0] GET proposal ngoài scope → 403
- [ ] **TC-592** [P0] GET /proposals/NOT_EXIST → 404
- [ ] **TC-593** [P0] PUT update response + responseDate
- [ ] **TC-594** [P0] DELETE soft delete
- [ ] **TC-595** [P0] Transition PENDING → SENT → RESPONDED
- [ ] **TC-596** [P0] Transition SENT → CANCELLED
- [ ] **TC-598** [P0] IDOR: POST relatedCaseId của team khác → 403
- [ ] **TC-599** [P0] XSS qua content
- [ ] **TC-602** [P0] PROPOSAL_CREATED audit log
- [ ] **TC-603** [P0] PROPOSAL_RESPONDED log với response timestamp
- [ ] **TC-610** [P0] VIEWER POST → 403
- [ ] **TC-611** [P0] JWT thiếu → 401
- [ ] **TC-616** [P0] Tạo Delegation với delegationNumber + receivingUnit
- [ ] **TC-617** [P0] delegationNumber rỗng → 400
- [ ] **TC-618** [P0] receivingUnit rỗng → 400
- [ ] **TC-619** [P0] content rỗng → 400
- [ ] **TC-620** [P0] delegationNumber duplicate → 409
- [ ] **TC-621** [P0] relatedCaseId optional
- [ ] **TC-622** [P0] relatedCaseId không tồn tại → 400
- [ ] **TC-623** [P0] GET list paginated
- [ ] **TC-624** [P0] GET detail
- [ ] **TC-625** [P0] GET ngoài scope → 403
- [ ] **TC-626** [P0] GET NOT_EXIST → 404
- [ ] **TC-627** [P0] PUT update content + completedDate
- [ ] **TC-628** [P0] DELETE soft delete
- [ ] **TC-629** [P0] Transition PENDING → IN_PROGRESS
- [ ] **TC-630** [P0] Transition IN_PROGRESS → COMPLETED
- [ ] **TC-631** [P0] Transition any → CANCELLED
- [ ] **TC-632** [P0] COMPLETED → PENDING (downgrade) → block
- [ ] **TC-633** [P0] IDOR relatedCaseId team khác
- [ ] **TC-634** [P0] XSS content
- [ ] **TC-639** [P0] DELEGATION_CREATED log
- [ ] **TC-640** [P0] DELEGATION_COMPLETED log với completedDate
- [ ] **TC-647** [P0] VIEWER POST → 403
- [ ] **TC-648** [P0] JWT thiếu → 401
- [ ] **TC-652** [P0] Tạo Supplement caseId + decisionNumber + reason + deadline
- [ ] **TC-653** [P0] caseId rỗng → 400
- [ ] **TC-654** [P0] type rỗng → 400
- [ ] **TC-655** [P0] decisionNumber rỗng → 400
- [ ] **TC-656** [P0] reason rỗng → 400
- [ ] **TC-660** [P0] GET list paginated
- [ ] **TC-661** [P0] GET detail
- [ ] **TC-662** [P0] GET ngoài scope → 403
- [ ] **TC-663** [P0] GET NOT_EXIST → 404
- [ ] **TC-664** [P0] PUT update reason
- [ ] **TC-665** [P0] DELETE soft delete
- [ ] **TC-666** [P0] Tạo supplement → Case.soLanGiaHan auto-increment
- [ ] **TC-667** [P0] Tạo supplement với deadline mới → Case.deadline update
- [ ] **TC-668** [P0] IDOR caseId team khác → 403
- [ ] **TC-669** [P0] XSS reason
- [ ] **TC-672** [P0] SUPPLEMENT_CREATED log
- [ ] **TC-678** [P0] VIEWER POST → 403
- [ ] **TC-679** [P0] JWT thiếu → 401
- [ ] **TC-682** [P0] Petition convertToCase (POST /petitions/:id/convert)
- [ ] **TC-683** [P0] convertToCase Petition đã linked → 400 'Đã khởi tố rồi'
- [ ] **TC-684** [P0] convertToCase ngoài scope → 403
- [ ] **TC-685** [P0] convertToCase Petition soft-deleted → 404
- [ ] **TC-686** [P0] Petition.status transition MOI_TIEP_NHAN → DA_CHUYEN_VU_AN (convert)
- [ ] **TC-687** [P0] Petition.status MOI_TIEP_NHAN → DANG_XU_LY (manual)
- [ ] **TC-688** [P0] Petition.status DANG_XU_LY → CHO_PHE_DUYET
- [ ] **TC-689** [P0] Petition.status CHO_PHE_DUYET → DA_GIAI_QUYET
- [ ] **TC-690** [P0] Petition.status any → DA_LUU_DON (lưu đơn)
- [ ] **TC-691** [P0] Petition.status → DA_CHUYEN_VU_VIEC (chuyển vụ việc)
- [ ] **TC-692** [P0] Petition DA_CHUYEN_VU_AN không sửa được
- [ ] **TC-693** [P0] Cascade: Update Case.metadata.petitionType → sync Petition.petitionType
- [ ] **TC-694** [P0] Update Case không có Petition link KHÔNG tạo phantom Petition
- [ ] **TC-695** [P0] STT auto-generate format DT-YYYY-NNNNN
- [ ] **TC-696** [P0] STT race condition: 2 concurrent create không tạo duplicate STT
- [ ] **TC-697** [P0] convertToCase Petition status không phải MOI_TIEP_NHAN/DANG_XU_LY → 400
- [ ] **TC-698** [P0] PETITION_CONVERTED log với linkedCaseId
- [ ] **TC-699** [P0] convertToCase atomic — race 2 user → 1 conflict
- [ ] **TC-702** [P0] Delete Petition KHÔNG delete linked Case (Restrict)
- [ ] **TC-703** [P0] Delete Case → Petition.linkedCaseId vẫn giữ (Restrict)
- [ ] **TC-705** [P0] convertToCase VIEWER → 403
- [ ] **TC-706** [P0] convertToCase JWT thiếu → 401
- [ ] **TC-709** [P0] CaseProvenance=FROM_PETITION → Case có linkedPetitionId set
- [ ] **TC-710** [P0] CaseProvenance ≠ FROM_PETITION → linkedPetitionId=null
- [ ] **TC-714** [P0] Petition convertToCase preserve provenance traceability
- [ ] **TC-715** [P0] E2E: Tạo Petition → review → convert Case → assign team
- [ ] **TC-717** [P0] Incident convertToCase
- [ ] **TC-718** [P0] Incident đã linked → 400
- [ ] **TC-719** [P0] Incident ngoài scope → 403
- [ ] **TC-720** [P0] Incident.status MOI_TIEP_NHAN → DANG_XAC_MINH
- [ ] **TC-721** [P0] Incident → TAM_DINH_CHI
- [ ] **TC-722** [P0] Incident → PHUC_HOI_NGUON_TIN
- [ ] **TC-723** [P0] Incident TAM_DINH_CHI → DANG_XAC_MINH (phục hồi)
- [ ] **TC-724** [P0] Incident → Case keep linkage (Incident.linkedCaseId)
- [ ] **TC-725** [P0] Delete Incident có linkedCase Restrict
- [ ] **TC-726** [P0] CaseProvenance=FROM_INCIDENT → linkedIncidentId set
- [ ] **TC-727** [P0] FROM_INCIDENT thiếu expectedIncidentUpdatedAt → 400
- [ ] **TC-728** [P0] Race convertToCase 2 user same Incident → 1×409
- [ ] **TC-729** [P0] INCIDENT_CONVERTED audit
- [ ] **TC-734** [P0] VIEWER convert → 403
- [ ] **TC-735** [P0] JWT thiếu → 401
- [ ] **TC-736** [P0] Incident TAM_DINH_CHI_LAI (lần 2 trở đi)
- [ ] **TC-740** [P0] Multiple Incidents linked to same Case (1-to-many)
- [ ] **TC-741** [P0] Delete Case có linkedIncidents → block 400
- [ ] **TC-742** [P0] Incident status transitions match BLTTHS
- [ ] **TC-743** [P0] IDOR convertToCase với Incident ID team khác
- [ ] **TC-744** [P0] Convert Incident → Case → Subject (E2E)
- [ ] **TC-745** [P0] convertToCase Incident soft-deleted → 404
- [ ] **TC-746** [P0] INCIDENT_STATUS_CHANGED audit có fromStatus/toStatus
- [ ] **TC-747** [P0] Update status TIEP_NHAN → DA_KET_LUAN skip middle steps
- [ ] **TC-748** [P0] Update status với 5 lần TAM_DINH_CHI ↔ DANG_DIEU_TRA
- [ ] **TC-749** [P0] Update concurrent: 2 user đổi status khác nhau
- [ ] **TC-750** [P0] Combination: ADMIN + Case DELETED 1 năm trước → restore OK
- [ ] **TC-751** [P0] Combination: dispatcher xem case ward → bypass scope
- [ ] **TC-752** [P0] Combination: ward officer + assignedTeamId override
- [ ] **TC-753** [P0] User MULTI-TEAM (thuộc 2 teams) — scope union
- [ ] **TC-754** [P0] User has writableTeamIds ⊂ teamIds (read-only some)
- [ ] **TC-755** [P0] forbidNonWhitelisted strict cho mọi DTO update
- [ ] **TC-756** [P0] Privilege escalation: change own role qua DTO mass-assign
- [ ] **TC-757** [P0] XSS reflected qua URL search → sanitize
- [ ] **TC-758** [P0] Open redirect: ?returnUrl=https://evil.com
- [ ] **TC-759** [P0] CSP header restrict inline script
- [ ] **TC-760** [P0] Referrer-Policy: strict-origin-when-cross-origin
- [ ] **TC-780** [P0] Audit log không expose internal trace/stack
- [ ] **TC-781** [P0] Cross-feature: tạo Case → KPI dashboard refresh count
- [ ] **TC-011** [P1] Tạo Case bỏ trống các field optional
- [ ] **TC-012** [P1] Tạo Case với investigatorId là user khác đang active
- [ ] **TC-013** [P1] Tạo Case với ngayKhoiTo lùi vài ngày trước hôm nay (đã khởi tố trước khi vào hệ thống)
- [ ] **TC-014** [P1] Dispatcher tạo Case có thể chọn assignedTeamId bất kỳ
- [ ] **TC-015** [P1] Tạo Case ADMIN với toàn bộ field bao gồm field hiếm dùng
- [ ] **TC-016** [P1] POST trả response message tiếng Việt 'Tạo vụ án thành công'
- [ ] **TC-023** [P1] name là object JSON → 400 IsString
- [ ] **TC-024** [P1] name là array → 400 IsString
- [ ] **TC-036** [P1] expectedPetitionUpdatedAt sai format (không ISO 8601) → 400
- [ ] **TC-038** [P1] capDoToiPham là invalid enum 'INVALID' → 400 với message tiếng Việt
- [ ] **TC-039** [P1] subjectsCount = -1 → 400 @Min(0)
- [ ] **TC-040** [P1] subjectsCount = 1.5 (float) → 400 IsInt
- [ ] **TC-041** [P1] subjectsCount = 'abc' string → 400 IsInt
- [ ] **TC-042** [P1] crime > 255 ký tự → 400 MaxLength
- [ ] **TC-043** [P1] unit > 255 ký tự → 400
- [ ] **TC-045** [P1] deadline format DD/MM/YYYY → 400 IsDateString chỉ accept ISO
- [ ] **TC-046** [P1] ngayKhoiTo='2026-13-45' (tháng 13, ngày 45) → 400
- [ ] **TC-047** [P1] sourceDocumentNote > 1000 ký tự → 400
- [ ] **TC-049** [P1] metadata là array thay vì object → 400 IsObject
- [ ] **TC-050** [P1] metadata là string → 400 IsObject
- [ ] **TC-051** [P1] assignedTeamId KHÔNG tồn tại trong teams (FK invalid) → ?
- [ ] **TC-052** [P1] POST kèm field lạ 'evilField=hack' với whitelist=true → 400
- [ ] **TC-057** [P1] deadline là object → 400 IsDateString
- [ ] **TC-058** [P1] investigatorId là number 12345 → 400 IsString
- [ ] **TC-059** [P1] assignedTeamId của team đã isActive=false → field saved nhưng cảnh báo
- [ ] **TC-060** [P1] caseProvenance=SELF_SURRENDER cần linkedPetitionId hoặc khác? — check enum
- [ ] **TC-063** [P1] name=' Vụ ABC ' (có space đầu/cuối) bị trim → save 'Vụ ABC'
- [ ] **TC-065** [P1] FROM_INCIDENT với Incident updatedAt mismatch → 409 'Vụ việc đã chỉnh sửa'
- [ ] **TC-066** [P1] FROM_PETITION + ngayKhoiTo trước Petition.receivedDate? — business logic cần check
- [ ] **TC-070** [P1] GET /cases?limit=0 → 400 @Min(1)
- [ ] **TC-071** [P1] GET /cases?limit=-5 → 400
- [ ] **TC-072** [P1] GET /cases?limit=abc (NaN) → 400 @IsInt
- [ ] **TC-074** [P1] Filter status=INVALID → 400 IsEnum
- [ ] **TC-079** [P1] Filter fromDate='2026-05-01' & toDate='2026-05-23' khoảng ngày
- [ ] **TC-080** [P1] toDate < fromDate → vẫn trả empty (không 400)
- [ ] **TC-081** [P1] Filter capDoToiPham=RAT_NGHIEM_TRONG
- [ ] **TC-082** [P1] Filter districtId qua subjects relation
- [ ] **TC-083** [P1] Sort sortBy=name sortOrder=asc
- [ ] **TC-084** [P1] sortBy='evil_field' → fallback về createdAt (security: prevent SQL injection)
- [ ] **TC-085** [P1] Pagination offset=20 trả page 2
- [ ] **TC-094** [P1] User được xem unassigned Case của team mình (assignedTeamId=null + teamIds.length>0)
- [ ] **TC-095** [P1] Filter wardTeamId='WARD-Q5' (cross-ward view PC02)
- [ ] **TC-096** [P1] Filter investigatorId trả case của ĐTV cụ thể
- [ ] **TC-106** [P1] Update → TAM_DINH_CHI thiếu lý do nhưng Case cũ (pre-migration) → SOFT warning
- [ ] **TC-109** [P1] ketQuaPhucHoiVuAn = invalid → 400
- [ ] **TC-110** [P1] lyDoTamDinhChiText > 500 ký tự → 400
- [ ] **TC-111** [P1] soQuyetDinhTamDinhChi > 100 ký tự → 400
- [ ] **TC-112** [P1] daRaSoat = 'yes' string thay vì boolean → 400 IsBoolean
- [ ] **TC-113** [P1] ngayRaSoat sai ISO format → 400
- [ ] **TC-114** [P1] Update KHÔNG đổi status → KHÔNG tạo CaseStatusHistory entry
- [ ] **TC-115** [P1] Update status với cùng giá trị status hiện tại → KHÔNG tạo history
- [ ] **TC-116** [P1] Update metadata.petitionType sync sang Petition liên kết
- [ ] **TC-117** [P1] Update metadata.petitionType mà KHÔNG có Petition link → silent ignore (no phantom create)
- [ ] **TC-119** [P1] PUT lyDoTamDinhChiVuAn nhưng status không phải TAM_DINH_CHI → vẫn save (no enforce)
- [ ] **TC-120** [P1] investigatorId không tồn tại → 400
- [ ] **TC-121** [P1] Update deadline=null xóa deadline cũ
- [ ] **TC-122** [P1] Update capDoToiPham đổi từ NGHIEM_TRONG sang DAC_BIET_NGHIEM_TRONG
- [ ] **TC-123** [P1] Update Case khi user role VIEWER → 403
- [ ] **TC-134** [P1] Idempotent: PUT cùng status không tạo history
- [ ] **TC-135** [P1] DA_LUU_TRU → DA_KET_LUAN (rollback) — có cho phép?
- [ ] **TC-166** [P1] Reason rỗng → 400 DTO validation
- [ ] **TC-174** [P1] investigatorId=null khi assign (chỉ chuyển team, chưa giao cho ĐTV cụ thể)
- [ ] **TC-179** [P1] Re-assign cùng team (no-op assignedTeamId không đổi) → audit không emit escalation
- [ ] **TC-180** [P1] Non-ward team → ward team (de-escalation) — audit không emit (chỉ from-ward)
- [ ] **TC-181** [P1] assignedTeamId rỗng '' → 400
- [ ] **TC-182** [P1] Thiếu assignedTeamId → 400 DTO required
- [ ] **TC-183** [P1] Dispatcher với expectedUpdatedAt KHỚP → success
- [ ] **TC-191** [P1] Export với take=500 limit cứng (không trả >500 row)
- [ ] **TC-194** [P1] Export ward không có data → vẫn trả Excel với header + empty body + footer
- [ ] **TC-195** [P1] Server crash giữa export (write fail) → response 500 JSON nếu chưa send header
- [ ] **TC-196** [P1] Audit CASE_EXPORTED capture filters đầy đủ
- [ ] **TC-198** [P1] Case không có status change → empty array
- [ ] **TC-200** [P1] changedBy=null khi changedById=null (audit cũ chưa có user)
- [ ] **TC-202** [P1] History entry có changedAt là datetime đầy đủ (ISO)
- [ ] **TC-205** [P1] PATCH lyDoTamDinhChiVuAn='INVALID' → 400/500 (validation cast)
- [ ] **TC-207** [P1] Banner TdcBackfillBanner ẩn sau khi backfill thành công
- [ ] **TC-237** [P1] Cases không fetch URL từ user input — N/A SSRF
- [ ] **TC-243** [P1] Tab qua đủ field trong CaseFormPage không bị trap
- [ ] **TC-244** [P1] Enter submit form khi focus ở input
- [ ] **TC-245** [P1] Esc đóng modal SuspensionModal
- [ ] **TC-246** [P1] Input có aria-label hoặc label[for] gắn id
- [ ] **TC-247** [P1] Status badge có aria-label='Trạng thái Đang điều tra'
- [ ] **TC-248** [P1] Error message gắn aria-describedby với input
- [ ] **TC-249** [P1] Text trên background đạt 4.5:1 (WCAG AA)
- [ ] **TC-250** [P1] Badge status có contrast đủ (text white trên red ≥ 4.5)
- [ ] **TC-251** [P1] Focus ring rõ ràng trên nút Lưu (outline 2px)
- [ ] **TC-254** [P1] Form error message tiếng Việt, không dùng chỉ màu để biểu thị error
- [ ] **TC-255** [P1] Page có h1, h2 hierarchical
- [ ] **TC-256** [P1] Icon button có aria-label (vd 'Xóa vụ án')
- [ ] **TC-258** [P1] Bảng danh sách dùng <table><th scope='col'>
- [ ] **TC-259** [P1] Modal mở thì focus trap inside; close trả focus về trigger
- [ ] **TC-260** [P1] Chrome 126 latest — toàn bộ flow OK
- [ ] **TC-261** [P1] Chrome 125 (latest-1) — backward compat
- [ ] **TC-262** [P1] Firefox 127 latest
- [ ] **TC-263** [P1] Safari 17 latest (macOS)
- [ ] **TC-264** [P1] Edge 126
- [ ] **TC-267** [P1] iOS Safari 17 trên iPhone 14 — list page responsive
- [ ] **TC-268** [P1] Android Chrome 126 trên Pixel 7
- [ ] **TC-269** [P1] 1920x1080 desktop — layout standard
- [ ] **TC-270** [P1] 1366x768 laptop common — không scroll horizontal
- [ ] **TC-271** [P1] 768x1024 tablet portrait
- [ ] **TC-272** [P1] 375x667 iPhone SE
- [ ] **TC-273** [P1] 3G slow — page load < 5s và loading indicator
- [ ] **TC-275** [P1] Windows 11 + Chrome — OK
- [ ] **TC-276** [P1] macOS Sonoma + Safari — OK
- [ ] **TC-277** [P1] GET /cases?limit=20 response < 500ms với 10k row DB
- [ ] **TC-278** [P1] GET /cases?search=trộm cắp < 1s với 10k row
- [ ] **TC-279** [P1] Filter overdue=true + pagination < 800ms
- [ ] **TC-280** [P1] GET /cases/:id (include petitions + investigator) < 300ms
- [ ] **TC-281** [P1] POST /cases (FROM_PETITION transaction) < 600ms
- [ ] **TC-282** [P1] Export 500 row Excel < 5s
- [ ] **TC-285** [P1] Deep pagination offset=5000 < 1s (with index)
- [ ] **TC-287** [P1] Audit log insert không block main response > 50ms
- [ ] **TC-288** [P1] name có dấu tiếng Việt 'Vụ án Nguyễn Quỳnh Trâm'
- [ ] **TC-289** [P1] name có dấu tổ hợp (Unicode NFC vs NFD) — normalize
- [ ] **TC-290** [P1] name có emoji 'Vụ test 🎉' — 4-byte UTF-8
- [ ] **TC-291** [P1] name='  Vụ ABC  ' leading/trailing trimmed
- [ ] **TC-292** [P1] name có zero-width space \u200B → save raw nhưng có thể gây bug filter
- [ ] **TC-293** [P1] name='\uFEFFVụ' BOM character ở đầu
- [ ] **TC-294** [P1] name='<b>Vụ</b>' tag HTML literal
- [ ] **TC-295** [P1] ngayKhoiTo='2026-02-29' (năm 2026 không nhuận) → 400 hoặc 2026-03-01?
- [ ] **TC-296** [P1] ngayKhoiTo='2026-05-23T00:00:00+07:00' (GMT+7 Vietnam)
- [ ] **TC-297** [P1] subjectsCount=Number.MAX_SAFE_INTEGER → save hoặc 400
- [ ] **TC-298** [P1] Multi-byte filename trong sourceDocumentNote
- [ ] **TC-299** [P1] name có '&', '<', '>', '"', "'" — SQL escape
- [ ] **TC-300** [P1] metadata.note 5000 ký tự (JSONB không limit)
- [ ] **TC-305** [P1] crime 255 ký tự (max) → pass
- [ ] **TC-306** [P1] crime 256 ký tự → 400
- [ ] **TC-307** [P1] unit 255 ký tự → pass
- [ ] **TC-308** [P1] unit 256 ký tự → 400
- [ ] **TC-309** [P1] Delete reason 10 ký tự (min) → pass
- [ ] **TC-310** [P1] Delete reason 9 ký tự → 400
- [ ] **TC-311** [P1] Delete reason 500 ký tự (max) → pass
- [ ] **TC-312** [P1] Delete reason 501 ký tự → 400
- [ ] **TC-313** [P1] subjectsCount = 0 (min) → pass
- [ ] **TC-314** [P1] subjectsCount = -1 → 400 @Min(0)
- [ ] **TC-315** [P1] subjectsCount = 1 → pass
- [ ] **TC-316** [P1] limit=1 (min) → pass
- [ ] **TC-317** [P1] limit=100 (max) → pass
- [ ] **TC-318** [P1] limit=101 → 400
- [ ] **TC-319** [P1] offset=0 (min) → pass
- [ ] **TC-320** [P1] offset=-1 → 400 @Min(0)
- [ ] **TC-321** [P1] sourceDocumentNote 1000 ký tự (max) → pass
- [ ] **TC-322** [P1] sourceDocumentNote 1001 → 400
- [ ] **TC-323** [P1] name class hợp lệ: chuỗi VN có dấu
- [ ] **TC-324** [P1] name class không hợp lệ: rỗng
- [ ] **TC-325** [P1] Provenance class valid enum (DIRECT_DISCOVERY)
- [ ] **TC-326** [P1] Provenance class invalid (string ngẫu nhiên)
- [ ] **TC-327** [P1] CapDoToiPham class IT_NGHIEM_TRONG
- [ ] **TC-328** [P1] CapDoToiPham class NGHIEM_TRONG
- [ ] **TC-329** [P1] CapDoToiPham class RAT_NGHIEM_TRONG
- [ ] **TC-330** [P1] CapDoToiPham class DAC_BIET_NGHIEM_TRONG
- [ ] **TC-331** [P1] Status valid class TIEP_NHAN
- [ ] **TC-332** [P1] Status invalid class 'OPEN' (Anh)
- [ ] **TC-350** [P1] Cross-feature: KPI dashboard reload khi Case mới capDoToiPham=DBNT tạo
- [ ] **TC-351** [P1] Update Case sync Petition.petitionType
- [ ] **TC-352** [P1] Assign Case ward → cross-ward → audit event chuỗi
- [ ] **TC-353** [P1] Soft delete → list deleted → restore → list active
- [ ] **TC-354** [P1] Multi-user race: A update + B delete đồng thời
- [ ] **TC-355** [P1] User đóng tab giữa form Create — KHÔNG tạo Case partial
- [ ] **TC-356** [P1] User refresh giữa form Create — clear state hoặc preserve draft?
- [ ] **TC-357** [P1] Mất mạng giữa POST /cases — frontend retry hoặc toast error
- [ ] **TC-358** [P1] Server 500 giữa POST — Case không tạo, audit không log
- [ ] **TC-359** [P1] Request timeout 30s + retry → idempotency?
- [ ] **TC-360** [P1] Audit fail giữa DELETE tx → rollback delete
- [ ] **TC-384** [P1] gender='UNKNOWN' không trong enum → 400
- [ ] **TC-385** [P1] gender bỏ trống → default MALE
- [ ] **TC-386** [P1] type=VICTIM (Bị hại) — không phải SUSPECT default
- [ ] **TC-387** [P1] type=WITNESS (Nhân chứng)
- [ ] **TC-388** [P1] type='INVALID' → 400
- [ ] **TC-389** [P1] status=DETAINED (Đang tạm giam)
- [ ] **TC-390** [P1] status=RELEASED khi đã thả
- [ ] **TC-391** [P1] status=WANTED (Truy nã)
- [ ] **TC-406** [P1] fullName tiếng Việt có dấu 'Nguyễn Quỳnh Trâm'
- [ ] **TC-407** [P1] fullName 200 ký tự (long name)
- [ ] **TC-408** [P1] address Unicode + multi-line
- [ ] **TC-409** [P1] CCCD đúng 9 chữ → pass min
- [ ] **TC-410** [P1] CCCD đúng 12 chữ → pass max
- [ ] **TC-411** [P1] CCCD 10 chữ → 400 (regex chỉ accept 9 hoặc 12)
- [ ] **TC-412** [P1] CCCD 11 chữ → 400
- [ ] **TC-416** [P1] Transition WANTED → DETAINED (bắt được rồi)
- [ ] **TC-417** [P1] Form Subject input có label gắn id
- [ ] **TC-418** [P1] Date picker dateOfBirth keyboard accessible
- [ ] **TC-419** [P1] GET /subjects?caseId=X với Case có 50 subjects < 500ms
- [ ] **TC-421** [P1] Cùng idNumber khác type (SUSPECT vs VICTIM) trong cùng Case OK
- [ ] **TC-423** [P1] PUT thay đổi caseId (chuyển vụ án) → có cho phép?
- [ ] **TC-424** [P1] occupationId không tồn tại directory → 400
- [ ] **TC-425** [P1] nationalityId không tồn tại → 400
- [ ] **TC-426** [P1] wardId không tồn tại → 400
- [ ] **TC-427** [P1] districtName denormalized preserve sau cải cách hành chính
- [ ] **TC-428** [P1] Subject Filter by status=DETAINED
- [ ] **TC-429** [P1] Subject Filter by type=SUSPECT
- [ ] **TC-432** [P1] fullName 1 ký tự 'A'
- [ ] **TC-433** [P1] fullName 1000 ký tự (no max)
- [ ] **TC-434** [P1] phone number format VN '0901234567'
- [ ] **TC-435** [P1] phone với ký tự đặc biệt '+84 901 234 567'
- [ ] **TC-436** [P1] Subject + Lawyer assign: thêm Lawyer cho 1 Subject cụ thể
- [ ] **TC-437** [P1] Delete Subject có Lawyer link — Lawyer.subjectId=NULL (SetNull)
- [ ] **TC-438** [P1] Atomic Subject create rollback nếu audit fail
- [ ] **TC-442** [P1] Lawyer không gắn subjectId (bào chữa chung)
- [ ] **TC-449** [P1] subjectId không cùng caseId (cross-case) → 400 business rule
- [ ] **TC-464** [P1] fullName tiếng Việt 'Luật sư Đỗ Thị Thanh Mai'
- [ ] **TC-465** [P1] lawFirm Unicode 'Văn phòng Luật sư Hà Nội & Cộng sự'
- [ ] **TC-466** [P1] phone số Quốc tế '+84 901 234 567'
- [ ] **TC-467** [P1] barNumber chứa whitespace 'LS 2026 12345' → save raw (gap)
- [ ] **TC-468** [P1] Lawyer được assign sang Subject khác (re-assign)
- [ ] **TC-469** [P1] Unassign Lawyer khỏi Subject (subjectId=null)
- [ ] **TC-470** [P1] Soft-delete Subject → Lawyer.subjectId auto null (onDelete SetNull)
- [ ] **TC-471** [P1] Form barNumber có pattern hint
- [ ] **TC-472** [P1] GET /lawyers với 100 lawyer DB < 500ms
- [ ] **TC-476** [P1] fullName 1 ký tự
- [ ] **TC-477** [P1] barNumber 1 ký tự
- [ ] **TC-478** [P1] fullName 500 ký tự
- [ ] **TC-479** [P1] POST với forbidNonWhitelisted field 'evil':'x' → 400
- [ ] **TC-480** [P1] phone format không validate (gap)
- [ ] **TC-481** [P1] Filter search by fullName
- [ ] **TC-482** [P1] Pagination limit/offset
- [ ] **TC-483** [P1] limit=200 → ? (max validation cần check)
- [ ] **TC-492** [P1] status='INVALID' → 400 IsEnum
- [ ] **TC-509** [P1] content rất dài (10K characters)
- [ ] **TC-510** [P1] content có format Markdown
- [ ] **TC-513** [P1] Form content textarea có aria-label
- [ ] **TC-514** [P1] Save conclusion với content 5K chars < 1s
- [ ] **TC-515** [P1] content 1 ký tự → pass
- [ ] **TC-516** [P1] content 100K ký tự → ? (no max limit)
- [ ] **TC-519** [P1] Filter status=DRAFT
- [ ] **TC-520** [P1] notes optional bỏ trống
- [ ] **TC-521** [P1] type tiếng Việt 'Kết luận đề nghị truy tố'
- [ ] **TC-522** [P1] Delete Case bị block khi có Conclusion linked
- [ ] **TC-539** [P1] documentType='INVALID' enum → 400
- [ ] **TC-552** [P1] title tiếng Việt có dấu
- [ ] **TC-553** [P1] filename Unicode 'tài liệu công văn.pdf'
- [ ] **TC-554** [P1] filename emoji '📄report.pdf'
- [ ] **TC-555** [P1] File 0 byte (empty) → 400
- [ ] **TC-556** [P1] File 1 byte (smallest non-empty) → 201
- [ ] **TC-557** [P1] File đúng limit 10MB → 201
- [ ] **TC-558** [P1] File 10MB+1 byte → 413
- [ ] **TC-559** [P1] Upload 5MB < 5s
- [ ] **TC-560** [P1] Download 5MB streaming không OOM
- [ ] **TC-561** [P1] Upload bị interrupt giữa chừng → no partial file
- [ ] **TC-562** [P1] Disk full khi upload → 500 cleanup
- [ ] **TC-567** [P1] File input có aria-label
- [ ] **TC-568** [P1] Upload progress có aria-live='polite' announce
- [ ] **TC-569** [P1] Drag-drop upload trên Chrome
- [ ] **TC-570** [P1] File picker mobile (iOS Safari)
- [ ] **TC-572** [P1] File trùng tên (cùng originalName) → vẫn lưu (fileName generated unique)
- [ ] **TC-574** [P1] Filter documentType=COURT_DECISION
- [ ] **TC-575** [P1] Filter by uploadedById
- [ ] **TC-578** [P1] Content-Sniffing: response header X-Content-Type-Options: nosniff
- [ ] **TC-579** [P1] mimeType validate against extension consistency
- [ ] **TC-580** [P1] Document gắn cả Subject (nếu schema cho phép)
- [ ] **TC-585** [P1] relatedCaseId optional bỏ trống (general proposal)
- [ ] **TC-588** [P1] sentDate format invalid → 400
- [ ] **TC-597** [P1] status='INVALID' → 400 IsEnum
- [ ] **TC-600** [P1] content rất dài (50K chars)
- [ ] **TC-601** [P1] content tiếng Việt có dấu
- [ ] **TC-604** [P1] proposalNumber 1 ký tự
- [ ] **TC-605** [P1] proposalNumber 500 ký tự
- [ ] **TC-606** [P1] List 100 proposals < 500ms
- [ ] **TC-607** [P1] Form input có label
- [ ] **TC-608** [P1] Chrome mobile OK
- [ ] **TC-609** [P1] Proposal sent → notification gửi đến receiver
- [ ] **TC-612** [P1] Atomic rollback nếu audit fail
- [ ] **TC-613** [P1] Filter status=PENDING
- [ ] **TC-614** [P1] Pagination
- [ ] **TC-615** [P1] Filter relatedCaseId
- [ ] **TC-635** [P1] receivingUnit tiếng Việt 'Phòng Cảnh sát Hà Nội'
- [ ] **TC-636** [P1] content nhiều paragraph
- [ ] **TC-637** [P1] delegationDate ISO format
- [ ] **TC-638** [P1] delegationDate format invalid → 400 (nếu IsDateString)
- [ ] **TC-641** [P1] delegationNumber 1 ký tự
- [ ] **TC-642** [P1] content 100K ký tự
- [ ] **TC-643** [P1] List 50 delegations < 500ms
- [ ] **TC-644** [P1] Form A11Y compliant
- [ ] **TC-645** [P1] Form mobile responsive
- [ ] **TC-646** [P1] Delegation cùng caseId hiển thị trong Case detail tab
- [ ] **TC-649** [P1] Filter status=PENDING
- [ ] **TC-650** [P1] Filter by receivingUnit
- [ ] **TC-651** [P1] Pagination
- [ ] **TC-657** [P1] decisionDate sai ISO → 400
- [ ] **TC-658** [P1] deadline sai ISO → 400
- [ ] **TC-659** [P1] deadline < decisionDate (deadline trước quyết định)
- [ ] **TC-670** [P1] reason tiếng Việt long text
- [ ] **TC-671** [P1] decisionNumber format 'XYZ-2026-001'
- [ ] **TC-673** [P1] decisionNumber 1 ký tự
- [ ] **TC-674** [P1] reason 50K ký tự
- [ ] **TC-675** [P1] List < 500ms
- [ ] **TC-676** [P1] Form A11Y
- [ ] **TC-677** [P1] Mobile responsive
- [ ] **TC-680** [P1] Filter by caseId
- [ ] **TC-681** [P1] Filter by type
- [ ] **TC-700** [P1] Petition senderName tiếng Việt có dấu
- [ ] **TC-701** [P1] stt cuối cùng năm 99999 → tràn?
- [ ] **TC-704** [P1] convertToCase < 1s
- [ ] **TC-707** [P1] Petition tab trong Case detail show linked petitions[]
- [ ] **TC-708** [P1] Multiple petitions linked to same Case (1-to-many)
- [ ] **TC-711** [P1] petitionType enum: TO_GIAC, KHIEU_NAI, TIN_BAO, ...
- [ ] **TC-712** [P1] petitionType='Vietnamese label' → 400 (v0.37.2.4 BUG)
- [ ] **TC-713** [P1] petitionType='TO_GIAC' enum value pass
- [ ] **TC-716** [P1] convertToCase fail giữa tx → Petition vẫn nguyên
- [ ] **TC-730** [P1] Incident có deadlineRuleVersionId tracked
- [ ] **TC-731** [P1] E2E: Tạo Incident → assign → convert Case → KPI dashboard update
- [ ] **TC-732** [P1] convertToCase < 1s
- [ ] **TC-733** [P1] convertToCase fail giữa tx → Incident nguyên
- [ ] **TC-737** [P1] Incident detail show ketQuaXacMinh + ngayPhucHoi
- [ ] **TC-738** [P1] Incident kết quả phục hồi enum khác Case
- [ ] **TC-739** [P1] Incident deadline override Case deadline?
- [ ] **TC-761** [P1] Cold start API after restart < 5s first request
- [ ] **TC-762** [P1] DB connection pool exhaustion test
- [ ] **TC-763** [P1] Memory leak test: 100 sequential create
- [ ] **TC-764** [P1] Audit log không slow main response
- [ ] **TC-765** [P1] Index hit on overdue+status query
- [ ] **TC-766** [P1] CaseListPage table có scope='col' th
- [ ] **TC-767** [P1] Pagination buttons có aria-label
- [ ] **TC-768** [P1] Sort indicator aria-sort='ascending|descending'
- [ ] **TC-769** [P1] Toast notification có role='alert' aria-live
- [ ] **TC-770** [P1] Skip-to-main-content link
- [ ] **TC-771** [P1] Form fieldset/legend cho group
- [ ] **TC-772** [P1] Color contrast cho disabled state ≥ 3:1
- [ ] **TC-773** [P1] Chrome incognito mode — cookies/localStorage
- [ ] **TC-774** [P1] Safari iOS pinch-zoom enabled
- [ ] **TC-775** [P1] High DPI display Retina — text crisp
- [ ] **TC-776** [P1] UTF-8 BOM handling trong file upload (CSV import)
- [ ] **TC-777** [P1] Date timezone conversion cross-timezone view
- [ ] **TC-778** [P1] Page refresh giữa edit Case → unsaved changes warning
- [ ] **TC-779** [P1] Token expired during form edit → re-login flow without losing data
- [ ] **TC-252** [P2] Zoom 200% không vỡ layout CaseListPage
- [ ] **TC-253** [P2] prefers-reduced-motion respect — animation tắt
- [ ] **TC-257** [P2] <html lang='vi'> để screen reader phát âm tiếng Việt
- [ ] **TC-265** [P2] Chrome 100 (legacy older) — graceful degradation
- [ ] **TC-266** [P2] IE11 — KHÔNG support (banner warning)
- [ ] **TC-274** [P2] Offline — toast 'Mất kết nối', retry button
- [ ] **TC-283** [P2] 10 user đồng thời POST /cases — không deadlock
- [ ] **TC-284** [P2] 50 user đồng thời GET list — không nghẽn
- [ ] **TC-286** [P2] Export 500 row không OOM (workbook stream)

---

_Generated by `uat-test-writer` skill on 23/05/2026 18:33_