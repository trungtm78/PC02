import type { FeatureManifest } from './feature-manifest';

export const FEATURE_FLAGS_MANIFEST: FeatureManifest = {
  key: 'feature-flags',
  label: 'Quản lý tính năng',
  description: 'Bật/tắt module runtime + feature packs',
  domain: 'core',
};
