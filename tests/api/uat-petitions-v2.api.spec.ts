// Quản lý Đơn thư (Petitions) — API smoke layer (Layer 1)
// Auto-generated by gen_uat_specs.py — KHÔNG sửa thủ công
import { test, expect } from '@playwright/test';

test.describe('UAT API — Quản lý Đơn thư (Petitions)', () => {

  test('TC-001-API: Tạo đơn TO_CAO hợp lệ tự sinh STT [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201 + stt='DT-2026-NNNNN'
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-001', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-002-API: Tạo đơn KHIEU_NAI hợp lệ [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-002', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-003-API: Tạo đơn KIEN_NGHI [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-003', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-004-API: Tạo đơn PHAN_ANH [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-004', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-005-API: Ward officer tự gán assignedTeamId [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201, assignedTeamId=wardTeamId
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-005', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-006-API: Draft STT preview trước save [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 200 next STT
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-006', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-007-API: List đơn thư default [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-008-API: Filter theo status MOI_TIEP_NHAN [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-009-API: Search theo stt + senderName [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-010-API: GET /linkable [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200, linkedCaseId=null + scope
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-011-API: GET /petitions/:id (route alias v0.67.1) [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-012-API: PUT update đơn thư với expectedUpdatedAt [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Update | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-012', _module: 'Petitions.Update' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-013-API: Convert đơn thư → Vụ án (atomic) [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-013', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-014-API: Convert → Vụ việc [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-014', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-015-API: Dispatcher assign đơn thư [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Assign | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-015', _module: 'Petitions.Assign' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-016-API: Export Excel danh sách [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-017-API: Export document Word (PHIEU_DE_XUAT) [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-018-API: Soft-delete đơn thư [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Delete | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-019-API: ADMIN restore [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Restore | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-019', _module: 'Petitions.Restore' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-020-API: GET /petitions/stats count theo 7 status [@GREEN @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.Stats | Expected: 200 đủ 7 key
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-021-API: Thiếu senderName [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-021', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-022-API: Thiếu petitionType [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-022', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-023-API: petitionType không thuộc enum [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-023', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-024-API: receivedDate tương lai [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400/200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-024', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-025-API: STT trùng (manual) [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 409 unique
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-025', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-026-API: senderEmail format sai [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-026', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-027-API: senderPhone format sai [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-027', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-028-API: OCC stale expectedUpdatedAt [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Update | Expected: 409
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-028', _module: 'Petitions.Update' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-029-API: Convert đơn đã linked Case [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 409/422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-029', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-030-API: Convert thiếu caseName [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-030', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-031-API: Convert thiếu expectedUpdatedAt (chống race) [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-031', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-032-API: Convert đơn đã soft-delete [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-033-API: Convert với jurisdiction enum sai [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-033', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-034-API: GET không JWT [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Auth | Expected: 401
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-035-API: User không có read:Petition [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Auth | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-036-API: Non-creator non-admin xóa [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Delete | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-037-API: Xóa đơn đã DA_CHUYEN_VU_AN [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Delete | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-038-API: Non-ADMIN restore [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Restore | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-038', _module: 'Petitions.Restore' },
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-039-API: User Team A xem petition Team B [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-040-API: limit > 100 [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-041-API: fromDate > toDate [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 400/empty
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-042-API: Non-dispatcher assign [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Assign | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-042', _module: 'Petitions.Assign' },
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-043-API: Assign thiếu assignedTeamId [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Assign | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-043', _module: 'Petitions.Assign' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-044-API: export-document docType không thuộc 6 template [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-045-API: Batch export ZIP với 0 ids [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-046-API: detailContent có HTML/script không sanitize [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-046', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-047-API: Senderr name > 255 ký tự [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-047', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-048-API: summary > 2000 ký tự [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-048', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-049-API: assignedToId không tồn tại [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-049', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-050-API: deadline tự nhập < receivedDate [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400/200 flag
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-050', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-051-API: Update đơn đã soft-delete [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Update | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-051', _module: 'Petitions.Update' },
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-052-API: sortBy injection [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-053-API: Export rate limit 5/min [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 429
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([429, 200, 201]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-054-API: Convert race — 2 user cùng convert [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 1 OK, 1 409
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-054', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-055-API: Bulk assign khác đơn vị [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Bulk | Expected: 403/partial
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-055', _module: 'Petitions.Bulk' },
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-056-API: Bulk delete >100 ids [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Bulk | Expected: 400/429
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-057-API: GET stats với status filter — phải strip [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200 đầy đủ 7 keys
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-058-API: DocumentNumberEngine fail giữa transaction [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 500/503
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-058', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-059-API: unit > 255 ký tự [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-059', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-060-API: export-word legacy bị disable [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 404/410
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-061-API: Tạo đơn có notes chứa SQL keyword [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201 (parametrized)
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-061', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-062-API: baoCaoBanGiamDoc enum không hợp lệ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400/200 ignore
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-062', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-063-API: Cache HIT ở user A leak data sang user B [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: Mỗi user thấy data riêng
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-064-API: Search với % wildcard SQL [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200, escape %
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-065-API: senderName = 1 ký tự (min) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-065', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-066-API: senderName = 255 (max) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-066', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-067-API: summary = 2000 (max) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-067', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-068-API: limit=1 [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-069-API: limit=100 [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-070-API: listLinkable limit=50 default [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200 50 row
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-071-API: STT counter rollover năm mới [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201 stt='DT-2026-00001'
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-071', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-072-API: status 7 partition [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-073-API: petitionType 4 partition [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-073', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-074-API: overdue=true vs false [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-075-API: docType 6 template values [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 200 .docx
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-076-API: MOI_TIEP_NHAN → DANG_XU_LY [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-077-API: DANG_XU_LY → CHO_PHE_DUYET [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-078-API: CHO_PHE_DUYET → DA_GIAI_QUYET [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-079-API: Convert-case auto đặt status=DA_CHUYEN_VU_AN [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.State | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-080-API: Convert-incident auto đặt status=DA_CHUYEN_VU_VIEC [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.State | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-081-API: DA_LUU_DON là terminal [@STATE @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.State | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-082-API: Deadline auto-calc theo petitionType (DT) [@DECISION @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Decision | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-083-API: DataScope filter — 4 role combinations [@DECISION @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Decision | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-084-API: SQL Injection trong search [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 200 không leak
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-085-API: XSS senderName [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-086-API: XSS detailContent (RichText) [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 201 + stripHtmlTags
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-087-API: IDOR GET /:id khác team [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-088-API: Mass assignment enteredById [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 201, server set actor
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-089-API: CSRF POST cross-origin [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: CORS reject
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-090-API: Path traversal export filename [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-091-API: Sensitive PII trong export — masking [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 200, có watermark / role-based redaction
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-092-API: Rate limit list endpoint [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 429
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([429, 200, 201]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-093-API: Convert race — bypass optimistic lock với expectedUpdatedAt giả [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 409
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-093', _module: 'Petitions.Security' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-094-API: docx template path injection [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-095-API: Audit log không chứa password/token [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-096-API: CORS — preflight evil origin [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-097-API: Unicode senderName tiếng Việt [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Data | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-098-API: Phone E.164 vs local [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Data | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-099-API: Trim senderName [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Data | Expected: 201 trimmed
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-100-API: Date receivedDate UTC+7 [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Data | Expected: ISO 8601
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-101-API: Empty optional fields → null [@DATA @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.Data | Expected: 201 null
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-102-API: 2 user concurrent create cùng giây — STT unique [@EDGE @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Race | Expected: Cả 2 thành công, STT khác nhau
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-103-API: Convert sau khi đã merge document [@EDGE @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Edge | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-104-API: Stats DB rỗng [@EDGE @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.Stats | Expected: 200 all=0
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-105-API: Form keyboard tab order [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-106-API: Label htmlFor đầy đủ [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-107-API: Contrast badge 7 status ≥ 4.5 [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-108-API: Error aria-live announce [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-109-API: Convert modal trap focus [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-110-API: PageHeader có heading h1 [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-111-API: Chrome 130 Win [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-112-API: Firefox 130 [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-113-API: Edge 130 [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-114-API: Safari 17 macOS [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-115-API: Mobile 375x667 [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-116-API: Tablet 768x1024 [@COMPAT @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-117-API: List 1000 đơn < 2s [@PERFORMANCE @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Perf | Expected: P95 < 2s
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-118-API: Convert đơn có 50 documents < 5s [@PERFORMANCE @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Perf | Expected: < 5s
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-119-API: Export 5000 record < 30s [@PERFORMANCE @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.Perf | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-120-API: STT auto-gen + DocumentNumberLog ghi đầy đủ [@INTEGRATION @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Doc | Expected: 201 stt
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-121-API: Assign → notification cho assignedToId [@INTEGRATION @P0]', async ({ request, baseURL }) => {
    // Module: Petitions.Notify | Expected: SSE event
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-122-API: senderBirthYear < 1900 [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-122', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-123-API: senderBirthYear > năm hiện tại [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-123', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-124-API: Update senderName trên đơn đã DA_CHUYEN_VU_AN [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Update | Expected: 422 hoặc 200 ignored
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-124', _module: 'Petitions.Update' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-125-API: Bulk-assign body ids rỗng [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Bulk | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-125', _module: 'Petitions.Bulk' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-126-API: export/duplicates với date range > 365 ngày [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Export | Expected: 400/clamp
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-127-API: Convert-incident thiếu name [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-127', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-128-API: raSoatTrung enum không hợp lệ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-128', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-129-API: Export-document với 6 docType lần lượt → 6 SeriesKey khác [@INTEGRATION @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Doc | Expected: 200 mỗi lần
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-130-API: Convert đơn đã linked Incident [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Convert | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-130', _module: 'Petitions.Convert' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-131-API: Ward page WardPetitionsPage user thường không thấy [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-132-API: Bulk action checkbox có label SR [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-133-API: Overdue badge có aria-label đầy đủ ngày [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-134-API: PWA install offline read cached petitions [@COMPAT @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-135-API: Print stylesheet cho export-document preview [@COMPAT @P2]', async ({ request, baseURL }) => {
    // Module: Petitions.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-136-API: Tạo đơn không thuộc đơn vị mình (unit param) [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 201 (free text) hoặc 403 nếu validated
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-136', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-137-API: Update senderEmail format sai [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Update | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-137', _module: 'Petitions.Update' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-138-API: ZIP slip qua batch-export filename [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 400 sanitize
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-139-API: Body content-type không phải application/json [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Create | Expected: 415
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-139', _module: 'Petitions.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-140-API: GET /:id/journey với page=0 [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Read | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-141-API: Document export OOXML macro injection [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Petitions.Security | Expected: 200 docx render literal text
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/petitions`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });

});