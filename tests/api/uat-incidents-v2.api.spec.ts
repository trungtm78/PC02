// Quản lý Vụ việc (Incidents) — API smoke layer (Layer 1)
// Auto-generated by gen_uat_specs.py — KHÔNG sửa thủ công
import { test, expect } from '@playwright/test';

test.describe('UAT API — Quản lý Vụ việc (Incidents)', () => {

  test('TC-001-API: Tạo vụ việc mới hợp lệ với name + loaiDonVu + nguonPhatTin [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 201 + code auto VV-2026-XXX
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-001', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-002-API: Auto-gen code VV-YYYY-NNN [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 201 code khác nhau, format VV-2026-001/002/003
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-002', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-003-API: Auto-calc deadline theo THOI_HAN_XAC_MINH rule [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 201 deadline đúng
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-003', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-004-API: Ward officer tự gán assignedTeamId từ wardTeamId [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 201, assignedTeamId=wardTeamId
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-004', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-005-API: List vụ việc default pagination [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-006-API: Filter theo phase=xac-minh [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200, status ∈ [DANG_XAC_MINH, DA_PHAN_CONG, QUA_HAN]
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-007-API: GET /incidents/linkable cho Case picker [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200, chỉ incident linkedCaseId=null + scope
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-008-API: GET stats exhaustive trả về tất cả status keys [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200, body có đủ 14 key status
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-009-API: PUT update vụ việc với expectedUpdatedAt [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Update | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-009', _module: 'Incidents.Update' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-010-API: PATCH status TIEP_NHAN → DANG_XAC_MINH [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-010', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-011-API: PATCH KHONG_KHOI_TO kèm lyDoKhongKhoiTo (Đ.157) [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-011', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-012-API: Khởi tố vụ việc → tạo Case (Branch-3) [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Prosecute | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-012', _module: 'Incidents.Prosecute' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-013-API: Gia hạn lần 1 trong giới hạn [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Extend | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-013', _module: 'Incidents.Extend' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-014-API: Dispatcher assign investigator → status DANG_XAC_MINH [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Assign | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-014', _module: 'Incidents.Assign' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-015-API: Gộp vụ việc A vào B [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Merge | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-015', _module: 'Incidents.Merge' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-016-API: Chuyển đơn vị [@GREEN @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Transfer | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-016', _module: 'Incidents.Transfer' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-017-API: Soft-delete TIEP_NHAN bởi creator trong 72h [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-018-API: ADMIN restore [@GREEN @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Restore | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-018', _module: 'Incidents.Restore' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-019-API: name < 5 ký tự [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-019', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-020-API: nguonPhatTin không khớp loaiDonVu [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-020', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-021-API: Transition không nằm trong VALID_TRANSITIONS [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-021', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-022-API: KHONG_KHOI_TO thiếu lyDoKhongKhoiTo [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-022', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-023-API: Chuyển từ terminal status (DA_GIAI_QUYET) [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-023', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-024-API: Gia hạn quá maxExtensionsSnapshot (lần 3) [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Extend | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-024', _module: 'Incidents.Extend' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-025-API: Xóa có linked petitions [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-026-API: Xóa có attached documents [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-027-API: Xóa khi status != TIEP_NHAN [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-028-API: Quá 72h kể từ created [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-029-API: Non-creator non-admin xóa [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-030-API: Non-dispatcher assign [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Assign | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-030', _module: 'Incidents.Assign' },
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-031-API: Prosecute thiếu caseName [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Prosecute | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-031', _module: 'Incidents.Prosecute' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-032-API: Prosecute incident đã DA_CHUYEN_VU_AN [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Prosecute | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-032', _module: 'Incidents.Prosecute' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-033-API: Merge vào chính nó (self) [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Merge | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-033', _module: 'Incidents.Merge' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-034-API: Merge vào incident terminal [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Merge | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-034', _module: 'Incidents.Merge' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-035-API: User ĐTV Team A xem incident Team B (DataScope) [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-036-API: Update với expectedUpdatedAt stale [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Update | Expected: 409
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-036', _module: 'Incidents.Update' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-037-API: Non-ADMIN restore [@RED @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Restore | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-037', _module: 'Incidents.Restore' },
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-038-API: Stats không strip status filter [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200, vẫn count đủ tất cả status (strip filter)
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-039-API: fromDate > toDate [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-039', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-040-API: name > 255 ký tự [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-040', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-041-API: sdtNguoiToGiac sai định dạng [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-041', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-042-API: cmndNguoiToGiac không đủ 9/12 số [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-042', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-043-API: investigatorId không thuộc đơn vị [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400/403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-043', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-044-API: phuongThucTiepNhan enum không hợp lệ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-044', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-045-API: loaiKetQua không thuộc Wireframe 5 [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-045', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-046-API: Update khi đã soft-delete [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Update | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-046', _module: 'Incidents.Update' },
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-047-API: Transfer thiếu chuyenDenDonVi [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Transfer | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-047', _module: 'Incidents.Transfer' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-048-API: GET không JWT [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Auth | Expected: 401
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-049-API: JWT expired [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Auth | Expected: 401
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-050-API: User không có permission write:Incident [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Auth | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-051-API: sourcePetitionId thuộc Petition không trong scope [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400/403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-051', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-052-API: Gia hạn với newDeadline < deadline hiện tại [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Extend | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-052', _module: 'Incidents.Extend' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-053-API: limit > 100 [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-054-API: phase không hợp lệ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 400 hoặc bỏ qua
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-055-API: sortBy injection [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 400 whitelist
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-056-API: Reason xóa < 10 ký tự [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-057-API: Bulk delete vượt rate 5/min [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Bulk | Expected: 429
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([429, 200, 201]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-058-API: Export ward không thuộc user [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Export | Expected: 200 nhưng data rỗng / 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-059-API: Status string sai chính tả [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-059', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-060-API: Linkable trả incident của team khác [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Linkable | Expected: 200 chỉ incident scope A
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-061-API: diaChiXayRa > 500 ký tự [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-061', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-062-API: Body JSON malformed [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-062', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-063-API: Status chuyển từ TAM_DINH_CHI sang DANG_XAC_MINH (không qua PHUC_HOI) [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-063', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-064-API: name = 5 ký tự (min) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-064', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-065-API: name = 4 ký tự (min-1) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-065', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-066-API: name = 255 ký tự (max) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-066', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-067-API: Số lần gia hạn = 2 (max) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Extend | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-067', _module: 'Incidents.Extend' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-068-API: Created cách đây đúng 72h (boundary) [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 200 hoặc 422 tùy chính sách
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-069-API: Created cách đây 72h+1s [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Delete | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-070-API: limit=1 [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200 1 row
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-071-API: limit=100 [@BOUNDARY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-072-API: VALID_TRANSITIONS — phủ tất cả 14 status start [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 200 cho hợp lệ
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-073-API: lyDoKhongKhoiTo — 7 enum giá trị [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-073', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-074-API: phuongThucTiepNhan — 5 enum TT28 [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-074', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-075-API: 4 phase × empty/filled scope [@EP @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-076-API: TIEP_NHAN → DA_PHAN_CONG (skip xác minh) [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-077-API: DA_PHAN_CONG → CHUYEN_XPHC [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-078-API: DANG_XAC_MINH → TAM_DINH_CHI có lyDoTamDinhChiVuViec [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-079-API: TAM_DINH_CHI → PHUC_HOI_NGUON_TIN [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-080-API: PHUC_HOI_NGUON_TIN → DANG_XAC_MINH [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 200
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-081-API: QUA_HAN auto-set khi deadline < now và status active [@STATE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-082-API: TDC_HET_THOI_HIEU là terminal [@STATE @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-083-API: DA_NHAP_VU_KHAC sau merge — không edit [@STATE @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.State | Expected: 422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-084-API: Phase mapping cho từng status [@DECISION @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Decision | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-085-API: DataScope rules — dispatcher/ĐTV/ward [@DECISION @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Decision | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-086-API: SQL Injection trong search [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 200 không leak
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-087-API: XSS trong name + description [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-088-API: IDOR PATCH /:id/status incident khác team [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-089-API: Mass assignment createdById [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 201, server set actor thật
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-090-API: CSRF POST cross-origin [@SECURITY @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: CORS reject
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-091-API: Insecure direct file path trong export [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-092-API: Tampering Authorization header [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 401
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-093-API: Privilege escalation qua assign endpoint [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-094-API: Rate limit list endpoint [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 429
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([429, 200, 201]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-095-API: Sensitive PII leak — sdt + cmnd trong journey API [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 404/mask
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-096-API: Open redirect qua returnPath param [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-097-API: Tampering with expectedUpdatedAt to force overwrite [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 409
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-097', _module: 'Incidents.Security' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-098-API: Unicode + diacritics trong name [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Data | Expected: 201
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-099-API: Trim space leading/trailing [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Data | Expected: 201 trimmed
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-100-API: Date format trả về UTC+7 frontend [@DATA @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Data | Expected: deadline ISO
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-101-API: Empty string vs null phân biệt [@DATA @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.Data | Expected: 201 null hoặc empty đồng nhất
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-102-API: Email format trong contact field [@DATA @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.Data | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-103-API: 2 user concurrent prosecute cùng incident [@EDGE @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Race | Expected: 1 thành công, 1 409/422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-104-API: Merge khi target đang được edit [@EDGE @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Merge | Expected: 409
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-104', _module: 'Incidents.Merge' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-105-API: Stats khi DB rỗng [@EDGE @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200 mọi key=0
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-106-API: Form keyboard navigation [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-107-API: Label đầy đủ cho enum select [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-108-API: Contrast 14 status badge ≥ 4.5:1 [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-109-API: Confirm dialog merge có aria-modal [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-110-API: Error live region announce [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-111-API: Icon-only buttons có aria-label [@A11Y @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-112-API: Chrome 130 Win [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-113-API: Firefox 130 macOS [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-114-API: Edge 130 [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-115-API: Safari iOS [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-116-API: Mobile 375x667 [@COMPAT @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-117-API: Tablet 768x1024 [@COMPAT @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-118-API: List 1000 incident < 2s [@PERFORMANCE @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Perf | Expected: P95 < 2s
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-119-API: 50 concurrent stats [@PERFORMANCE @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Perf | Expected: P95 < 1s
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-120-API: Export ward 2000 record [@PERFORMANCE @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.Perf | Expected: 200, < 30s
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-121-API: Assign → SSE notification investigator [@INTEGRATION @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Notify | Expected: SSE event INCIDENT_ASSIGNED
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-122-API: Escalate ward → non-ward emit event INCIDENT_ESCALATED_FROM_WARD [@INTEGRATION @P0]', async ({ request, baseURL }) => {
    // Module: Incidents.Notify | Expected: Event emitted
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-123-API: Journey combine statusHistory + AuditLog [@INTEGRATION @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Journey | Expected: 200 events sorted
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-124-API: GET /:id incident đã soft-delete bằng user thường [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 404
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-125-API: Prosecute với prosecutionDecision không hợp lệ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Prosecute | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-125', _module: 'Incidents.Prosecute' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-126-API: Bulk delete ids gồm incident terminal [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Bulk | Expected: 422/partial
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.delete(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([422, 400]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-127-API: Body có field không tồn tại — strict mode [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400 hoặc bỏ qua
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-127', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-128-API: Merge khi A và B khác đơn vị [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Merge | Expected: 403/422
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-128', _module: 'Incidents.Merge' },
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-129-API: Search SQL keyword UNION [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 200 không leak
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-130-API: Insufficient logging — không log failed assign attempt [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: 403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([401, 403, 404]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-131-API: Tab order trong form Phase 4 sections logic [@A11Y @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-132-API: Android Chrome 5’’ viewport [@COMPAT @P2]', async ({ request, baseURL }) => {
    // Module: Incidents.UI | Expected: 
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-133-API: deadline trong quá khứ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400 hoặc flag overdue
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-133', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-134-API: Filter trangThaiThoiHieu enum không hợp lệ [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Read | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-135-API: Đổi status qua PUT thay vì PATCH /status [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Update | Expected: 200 ignore status field hoặc 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-135', _module: 'Incidents.Update' },
      failOnStatusCode: false,
    });
    expect([200, 201, 400, 401, 403, 404, 409, 422]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-136-API: Time-based timing attack — phân biệt 404 vs 403 [@SECURITY @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Security | Expected: Cả 2 ≈ 404 same timing
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.get(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      
      failOnStatusCode: false,
    });
    expect([404, 403]).toContain(response.status());
    expect(response.headers()).toBeTruthy();
  });


  test('TC-137-API: Tạo incident với canBoNhapId user khác đơn vị [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Create | Expected: 400/403
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.post(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-137', _module: 'Incidents.Create' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });


  test('TC-138-API: PATCH /status request body rỗng [@RED @P1]', async ({ request, baseURL }) => {
    // Module: Incidents.Status | Expected: 400
    const url = `${baseURL ?? 'http://localhost:3000'}/api/v1/incidents`;
    const response = await request.put(url, {
      headers: { Authorization: `Bearer ${process.env.UAT_TOKEN || 'mock-token'}` },
      data: { _tc: 'TC-138', _module: 'Incidents.Status' },
      failOnStatusCode: false,
    });
    expect(response.status()).toBeGreaterThanOrEqual(400);
    expect(response.headers()).toBeTruthy();
  });

});