// Auto-generated by scripts/uat_spec_generator.py — uat-test-runner
// Feature: cases | Source: docs/uat/cases/uat.json
// Layer: UI E2E Chromium (Page Object Model, ≥3 UI assertion/TC)
// Total TC: 130

import { test, expect } from '@playwright/test';
import { CasesPage } from '../pages/CasesPage';
import { loginToPage } from '../helpers/auth';

test.describe('CASES — UAT E2E layer', () => {
  test('TC-CASE-001-E2E: [P0] Tạo vụ án DIRECT_DISCOVERY hợp lệ', async ({ page }) => {
    // Pre: Đăng nhập role INVESTIGATOR có write/Case
    // Steps: 1. POST body {name:\'Vụ án trộm cắp tài sản\', caseProvenance:\'DIRECT_DISCOVERY\', crime:\'138 BLHS\', capDoToiPham:\'NGHIEM_TRONG\'}
    // Expected: HTTP 201, response.id≠null, status=TIEP_NHAN, createdById=user, audit_log có entry CREATE
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-002-E2E: [P0] Tạo vụ án FROM_PETITION với linkedPetitionId + expectedPetitionUpdatedAt hợp lệ', async ({ page }) => {
    // Pre: Có petition.id={{petition_id}} với updatedAt={{petition_updated_at}}
    // Steps: 1. POST {name:\'VA từ đơn thư\', caseProvenance:\'FROM_PETITION\', linkedPetitionId:\'{{petition_id}}\', expectedPetitionUpdatedAt:\'{{petition_updated_at}}\'}
    // Expected: HTTP 201, response.linkedPetitionId set, petition.linkedCaseId được update
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-003-E2E: [P0] Tạo vụ án FROM_INCIDENT với linkedIncidentId hợp lệ', async ({ page }) => {
    // Pre: Có incident.id={{incident_id}}
    // Steps: 1. POST {name:\'VA từ vụ việc\', caseProvenance:\'FROM_INCIDENT\', linkedIncidentId:\'{{incident_id}}\', expectedIncidentUpdatedAt:\'{{incident_updated_at}}\'}
    // Expected: HTTP 201, incident.linkedCaseId set
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-004-E2E: [P0] Tạo vụ án với subjects[] inline (atomic)', async ({ page }) => {
    // Pre: Đăng nhập + có crimeId={{crime_id}}
    // Steps: 1. POST {name, caseProvenance:\'DIRECT_DISCOVERY\', subjects:[{fullName:\'Nguyễn Văn A\', dateOfBirth:\'1990-01-01\', idNumber:\'001090012345\', address:\'Số 1 Trần Phú\', crimeId:\'{{crime_id}}\'}]}
    // Expected: HTTP 201, response.subjects[0].id≠null, subjects được create trong cùng transaction
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-005-E2E: [P0] Tạo vụ án với evidences[] inline', async ({ page }) => {
    // Pre: Đăng nhập
    // Steps: 1. POST {name, caseProvenance:\'DIRECT_DISCOVERY\', evidences:[{code:\'VC-001\', name:\'Điện thoại iPhone\', quantity:1, unit:\'cái\'}]}
    // Expected: HTTP 201, response.evidences[0].id≠null
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-006-E2E: [P0] WARD_OFFICER tạo vụ án — assignedTeamId auto-set từ dataScope', async ({ page }) => {
    // Pre: Đăng nhập WARD_OFFICER có ward team assignment
    // Steps: 1. POST {name, caseProvenance:\'DIRECT_DISCOVERY\'} KHÔNG truyền assignedTeamId
    // Expected: HTTP 201, response.assignedTeamId=team của ward officer
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-007-E2E: [P0] Liệt kê vụ án mặc định trang 1 limit 20', async ({ page }) => {
    // Pre: DB có ≥20 cases trong scope user
    // Steps: 1. GET /api/v1/cases
    // Expected: HTTP 200, response.items.length=20, response.total≥20, sorted createdAt desc
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-008-E2E: [P0] Lấy stats counts by status', async ({ page }) => {
    // Pre: DB có cases với 10 status khác nhau
    // Steps: 1. GET /api/v1/cases/stats
    // Expected: HTTP 200, response.byStatus có đủ 10 key (TIEP_NHAN..DA_LUU_TRU) với count≥0
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-009-E2E: [P0] Xem chi tiết vụ án thuộc scope', async ({ page }) => {
    // Pre: Có case.id={{case_id}} trong scope user
    // Steps: 1. GET /api/v1/cases/{{case_id}}
    // Expected: HTTP 200, response.id={{case_id}}, đủ field tdc/utdt/relations
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-010-E2E: [P0] Cập nhật vụ án (đổi status từ TIEP_NHAN sang DANG_XAC_MINH)', async ({ page }) => {
    // Pre: Có case status=TIEP_NHAN
    // Steps: 1. PUT /api/v1/cases/{{case_id}} body {status:\'DANG_XAC_MINH\', expectedUpdatedAt:\'{{updated_at}}\'}
    // Expected: HTTP 200, status=DANG_XAC_MINH, status_history thêm entry
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-011-E2E: [P0] Xóa mềm vụ án với reason hợp lệ', async ({ page }) => {
    // Pre: Là creator của case, chưa linked Petition/Incident
    // Steps: 1. DELETE /api/v1/cases/{{case_id}} body {reason:\'Lý do xóa hợp lệ 10+ ký tự\'}
    // Expected: HTTP 200, case.deletedAt≠null, audit log DELETE
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-012-E2E: [P1] ADMIN khôi phục vụ án đã xóa mềm', async ({ page }) => {
    // Pre: Có case.deletedAt≠null
    // Steps: 1. POST /api/v1/cases/{{case_id}}/restore body {reason:\'Khôi phục theo yêu cầu cấp trên\'}
    // Expected: HTTP 200, case.deletedAt=null, audit log RESTORE
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-013-E2E: [P1] DISPATCHER phân công ĐTV', async ({ page }) => {
    // Pre: Đăng nhập role có canDispatch=true
    // Steps: 1. PATCH /api/v1/cases/{{case_id}}/assign body {investigatorId:\'{{inv_id}}\'}
    // Expected: HTTP 200, case.investigatorId set, notification gửi investigator
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-014-E2E: [P1] Xem hành trình hồ sơ vụ án', async ({ page }) => {
    // Pre: Có case với ≥3 events (create, assign, update)
    // Steps: 1. GET /api/v1/cases/{{case_id}}/journey
    // Expected: HTTP 200, response.events≥3, sorted timestamp desc, pagination meta
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-015-E2E: [P1] Export Excel theo phường', async ({ page }) => {
    // Pre: DB có cases với subjects.wardId
    // Steps: 1. GET /api/v1/cases/export/ward?unitId={{unit_id}}&fromDate=2026-01-01&toDate=2026-12-31
    // Expected: HTTP 200, Content-Type application/vnd.openxmlformats…, Content-Disposition attachment, file size > 0
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-016-E2E: [P1] Cập nhật TĐC (TAM_DINH_CHI) với lyDoTamDinhChiVuAn hợp lệ', async ({ page }) => {
    // Pre: Case status DANG_DIEU_TRA
    // Steps: 1. PUT body {status:\'TAM_DINH_CHI\', lyDoTamDinhChiVuAn:\'CHUA_XAC_DINH_BI_CAN\', soQuyetDinhTamDinhChi:\'QĐ-01/2026\', ngayTamDinhChi:\'2026-05-30\', expectedUpdatedAt}
    // Expected: HTTP 200, case.status=TAM_DINH_CHI, lyDoTamDinhChiVuAn lưu đúng enum
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-017-E2E: [P0] Thiếu caseProvenance → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name:\'Vụ án test\'} không có caseProvenance
    // Expected: HTTP 400, message khớp \'caseProvenance bắt buộc\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-018-E2E: [P0] Thiếu name → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {caseProvenance:\'DIRECT_DISCOVERY\'} không có name
    // Expected: HTTP 400, message \'Tên vụ án bắt buộc\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-019-E2E: [P0] name là whitespace-only (trim → empty) → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name:\'   \', caseProvenance:\'DIRECT_DISCOVERY\'}
    // Expected: HTTP 400, IsNotEmpty fail sau khi trim
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-020-E2E: [P0] caseProvenance=FROM_PETITION thiếu linkedPetitionId → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'FROM_PETITION\'} không có linkedPetitionId
    // Expected: HTTP 400, message \'linkedPetitionId required when caseProvenance is FROM_PETITION\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-021-E2E: [P0] caseProvenance=FROM_INCIDENT thiếu linkedIncidentId → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'FROM_INCIDENT\'}
    // Expected: HTTP 400, message \'linkedIncidentId required when caseProvenance is FROM_INCIDENT\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-022-E2E: [P0] caseProvenance không thuộc enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'INVALID_VALUE\'}
    // Expected: HTTP 400, IsEnum validator fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-023-E2E: [P0] FROM_PETITION với linkedPetitionId không tồn tại → 404/400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'FROM_PETITION\', linkedPetitionId:\'non-existent-uuid\', expectedPetitionUpdatedAt:\'2026-01-01T00:00:00Z\'}
    // Expected: HTTP 404 hoặc 400, không tạo orphan case
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-024-E2E: [P0] FROM_PETITION với expectedPetitionUpdatedAt stale → 409 optimistic lock', async ({ page }) => {
    // Pre: Petition.updatedAt mới hơn expected
    // Steps: 1. POST với expectedPetitionUpdatedAt là giá trị cũ trước khi Petition bị edit
    // Expected: HTTP 409 Conflict, message về optimistic lock
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-025-E2E: [P1] capDoToiPham không thuộc enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'DIRECT_DISCOVERY\', capDoToiPham:\'SUPER_HEAVY\'}
    // Expected: HTTP 400, message gợi ý IT_NGHIEM_TRONG/NGHIEM_TRONG/RAT_NGHIEM_TRONG/DAC_BIET_NGHIEM_TRONG
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-026-E2E: [P1] deadline không phải ISO8601 → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'DIRECT_DISCOVERY\', deadline:\'30/05/2026\'}
    // Expected: HTTP 400, IsDateString validator fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-027-E2E: [P0] DELETE thiếu body.reason → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. DELETE /api/v1/cases/{{case_id}} body {}
    // Expected: HTTP 400, message \'Lý do xóa bắt buộc\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-028-E2E: [P0] DELETE reason < 10 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. DELETE body {reason:\'Sai\'}
    // Expected: HTTP 400, message \'Lý do xóa phải có ít nhất 10 ký tự\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-029-E2E: [P0] DELETE bởi user không phải creator → 403', async ({ page }) => {
    // Pre: Case có createdById khác user hiện tại
    // Steps: 1. DELETE body {reason:\'Lý do hợp lệ test creator-only\'}
    // Expected: HTTP 403, message \'Chỉ creator mới được xóa\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-030-E2E: [P0] DELETE case có linkedPetition → 409', async ({ page }) => {
    // Pre: Case có linkedPetitionId set
    // Steps: 1. DELETE body {reason:\'…\'}
    // Expected: HTTP 409, message liên kết Đơn thư, gợi ý unlink trước
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-031-E2E: [P0] VIEWER không có write/Case → 403', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST /api/v1/cases {name, caseProvenance:\'DIRECT_DISCOVERY\'}
    // Expected: HTTP 403, ForbiddenException
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-032-E2E: [P0] Không có JWT (anonymous) → 401', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST không header Authorization
    // Expected: HTTP 401, Unauthorized
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-033-E2E: [P0] JWT expired → 401', async ({ page }) => {
    // Pre: Dùng token expired 24h trước
    // Steps: 1. POST với JWT expired
    // Expected: HTTP 401, token expired
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-034-E2E: [P0] Account locked → 401/403', async ({ page }) => {
    // Pre: User.lockedAt≠null
    // Steps: 1. POST với JWT của locked account
    // Expected: HTTP 401 hoặc 403, account locked
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-035-E2E: [P1] subjects[] vượt 100 phần tử → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST với subjects mảng 101 items
    // Expected: HTTP 400, \'subjects[] tối đa 100 đối tượng\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-036-E2E: [P1] evidences[] vượt 100 phần tử → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST với evidences mảng 101 items
    // Expected: HTTP 400, \'evidences[] tối đa 100 vật chứng\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-037-E2E: [P1] documentIds[] vượt 50 phần tử → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST documentIds 51 items
    // Expected: HTTP 400, \'documentIds[] tối đa 50 file\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-038-E2E: [P0] subject thiếu crimeId → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST subjects:[{fullName,...}] không có crimeId
    // Expected: HTTP 400, \'crimeId required cho mỗi Subject\'
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-039-E2E: [P1] subject idNumber vượt 20 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST subject.idNumber 21 chars
    // Expected: HTTP 400, MaxLength fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-040-E2E: [P0] PUT vào case không thuộc scope → 403/404', async ({ page }) => {
    // Pre: Case thuộc team khác
    // Steps: 1. PUT /api/v1/cases/{{other_team_case_id}}
    // Expected: HTTP 403/404, scope filter chặn
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-041-E2E: [P0] PUT với expectedUpdatedAt stale → 409 optimistic lock', async ({ page }) => {
    // Pre: Case đã bị edit bởi user khác
    // Steps: 1. PUT với expectedUpdatedAt cũ
    // Expected: HTTP 409, message optimistic lock conflict
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-042-E2E: [P1] PUT lyDoTamDinhChiVuAn ngoài enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. PUT body {lyDoTamDinhChiVuAn:\'INVALID\'}
    // Expected: HTTP 400, message BLTTHS Điều 229
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-043-E2E: [P1] PUT ketQuaPhucHoiVuAn ngoài enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. PUT {ketQuaPhucHoiVuAn:\'WRONG\'}
    // Expected: HTTP 400, IsEnum fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-044-E2E: [P0] INVESTIGATOR (không DispatchGuard) gọi assign → 403', async ({ page }) => {
    // Pre: -
    // Steps: 1. PATCH /assign body {investigatorId}
    // Expected: HTTP 403, DispatchGuard fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-045-E2E: [P0] GET case không tồn tại → 404', async ({ page }) => {
    // Pre: -
    // Steps: 1. GET /api/v1/cases/00000000-0000-0000-0000-000000000000
    // Expected: HTTP 404, NotFoundException
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-046-E2E: [P1] Query status không thuộc enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. GET /api/v1/cases?status=INVALID
    // Expected: HTTP 400, ValidationPipe whitelist
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-047-E2E: [P1] Query search > 200 chars → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. GET ?search=<chuỗi 201 ký tự>
    // Expected: HTTP 400, MaxLength fail (bảo vệ JSONB ILIKE)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-048-E2E: [P2] Query limit > 100 → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. GET ?limit=200
    // Expected: HTTP 400, IsInt Max 100
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-049-E2E: [P1] name = 500 ký tự (max) → 201', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST name length=500
    // Expected: HTTP 201
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-050-E2E: [P1] name = 501 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST name length=501
    // Expected: HTTP 400, MaxLength
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-051-E2E: [P1] crime = 255 ký tự (max) → 201', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST crime length=255
    // Expected: HTTP 201
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-052-E2E: [P1] sourceDocumentNote = 1000 ký tự (max) → 201', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST sourceDocumentNote length=1000
    // Expected: HTTP 201
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-053-E2E: [P1] sourceDocumentNote = 1001 → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST sourceDocumentNote length=1001
    // Expected: HTTP 400
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-054-E2E: [P1] subjects = 100 items (max) → 201', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST subjects 100 items
    // Expected: HTTP 201, tất cả subjects được create
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-055-E2E: [P0] reason = 10 ký tự (min) → 200', async ({ page }) => {
    // Pre: -
    // Steps: 1. DELETE reason=\'1234567890\'
    // Expected: HTTP 200
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-056-E2E: [P0] reason = 500 ký tự (max) → 200', async ({ page }) => {
    // Pre: -
    // Steps: 1. DELETE reason 500 chars
    // Expected: HTTP 200
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-057-E2E: [P0] reason = 501 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: 1. DELETE reason length=501
    // Expected: HTTP 400, MaxLength 500
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-058-E2E: [P1] limit=200 (max clamp) → 200', async ({ page }) => {
    // Pre: -
    // Steps: 1. GET ?limit=500
    // Expected: HTTP 200, response.events.length≤200 (clamp)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-059-E2E: [P2] Tạo vụ án với metadata = {} rỗng', async ({ page }) => {
    // Pre: -
    // Steps: 1. POST {name, caseProvenance:\'DIRECT_DISCOVERY\', metadata:{}}
    // Expected: HTTP 201, metadata lưu {}
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-060-E2E: [P1] capDoToiPham đủ 4 partition (EP class hợp lệ)', async ({ page }) => {
    // Pre: -
    // Steps: Loop 4 giá trị IT_NGHIEM_TRONG, NGHIEM_TRONG, RAT_NGHIEM_TRONG, DAC_BIET_NGHIEM_TRONG
    // Expected: Mỗi giá trị → HTTP 201
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-061-E2E: [P1] caseProvenance đủ 8 partition', async ({ page }) => {
    // Pre: -
    // Steps: Loop 8 enum CaseProvenance, đảm bảo điều kiện kèm theo (FROM_PETITION/FROM_INCIDENT cần linked id; UY_THAC_DIEU_TRA cần UTDT fields)
    // Expected: Mỗi partition GREEN/RED đúng theo điều kiện
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-062-E2E: [P1] LyDoTamDinhChiVuAn đủ 8 partition', async ({ page }) => {
    // Pre: Case status DANG_DIEU_TRA
    // Steps: Loop 8 enum lyDo, mỗi lần PUT status=TAM_DINH_CHI
    // Expected: Mỗi enum value HTTP 200
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-063-E2E: [P1] KetQuaPhucHoiVuAn đủ 5 partition', async ({ page }) => {
    // Pre: Case status TAM_DINH_CHI, daRaSoat=true
    // Steps: Loop 5 enum
    // Expected: HTTP 200 mỗi value
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-064-E2E: [P1] Filter caseType=REGULAR vs UY_THAC_DIEU_TRA', async ({ page }) => {
    // Pre: DB có cả 2 loại
    // Steps: GET ?caseType=REGULAR rồi ?caseType=UY_THAC_DIEU_TRA
    // Expected: Mỗi truy vấn trả đúng loại, không lẫn
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-065-E2E: [P2] sortOrder ∈ {asc, desc} — partition', async ({ page }) => {
    // Pre: -
    // Steps: GET ?sortBy=createdAt&sortOrder=asc rồi desc
    // Expected: Order đảo đúng
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-066-E2E: [P1] overdue=true filter', async ({ page }) => {
    // Pre: DB có case deadline < now
    // Steps: GET ?overdue=true
    // Expected: Tất cả items có deadline < today
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-067-E2E: [P2] capDoToiPham partition vs status tương ứng', async ({ page }) => {
    // Pre: -
    // Steps: 4 case capDoToiPham × status mặc định TIEP_NHAN
    // Expected: HTTP 201 đủ 4, capDoToiPham lưu đúng
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-068-E2E: [P1] tdc-backfill chấp nhận 8 partition lyDoTamDinhChiVuAn', async ({ page }) => {
    // Pre: Case có status TAM_DINH_CHI nhưng lyDo trống
    // Steps: Loop 8 enum
    // Expected: HTTP 200 mỗi value
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-069-E2E: [P1] trangThaiPhanHoi 4 partition (cho UTDT cases)', async ({ page }) => {
    // Pre: DB có UTDT cases ở mỗi state
    // Steps: Loop 4 ?trangThaiPhanHoi
    // Expected: Mỗi truy vấn trả đúng tập
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-070-E2E: [P1] Query offset âm → 400', async ({ page }) => {
    // Pre: -
    // Steps: GET ?offset=-1
    // Expected: HTTP 400, Min 0
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-071-E2E: [P1] limit=0 → 400', async ({ page }) => {
    // Pre: -
    // Steps: GET ?limit=0
    // Expected: HTTP 400, Min 1
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-072-E2E: [P1] unknown field gửi qua (whitelist:true forbidNonWhitelisted) → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST {name, caseProvenance:\'DIRECT_DISCOVERY\', randomField:\'x\'}
    // Expected: HTTP 400, property randomField should not exist
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-073-E2E: [P0] ngayKhoiTo không đúng ISO8601 → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST {name, caseProvenance:\'DIRECT_DISCOVERY\', ngayKhoiTo:\'2026-13-99\'}
    // Expected: HTTP 400
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-074-E2E: [P0] FROM_INCIDENT với expectedIncidentUpdatedAt sai format → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST {caseProvenance:\'FROM_INCIDENT\', linkedIncidentId, expectedIncidentUpdatedAt:\'invalid\'}
    // Expected: HTTP 400, IsISO8601 fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-075-E2E: [P1] subject dateOfBirth sai format → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST subjects:[{dateOfBirth:\'invalid-date\',...}]
    // Expected: HTTP 400, IsDateString validator fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-076-E2E: [P1] evidence.quantity = 0 → 400 (Min 1)', async ({ page }) => {
    // Pre: -
    // Steps: POST evidences:[{code,name,quantity:0}]
    // Expected: HTTP 400, Min 1
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-077-E2E: [P1] evidence.quantity âm → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST quantity:-5
    // Expected: HTTP 400
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-078-E2E: [P0] DELETE case đã deletedAt≠null → 410 Gone hoặc 404', async ({ page }) => {
    // Pre: Case đã soft-deleted
    // Steps: DELETE lần 2
    // Expected: HTTP 410/404, không double-delete
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-079-E2E: [P0] Restore reason < 10 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST /restore body {reason:\'short\'}
    // Expected: HTTP 400
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-080-E2E: [P0] OFFICER không có restore permission → 403', async ({ page }) => {
    // Pre: -
    // Steps: POST /restore
    // Expected: HTTP 403
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-081-E2E: [P1] OFFICER gọi admin/deleted → 403', async ({ page }) => {
    // Pre: -
    // Steps: GET /admin/deleted
    // Expected: HTTP 403
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-082-E2E: [P0] IDOR — truy cập case team khác → 403/404', async ({ page }) => {
    // Pre: Case thuộc team B, officer thuộc team A
    // Steps: GET /api/v1/cases/{{teamB_case_id}}
    // Expected: HTTP 403/404, scope filter chặn, không leak data
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-083-E2E: [P0] SQL Injection trong name field', async ({ page }) => {
    // Pre: -
    // Steps: POST {name:'\'; DROP TABLE cases; --', caseProvenance:\'DIRECT_DISCOVERY\'}
    // Expected: HTTP 201, name lưu literal string (Prisma parameterize). KHÔNG table drop
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-084-E2E: [P0] XSS payload trong sourceDocumentNote', async ({ page }) => {
    // Pre: -
    // Steps: POST {sourceDocumentNote:\'<script>alert(1)</script>\', ...}
    // Expected: HTTP 201, lưu literal, khi render UI escape không execute
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-085-E2E: [P0] NoSQL/Prisma injection qua metadata JSONB', async ({ page }) => {
    // Pre: -
    // Steps: POST {metadata:{$ne:null}, ...}
    // Expected: HTTP 201, metadata lưu raw, không bypass
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-086-E2E: [P0] Mass assignment — gửi createdById, deletedAt', async ({ page }) => {
    // Pre: -
    // Steps: PUT {createdById:\'attacker_id\', deletedAt:null}
    // Expected: HTTP 400 (whitelist:true forbidNonWhitelisted) hoặc field bị strip
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-087-E2E: [P0] JWT tampering — đổi role claim trong JWT', async ({ page }) => {
    // Pre: -
    // Steps: GET với JWT có role=ADMIN giả mạo (chữ ký sai)
    // Expected: HTTP 401, signature verification fail
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-088-E2E: [P0] CSRF — DELETE không có Origin/Referer match', async ({ page }) => {
    // Pre: -
    // Steps: DELETE từ origin khác, không CSRF token
    // Expected: HTTP 403 hoặc 401 (CORS/CSRF guard)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-089-E2E: [P1] Path traversal trong search ?search=../../..etc/passwd', async ({ page }) => {
    // Pre: -
    // Steps: GET ?search=../../../etc/passwd
    // Expected: HTTP 200, ILIKE literal, không leak filesystem
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-090-E2E: [P0] Throttle export 5 req/60s', async ({ page }) => {
    // Pre: -
    // Steps: 6 lần GET export/ward trong 60s
    // Expected: 6th request HTTP 429 Too Many Requests
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-091-E2E: [P0] Concurrent create với cùng linkedPetitionId → 1 thành công 1 fail', async ({ page }) => {
    // Pre: Petition chưa linked
    // Steps: 2 request POST cùng linkedPetitionId song song
    // Expected: 1 HTTP 201, 1 HTTP 409 (unique constraint linkedPetitionId)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-092-E2E: [P1] PUT status=DA_LUU_TRU từ TIEP_NHAN (skip state) → 400', async ({ page }) => {
    // Pre: Case status TIEP_NHAN
    // Steps: PUT {status:\'DA_LUU_TRU\'}
    // Expected: HTTP 400, invalid state transition
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-093-E2E: [P1] page âm → 400 hoặc fallback 1', async ({ page }) => {
    // Pre: -
    // Steps: GET ?page=-5
    // Expected: HTTP 200 với page clamp 1 (Math.max(1,page))
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-094-E2E: [P1] Tạo case với deadline trong quá khứ → 400 hoặc warning', async ({ page }) => {
    // Pre: -
    // Steps: POST {name, caseProvenance:\'DIRECT_DISCOVERY\', deadline:\'2020-01-01\'}
    // Expected: HTTP 201 (validation chỉ check ISO8601) hoặc 400 nếu có business rule deadline > today
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-095-E2E: [P1] Tạo case với assignedTeamId không thuộc DB → 400/404', async ({ page }) => {
    // Pre: -
    // Steps: POST {assignedTeamId:\'00000000-…\'}
    // Expected: HTTP 400/404, FK Team không tồn tại
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-096-E2E: [P1] PUT thiếu expectedUpdatedAt (optimistic lock required) → 400', async ({ page }) => {
    // Pre: -
    // Steps: PUT body không có expectedUpdatedAt
    // Expected: HTTP 400 hoặc 200 (tuỳ enforcement) — verify service nếu reject
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-097-E2E: [P0] FROM_PETITION với petition đã có linkedCaseId → 409', async ({ page }) => {
    // Pre: Petition.linkedCaseId đã set (đã chuyển case khác)
    // Steps: POST {caseProvenance:\'FROM_PETITION\', linkedPetitionId:\'{{petition_id}}\'}
    // Expected: HTTP 409, message petition đã được liên kết
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-098-E2E: [P1] subject phone vượt 20 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST subject.phone=\'1\'.repeat(21)
    // Expected: HTTP 400
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-099-E2E: [P0] Transition đúng: TIEP_NHAN → DANG_XAC_MINH', async ({ page }) => {
    // Pre: Status TIEP_NHAN
    // Steps: PUT status=DANG_XAC_MINH
    // Expected: HTTP 200, status_history += entry
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-100-E2E: [P0] Transition: DANG_DIEU_TRA → TAM_DINH_CHI (kèm lyDo)', async ({ page }) => {
    // Pre: Status DANG_DIEU_TRA
    // Steps: PUT status=TAM_DINH_CHI + lyDoTamDinhChiVuAn
    // Expected: HTTP 200
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-101-E2E: [P0] Transition: TAM_DINH_CHI → DANG_DIEU_TRA (phục hồi)', async ({ page }) => {
    // Pre: Status TAM_DINH_CHI, daRaSoat=true, ketQuaPhucHoiVuAn=DANG_DIEU_TRA_XAC_MINH
    // Steps: PUT status=DANG_DIEU_TRA
    // Expected: HTTP 200, transition hợp lệ
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-102-E2E: [P1] Transition: DA_KET_LUAN → DANG_TRUY_TO', async ({ page }) => {
    // Pre: Status DA_KET_LUAN
    // Steps: PUT status=DANG_TRUY_TO
    // Expected: HTTP 200
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-103-E2E: [P0] Transition invalid: DA_LUU_TRU → DANG_XAC_MINH (final state) → 400', async ({ page }) => {
    // Pre: Status DA_LUU_TRU
    // Steps: PUT status=DANG_XAC_MINH
    // Expected: HTTP 400, terminal state
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-104-E2E: [P0] Decision table caseProvenance × linked_id_required matrix (8×2=16 cells)', async ({ page }) => {
    // Pre: -
    // Steps: Cover 16 cell — mỗi provenance × (có/không linkedXxxId)
    // Expected: FROM_PETITION/FROM_INCIDENT yêu cầu linked → 400 nếu missing; còn lại không yêu cầu → 201
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-105-E2E: [P0] Decision table TĐC: lyDo × status × daRaSoat', async ({ page }) => {
    // Pre: -
    // Steps: 4 combination key: (TAM_DINH_CHI + lyDo set + daRaSoat=false), (TAM_DINH_CHI + lyDo trống), (DANG_DIEU_TRA + lyDo set), (TAM_DINH_CHI + lyDo + daRaSoat=true + ketQua)
    // Expected: Combo 1 OK, 2 400, 3 OK (lyDo ignored), 4 OK với phục hồi
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-106-E2E: [P1] Decision table delete preconditions', async ({ page }) => {
    // Pre: -
    // Steps: 6 case: (creator + no_link), (non-creator + no_link), (creator + linked_petition), (creator + linked_incident), (admin override + linked), (already deleted)
    // Expected: Hành vi theo 6 business rules delete
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-107-E2E: [P1] Insecure deserialization — metadata field chứa __proto__', async ({ page }) => {
    // Pre: -
    // Steps: POST {metadata:{__proto__:{admin:true}}}
    // Expected: HTTP 201, không pollute prototype object server
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-108-E2E: [P0] Audit log integrity — verify createdBy, ipAddress, userAgent lưu đúng', async ({ page }) => {
    // Pre: -
    // Steps: POST + GET audit_log mới nhất
    // Expected: audit_log entry có userId=current, ipAddress=req.ip, userAgent header value
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-109-E2E: [P1] Journey không leak data ngoài scope', async ({ page }) => {
    // Pre: Case có cross-team activity
    // Steps: GET /journey
    // Expected: Chỉ trả events thuộc dataScope user, không leak event team khác
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-110-E2E: [P0] Rate limit per-user — chống mass create', async ({ page }) => {
    // Pre: -
    // Steps: 100 POST trong 60s từ 1 user
    // Expected: Sau N (theo throttler config) request 429
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/cases');
    expect(page.url()).toMatch(/\/cases|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-CASE-111-E2E: [P1] i18n — name chứa ký tự Unicode tiếng Việt có dấu', async ({ page }) => {
    // Pre: -
    // Steps: POST name=\'Vụ án trộm cắp tài sản — đối tượng Nguyễn Văn Đại\'
    // Expected: HTTP 201, lưu nguyên giá trị, GET trả lại đúng dấu
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-112-E2E: [P1] Emoji + special unicode trong name', async ({ page }) => {
    // Pre: -
    // Steps: POST name=\'Vụ án 🚓👮 đặc biệt\'
    // Expected: HTTP 201, GET trả lại emoji
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-113-E2E: [P1] Whitespace mixed (tab, nbsp) trong name — trim hoạt động', async ({ page }) => {
    // Pre: -
    // Steps: POST name=\'\\t  Tên vụ án  \\u00a0\'
    // Expected: HTTP 201, name lưu=\'Tên vụ án\' (trim cả nbsp nếu transform hỗ trợ; nếu không, vẫn pass với leading/trailing)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL chứa /cases').toContain('/cases');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-CASE-114-E2E: [P1] Trang list /cases — keyboard navigation Tab order', async ({ page }) => {
    // Pre: Login UI
    // Steps: 1. Open /cases 2. Tab qua từng element 3. Verify focus visible
    // Expected: Tab order logic: search → filter chips → table rows → pagination; mỗi focus có outline ring rõ
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-115-E2E: [P1] Form tạo vụ án — aria-label cho input bắt buộc', async ({ page }) => {
    // Pre: -
    // Steps: Inspect DOM form #case-create
    // Expected: Tất cả <input required> có aria-required=\'true\' + aria-label/aria-labelledby
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-116-E2E: [P1] Color contrast status badge ≥4.5:1', async ({ page }) => {
    // Pre: -
    // Steps: Render status chip mỗi state, đo contrast với background
    // Expected: Tất cả 10 status pair có ratio ≥4.5:1 (WCAG 2.1 AA)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-117-E2E: [P2] Detail page — H1 duy nhất, heading hierarchy hợp lệ', async ({ page }) => {
    // Pre: -
    // Steps: Inspect HTML headings
    // Expected: 1 <h1>, không skip level (h2 → h4 fail)
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-118-E2E: [P2] Form error message — aria-live region', async ({ page }) => {
    // Pre: -
    // Steps: Submit form thiếu name → error
    // Expected: Error hiển thị trong <div role=\'alert\' aria-live=\'polite\'> để screen reader đọc
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-119-E2E: [P1] Table — th có scope=\'col\', row có scope=\'row\'', async ({ page }) => {
    // Pre: -
    // Steps: Inspect table headings
    // Expected: <th scope=\'col\'> trên header, screen reader phát đúng cột
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-120-E2E: [P2] Skip-to-content link', async ({ page }) => {
    // Pre: -
    // Steps: Tab đầu tiên trên page
    // Expected: Skip link visible khi focus, click bỏ qua sidebar
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/cases');
    expect(page.url(), 'URL phải chứa /cases').toContain('/cases');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-CASE-121-E2E: [P1] Chromium 120+ desktop 1920×1080 render', async ({ page }) => {
    // Pre: -
    // Steps: Open /cases trên Chromium
    // Expected: Layout không vỡ, table sticky header, sidebar fixed
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-122-E2E: [P1] Firefox 121+ desktop render', async ({ page }) => {
    // Pre: -
    // Steps: Open /cases trên Firefox
    // Expected: Layout đồng nhất, không CSS-specific Chrome bị break
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-123-E2E: [P1] Safari/WebKit 17+ desktop', async ({ page }) => {
    // Pre: -
    // Steps: Open /cases trên WebKit
    // Expected: Date input picker render, gap không vỡ
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-124-E2E: [P0] Mobile 375×667 (iPhone SE) — drawer sidebar PWA', async ({ page }) => {
    // Pre: -
    // Steps: Set viewport 375×667
    // Expected: Sidebar collapse drawer, table horizontal scroll, no overflow
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await page.setViewportSize({ width: 375, height: 667 });
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-125-E2E: [P1] Tablet 768×1024 (iPad)', async ({ page }) => {
    // Pre: -
    // Steps: Viewport 768
    // Expected: Sidebar visible, table giữ 6 cột chính
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await page.setViewportSize({ width: 768, height: 1024 });
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-126-E2E: [P2] Print stylesheet — print preview rõ ràng', async ({ page }) => {
    // Pre: -
    // Steps: Ctrl+P trên detail page
    // Expected: Print view ẩn sidebar, header gọn, không cắt bảng
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-127-E2E: [P2] Dark mode (prefers-color-scheme: dark) — contrast giữ AA', async ({ page }) => {
    // Pre: OS dark mode
    // Steps: Open /cases
    // Expected: Toàn bộ UI có dark variant, không trắng trên trắng
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/cases');
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Quản lý vụ án|Danh sách vụ án|Vụ án/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-CASE-128-E2E: [P0] List 10k cases — p95 < 800ms', async ({ page }) => {
    // Pre: DB seed 10000 cases
    // Steps: 100 GET ?limit=20 concurrent
    // Expected: p95 latency < 800ms, không OOM
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    const start = Date.now();
    await loginToPage(page, '/cases');
    const elapsed = Date.now() - start;
    expect(elapsed, `Page load ${elapsed}ms phải < 5000ms`).toBeLessThan(5000);
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
  });
  test('TC-CASE-129-E2E: [P0] Stats aggregation trên 10k cases < 500ms', async ({ page }) => {
    // Pre: -
    // Steps: 50 GET /stats concurrent
    // Expected: p95 < 500ms, query plan dùng index status
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    const start = Date.now();
    await loginToPage(page, '/cases');
    const elapsed = Date.now() - start;
    expect(elapsed, `Page load ${elapsed}ms phải < 5000ms`).toBeLessThan(5000);
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
  });
  test('TC-CASE-130-E2E: [P0] Export Excel 5000 rows < 10s', async ({ page }) => {
    // Pre: -
    // Steps: GET export/ward fromDate=…toDate=cover 5000 cases
    // Expected: Response < 10s, file size hợp lý, không timeout
    const casesPage = new CasesPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    const start = Date.now();
    await loginToPage(page, '/cases');
    const elapsed = Date.now() - start;
    expect(elapsed, `Page load ${elapsed}ms phải < 5000ms`).toBeLessThan(5000);
    expect(page.url()).toContain('/cases');
    await expect(page.locator('body')).toBeVisible();
  });

});
