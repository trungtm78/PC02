// Quản lý Vụ việc (Incidents) — E2E UI layer (Layer 2)
// Auto-generated by gen_uat_specs.py
import { test, expect } from '@playwright/test';

test.describe('UAT E2E — Quản lý Vụ việc (Incidents)', () => {
  test.beforeEach(async ({ page }) => {
    // Login skeleton — adapt cho project khi expand
    // await page.goto('/login');
  });


  test('TC-001-E2E: Tạo vụ việc mới hợp lệ với name + loaiDonVu + nguonPhatTin [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-002-E2E: Auto-gen code VV-YYYY-NNN [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-003-E2E: Auto-calc deadline theo THOI_HAN_XAC_MINH rule [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-004-E2E: Ward officer tự gán assignedTeamId từ wardTeamId [@GREEN @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-005-E2E: List vụ việc default pagination [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-006-E2E: Filter theo phase=xac-minh [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-007-E2E: GET /incidents/linkable cho Case picker [@GREEN @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-008-E2E: GET stats exhaustive trả về tất cả status keys [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-009-E2E: PUT update vụ việc với expectedUpdatedAt [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Update
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-010-E2E: PATCH status TIEP_NHAN → DANG_XAC_MINH [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-011-E2E: PATCH KHONG_KHOI_TO kèm lyDoKhongKhoiTo (Đ.157) [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-012-E2E: Khởi tố vụ việc → tạo Case (Branch-3) [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Prosecute
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-013-E2E: Gia hạn lần 1 trong giới hạn [@GREEN @P1]', async ({ page }) => {
    // Module: Incidents.Extend
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-014-E2E: Dispatcher assign investigator → status DANG_XAC_MINH [@GREEN @P1]', async ({ page }) => {
    // Module: Incidents.Assign
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-015-E2E: Gộp vụ việc A vào B [@GREEN @P1]', async ({ page }) => {
    // Module: Incidents.Merge
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-016-E2E: Chuyển đơn vị [@GREEN @P1]', async ({ page }) => {
    // Module: Incidents.Transfer
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-017-E2E: Soft-delete TIEP_NHAN bởi creator trong 72h [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-018-E2E: ADMIN restore [@GREEN @P0]', async ({ page }) => {
    // Module: Incidents.Restore
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-019-E2E: name < 5 ký tự [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-020-E2E: nguonPhatTin không khớp loaiDonVu [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-021-E2E: Transition không nằm trong VALID_TRANSITIONS [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-022-E2E: KHONG_KHOI_TO thiếu lyDoKhongKhoiTo [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-023-E2E: Chuyển từ terminal status (DA_GIAI_QUYET) [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-024-E2E: Gia hạn quá maxExtensionsSnapshot (lần 3) [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Extend
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-025-E2E: Xóa có linked petitions [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-026-E2E: Xóa có attached documents [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-027-E2E: Xóa khi status != TIEP_NHAN [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-028-E2E: Quá 72h kể từ created [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-029-E2E: Non-creator non-admin xóa [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-030-E2E: Non-dispatcher assign [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Assign
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-031-E2E: Prosecute thiếu caseName [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Prosecute
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-032-E2E: Prosecute incident đã DA_CHUYEN_VU_AN [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Prosecute
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-033-E2E: Merge vào chính nó (self) [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Merge
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-034-E2E: Merge vào incident terminal [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Merge
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-035-E2E: User ĐTV Team A xem incident Team B (DataScope) [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-036-E2E: Update với expectedUpdatedAt stale [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Update
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-037-E2E: Non-ADMIN restore [@RED @P0]', async ({ page }) => {
    // Module: Incidents.Restore
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-038-E2E: Stats không strip status filter [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-039-E2E: fromDate > toDate [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-040-E2E: name > 255 ký tự [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-041-E2E: sdtNguoiToGiac sai định dạng [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-042-E2E: cmndNguoiToGiac không đủ 9/12 số [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-043-E2E: investigatorId không thuộc đơn vị [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-044-E2E: phuongThucTiepNhan enum không hợp lệ [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-045-E2E: loaiKetQua không thuộc Wireframe 5 [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-046-E2E: Update khi đã soft-delete [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Update
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-047-E2E: Transfer thiếu chuyenDenDonVi [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Transfer
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-048-E2E: GET không JWT [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Auth
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-049-E2E: JWT expired [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Auth
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-050-E2E: User không có permission write:Incident [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Auth
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-051-E2E: sourcePetitionId thuộc Petition không trong scope [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-052-E2E: Gia hạn với newDeadline < deadline hiện tại [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Extend
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-053-E2E: limit > 100 [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-054-E2E: phase không hợp lệ [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-055-E2E: sortBy injection [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-056-E2E: Reason xóa < 10 ký tự [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-057-E2E: Bulk delete vượt rate 5/min [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Bulk
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-058-E2E: Export ward không thuộc user [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Export
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-059-E2E: Status string sai chính tả [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-060-E2E: Linkable trả incident của team khác [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Linkable
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-061-E2E: diaChiXayRa > 500 ký tự [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-062-E2E: Body JSON malformed [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-063-E2E: Status chuyển từ TAM_DINH_CHI sang DANG_XAC_MINH (không qua PHUC_HOI) [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-064-E2E: name = 5 ký tự (min) [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-065-E2E: name = 4 ký tự (min-1) [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-066-E2E: name = 255 ký tự (max) [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-067-E2E: Số lần gia hạn = 2 (max) [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Extend
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-068-E2E: Created cách đây đúng 72h (boundary) [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-069-E2E: Created cách đây 72h+1s [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Delete
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-070-E2E: limit=1 [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-071-E2E: limit=100 [@BOUNDARY @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-072-E2E: VALID_TRANSITIONS — phủ tất cả 14 status start [@EP @P1]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-073-E2E: lyDoKhongKhoiTo — 7 enum giá trị [@EP @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-074-E2E: phuongThucTiepNhan — 5 enum TT28 [@EP @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-075-E2E: 4 phase × empty/filled scope [@EP @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-076-E2E: TIEP_NHAN → DA_PHAN_CONG (skip xác minh) [@STATE @P0]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-077-E2E: DA_PHAN_CONG → CHUYEN_XPHC [@STATE @P0]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-078-E2E: DANG_XAC_MINH → TAM_DINH_CHI có lyDoTamDinhChiVuViec [@STATE @P0]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-079-E2E: TAM_DINH_CHI → PHUC_HOI_NGUON_TIN [@STATE @P0]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-080-E2E: PHUC_HOI_NGUON_TIN → DANG_XAC_MINH [@STATE @P0]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-081-E2E: QUA_HAN auto-set khi deadline < now và status active [@STATE @P0]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-082-E2E: TDC_HET_THOI_HIEU là terminal [@STATE @P1]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-083-E2E: DA_NHAP_VU_KHAC sau merge — không edit [@STATE @P1]', async ({ page }) => {
    // Module: Incidents.State
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-084-E2E: Phase mapping cho từng status [@DECISION @P0]', async ({ page }) => {
    // Module: Incidents.Decision
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-085-E2E: DataScope rules — dispatcher/ĐTV/ward [@DECISION @P0]', async ({ page }) => {
    // Module: Incidents.Decision
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-086-E2E: SQL Injection trong search [@SECURITY @P0]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-087-E2E: XSS trong name + description [@SECURITY @P0]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-088-E2E: IDOR PATCH /:id/status incident khác team [@SECURITY @P0]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-089-E2E: Mass assignment createdById [@SECURITY @P0]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-090-E2E: CSRF POST cross-origin [@SECURITY @P0]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-091-E2E: Insecure direct file path trong export [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-092-E2E: Tampering Authorization header [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-093-E2E: Privilege escalation qua assign endpoint [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-094-E2E: Rate limit list endpoint [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-095-E2E: Sensitive PII leak — sdt + cmnd trong journey API [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-096-E2E: Open redirect qua returnPath param [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-097-E2E: Tampering with expectedUpdatedAt to force overwrite [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-098-E2E: Unicode + diacritics trong name [@DATA @P1]', async ({ page }) => {
    // Module: Incidents.Data
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-099-E2E: Trim space leading/trailing [@DATA @P1]', async ({ page }) => {
    // Module: Incidents.Data
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-100-E2E: Date format trả về UTC+7 frontend [@DATA @P1]', async ({ page }) => {
    // Module: Incidents.Data
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-101-E2E: Empty string vs null phân biệt [@DATA @P2]', async ({ page }) => {
    // Module: Incidents.Data
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-102-E2E: Email format trong contact field [@DATA @P2]', async ({ page }) => {
    // Module: Incidents.Data
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-103-E2E: 2 user concurrent prosecute cùng incident [@EDGE @P1]', async ({ page }) => {
    // Module: Incidents.Race
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-104-E2E: Merge khi target đang được edit [@EDGE @P1]', async ({ page }) => {
    // Module: Incidents.Merge
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-105-E2E: Stats khi DB rỗng [@EDGE @P2]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-106-E2E: Form keyboard navigation [@A11Y @P1]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-107-E2E: Label đầy đủ cho enum select [@A11Y @P1]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-108-E2E: Contrast 14 status badge ≥ 4.5:1 [@A11Y @P1]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-109-E2E: Confirm dialog merge có aria-modal [@A11Y @P2]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-110-E2E: Error live region announce [@A11Y @P2]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-111-E2E: Icon-only buttons có aria-label [@A11Y @P2]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-112-E2E: Chrome 130 Win [@COMPAT @P1]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-113-E2E: Firefox 130 macOS [@COMPAT @P1]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-114-E2E: Edge 130 [@COMPAT @P1]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-115-E2E: Safari iOS [@COMPAT @P1]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-116-E2E: Mobile 375x667 [@COMPAT @P1]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-117-E2E: Tablet 768x1024 [@COMPAT @P2]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-118-E2E: List 1000 incident < 2s [@PERFORMANCE @P0]', async ({ page }) => {
    // PERFORMANCE test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual PERFORMANCE verification required');
  });


  test('TC-119-E2E: 50 concurrent stats [@PERFORMANCE @P1]', async ({ page }) => {
    // PERFORMANCE test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual PERFORMANCE verification required');
  });


  test('TC-120-E2E: Export ward 2000 record [@PERFORMANCE @P2]', async ({ page }) => {
    // PERFORMANCE test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual PERFORMANCE verification required');
  });


  test('TC-121-E2E: Assign → SSE notification investigator [@INTEGRATION @P0]', async ({ page }) => {
    // Module: Incidents.Notify
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-122-E2E: Escalate ward → non-ward emit event INCIDENT_ESCALATED_FROM_WARD [@INTEGRATION @P0]', async ({ page }) => {
    // Module: Incidents.Notify
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-123-E2E: Journey combine statusHistory + AuditLog [@INTEGRATION @P1]', async ({ page }) => {
    // Module: Incidents.Journey
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-124-E2E: GET /:id incident đã soft-delete bằng user thường [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-125-E2E: Prosecute với prosecutionDecision không hợp lệ [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Prosecute
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-126-E2E: Bulk delete ids gồm incident terminal [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Bulk
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-127-E2E: Body có field không tồn tại — strict mode [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-128-E2E: Merge khi A và B khác đơn vị [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Merge
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-129-E2E: Search SQL keyword UNION [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-130-E2E: Insufficient logging — không log failed assign attempt [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-131-E2E: Tab order trong form Phase 4 sections logic [@A11Y @P1]', async ({ page }) => {
    // A11Y test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual A11Y verification required');
  });


  test('TC-132-E2E: Android Chrome 5’’ viewport [@COMPAT @P2]', async ({ page }) => {
    // COMPAT test — kiểm thủ công (axe-core / lighthouse / k6)
    test.skip(true, 'Manual COMPAT verification required');
  });


  test('TC-133-E2E: deadline trong quá khứ [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-134-E2E: Filter trangThaiThoiHieu enum không hợp lệ [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Read
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-135-E2E: Đổi status qua PUT thay vì PATCH /status [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Update
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-136-E2E: Time-based timing attack — phân biệt 404 vs 403 [@SECURITY @P1]', async ({ page }) => {
    // Module: Incidents.Security
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-137-E2E: Tạo incident với canBoNhapId user khác đơn vị [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Create
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });


  test('TC-138-E2E: PATCH /status request body rỗng [@RED @P1]', async ({ page }) => {
    // Module: Incidents.Status
    await page.goto('/incidents', { waitUntil: 'domcontentloaded' });
    // 3 UI assertions: URL + body visible + có content
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });

});