// Auto-generated by scripts/uat_spec_generator.py — uat-test-runner
// Feature: petitions | Source: docs/uat/petitions/uat.json
// Layer: UI E2E Chromium (Page Object Model, ≥3 UI assertion/TC)
// Total TC: 120

import { test, expect } from '@playwright/test';
import { PetitionsPage } from '../pages/PetitionsPage';
import { loginToPage } from '../helpers/auth';

test.describe('PETITIONS — UAT E2E layer', () => {
  test('TC-PET-001-E2E: [P0] Tạo đơn tố cáo với senderName + receivedDate + petitionType=TO_CAO', async ({ page }) => {
    // Pre: -
    // Steps: POST {senderName:\'Nguyễn Văn A\', receivedDate:\'2026-05-30\', petitionType:\'TO_CAO\'}
    // Expected: HTTP 201, response.id≠null, stt format DT-2026-NNNNN auto-gen, status=MOI_TIEP_NHAN
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-002-E2E: [P0] Tạo đơn KHIEU_NAI', async ({ page }) => {
    // Pre: -
    // Steps: POST {senderName, receivedDate, petitionType:\'KHIEU_NAI\'}
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-003-E2E: [P0] Tạo đơn KIEN_NGHI', async ({ page }) => {
    // Pre: -
    // Steps: POST petitionType=KIEN_NGHI
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-004-E2E: [P0] Tạo đơn PHAN_ANH', async ({ page }) => {
    // Pre: -
    // Steps: POST petitionType=PHAN_ANH
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-005-E2E: [P0] Tạo đơn với stt tự cung cấp (override engine)', async ({ page }) => {
    // Pre: -
    // Steps: POST {stt:\'DT-2026-99999\', senderName, receivedDate, petitionType:\'TO_CAO\'}
    // Expected: HTTP 201, stt=\'DT-2026-99999\'
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-006-E2E: [P0] Tạo đơn đủ thông tin sender (phone, email, address, birthYear)', async ({ page }) => {
    // Pre: -
    // Steps: POST đủ field sender
    // Expected: HTTP 201, sender info lưu đúng
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-007-E2E: [P0] List petitions default pagination', async ({ page }) => {
    // Pre: DB ≥20 đơn
    // Steps: GET /api/v1/petitions
    // Expected: HTTP 200, items.length≤20, sorted receivedDate desc
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-008-E2E: [P0] Stats 7 PetitionStatus', async ({ page }) => {
    // Pre: -
    // Steps: GET /stats
    // Expected: HTTP 200, byStatus đủ 7 key
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-009-E2E: [P0] Xem chi tiết đơn', async ({ page }) => {
    // Pre: Có petition
    // Steps: GET /api/v1/petitions/{{petition_id}}
    // Expected: HTTP 200, đủ field
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-010-E2E: [P0] Cập nhật nội dung đơn', async ({ page }) => {
    // Pre: -
    // Steps: PUT {summary:\'Tóm tắt cập nhật\'}
    // Expected: HTTP 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-011-E2E: [P0] Convert đơn thành Vụ việc (atomic)', async ({ page }) => {
    // Pre: Petition đang DANG_XU_LY
    // Steps: POST /convert-incident body {incidentName, loaiDonVu, nguonPhatTin}
    // Expected: HTTP 200/201, petition.linkedIncidentId set, petition.status=DA_CHUYEN_VU_VIEC, Incident mới tạo
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-012-E2E: [P0] Convert đơn thành Vụ án (atomic)', async ({ page }) => {
    // Pre: -
    // Steps: POST /convert-case body {caseName, caseProvenance:\'FROM_PETITION\'}
    // Expected: HTTP 200/201, petition.linkedCaseId set, status=DA_CHUYEN_VU_AN
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-013-E2E: [P0] Soft delete đơn', async ({ page }) => {
    // Pre: Chưa convert
    // Steps: DELETE body {reason:\'Lý do hợp lệ\'}
    // Expected: HTTP 200, deletedAt set
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-014-E2E: [P1] Phân công xử lý đơn', async ({ page }) => {
    // Pre: -
    // Steps: PATCH /assign body {assignedToId:\'{{officer_id}}\'}
    // Expected: HTTP 200, assignedToId set
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-015-E2E: [P1] Export PHIEU_DE_XUAT docx', async ({ page }) => {
    // Pre: Petition có đủ field business
    // Steps: GET /export-document?docType=PHIEU_DE_XUAT
    // Expected: HTTP 200, Content-Type docx, file size > 0
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-016-E2E: [P1] Batch export 3 đơn → ZIP', async ({ page }) => {
    // Pre: -
    // Steps: POST {petitionIds:[\'{{p1}}\',\'{{p2}}\',\'{{p3}}\'], docType:\'PHIEU_DE_XUAT\'}
    // Expected: HTTP 200, Content-Type zip, manifest.json bên trong
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-017-E2E: [P0] Thiếu senderName → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST không senderName
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-018-E2E: [P0] Thiếu receivedDate → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST không receivedDate
    // Expected: HTTP 400, IsDateString fail
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-019-E2E: [P0] Thiếu petitionType → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST không petitionType
    // Expected: HTTP 400, \'Loại đơn thư là bắt buộc\'
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-020-E2E: [P0] petitionType ngoài enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST petitionType=\'INVALID\'
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-021-E2E: [P0] senderEmail không hợp lệ → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST senderEmail=\'not-an-email\'
    // Expected: HTTP 400, \'Email không đúng định dạng\'
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-022-E2E: [P0] senderPhone chứa chữ cái → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST senderPhone=\'abc123\'
    // Expected: HTTP 400, \'Số điện thoại không hợp lệ\'
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-023-E2E: [P0] Không JWT → 401', async ({ page }) => {
    // Pre: -
    // Steps: POST không Authorization
    // Expected: HTTP 401
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-024-E2E: [P0] Convert đơn đã DA_CHUYEN_VU_VIEC → 409', async ({ page }) => {
    // Pre: Petition đã chuyển VV
    // Steps: POST /convert-incident lần 2
    // Expected: HTTP 409
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-025-E2E: [P0] Convert đơn đã DA_CHUYEN_VU_AN → 409', async ({ page }) => {
    // Pre: -
    // Steps: POST /convert-case lần 2
    // Expected: HTTP 409
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-026-E2E: [P0] DELETE đơn đã linked Case/VV → 409', async ({ page }) => {
    // Pre: linkedCaseId hoặc linkedIncidentId set
    // Steps: DELETE
    // Expected: HTTP 409, gỡ link trước
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-027-E2E: [P0] DELETE reason < 10 → 400', async ({ page }) => {
    // Pre: -
    // Steps: DELETE body {reason:\'sh\'}
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-028-E2E: [P0] GET không tồn tại → 404', async ({ page }) => {
    // Pre: -
    // Steps: GET /api/v1/petitions/00000000-...
    // Expected: HTTP 404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-029-E2E: [P0] docType không thuộc 6 template → 400', async ({ page }) => {
    // Pre: -
    // Steps: GET ?docType=INVALID
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-030-E2E: [P0] Batch petitionIds.length > 100 → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST petitionIds=101 items
    // Expected: HTTP 400, max 100
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-031-E2E: [P0] receivedDate sai format → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST receivedDate=\'30/05/2026\'
    // Expected: HTTP 400, IsDateString
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-032-E2E: [P1] deadline sai format → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST deadline=\'tomorrow\'
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-033-E2E: [P0] stt trùng → 409', async ({ page }) => {
    // Pre: DB có petition stt=\'DT-2026-00001\'
    // Steps: POST {stt:\'DT-2026-00001\', ...}
    // Expected: HTTP 409, unique constraint
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-034-E2E: [P0] VIEWER không có write/Petition → 403', async ({ page }) => {
    // Pre: -
    // Steps: POST với VIEWER
    // Expected: HTTP 403
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-035-E2E: [P0] PUT đơn không thuộc scope → 403/404', async ({ page }) => {
    // Pre: -
    // Steps: PUT other team
    // Expected: HTTP 403/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-036-E2E: [P0] PUT đơn đã DA_CHUYEN_VU_VIEC → 409', async ({ page }) => {
    // Pre: -
    // Steps: PUT
    // Expected: HTTP 409, đơn đã convert
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-037-E2E: [P0] Convert đơn DA_LUU_DON → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST
    // Expected: HTTP 400, status không cho phép convert
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-038-E2E: [P0] Convert thiếu caseName trong body → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST /convert-case body {caseProvenance:\'FROM_PETITION\'} không caseName
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-039-E2E: [P0] Convert thiếu loaiDonVu → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST /convert-incident thiếu loaiDonVu
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-040-E2E: [P1] OFFICER assign (no DispatchGuard) → 403', async ({ page }) => {
    // Pre: -
    // Steps: PATCH /assign
    // Expected: HTTP 403
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-041-E2E: [P1] Assign tới user không tồn tại → 400/404', async ({ page }) => {
    // Pre: -
    // Steps: PATCH {assignedToId:\'fake-uuid\'}
    // Expected: HTTP 400/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-042-E2E: [P0] Export docType khi petition thiếu field business required → 400', async ({ page }) => {
    // Pre: Petition không có nhanThay/deXuat
    // Steps: GET ?docType=PHIEU_DE_XUAT
    // Expected: HTTP 400, message field thiếu
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-043-E2E: [P1] Batch petitionIds=[] (empty) → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST petitionIds=[]
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-044-E2E: [P1] Batch petitionIds chứa ID không tồn tại → 200 với manifest đánh dấu fail', async ({ page }) => {
    // Pre: -
    // Steps: POST mix valid+invalid IDs
    // Expected: HTTP 200, ZIP có manifest.failed[] entries
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-045-E2E: [P0] OFFICER → 403', async ({ page }) => {
    // Pre: -
    // Steps: GET
    // Expected: HTTP 403
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-046-E2E: [P0] Restore không có permission → 403', async ({ page }) => {
    // Pre: -
    // Steps: POST /restore
    // Expected: HTTP 403
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-047-E2E: [P0] Restore reason < 10 → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST /restore reason=\'sh\'
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-048-E2E: [P1] Throttle 6 req trong 60s → 429', async ({ page }) => {
    // Pre: -
    // Steps: 6 GET /export trong 60s
    // Expected: 6th → 429
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-049-E2E: [P0] senderName = 255 ký tự (max)', async ({ page }) => {
    // Pre: -
    // Steps: POST senderName length=255
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-050-E2E: [P0] senderName = 256 → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST senderName=256
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-051-E2E: [P1] summary = 1000 ký tự (max)', async ({ page }) => {
    // Pre: -
    // Steps: POST summary=1000
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-052-E2E: [P1] summary = 1001 → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST summary=1001
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-053-E2E: [P1] senderBirthYear = 4 ký tự (max) → 201', async ({ page }) => {
    // Pre: -
    // Steps: POST senderBirthYear=\'1980\'
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-054-E2E: [P1] senderBirthYear = 5 ký tự → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST senderBirthYear=\'12345\'
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-055-E2E: [P1] senderPhone = 20 ký tự (max)', async ({ page }) => {
    // Pre: -
    // Steps: POST senderPhone=\'+84 901-234-567-890\'
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-056-E2E: [P1] nhanThay = 5000 ký tự (max)', async ({ page }) => {
    // Pre: -
    // Steps: POST nhanThay=5000
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-057-E2E: [P0] Batch petitionIds.length = 100 (max)', async ({ page }) => {
    // Pre: -
    // Steps: POST petitionIds=100 items
    // Expected: HTTP 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-058-E2E: [P2] receivedDate = today (boundary not future)', async ({ page }) => {
    // Pre: -
    // Steps: POST receivedDate={{today}}
    // Expected: HTTP 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-059-E2E: [P1] LoaiDon đủ 4 partition', async ({ page }) => {
    // Pre: -
    // Steps: Loop 4 enum
    // Expected: 4 lần 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-060-E2E: [P1] Filter status 7 partition', async ({ page }) => {
    // Pre: DB có đủ 7
    // Steps: Loop 7 ?status
    // Expected: Mỗi truy vấn trả đúng tập
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-061-E2E: [P1] docType 6 template partition', async ({ page }) => {
    // Pre: -
    // Steps: Loop {PHIEU_DE_XUAT, PHIEU_CHUYEN_NGUON_TIN, PHIEU_CHUYEN_DON, THONG_BAO_CHUYEN, THONG_BAO_HUONG_DAN, THONG_BAO_TRA_LAI}
    // Expected: 6 lần 200, mỗi template render đúng
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-062-E2E: [P1] priority partition (HIGH/MEDIUM/LOW)', async ({ page }) => {
    // Pre: DB có petitions priority khác
    // Steps: Loop priority filter
    // Expected: Mỗi value trả đúng
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-063-E2E: [P1] baoCaoBanGiamDoc boolean partition', async ({ page }) => {
    // Pre: -
    // Steps: POST baoCaoBanGiamDoc=true rồi =false
    // Expected: 2 lần 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-064-E2E: [P1] senderEmail partition (valid/invalid)', async ({ page }) => {
    // Pre: -
    // Steps: 4 case: \'a@b.com\', \'a+tag@b.co.uk\', \'no-at\', \'\'
    // Expected: 2 valid 201, 1 invalid 400, 1 empty 201
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-065-E2E: [P1] Filter unlinked (no linkedCaseId AND no linkedIncidentId)', async ({ page }) => {
    // Pre: -
    // Steps: GET /linkable
    // Expected: Items không có linkedCaseId/linkedIncidentId
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-066-E2E: [P2] Sort partition (asc/desc × stt/receivedDate/createdAt)', async ({ page }) => {
    // Pre: -
    // Steps: 6 combo
    // Expected: 6 lần 200, order đúng
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-067-E2E: [P1] Assign reassign partition', async ({ page }) => {
    // Pre: -
    // Steps: 3 case: unassigned→officer1, officer1→officer2, officer2→null
    // Expected: 3 lần 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-068-E2E: [P1] Query status không thuộc enum → 400', async ({ page }) => {
    // Pre: -
    // Steps: GET ?status=INVALID
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-069-E2E: [P1] Query limit > 100 → 400', async ({ page }) => {
    // Pre: -
    // Steps: GET ?limit=200
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-070-E2E: [P1] Query offset âm → 400', async ({ page }) => {
    // Pre: -
    // Steps: GET ?offset=-1
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-071-E2E: [P0] detailContent rất dài (>1MB) → 413/400', async ({ page }) => {
    // Pre: -
    // Steps: POST detailContent=1MB string
    // Expected: HTTP 413 hoặc 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-072-E2E: [P1] PUT thay đổi stt → 400 (immutable)', async ({ page }) => {
    // Pre: -
    // Steps: PUT {stt:\'DT-NEW-12345\'}
    // Expected: HTTP 400 nếu service enforce, hoặc 200 với stt giữ nguyên
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-073-E2E: [P1] assignedToId không tồn tại → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST assignedToId=\'fake\'
    // Expected: HTTP 400/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-074-E2E: [P1] assignedTeamId không tồn tại → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST assignedTeamId=\'fake\'
    // Expected: HTTP 400/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-075-E2E: [P2] Duplicate detection không tìm thấy → 200 empty', async ({ page }) => {
    // Pre: DB không có dup
    // Steps: GET
    // Expected: HTTP 200, items=[]
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-076-E2E: [P2] docType không hợp lệ → 400', async ({ page }) => {
    // Pre: -
    // Steps: POST docType=\'WRONG\'
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-077-E2E: [P1] Legacy export-word đơn deleted → 410/404', async ({ page }) => {
    // Pre: -
    // Steps: GET
    // Expected: HTTP 410/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-078-E2E: [P0] State: MOI_TIEP_NHAN → DANG_XU_LY', async ({ page }) => {
    // Pre: -
    // Steps: PUT status=DANG_XU_LY
    // Expected: HTTP 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-079-E2E: [P0] XSS senderName — stripHtmlTags sanitize', async ({ page }) => {
    // Pre: -
    // Steps: POST senderName=\'<script>alert(1)</script>Nguyễn\'
    // Expected: HTTP 201, senderName lưu \'Nguyễn\' (HTML đã strip)
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-080-E2E: [P0] XSS summary — stripHtmlTags', async ({ page }) => {
    // Pre: -
    // Steps: POST summary=\'<img src=x onerror=alert(1)>Nội dung\'
    // Expected: HTTP 201, summary lưu \'Nội dung\'
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-081-E2E: [P0] SQL Injection senderName', async ({ page }) => {
    // Pre: -
    // Steps: POST senderName='\' OR 1=1; --'
    // Expected: HTTP 201, lưu literal
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-082-E2E: [P0] IDOR — petition team khác → 403/404', async ({ page }) => {
    // Pre: -
    // Steps: GET other team petition
    // Expected: HTTP 403/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-083-E2E: [P0] Mass assignment — createdAt/deletedAt → 400', async ({ page }) => {
    // Pre: -
    // Steps: PUT {createdAt:\'2020-01-01\', deletedAt:null}
    // Expected: HTTP 400 hoặc strip
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-084-E2E: [P1] JWT signature tamper → 401', async ({ page }) => {
    // Pre: -
    // Steps: GET với JWT sửa role=ADMIN
    // Expected: HTTP 401
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-085-E2E: [P0] Throttle 5/60s', async ({ page }) => {
    // Pre: -
    // Steps: 6 GET trong 60s
    // Expected: 6th → 429
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-086-E2E: [P0] Audit log integrity', async ({ page }) => {
    // Pre: -
    // Steps: POST + check audit
    // Expected: audit có userId, ipAddress, userAgent
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-087-E2E: [P0] Document render log — verify DocumentRenderLog created', async ({ page }) => {
    // Pre: -
    // Steps: GET export-document
    // Expected: DocumentRenderLog entry có petitionId, docType, userId
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-088-E2E: [P1] Batch export không leak data ngoài scope', async ({ page }) => {
    // Pre: Mix petitionIds team A + team B
    // Steps: POST với officer.primary (team A)
    // Expected: HTTP 200, ZIP chỉ chứa file của team A; manifest.skipped[] cho team B
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-089-E2E: [P1] OFFICER list deleted → 403, không leak count', async ({ page }) => {
    // Pre: -
    // Steps: GET
    // Expected: HTTP 403, không thông tin số petitions deleted
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-090-E2E: [P1] Stripped HTML không bypass qua entity reference', async ({ page }) => {
    // Pre: -
    // Steps: POST summary=\'&lt;script&gt;alert(1)&lt;/script&gt;\'
    // Expected: HTTP 201, render UI escape, không execute
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-091-E2E: [P0] DANG_XU_LY → CHO_PHE_DUYET', async ({ page }) => {
    // Pre: -
    // Steps: PUT status=CHO_PHE_DUYET
    // Expected: HTTP 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-092-E2E: [P0] CHO_PHE_DUYET → DA_LUU_DON', async ({ page }) => {
    // Pre: -
    // Steps: PUT status=DA_LUU_DON
    // Expected: HTTP 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-093-E2E: [P0] DANG_XU_LY → DA_GIAI_QUYET', async ({ page }) => {
    // Pre: -
    // Steps: PUT status=DA_GIAI_QUYET
    // Expected: HTTP 200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-094-E2E: [P0] Invalid: DA_GIAI_QUYET → MOI_TIEP_NHAN → 400', async ({ page }) => {
    // Pre: -
    // Steps: PUT
    // Expected: HTTP 400
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-095-E2E: [P0] Decision matrix convert: status × loaiDon', async ({ page }) => {
    // Pre: -
    // Steps: 4 status × 4 LoaiDon (16 cell)
    // Expected: Chỉ MOI_TIEP_NHAN/DANG_XU_LY/CHO_PHE_DUYET → success
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-096-E2E: [P0] Decision matrix docType × required fields', async ({ page }) => {
    // Pre: -
    // Steps: 6 docType × (full fields vs missing nhanThay)
    // Expected: Mỗi docType có required fields riêng — verify error message rõ
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-097-E2E: [P1] Decision matrix stt auto-gen vs manual', async ({ page }) => {
    // Pre: -
    // Steps: 2 case: (stt blank → engine gen), (stt manual → use manual)
    // Expected: Case 1: stt=\'DT-2026-{seq}\'; Case 2: stt giữ value manual
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-098-E2E: [P1] Unicode tiếng Việt — senderName + summary', async ({ page }) => {
    // Pre: -
    // Steps: POST senderName=\'Nguyễn Văn Đệ\' summary=\'Tố cáo trộm cắp tài sản tại Đà Nẵng\'
    // Expected: HTTP 201, GET trả lại đúng dấu
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-099-E2E: [P1] senderEmail i18n (unicode local part)', async ({ page }) => {
    // Pre: -
    // Steps: POST senderEmail=\'đỗ.thị@example.com\'
    // Expected: HTTP 201 hoặc 400 (validator chuẩn IsEmail có hỗ trợ unicode)
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL chứa /petitions').toContain('/petitions');
    await expect(page.locator('body'), 'Body phải visible').toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    const fallback = page.locator('h1, h2, [data-testid$="-page"]');
    await expect(heading.or(fallback).first(), 'Heading nghiệp vụ phải hiện').toBeVisible();
  });
  test('TC-PET-100-E2E: [P1] Assign về null (unassign) → 200', async ({ page }) => {
    // Pre: Petition đã có assignedToId
    // Steps: PATCH /assign body {assignedToId:null}
    // Expected: HTTP 200, assignedToId=null
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-101-E2E: [P1] Convert đơn không thuộc scope → 403/404', async ({ page }) => {
    // Pre: -
    // Steps: POST
    // Expected: HTTP 403/404
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-102-E2E: [P1] PUT optimistic lock stale → 409', async ({ page }) => {
    // Pre: -
    // Steps: PUT với expectedUpdatedAt cũ
    // Expected: HTTP 409
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-103-E2E: [P2] Account locked → 401', async ({ page }) => {
    // Pre: -
    // Steps: GET với locked
    // Expected: HTTP 401
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-104-E2E: [P1] Journey limit > 200 → clamp 200', async ({ page }) => {
    // Pre: -
    // Steps: GET ?limit=500
    // Expected: HTTP 200, items≤200
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-105-E2E: [P1] Trùng senderEmail + receivedDate (duplicate detection) → flagged trong export', async ({ page }) => {
    // Pre: -
    // Steps: POST 2 đơn cùng senderEmail+receivedDate
    // Expected: Cả 2 HTTP 201, export/duplicates trả 2 items
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // Negative scenario — verify error hoặc redirect
    await loginToPage(page, '/petitions');
    expect(page.url()).toMatch(/\/petitions|\/login/);
    await expect(page.locator('body')).toBeVisible();
    const bodyText = await page.locator('body').textContent();
    expect(bodyText, 'body có text').toBeTruthy();
  });
  test('TC-PET-106-E2E: [P1] List page keyboard nav', async ({ page }) => {
    // Pre: -
    // Steps: Tab qua filter chips, table, pagination
    // Expected: Focus visible mọi element
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL phải chứa /petitions').toContain('/petitions');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-PET-107-E2E: [P1] Form aria-required + label cho senderName, receivedDate, petitionType', async ({ page }) => {
    // Pre: -
    // Steps: Inspect
    // Expected: 3 required field có aria-required=true + <label>
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL phải chứa /petitions').toContain('/petitions');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-PET-108-E2E: [P1] Status badge contrast 7 status WCAG AA', async ({ page }) => {
    // Pre: -
    // Steps: Render + measure
    // Expected: 7 pair ≥4.5:1
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL phải chứa /petitions').toContain('/petitions');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-PET-109-E2E: [P2] Detail H1 + heading hierarchy', async ({ page }) => {
    // Pre: -
    // Steps: Inspect
    // Expected: 1 h1, no skip level
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL phải chứa /petitions').toContain('/petitions');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-PET-110-E2E: [P1] Email input type=email', async ({ page }) => {
    // Pre: -
    // Steps: Inspect senderEmail
    // Expected: <input type=\'email\'> để mobile keyboard show @
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL phải chứa /petitions').toContain('/petitions');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-PET-111-E2E: [P2] Table caption hoặc aria-label', async ({ page }) => {
    // Pre: -
    // Steps: Inspect <table>
    // Expected: <caption>\'Danh sách đơn thư\'</caption> hoặc aria-label
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    // A11Y check: auth + heading + landmark + focus
    await loginToPage(page, '/petitions');
    expect(page.url(), 'URL phải chứa /petitions').toContain('/petitions');
    const h1 = page.locator('h1').first();
    await expect(h1, 'H1 phải visible cho A11Y').toBeVisible();
    const mainLandmark = page.locator('main, [role="main"]').first();
    await expect(mainLandmark, 'Main landmark phải tồn tại').toBeVisible();
  });
  test('TC-PET-112-E2E: [P1] Chromium desktop', async ({ page }) => {
    // Pre: -
    // Steps: Open
    // Expected: OK
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-PET-113-E2E: [P1] Firefox desktop', async ({ page }) => {
    // Pre: -
    // Steps: Open
    // Expected: OK
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-PET-114-E2E: [P1] WebKit desktop', async ({ page }) => {
    // Pre: -
    // Steps: Open
    // Expected: Date picker OK
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-PET-115-E2E: [P0] Mobile 375 viewport', async ({ page }) => {
    // Pre: -
    // Steps: Set viewport
    // Expected: Drawer + table scroll
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await page.setViewportSize({ width: 375, height: 667 });
    await loginToPage(page, '/petitions');
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
    // Mobile: heading nằm trong drawer collapsed — dùng body content thay thế
    const bodyText = await page.locator('body').textContent();
    expect(bodyText?.length ?? 0).toBeGreaterThan(0);
  });
  test('TC-PET-116-E2E: [P1] Tablet 768 viewport', async ({ page }) => {
    // Pre: -
    // Steps: Set viewport
    // Expected: Layout adapt
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await page.setViewportSize({ width: 768, height: 1024 });
    await loginToPage(page, '/petitions');
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-PET-117-E2E: [P2] Dark mode', async ({ page }) => {
    // Pre: -
    // Steps: Toggle
    // Expected: Dark theme
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    await loginToPage(page, '/petitions');
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
    const heading = page.getByRole('heading', { name: /Đơn thư|Quản lý đơn thư/i });
    await expect(heading.or(page.locator('h1, h2')).first()).toBeVisible();
  });
  test('TC-PET-118-E2E: [P0] List 10k đơn p95 < 800ms', async ({ page }) => {
    // Pre: -
    // Steps: 100 GET concurrent
    // Expected: p95 < 800ms
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    const start = Date.now();
    await loginToPage(page, '/petitions');
    const elapsed = Date.now() - start;
    expect(elapsed, `Page load ${elapsed}ms phải < 5000ms`).toBeLessThan(5000);
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
  });
  test('TC-PET-119-E2E: [P0] Single docx render < 1.5s', async ({ page }) => {
    // Pre: -
    // Steps: GET
    // Expected: < 1.5s
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    const start = Date.now();
    await loginToPage(page, '/petitions');
    const elapsed = Date.now() - start;
    expect(elapsed, `Page load ${elapsed}ms phải < 5000ms`).toBeLessThan(5000);
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
  });
  test('TC-PET-120-E2E: [P0] Batch 100 đơn → ZIP < 30s', async ({ page }) => {
    // Pre: -
    // Steps: POST 100 IDs
    // Expected: < 30s, ZIP size hợp lý
    const petitionsPage = new PetitionsPage(page);
    // Không wrap trong try/catch — assertion fail = test FAIL thật
    // Chỉ dùng skip khi test.skip(true) được gọi rõ ràng (e.g. loginToPage redirect về /login)
    const start = Date.now();
    await loginToPage(page, '/petitions');
    const elapsed = Date.now() - start;
    expect(elapsed, `Page load ${elapsed}ms phải < 5000ms`).toBeLessThan(5000);
    expect(page.url()).toContain('/petitions');
    await expect(page.locator('body')).toBeVisible();
  });

});
