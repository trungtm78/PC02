node_modules/
dist/
build/
.env
.env.local
.env.test
*.pem
!**/public.pem
coverage/
playwright-report/
test-results/

# Backend test coverage
backend/src/coverage/

# Address-mapping API snapshots — runtime artifacts (~50KB per province per run)
backend/prisma/data/snapshots/*.json

# IDE / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store
.gstack/
maestro/.env
*.png
!docs/user-guide/screenshots/*.png

# Docker production secrets (never commit)
.env
/keys/

# Runtime user uploads — never commit
backend/uploads/
test-results/

# UAT test credentials — passwords in .env.test (gitignored), not committed
docs/uat/_shared/test-accounts.json

# Python test cache
scripts/tests/__pycache__/
backend/_be-triage.log

backend/_be-*.log
scripts/__pycache__/
temp_failed.json

# Legacy Mongo dumps (PII — never commit raw data)
legacy-dumps/
docs/legacy/*.ejsonl
**/*.ejsonl
node_modules

# ─── Bí mật: KHÔNG BAO GIỜ commit (git giữ vĩnh viễn, đẩy lên là lộ) ─────────
# Hồ sơ cấp tài khoản: chứa mật khẩu tài khoản production dạng chữ rõ.
docs/Requirements/
# Công cụ cấp tài khoản một lần: ghi cứng số điện thoại dùng làm mật khẩu.
backend/src/legacy-migration/cli/provision-doi2-accounts.ts

# ─── Bản kết xuất hệ cũ (~258 MB, phình repo vĩnh viễn) ──────────────────────
docs/pc02hcm.com-*.zip
docs/pc02hcm.com-new/

# ─── Rác cục bộ ──────────────────────────────────────────────────────────────
# 'nul' sinh do lỡ chuyển hướng kiểu Windows trong shell POSIX.
nul
**/nul
*.full-jest.log
backend/.full-jest.log
setup-claude-autonomous.bat
test-claude-hook.bat
.claude/

# Hộp cát của Stryker — tệp tạm, không bao giờ commit.
.stryker-tmp/
backend/.stryker-tmp/
backend/reports/mutation/
