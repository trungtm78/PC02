node_modules/
dist/
build/
.env
.env.local
.env.test
*.pem
!**/public.pem
coverage/
playwright-report/
test-results/

# Backend test coverage
backend/src/coverage/

# Address-mapping API snapshots — runtime artifacts (~50KB per province per run)
backend/prisma/data/snapshots/*.json

# IDE / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store
.gstack/
maestro/.env
*.png
!docs/user-guide/screenshots/*.png

# Docker production secrets (never commit)
.env
/keys/

# Runtime user uploads — never commit
backend/uploads/
test-results/

# UAT test credentials — passwords in .env.test (gitignored), not committed
docs/uat/_shared/test-accounts.json

# Python test cache
scripts/tests/__pycache__/
backend/_be-triage.log
