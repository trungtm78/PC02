# Test environment variables — copy to .env.test and fill in
# .env.test is gitignored; never commit real credentials.
TEST_EMAIL_DTV=dtv@pc02.catp.gov.vn
TEST_PASS_DTV=CHANGE_ME

# Seed scripts read these (fail fast if missing):
SEED_ADMIN_PASSWORD=CHANGE_ME
SEED_DTV_PASSWORD=CHANGE_ME
SEED_OFFICER_PASSWORD=CHANGE_ME
